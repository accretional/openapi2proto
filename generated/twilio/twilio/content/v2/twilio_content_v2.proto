// Code generated by openapi2proto. DO NOT EDIT.
// Source: twilio_content_v2.json

syntax = "proto3";

package twilio.content.v2;

option go_package = "twilio/content/v2;twiliocontentv2";

import "google/api/annotations.proto";

message ContentV1Content {
  // The SID of the [Account](https://www.twilio.com/docs/usage/api/account) that created Content resource.
  string account_sid = 1;
  // The date and time in GMT that the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 2;
  // The date and time in GMT that the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 3;
  // A string name used to describe the Content resource. Not visible to the end recipient.
  string friendly_name = 4;
  // Two-letter (ISO 639-1) language code (e.g., en) identifying the language the Content resource is in.
  string language = 5;
  // A list of links related to the Content resource, such as approval_fetch and approval_create
  ContentV1ContentLinks links = 6;
  // The unique string that that we created to identify the Content resource.
  string sid = 7;
  // The [Content types](https://www.twilio.com/docs/content/content-types-overview) (e.g. twilio/text) for this Content resource.
  ContentV1ContentTypes types = 8;
  // The URL of the resource, relative to `https://content.twilio.com`.
  string url = 9;
  // Defines the default placeholder values for variables included in the Content resource. e.g. {"1": "Customer_Name"}.
  ContentV1ContentVariables variables = 10;
}

// A list of links related to the Content resource, such as approval_fetch and approval_create
message ContentV1ContentLinks {
  // Scalar value wrapper.
  string value = 1;
}

// The [Content types](https://www.twilio.com/docs/content/content-types-overview) (e.g. twilio/text) for this Content resource.
message ContentV1ContentTypes {
  // Scalar value wrapper.
  string value = 1;
}

// Defines the default placeholder values for variables included in the Content resource. e.g. {"1": "Customer_Name"}.
message ContentV1ContentVariables {
  // Scalar value wrapper.
  string value = 1;
}

message ContentV1ContentApprovalFetch {
  // The SID of the [Account](https://www.twilio.com/docs/usage/api/account) that created Content resource.
  string account_sid = 1;
  // The unique string that that we created to identify the Content resource.
  string sid = 2;
  // The URL of the resource, relative to `https://content.twilio.com`.
  string url = 3;
  // Contains the whatsapp approval information for the Content resource, with fields such as approval status, rejection reason, and category, amongst others.
  string whatsapp = 4;
}

message ContentV1ContentAndApprovals {
  // The SID of the [Account](https://www.twilio.com/docs/usage/api/account) that created Content resource.
  string account_sid = 1;
  // The submitted information and approval request status of the Content resource.
  ContentV1ContentAndApprovalsApprovalRequests approval_requests = 2;
  // The date and time in GMT that the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 3;
  // The date and time in GMT that the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 4;
  // A string name used to describe the Content resource. Not visible to the end recipient.
  string friendly_name = 5;
  // Two-letter (ISO 639-1) language code (e.g., en) identifying the language the Content resource is in.
  string language = 6;
  // The unique string that that we created to identify the Content resource.
  string sid = 7;
  // The [Content types](https://www.twilio.com/docs/content/content-types-overview) (e.g. twilio/text) for this Content resource.
  ContentV1ContentAndApprovalsTypes types = 8;
  // Defines the default placeholder values for variables included in the Content resource. e.g. {"1": "Customer_Name"}.
  ContentV1ContentAndApprovalsVariables variables = 9;
}

// The submitted information and approval request status of the Content resource.
message ContentV1ContentAndApprovalsApprovalRequests {
  // Scalar value wrapper.
  string value = 1;
}

// The [Content types](https://www.twilio.com/docs/content/content-types-overview) (e.g. twilio/text) for this Content resource.
message ContentV1ContentAndApprovalsTypes {
  // Scalar value wrapper.
  string value = 1;
}

// Defines the default placeholder values for variables included in the Content resource. e.g. {"1": "Customer_Name"}.
message ContentV1ContentAndApprovalsVariables {
  // Scalar value wrapper.
  string value = 1;
}

message ContentV1LegacyContent {
  // The SID of the [Account](https://www.twilio.com/docs/usage/api/account) that created Content resource.
  string account_sid = 1;
  // The date and time in GMT that the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 2;
  // The date and time in GMT that the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 3;
  // A string name used to describe the Content resource. Not visible to the end recipient.
  string friendly_name = 4;
  // Two-letter (ISO 639-1) language code (e.g., en) identifying the language the Content resource is in.
  string language = 5;
  // The string body field of the legacy content template associated with this Content resource
  string legacy_body = 6;
  // The string name of the legacy content template associated with this Content resource, unique across all template names for its account.  Only lowercase letters, numbers and underscores are allowed
  string legacy_template_name = 7;
  // The unique string that that we created to identify the Content resource.
  string sid = 8;
  // The [Content types](https://www.twilio.com/docs/content/content-types-overview) (e.g. twilio/text) for this Content resource.
  string types = 9;
  // The URL of the resource, relative to `https://content.twilio.com`.
  string url = 10;
  // Defines the default placeholder values for variables included in the Content resource. e.g. {"1": "Customer_Name"}.
  string variables = 11;
}

// ListContentResponse
message ListContentResponseBody {
  repeated ContentV1Content contents = 1;
  ListContentResponseBodyMeta meta = 2;
}

message ListContentResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of Contents belonging to the account used to make the request
message ListContentRequest {
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 1;
  // The page index. This value is simply for client state.
  int64 page = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
  // Whether to sort by ascending or descending date updated
  string sort_by_date = 4;
  // Whether to sort by ascending or descending content name
  string sort_by_content_name = 5;
  // Filter by >=[date-time]
  string date_created_after = 6;
  // Filter by <=[date-time]
  string date_created_before = 7;
  // Filter by Regex Pattern in content name
  string content_name = 8;
  // Filter by Regex Pattern in template content
  string content = 9;
  // Filter by array of valid language(s)
  repeated string language = 10;
  // Filter by array of contentType(s)
  repeated string content_type = 11;
  // Filter by array of ChannelEligibility(s), where ChannelEligibility=<channel>:<status>
  repeated string channel_eligibility = 12;
}

// Canonical success response.
message ListContentResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListContentResponseBody body = 2;
}

// ListContentAndApprovalsResponse
message ListContentAndApprovalsResponseBody {
  repeated ContentV1ContentAndApprovals contents = 1;
  ListContentAndApprovalsResponseBodyMeta meta = 2;
}

message ListContentAndApprovalsResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of Contents with approval statuses belonging to the account used to make the request
message ListContentAndApprovalsRequest {
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 1;
  // The page index. This value is simply for client state.
  int64 page = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
  // Whether to sort by ascending or descending date updated
  string sort_by_date = 4;
  // Whether to sort by ascending or descending content name
  string sort_by_content_name = 5;
  // Filter by >=[date-time]
  string date_created_after = 6;
  // Filter by <=[date-time]
  string date_created_before = 7;
  // Filter by Regex Pattern in content name
  string content_name = 8;
  // Filter by Regex Pattern in template content
  string content = 9;
  // Filter by array of valid language(s)
  repeated string language = 10;
  // Filter by array of contentType(s)
  repeated string content_type = 11;
  // Filter by array of ChannelEligibility(s), where ChannelEligibility=<channel>:<status>
  repeated string channel_eligibility = 12;
}

// Canonical success response.
message ListContentAndApprovalsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListContentAndApprovalsResponseBody body = 2;
}

// Generated from ContentV2Content operations.
service ContentV2ContentService {
  // Retrieve a list of Contents belonging to the account used to make the request
  rpc ListContent(ListContentRequest) returns (ListContentResponse) {
    option (google.api.http) = {
      get: "/v2/Content"
      response_body: "body"
    };
  }
}

// Generated from ContentV2ContentAndApprovals operations.
service ContentV2ContentAndApprovalsService {
  // Retrieve a list of Contents with approval statuses belonging to the account used to make the request
  rpc ListContentAndApprovals(ListContentAndApprovalsRequest) returns (ListContentAndApprovalsResponse) {
    option (google.api.http) = {
      get: "/v2/ContentAndApprovals"
      response_body: "body"
    };
  }
}

