// Code generated by openapi2proto. DO NOT EDIT.
// Source: twilio_studio_v1.json

syntax = "proto3";

package twilio.studio.v1;

option go_package = "twilio/studio/v1;twiliostudiov1";

import "google/api/annotations.proto";

// The status of the Engagement. Can be: `active` or `ended`.
message EngagementEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

// The status of the Execution. Can be: `active` or `ended`.
message ExecutionEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

// The status of the Flow. Can be: `draft` or `published`.
message FlowEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

message StudioV1Flow {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Flow resource.
  string account_sid = 1;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 2;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 3;
  // The string that you assigned to describe the Flow.
  string friendly_name = 4;
  // The URLs of the Flow's nested resources.
  StudioV1FlowLinks links = 5;
  // The unique string that we created to identify the Flow resource.
  string sid = 6;
  // OpenAPI property "status".
  FlowEnumStatus status = 7;
  // The absolute URL of the resource.
  string url = 8;
  // The latest version number of the Flow's definition.
  int64 version = 9;
}

// The URLs of the Flow's nested resources.
message StudioV1FlowLinks {
  // Scalar value wrapper.
  string value = 1;
}

message StudioV1FlowEngagement {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Engagement resource.
  string account_sid = 1;
  // The phone number, SIP address or Client identifier that triggered this Engagement. Phone numbers are in E.164 format (+16175551212). SIP addresses are formatted as `name@company.com`. Client identifiers are formatted `client:name`.
  string contact_channel_address = 2;
  // The SID of the Contact.
  string contact_sid = 3;
  // The current state of the execution flow. As your flow executes, we save the state in a flow context. Your widgets can access the data in the flow context as variables, either in configuration fields or in text areas as variable substitution.
  string context = 4;
  // The date and time in GMT when the Engagement was created in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 5;
  // The date and time in GMT when the Engagement was updated in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 6;
  // The SID of the Flow.
  string flow_sid = 7;
  // The URLs of the Engagement's nested resources.
  StudioV1FlowEngagementLinks links = 8;
  // The unique string that we created to identify the Engagement resource.
  string sid = 9;
  // OpenAPI property "status".
  EngagementEnumStatus status = 10;
  // The absolute URL of the resource.
  string url = 11;
}

// The URLs of the Engagement's nested resources.
message StudioV1FlowEngagementLinks {
  // Scalar value wrapper.
  string value = 1;
}

message StudioV1FlowEngagementEngagementContext {
  // The SID of the Account.
  string account_sid = 1;
  // As your flow executes, we save the state in what's called the Flow Context. Any data in the flow context can be accessed by your widgets as variables, either in configuration fields or in text areas as variable substitution.
  string context = 2;
  // The SID of the Engagement.
  string engagement_sid = 3;
  // The SID of the Flow.
  string flow_sid = 4;
  // The URL of the resource.
  string url = 5;
}

message StudioV1FlowEngagementStep {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Step resource.
  string account_sid = 1;
  // The current state of the Flow's Execution. As a flow executes, we save its state in this context. We save data that your widgets can access as variables in configuration fields or in text areas as variable substitution.
  string context = 2;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 4;
  // The SID of the Engagement.
  string engagement_sid = 5;
  // The SID of the Flow.
  string flow_sid = 6;
  // The URLs of related resources.
  StudioV1FlowEngagementStepLinks links = 7;
  // The event that caused the Flow to transition to the Step.
  string name = 8;
  // The SID of the parent Step.
  string parent_step_sid = 9;
  // The unique string that we created to identify the Step resource.
  string sid = 10;
  // The Widget that preceded the Widget for the Step.
  string transitioned_from = 11;
  // The Widget that will follow the Widget for the Step.
  string transitioned_to = 12;
  // The type of the widget that was executed.
  string type = 13;
  // The absolute URL of the resource.
  string url = 14;
}

// The URLs of related resources.
message StudioV1FlowEngagementStepLinks {
  // Scalar value wrapper.
  string value = 1;
}

message StudioV1FlowEngagementStepStepContext {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the StepContext resource.
  string account_sid = 1;
  // The current state of the Flow's Execution. As a flow executes, we save its state in this context. We save data that your widgets can access as variables in configuration fields or in text areas as variable substitution.
  string context = 2;
  // The SID of the Engagement.
  string engagement_sid = 3;
  // The SID of the Flow.
  string flow_sid = 4;
  // The SID of the Step the context is associated with.
  string step_sid = 5;
  // The absolute URL of the resource.
  string url = 6;
}

message StudioV1FlowExecution {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Execution resource.
  string account_sid = 1;
  // The phone number, SIP address or Client identifier that triggered the Execution. Phone numbers are in E.164 format (e.g. +16175551212). SIP addresses are formatted as `name@company.com`. Client identifiers are formatted `client:name`.
  string contact_channel_address = 2;
  // The SID of the Contact.
  string contact_sid = 3;
  // The current state of the Flow's Execution. As a flow executes, we save its state in this context. We save data that your widgets can access as variables in configuration fields or in text areas as variable substitution.
  string context = 4;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 5;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 6;
  // The SID of the Flow.
  string flow_sid = 7;
  // The URLs of nested resources.
  StudioV1FlowExecutionLinks links = 8;
  // The unique string that we created to identify the Execution resource.
  string sid = 9;
  // OpenAPI property "status".
  ExecutionEnumStatus status = 10;
  // The absolute URL of the resource.
  string url = 11;
}

// The URLs of nested resources.
message StudioV1FlowExecutionLinks {
  // Scalar value wrapper.
  string value = 1;
}

message StudioV1FlowExecutionExecutionContext {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the ExecutionContext resource.
  string account_sid = 1;
  // The current state of the Flow's Execution. As a flow executes, we save its state in this context. We save data that your widgets can access as variables in configuration fields or in text areas as variable substitution.
  string context = 2;
  // The SID of the context's Execution resource.
  string execution_sid = 3;
  // The SID of the Flow.
  string flow_sid = 4;
  // The absolute URL of the resource.
  string url = 5;
}

message StudioV1FlowExecutionExecutionStep {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the ExecutionStep resource.
  string account_sid = 1;
  // The current state of the Flow's Execution. As a flow executes, we save its state in this context. We save data that your widgets can access as variables in configuration fields or in text areas as variable substitution.
  string context = 2;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 4;
  // The SID of the Step's Execution resource.
  string execution_sid = 5;
  // The SID of the Flow.
  string flow_sid = 6;
  // The URLs of related resources.
  StudioV1FlowExecutionExecutionStepLinks links = 7;
  // The event that caused the Flow to transition to the Step.
  string name = 8;
  // This field shows the Step SID of the Widget in the parent Flow that started the Subflow. If this Step is not part of a Subflow execution, the value is null.
  string parent_step_sid = 9;
  // The unique string that we created to identify the ExecutionStep resource.
  string sid = 10;
  // The Widget that preceded the Widget for the Step.
  string transitioned_from = 11;
  // The Widget that will follow the Widget for the Step.
  string transitioned_to = 12;
  // The type of the widget that was executed.
  string type = 13;
  // The absolute URL of the resource.
  string url = 14;
}

// The URLs of related resources.
message StudioV1FlowExecutionExecutionStepLinks {
  // Scalar value wrapper.
  string value = 1;
}

message StudioV1FlowExecutionExecutionStepExecutionStepContext {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the ExecutionStepContext resource.
  string account_sid = 1;
  // The current state of the Flow's Execution. As a flow executes, we save its state in this context. We save data that your widgets can access as variables in configuration fields or in text areas as variable substitution.
  string context = 2;
  // The SID of the context's Execution resource.
  string execution_sid = 3;
  // The SID of the Flow.
  string flow_sid = 4;
  // The SID of the Step that the context is associated with.
  string step_sid = 5;
  // The absolute URL of the resource.
  string url = 6;
}

// ListFlowResponse
message ListFlowResponseBody {
  repeated StudioV1Flow flows = 1;
  ListFlowResponseBodyMeta meta = 2;
}

message ListFlowResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Flows.
message ListFlowRequest {
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 1;
  // The page index. This value is simply for client state.
  int64 page = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
}

// Canonical success response.
message ListFlowResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListFlowResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListEngagementResponse
message ListEngagementResponseBody {
  repeated StudioV1FlowEngagement engagements = 1;
  ListEngagementResponseBodyMeta meta = 2;
}

message ListEngagementResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Engagements for the Flow.
message ListEngagementRequest {
  // The SID of the Flow to read Engagements from. Required.
  string flow_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListEngagementResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListEngagementResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateEngagementRequest
message CreateEngagementRequestBody {
  // The Twilio phone number to send messages or initiate calls from during the Flow Engagement. Available as variable `{{flow.channel.address}}`
  string from = 1;
  // A JSON string we will add to your flow's context and that you can access as variables inside your flow. For example, if you pass in `Parameters={'name':'Zeke'}` then inside a widget you can reference the variable `{{flow.data.name}}` which will return the string 'Zeke'. Note: the JSON value must explicitly be passed as a string, not as a hash object. Depending on your particular HTTP library, you may need to add quotes or URL encode your JSON string.
  string parameters = 2;
  // The Contact phone number to start a Studio Flow Engagement, available as variable `{{contact.channel.address}}`.
  string to_field = 3;
}

// Triggers a new Engagement for the Flow
message CreateEngagementRequest {
  // The SID of the Flow. Required.
  string flow_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateEngagementRequestBody body = 2;
}

// Canonical success response.
message CreateEngagementResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  StudioV1FlowEngagement body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Retrieve the most recent context for an Engagement.
message FetchEngagementContextRequest {
  // The SID of the Flow. Required.
  string flow_sid = 1;
  // The SID of the Engagement. Required.
  string engagement_sid = 2;
}

// Canonical success response.
message FetchEngagementContextResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  StudioV1FlowEngagementEngagementContext body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListStepResponse
message ListStepResponseBody {
  ListStepResponseBodyMeta meta = 1;
  repeated StudioV1FlowEngagementStep steps = 2;
}

message ListStepResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Steps for an Engagement.
message ListStepRequest {
  // The SID of the Flow with the Step to read. Required.
  string flow_sid = 1;
  // The SID of the Engagement with the Step to read. Required.
  string engagement_sid = 2;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 3;
  // The page index. This value is simply for client state.
  int64 page = 4;
  // The page token. This is provided by the API.
  string page_token = 5;
}

// Canonical success response.
message ListStepResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListStepResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Retrieve a Step.
message FetchStepRequest {
  // The SID of the Flow with the Step to fetch. Required.
  string flow_sid = 1;
  // The SID of the Engagement with the Step to fetch. Required.
  string engagement_sid = 2;
  // The SID of the Step resource to fetch. Required.
  string sid = 3;
}

// Canonical success response.
message FetchStepResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  StudioV1FlowEngagementStep body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Retrieve the context for an Engagement Step.
message FetchStepContextRequest {
  // The SID of the Flow with the Step to fetch. Required.
  string flow_sid = 1;
  // The SID of the Engagement with the Step to fetch. Required.
  string engagement_sid = 2;
  // The SID of the Step to fetch Required.
  string step_sid = 3;
}

// Canonical success response.
message FetchStepContextResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  StudioV1FlowEngagementStepStepContext body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Retrieve an Engagement
message FetchEngagementRequest {
  // The SID of the Flow. Required.
  string flow_sid = 1;
  // The SID of the Engagement resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchEngagementResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  StudioV1FlowEngagement body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete this Engagement and all Steps relating to it.
message DeleteEngagementRequest {
  // The SID of the Flow to delete Engagements from. Required.
  string flow_sid = 1;
  // The SID of the Engagement resource to delete. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteEngagementResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListExecutionResponse
message ListExecutionResponseBody {
  repeated StudioV1FlowExecution executions = 1;
  ListExecutionResponseBodyMeta meta = 2;
}

message ListExecutionResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Executions for the Flow.
message ListExecutionRequest {
  // The SID of the Flow with the Execution resources to read. Required.
  string flow_sid = 1;
  // Only show Execution resources starting on or after this [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date-time, given as `YYYY-MM-DDThh:mm:ss-hh:mm`.
  string date_created_from = 2;
  // Only show Execution resources starting before this [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date-time, given as `YYYY-MM-DDThh:mm:ss-hh:mm`.
  string date_created_to = 3;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 4;
  // The page index. This value is simply for client state.
  int64 page = 5;
  // The page token. This is provided by the API.
  string page_token = 6;
}

// Canonical success response.
message ListExecutionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListExecutionResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateExecutionRequest
message CreateExecutionRequestBody {
  // The Twilio phone number to send messages or initiate calls from during the Flow's Execution. Available as variable `{{flow.channel.address}}`. For SMS, this can also be a Messaging Service SID.
  string from = 1;
  // JSON data that will be added to the Flow's context and that can be accessed as variables inside your Flow. For example, if you pass in `Parameters={"name":"Zeke"}`, a widget in your Flow can reference the variable `{{flow.data.name}}`, which returns "Zeke". Note: the JSON value must explicitly be passed as a string, not as a hash object. Depending on your particular HTTP library, you may need to add quotes or URL encode the JSON string.
  string parameters = 2;
  // The Contact phone number to start a Studio Flow Execution, available as variable `{{contact.channel.address}}`.
  string to_field = 3;
}

// Triggers a new Execution for the Flow
message CreateExecutionRequest {
  // The SID of the Excecution's Flow. Required.
  string flow_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateExecutionRequestBody body = 2;
}

// Canonical success response.
message CreateExecutionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  StudioV1FlowExecution body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Retrieve the most recent context for an Execution.
message FetchExecutionContextRequest {
  // The SID of the Flow with the Execution context to fetch. Required.
  string flow_sid = 1;
  // The SID of the Execution context to fetch. Required.
  string execution_sid = 2;
}

// Canonical success response.
message FetchExecutionContextResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  StudioV1FlowExecutionExecutionContext body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListExecutionStepResponse
message ListExecutionStepResponseBody {
  ListExecutionStepResponseBodyMeta meta = 1;
  repeated StudioV1FlowExecutionExecutionStep steps = 2;
}

message ListExecutionStepResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Steps for an Execution.
message ListExecutionStepRequest {
  // The SID of the Flow with the Steps to read. Required.
  string flow_sid = 1;
  // The SID of the Execution with the Steps to read. Required.
  string execution_sid = 2;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 3;
  // The page index. This value is simply for client state.
  int64 page = 4;
  // The page token. This is provided by the API.
  string page_token = 5;
}

// Canonical success response.
message ListExecutionStepResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListExecutionStepResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Retrieve a Step.
message FetchExecutionStepRequest {
  // The SID of the Flow with the Step to fetch. Required.
  string flow_sid = 1;
  // The SID of the Execution resource with the Step to fetch. Required.
  string execution_sid = 2;
  // The SID of the ExecutionStep resource to fetch. Required.
  string sid = 3;
}

// Canonical success response.
message FetchExecutionStepResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  StudioV1FlowExecutionExecutionStep body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Retrieve the context for an Execution Step.
message FetchExecutionStepContextRequest {
  // The SID of the Flow with the Step to fetch. Required.
  string flow_sid = 1;
  // The SID of the Execution resource with the Step to fetch. Required.
  string execution_sid = 2;
  // The SID of the Step to fetch. Required.
  string step_sid = 3;
}

// Canonical success response.
message FetchExecutionStepContextResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  StudioV1FlowExecutionExecutionStepExecutionStepContext body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Retrieve an Execution
message FetchExecutionRequest {
  // The SID of the Flow with the Execution resource to fetch Required.
  string flow_sid = 1;
  // The SID of the Execution resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchExecutionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  StudioV1FlowExecution body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateExecutionRequest
message UpdateExecutionRequestBody {
  // OpenAPI property "Status".
  ExecutionEnumStatus status = 1;
}

// Update the status of an Execution to `ended`.
message UpdateExecutionRequest {
  // The SID of the Flow with the Execution resources to update. Required.
  string flow_sid = 1;
  // The SID of the Execution resource to update. Required.
  string sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateExecutionRequestBody body = 3;
}

// Canonical success response.
message UpdateExecutionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  StudioV1FlowExecution body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete the Execution and all Steps relating to it.
message DeleteExecutionRequest {
  // The SID of the Flow with the Execution resources to delete. Required.
  string flow_sid = 1;
  // The SID of the Execution resource to delete. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteExecutionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// Retrieve a specific Flow.
message FetchFlowRequest {
  // The SID of the Flow resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchFlowResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  StudioV1Flow body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete a specific Flow.
message DeleteFlowRequest {
  // The SID of the Flow resource to delete. Required.
  string sid = 1;
}

// Canonical success response.
message DeleteFlowResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// Generated from StudioV1Flow operations.
service StudioV1FlowService {
  // Retrieve a list of all Flows.
  rpc ListFlow(ListFlowRequest) returns (ListFlowResponse) {
    option (google.api.http) = {
      get: "/v1/Flows"
      response_body: "body"
    };
  }
  // Retrieve a specific Flow.
  rpc FetchFlow(FetchFlowRequest) returns (FetchFlowResponse) {
    option (google.api.http) = {
      get: "/v1/Flows/{sid}"
      response_body: "body"
    };
  }
  // Delete a specific Flow.
  rpc DeleteFlow(DeleteFlowRequest) returns (DeleteFlowResponse) {
    option (google.api.http) = {
      delete: "/v1/Flows/{sid}"
    };
  }
}

// Generated from StudioV1Engagement operations.
service StudioV1EngagementService {
  // Retrieve a list of all Engagements for the Flow.
  rpc ListEngagement(ListEngagementRequest) returns (ListEngagementResponse) {
    option (google.api.http) = {
      get: "/v1/Flows/{flow_sid}/Engagements"
      response_body: "body"
    };
  }
  // Triggers a new Engagement for the Flow
  rpc CreateEngagement(CreateEngagementRequest) returns (CreateEngagementResponse) {
    option (google.api.http) = {
      post: "/v1/Flows/{flow_sid}/Engagements"
      body: "body"
      response_body: "body"
    };
  }
  // Retrieve an Engagement
  rpc FetchEngagement(FetchEngagementRequest) returns (FetchEngagementResponse) {
    option (google.api.http) = {
      get: "/v1/Flows/{flow_sid}/Engagements/{sid}"
      response_body: "body"
    };
  }
  // Delete this Engagement and all Steps relating to it.
  rpc DeleteEngagement(DeleteEngagementRequest) returns (DeleteEngagementResponse) {
    option (google.api.http) = {
      delete: "/v1/Flows/{flow_sid}/Engagements/{sid}"
    };
  }
}

// Generated from StudioV1EngagementContext operations.
service StudioV1EngagementContextService {
  // Retrieve the most recent context for an Engagement.
  rpc FetchEngagementContext(FetchEngagementContextRequest) returns (FetchEngagementContextResponse) {
    option (google.api.http) = {
      get: "/v1/Flows/{flow_sid}/Engagements/{engagement_sid}/Context"
      response_body: "body"
    };
  }
}

// Generated from StudioV1Step operations.
service StudioV1StepService {
  // Retrieve a list of all Steps for an Engagement.
  rpc ListStep(ListStepRequest) returns (ListStepResponse) {
    option (google.api.http) = {
      get: "/v1/Flows/{flow_sid}/Engagements/{engagement_sid}/Steps"
      response_body: "body"
    };
  }
  // Retrieve a Step.
  rpc FetchStep(FetchStepRequest) returns (FetchStepResponse) {
    option (google.api.http) = {
      get: "/v1/Flows/{flow_sid}/Engagements/{engagement_sid}/Steps/{sid}"
      response_body: "body"
    };
  }
}

// Generated from StudioV1StepContext operations.
service StudioV1StepContextService {
  // Retrieve the context for an Engagement Step.
  rpc FetchStepContext(FetchStepContextRequest) returns (FetchStepContextResponse) {
    option (google.api.http) = {
      get: "/v1/Flows/{flow_sid}/Engagements/{engagement_sid}/Steps/{step_sid}/Context"
      response_body: "body"
    };
  }
}

// Generated from StudioV1Execution operations.
service StudioV1ExecutionService {
  // Retrieve a list of all Executions for the Flow.
  rpc ListExecution(ListExecutionRequest) returns (ListExecutionResponse) {
    option (google.api.http) = {
      get: "/v1/Flows/{flow_sid}/Executions"
      response_body: "body"
    };
  }
  // Triggers a new Execution for the Flow
  rpc CreateExecution(CreateExecutionRequest) returns (CreateExecutionResponse) {
    option (google.api.http) = {
      post: "/v1/Flows/{flow_sid}/Executions"
      body: "body"
      response_body: "body"
    };
  }
  // Retrieve an Execution
  rpc FetchExecution(FetchExecutionRequest) returns (FetchExecutionResponse) {
    option (google.api.http) = {
      get: "/v1/Flows/{flow_sid}/Executions/{sid}"
      response_body: "body"
    };
  }
  // Update the status of an Execution to `ended`.
  rpc UpdateExecution(UpdateExecutionRequest) returns (UpdateExecutionResponse) {
    option (google.api.http) = {
      post: "/v1/Flows/{flow_sid}/Executions/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete the Execution and all Steps relating to it.
  rpc DeleteExecution(DeleteExecutionRequest) returns (DeleteExecutionResponse) {
    option (google.api.http) = {
      delete: "/v1/Flows/{flow_sid}/Executions/{sid}"
    };
  }
}

// Generated from StudioV1ExecutionContext operations.
service StudioV1ExecutionContextService {
  // Retrieve the most recent context for an Execution.
  rpc FetchExecutionContext(FetchExecutionContextRequest) returns (FetchExecutionContextResponse) {
    option (google.api.http) = {
      get: "/v1/Flows/{flow_sid}/Executions/{execution_sid}/Context"
      response_body: "body"
    };
  }
}

// Generated from StudioV1ExecutionStep operations.
service StudioV1ExecutionStepService {
  // Retrieve a list of all Steps for an Execution.
  rpc ListExecutionStep(ListExecutionStepRequest) returns (ListExecutionStepResponse) {
    option (google.api.http) = {
      get: "/v1/Flows/{flow_sid}/Executions/{execution_sid}/Steps"
      response_body: "body"
    };
  }
  // Retrieve a Step.
  rpc FetchExecutionStep(FetchExecutionStepRequest) returns (FetchExecutionStepResponse) {
    option (google.api.http) = {
      get: "/v1/Flows/{flow_sid}/Executions/{execution_sid}/Steps/{sid}"
      response_body: "body"
    };
  }
}

// Generated from StudioV1ExecutionStepContext operations.
service StudioV1ExecutionStepContextService {
  // Retrieve the context for an Execution Step.
  rpc FetchExecutionStepContext(FetchExecutionStepContextRequest) returns (FetchExecutionStepContextResponse) {
    option (google.api.http) = {
      get: "/v1/Flows/{flow_sid}/Executions/{execution_sid}/Steps/{step_sid}/Context"
      response_body: "body"
    };
  }
}

