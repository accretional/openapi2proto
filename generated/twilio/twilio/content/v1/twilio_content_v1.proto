// Code generated by openapi2proto. DO NOT EDIT.
// Source: twilio_content_v1.json

syntax = "proto3";

package twilio.content.v1;

option go_package = "twilio/content/v1;twiliocontentv1";

import "google/api/annotations.proto";

// Content approval request body
message ContentApprovalRequest {
  // A WhatsApp recognized template category.
  string category = 1;
  // Name of the template.
  string name = 2;
}

// Content creation request body
message ContentCreateRequest {
  // User defined name of the content
  string friendly_name = 1;
  // Language code for the content
  string language = 2;
  // OpenAPI property "types".
  Types types = 3;
  // Key value pairs of variable name to value
  map<string, string> variables = 4;
}

// Content types
message Types {
  // OpenAPI property "twilio/call-to-action".
  TwilioCallToAction twilio_call_to_action = 1;
  // OpenAPI property "twilio/card".
  TwilioCard twilio_card = 2;
  // OpenAPI property "twilio/carousel".
  TwilioCarousel twilio_carousel = 3;
  // OpenAPI property "twilio/catalog".
  TwilioCatalog twilio_catalog = 4;
  // OpenAPI property "twilio/flows".
  TwilioFlows twilio_flows = 5;
  // OpenAPI property "twilio/list-picker".
  TwilioListPicker twilio_list_picker = 6;
  // OpenAPI property "twilio/location".
  TwilioLocation twilio_location = 7;
  // OpenAPI property "twilio/media".
  TwilioMedia twilio_media = 8;
  // OpenAPI property "twilio/quick-reply".
  TwilioQuickReply twilio_quick_reply = 9;
  // OpenAPI property "twilio/schedule".
  TwilioSchedule twilio_schedule = 10;
  // OpenAPI property "twilio/text".
  TwilioText twilio_text = 11;
  // OpenAPI property "whatsapp/authentication".
  WhatsappAuthentication whatsapp_authentication = 12;
  // OpenAPI property "whatsapp/card".
  WhatsappCard whatsapp_card = 13;
  // OpenAPI property "whatsapp/flows".
  WhatsappFlows whatsapp_flows = 14;
}

// twilio/call-to-action buttons let recipients tap to trigger actions such as launching a website or making a phone call.
message TwilioCallToAction {
  // OpenAPI property "actions".
  CallToActionActions actions = 1;
  string body = 2;
}

message CallToActionActions {
  // Array values.
  repeated CallToActionAction items = 1;
}

message CallToActionAction {
  string code = 1;
  string id = 2;
  string phone = 3;
  string title = 4;
  // OpenAPI property "type".
  CallToActionActionType type = 5;
  string url = 6;
}

message CallToActionActionType {
  // Scalar value wrapper.
  string value = 1;
}

// twilio/card is a structured template which can be used to send a series of related information. It must include a title and at least one additional field.
message TwilioCard {
  // OpenAPI property "actions".
  CardActions actions = 1;
  repeated string media = 2;
  string subtitle = 3;
  string title = 4;
}

message CardActions {
  // Array values.
  repeated CardAction items = 1;
}

message CardAction {
  string code = 1;
  string id = 2;
  string phone = 3;
  string title = 4;
  // OpenAPI property "type".
  CardActionType type = 5;
  string url = 6;
  // OpenAPI property "webview_size".
  WebviewSizeType webview_size = 7;
}

message CardActionType {
  // Scalar value wrapper.
  string value = 1;
}

message WebviewSizeType {
  // Scalar value wrapper.
  string value = 1;
}

// twilio/carousel templates allow you to send a single text message accompanied by a set of up to 10 carousel cards in a horizontally scrollable view
message TwilioCarousel {
  string body = 1;
  // OpenAPI property "cards".
  CarouselCards cards = 2;
}

message CarouselCards {
  // Array values.
  repeated CarouselCard items = 1;
}

message CarouselCard {
  // OpenAPI property "actions".
  CarouselActions actions = 1;
  string body = 2;
  string media = 3;
  string title = 4;
}

message CarouselActions {
  // Array values.
  repeated CarouselAction items = 1;
}

message CarouselAction {
  string id = 1;
  string phone = 2;
  string title = 3;
  // OpenAPI property "type".
  CarouselActionType type = 4;
  string url = 5;
}

message CarouselActionType {
  // Scalar value wrapper.
  string value = 1;
}

// twilio/catalog type lets recipients view list of catalog products, ask questions about products, order products.
message TwilioCatalog {
  string body = 1;
  string dynamic_items = 2;
  string id = 3;
  // OpenAPI property "items".
  CatalogItems items = 4;
  string subtitle = 5;
  string title = 6;
}

message CatalogItems {
  // Array values.
  repeated CatalogItem items = 1;
}

message CatalogItem {
  string description = 1;
  string id = 2;
  string media_url = 3;
  string name = 4;
  double price = 5;
  string section_title = 6;
}

// twilio/flows templates allow you to send multiple messages in a set order with text or select options
message TwilioFlows {
  string body = 1;
  string button_text = 2;
  string media_url = 3;
  // OpenAPI property "pages".
  FlowsPages pages = 4;
  string subtitle = 5;
  string type = 6;
}

message FlowsPages {
  // Array values.
  repeated FlowsPage items = 1;
}

message FlowsPage {
  string id = 1;
  // OpenAPI property "layout".
  FlowsPageComponents layout = 2;
  string next_page_id = 3;
  string subtitle = 4;
  string title = 5;
}

message FlowsPageComponents {
  // Array values.
  repeated FlowsPageComponent items = 1;
}

message FlowsPageComponent {
  string label = 1;
  string type = 2;
}

// twilio/list-picker includes a menu of up to 10 options, which offers a simple way for users to make a selection.
message TwilioListPicker {
  string body = 1;
  string button = 2;
  // OpenAPI property "items".
  ListItems items = 3;
}

message ListItems {
  // Array values.
  repeated ListItem items = 1;
}

message ListItem {
  string description = 1;
  string id = 2;
  string item = 3;
}

// twilio/location type contains a location pin and an optional label, which can be used to enhance delivery notifications or connect recipients to physical experiences you offer.
message TwilioLocation {
  string address = 1;
  string id = 2;
  string label = 3;
  double latitude = 4;
  double longitude = 5;
}

// twilio/media is used to send file attachments, or to send long text via MMS in the US and Canada. As such, the twilio/media type must contain at least ONE of text or media content.
message TwilioMedia {
  string body = 1;
  repeated string media = 2;
}

// twilio/quick-reply templates let recipients tap, rather than type, to respond to the message.
message TwilioQuickReply {
  // OpenAPI property "actions".
  QuickReplyActions actions = 1;
  string body = 2;
}

message QuickReplyActions {
  // Array values.
  repeated QuickReplyAction items = 1;
}

message QuickReplyAction {
  string id = 1;
  string title = 2;
  // OpenAPI property "type".
  QuickReplyActionType type = 3;
}

message QuickReplyActionType {
  // Scalar value wrapper.
  string value = 1;
}

// twilio/schedule templates allow us to send a message with a schedule with different time slots
message TwilioSchedule {
  string id = 1;
  string time_slots = 2;
  string title = 3;
}

// Type containing only plain text-based content
message TwilioText {
  string body = 1;
}

// whatsApp/authentication templates let companies deliver WA approved one-time-password button.
message WhatsappAuthentication {
  // OpenAPI property "actions".
  AuthenticationActions actions = 1;
  bool add_security_recommendation = 2;
  double code_expiration_minutes = 3;
}

message AuthenticationActions {
  // Array values.
  repeated AuthenticationAction items = 1;
}

message AuthenticationAction {
  string copy_code_text = 1;
  // OpenAPI property "type".
  AuthenticationActionType type = 2;
}

message AuthenticationActionType {
  // Scalar value wrapper.
  string value = 1;
}

// whatsapp/card is a structured template which can be used to send a series of related information. It must include a body and at least one additional field.
message WhatsappCard {
  // OpenAPI property "actions".
  CardActions actions = 1;
  string body = 2;
  string footer = 3;
  string header_text = 4;
  repeated string media = 5;
}

// whatsapp/flows templates allow you to send multiple messages in a set order with text or select options
message WhatsappFlows {
  string body = 1;
  string button_text = 2;
  string flow_first_page_id = 3;
  string flow_id = 4;
  string flow_token = 5;
  bool is_flow_first_page_endpoint = 6;
  string media_url = 7;
  string subtitle = 8;
}

// Content update request body
message ContentUpdateRequest {
  // User defined name of the content
  string friendly_name = 1;
  // Language code for the content
  string language = 2;
  // OpenAPI property "types".
  Types types = 3;
  // Key value pairs of variable name to value
  map<string, string> variables = 4;
}

message ContentV1Content {
  // The SID of the [Account](https://www.twilio.com/docs/usage/api/account) that created Content resource.
  string account_sid = 1;
  // The date and time in GMT that the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 2;
  // The date and time in GMT that the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 3;
  // A string name used to describe the Content resource. Not visible to the end recipient.
  string friendly_name = 4;
  // Two-letter (ISO 639-1) language code (e.g., en) identifying the language the Content resource is in.
  string language = 5;
  // A list of links related to the Content resource, such as approval_fetch and approval_create
  ContentV1ContentLinks links = 6;
  // The unique string that that we created to identify the Content resource.
  string sid = 7;
  // The [Content types](https://www.twilio.com/docs/content-api/content-types-overview) (e.g. twilio/text) for this Content resource.
  ContentV1ContentTypes types = 8;
  // The URL of the resource, relative to `https://content.twilio.com`.
  string url = 9;
  // Defines the default placeholder values for variables included in the Content resource. e.g. {"1": "Customer_Name"}.
  ContentV1ContentVariables variables = 10;
}

// A list of links related to the Content resource, such as approval_fetch and approval_create
message ContentV1ContentLinks {
  // Scalar value wrapper.
  string value = 1;
}

// The [Content types](https://www.twilio.com/docs/content-api/content-types-overview) (e.g. twilio/text) for this Content resource.
message ContentV1ContentTypes {
  // Scalar value wrapper.
  string value = 1;
}

// Defines the default placeholder values for variables included in the Content resource. e.g. {"1": "Customer_Name"}.
message ContentV1ContentVariables {
  // Scalar value wrapper.
  string value = 1;
}

message ContentV1ContentApprovalCreate {
  bool allow_category_change = 1;
  string category = 2;
  string content_type = 3;
  string name = 4;
  string rejection_reason = 5;
  string status = 6;
}

message ContentV1ContentApprovalFetch {
  // The SID of the [Account](https://www.twilio.com/docs/usage/api/account) that created Content resource.
  string account_sid = 1;
  // The unique string that that we created to identify the Content resource.
  string sid = 2;
  // The URL of the resource, relative to `https://content.twilio.com`.
  string url = 3;
  // Contains the whatsapp approval information for the Content resource, with fields such as approval status, rejection reason, and category, amongst others.
  ContentV1ContentApprovalFetchWhatsapp whatsapp = 4;
}

// Contains the whatsapp approval information for the Content resource, with fields such as approval status, rejection reason, and category, amongst others.
message ContentV1ContentApprovalFetchWhatsapp {
  // Scalar value wrapper.
  string value = 1;
}

message ContentV1ContentAndApprovals {
  // The SID of the [Account](https://www.twilio.com/docs/usage/api/account) that created Content resource.
  string account_sid = 1;
  // The submitted information and approval request status of the Content resource.
  ContentV1ContentAndApprovalsApprovalRequests approval_requests = 2;
  // The date and time in GMT that the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 3;
  // The date and time in GMT that the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 4;
  // A string name used to describe the Content resource. Not visible to the end recipient.
  string friendly_name = 5;
  // Two-letter (ISO 639-1) language code (e.g., en) identifying the language the Content resource is in.
  string language = 6;
  // The unique string that that we created to identify the Content resource.
  string sid = 7;
  // The [Content types](https://www.twilio.com/docs/content-api/content-types-overview) (e.g. twilio/text) for this Content resource.
  ContentV1ContentAndApprovalsTypes types = 8;
  // Defines the default placeholder values for variables included in the Content resource. e.g. {"1": "Customer_Name"}.
  ContentV1ContentAndApprovalsVariables variables = 9;
}

// The submitted information and approval request status of the Content resource.
message ContentV1ContentAndApprovalsApprovalRequests {
  // Scalar value wrapper.
  string value = 1;
}

// The [Content types](https://www.twilio.com/docs/content-api/content-types-overview) (e.g. twilio/text) for this Content resource.
message ContentV1ContentAndApprovalsTypes {
  // Scalar value wrapper.
  string value = 1;
}

// Defines the default placeholder values for variables included in the Content resource. e.g. {"1": "Customer_Name"}.
message ContentV1ContentAndApprovalsVariables {
  // Scalar value wrapper.
  string value = 1;
}

message ContentV1LegacyContent {
  // The SID of the [Account](https://www.twilio.com/docs/usage/api/account) that created Content resource.
  string account_sid = 1;
  // The date and time in GMT that the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 2;
  // The date and time in GMT that the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 3;
  // A string name used to describe the Content resource. Not visible to the end recipient.
  string friendly_name = 4;
  // Two-letter (ISO 639-1) language code (e.g., en) identifying the language the Content resource is in.
  string language = 5;
  // The string body field of the legacy content template associated with this Content resource
  string legacy_body = 6;
  // The string name of the legacy content template associated with this Content resource, unique across all template names for its account.  Only lowercase letters, numbers and underscores are allowed
  string legacy_template_name = 7;
  // The unique string that that we created to identify the Content resource.
  string sid = 8;
  // The [Content types](https://www.twilio.com/docs/content-api/content-types-overview) (e.g. twilio/text) for this Content resource.
  ContentV1LegacyContentTypes types = 9;
  // The URL of the resource, relative to `https://content.twilio.com`.
  string url = 10;
  // Defines the default placeholder values for variables included in the Content resource. e.g. {"1": "Customer_Name"}.
  ContentV1LegacyContentVariables variables = 11;
}

// The [Content types](https://www.twilio.com/docs/content-api/content-types-overview) (e.g. twilio/text) for this Content resource.
message ContentV1LegacyContentTypes {
  // Scalar value wrapper.
  string value = 1;
}

// Defines the default placeholder values for variables included in the Content resource. e.g. {"1": "Customer_Name"}.
message ContentV1LegacyContentVariables {
  // Scalar value wrapper.
  string value = 1;
}

// ListContentResponse
message ListContentResponseBody {
  repeated ContentV1Content contents = 1;
  ListContentResponseBodyMeta meta = 2;
}

message ListContentResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// List Contents
message ListContentRequest {
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 1;
  // The page index. This value is simply for client state.
  int64 page = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
}

// Canonical success response.
message ListContentResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListContentResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Create Content
message CreateContentRequest {
  // Request body from media type "application/json".
  ContentCreateRequest body = 1;
}

// Canonical success response.
message CreateContentResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ContentV1Content body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Create Content Approval Request
message CreateApprovalCreateRequest {
  // OpenAPI path parameter "ContentSid". Required.
  string content_sid = 1;
  // Request body from media type "application/json".
  ContentApprovalRequest body = 2;
}

// Canonical success response.
message CreateApprovalCreateResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ContentV1ContentApprovalCreate body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch Content
message FetchContentRequest {
  // The Twilio-provided string that uniquely identifies the Content resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchContentResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ContentV1Content body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Update Content
message UpdateContentRequest {
  // The Twilio-provided string that uniquely identifies the Content resource to update. Required.
  string sid = 1;
  // Request body from media type "application/json".
  ContentUpdateRequest body = 2;
}

// Canonical success response.
message UpdateContentResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ContentV1Content body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete Content
message DeleteContentRequest {
  // The Twilio-provided string that uniquely identifies the Content resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message DeleteContentResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// Fetch Approval Status
message FetchApprovalFetchRequest {
  // The Twilio-provided string that uniquely identifies the Content resource whose approval information to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchApprovalFetchResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ContentV1ContentApprovalFetch body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListContentAndApprovalsResponse
message ListContentAndApprovalsResponseBody {
  repeated ContentV1ContentAndApprovals contents = 1;
  ListContentAndApprovalsResponseBodyMeta meta = 2;
}

message ListContentAndApprovalsResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// List Content Approvals
message ListContentAndApprovalsRequest {
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 1;
  // The page index. This value is simply for client state.
  int64 page = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
}

// Canonical success response.
message ListContentAndApprovalsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListContentAndApprovalsResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListLegacyContentResponse
message ListLegacyContentResponseBody {
  repeated ContentV1LegacyContent contents = 1;
  ListLegacyContentResponseBodyMeta meta = 2;
}

message ListLegacyContentResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// List Legacy Contents
message ListLegacyContentRequest {
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 1;
  // The page index. This value is simply for client state.
  int64 page = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
}

// Canonical success response.
message ListLegacyContentResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListLegacyContentResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Generated from Contentv1Content operations.
service Contentv1ContentService {
  // List Contents
  rpc ListContent(ListContentRequest) returns (ListContentResponse) {
    option (google.api.http) = {
      get: "/v1/Content"
      response_body: "body"
    };
  }
  // Create Content
  rpc CreateContent(CreateContentRequest) returns (CreateContentResponse) {
    option (google.api.http) = {
      post: "/v1/Content"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch Content
  rpc FetchContent(FetchContentRequest) returns (FetchContentResponse) {
    option (google.api.http) = {
      get: "/v1/Content/{sid}"
      response_body: "body"
    };
  }
  // Update Content
  rpc UpdateContent(UpdateContentRequest) returns (UpdateContentResponse) {
    option (google.api.http) = {
      put: "/v1/Content/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete Content
  rpc DeleteContent(DeleteContentRequest) returns (DeleteContentResponse) {
    option (google.api.http) = {
      delete: "/v1/Content/{sid}"
    };
  }
}

// Generated from Contentv1ApprovalCreate operations.
service Contentv1ApprovalCreateService {
  // Create Content Approval Request
  rpc CreateApprovalCreate(CreateApprovalCreateRequest) returns (CreateApprovalCreateResponse) {
    option (google.api.http) = {
      post: "/v1/Content/{content_sid}/ApprovalRequests/whatsapp"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from Contentv1ApprovalFetch operations.
service Contentv1ApprovalFetchService {
  // Fetch Approval Status
  rpc FetchApprovalFetch(FetchApprovalFetchRequest) returns (FetchApprovalFetchResponse) {
    option (google.api.http) = {
      get: "/v1/Content/{sid}/ApprovalRequests"
      response_body: "body"
    };
  }
}

// Generated from Contentv1ContentAndApprovals operations.
service Contentv1ContentAndApprovalsService {
  // List Content Approvals
  rpc ListContentAndApprovals(ListContentAndApprovalsRequest) returns (ListContentAndApprovalsResponse) {
    option (google.api.http) = {
      get: "/v1/ContentAndApprovals"
      response_body: "body"
    };
  }
}

// Generated from Contentv1LegacyContent operations.
service Contentv1LegacyContentService {
  // List Legacy Contents
  rpc ListLegacyContent(ListLegacyContentRequest) returns (ListLegacyContentResponse) {
    option (google.api.http) = {
      get: "/v1/LegacyContent"
      response_body: "body"
    };
  }
}

