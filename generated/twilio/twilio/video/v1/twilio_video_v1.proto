// Code generated by openapi2proto. DO NOT EDIT.
// Source: twilio_video_v1.json

syntax = "proto3";

package twilio.video.v1;

option go_package = "twilio/video/v1;twiliovideov1";

import "google/api/annotations.proto";

// The container format of the composition's media files as specified in the POST request that created the Composition resource. See [POST Parameters](https://www.twilio.com/docs/video/api/compositions-resource#http-post-parameters) for more information.
message CompositionEnumFormat {
  // Scalar value wrapper.
  string value = 1;
}

// The status of the composition. Can be: `enqueued`, `processing`, `completed`, `deleted` or `failed`. `enqueued` is the initial state and indicates that the composition request has been received and is scheduled for processing; `processing` indicates the composition is being processed; `completed` indicates the composition has been completed and is available for download; `deleted` means the composition media has been deleted from the system, but its metadata is still available for 30 days; `failed` indicates the composition failed to execute the media processing task.
message CompositionEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

// The container format of the media files used by the compositions created by the composition hook. If `mp4` or `webm`, `audio_sources` must have one or more tracks and/or a `video_layout` element must contain a valid `video_sources` list, otherwise an error occurs.
message CompositionHookEnumFormat {
  // Scalar value wrapper.
  string value = 1;
}

// The codec used to encode the track. Can be: `VP8`, `H264`, `OPUS`, and `PCMU`.
message RecordingEnumCodec {
  // Scalar value wrapper.
  string value = 1;
}

message RecordingEnumFormat {
  // Scalar value wrapper.
  string value = 1;
}

// The status of the recording. Can be: `processing`, `completed`, or `deleted`. `processing` indicates the recording is still being captured; `completed` indicates the recording has been captured and is now available for download. `deleted` means the recording media has been deleted from the system, but its metadata is still available.
message RecordingEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

// The recording's media type. Can be: `audio` or `video`.
message RecordingEnumType {
  // Scalar value wrapper.
  string value = 1;
}

message RoomEnumRoomStatus {
  // Scalar value wrapper.
  string value = 1;
}

message RoomEnumRoomType {
  // Scalar value wrapper.
  string value = 1;
}

message RoomEnumVideoCodec {
  // Scalar value wrapper.
  string value = 1;
}

// The status of the Participant. Can be: `connected` or `disconnected`.
message RoomParticipantAnonymizeEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

// The status of the Participant. Can be: `connected` or `disconnected`.
message RoomParticipantEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

// The track type. Can be: `audio`, `video` or `data`.
message RoomParticipantPublishedTrackEnumKind {
  // Scalar value wrapper.
  string value = 1;
}

// The track type. Can be: `audio`, `video` or `data`.
message RoomParticipantSubscribedTrackEnumKind {
  // Scalar value wrapper.
  string value = 1;
}

// The codec used for the recording. Can be: `VP8` or `H264`.
message RoomRecordingEnumCodec {
  // Scalar value wrapper.
  string value = 1;
}

message RoomRecordingEnumFormat {
  // Scalar value wrapper.
  string value = 1;
}

// The status of the recording. Can be: `processing`, `completed`, or `deleted`. `processing` indicates the Recording is still being captured. `completed` indicates the Recording has been captured and is now available for download. `deleted` means the recording media has been deleted from the system, but its metadata is still available for historical purposes.
message RoomRecordingEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

// The recording's media type. Can be: `audio` or `video`.
message RoomRecordingEnumType {
  // Scalar value wrapper.
  string value = 1;
}

// The status of the transcriptions resource.
message RoomTranscriptionsEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

message VideoV1Composition {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Composition resource.
  string account_sid = 1;
  // The array of track names to include in the composition. The composition includes all audio sources specified in `audio_sources` except those specified in `audio_sources_excluded`. The track names in this property can include an asterisk as a wild card character, which matches zero or more characters in a track name. For example, `student*` includes tracks named `student` as well as `studentTeam`.
  repeated string audio_sources = 2;
  // The array of track names to exclude from the composition. The composition includes all audio sources specified in `audio_sources` except for those specified in `audio_sources_excluded`. The track names in this property can include an asterisk as a wild card character, which matches zero or more characters in a track name. For example, `student*` excludes `student` as well as `studentTeam`. This parameter can also be empty.
  repeated string audio_sources_excluded = 3;
  // The average bit rate of the composition's media.
  int64 bitrate = 4;
  // The date and time in GMT when the composition's media processing task finished, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_completed = 5;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 6;
  // The date and time in GMT when the composition generated media was deleted, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_deleted = 7;
  // The duration of the composition's media file in seconds.
  int64 duration = 8;
  // OpenAPI property "format".
  CompositionEnumFormat format = 9;
  // The URL of the media file associated with the composition.
  VideoV1CompositionLinks links = 10;
  // The URL of the media file associated with the composition when stored externally. See [External S3 Compositions](/docs/video/api/external-s3-compositions) for more details.
  string media_external_location = 11;
  // The dimensions of the video image in pixels expressed as columns (width) and rows (height). The string's format is `{width}x{height}`, such as `640x480`.
  string resolution = 12;
  // The SID of the Group Room that generated the audio and video tracks used in the composition. All media sources included in a composition must belong to the same Group Room.
  string room_sid = 13;
  // The unique string that we created to identify the Composition resource.
  string sid = 14;
  // The size of the composed media file in bytes.
  int64 size = 15;
  // OpenAPI property "status".
  CompositionEnumStatus status = 16;
  // The URL called using the `status_callback_method` to send status information on every composition event.
  string status_callback = 17;
  // The HTTP method used to call `status_callback`. Can be: `POST` or `GET`, defaults to `POST`.
  string status_callback_method = 18;
  // Whether to remove intervals with no media, as specified in the POST request that created the composition. Compositions with `trim` enabled are shorter when the Room is created and no Participant joins for a while as well as if all the Participants leave the room and join later, because those gaps will be removed. See [Specifying Video Layouts](https://www.twilio.com/docs/video/api/compositions-resource#specifying-video-layouts) for more info.
  bool trim = 19;
  // The absolute URL of the resource.
  string url = 20;
  // An object that describes the video layout of the composition in terms of regions. See [Specifying Video Layouts](https://www.twilio.com/docs/video/api/compositions-resource#specifying-video-layouts) for more info.
  string video_layout = 21;
}

// The URL of the media file associated with the composition.
message VideoV1CompositionLinks {
  // Scalar value wrapper.
  string value = 1;
}

message VideoV1CompositionHook {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the CompositionHook resource.
  string account_sid = 1;
  // The array of track names to include in the compositions created by the composition hook. A composition triggered by the composition hook includes all audio sources specified in `audio_sources` except those specified in `audio_sources_excluded`. The track names in this property can include an asterisk as a wild card character, which matches zero or more characters in a track name. For example, `student*` includes tracks named `student` as well as `studentTeam`. Please, be aware that either video_layout or audio_sources have to be provided to get a valid creation request
  repeated string audio_sources = 2;
  // The array of track names to exclude from the compositions created by the composition hook. A composition triggered by the composition hook includes all audio sources specified in `audio_sources` except for those specified in `audio_sources_excluded`. The track names in this property can include an asterisk as a wild card character, which matches zero or more characters in a track name. For example, `student*` excludes `student` as well as `studentTeam`. This parameter can also be empty.
  repeated string audio_sources_excluded = 3;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 4;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 5;
  // Whether the CompositionHook is active. When `true`, the CompositionHook is triggered for every completed Group Room on the account. When `false`, the CompositionHook is never triggered.
  bool enabled = 6;
  // OpenAPI property "format".
  CompositionHookEnumFormat format = 7;
  // The string that you assigned to describe the resource. Can be up to 100 characters long and must be unique within the account.
  string friendly_name = 8;
  // The dimensions of the video image in pixels expressed as columns (width) and rows (height). The string's format is `{width}x{height}`, such as `640x480`.
  string resolution = 9;
  // The unique string that we created to identify the CompositionHook resource.
  string sid = 10;
  // The URL we call using the `status_callback_method` to send status information to your application.
  string status_callback = 11;
  // The HTTP method we should use to call `status_callback`. Can be `POST` or `GET` and defaults to `POST`.
  string status_callback_method = 12;
  // Whether intervals with no media are clipped, as specified in the POST request that created the CompositionHook resource. Compositions with `trim` enabled are shorter when the Room is created and no Participant joins for a while as well as if all the Participants leave the room and join later, because those gaps will be removed. See [Specifying Video Layouts](https://www.twilio.com/docs/video/api/compositions-resource#specifying-video-layouts) for more info.
  bool trim = 13;
  // The absolute URL of the resource.
  string url = 14;
  // A JSON object that describes the video layout of the composition in terms of regions as specified in the HTTP POST request that created the CompositionHook resource. See [POST Parameters](https://www.twilio.com/docs/video/api/compositions-resource#http-post-parameters) for more information. Please, be aware that either video_layout or audio_sources have to be provided to get a valid creation request
  string video_layout = 15;
}

message VideoV1CompositionSettings {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the CompositionSettings resource.
  string account_sid = 1;
  // The SID of the stored Credential resource.
  string aws_credentials_sid = 2;
  // The URL of the AWS S3 bucket where the compositions are stored. We only support DNS-compliant URLs like `https://documentation-example-twilio-bucket/compositions`, where `compositions` is the path in which you want the compositions to be stored. This URL accepts only URI-valid characters, as described in the [RFC 3986](https://tools.ietf.org/html/rfc3986#section-2).
  string aws_s3_url = 3;
  // Whether all compositions are written to the `aws_s3_url`. When `false`, all compositions are stored in our cloud.
  bool aws_storage_enabled = 4;
  // Whether all compositions are stored in an encrypted form. The default is `false`.
  bool encryption_enabled = 5;
  // The SID of the Public Key resource used for encryption.
  string encryption_key_sid = 6;
  // The string that you assigned to describe the resource and that will be shown in the console
  string friendly_name = 7;
  // The absolute URL of the resource.
  string url = 8;
}

message VideoV1Recording {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Recording resource.
  string account_sid = 1;
  // OpenAPI property "codec".
  RecordingEnumCodec codec = 2;
  // OpenAPI property "container_format".
  RecordingEnumFormat container_format = 3;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 4;
  // The duration of the recording in seconds rounded to the nearest second. Sub-second tracks have a `Duration` property of 1 second
  int64 duration = 5;
  // A list of SIDs related to the recording. Includes the `room_sid` and `participant_sid`.
  string grouping_sids = 6;
  // The URLs of related resources.
  VideoV1RecordingLinks links = 7;
  // The URL of the media file associated with the recording when stored externally. See [External S3 Recordings](/docs/video/api/external-s3-recordings) for more details.
  string media_external_location = 8;
  // The time in milliseconds elapsed between an arbitrary point in time, common to all group rooms, and the moment when the source room of this track started. This information provides a synchronization mechanism for recordings belonging to the same room.
  int64 offset = 9;
  // The unique string that we created to identify the Recording resource.
  string sid = 10;
  // The size of the recorded track, in bytes.
  int64 size = 11;
  // The SID of the recording source. For a Room Recording, this value is a `track_sid`.
  string source_sid = 12;
  // OpenAPI property "status".
  RecordingEnumStatus status = 13;
  // The URL called using the `status_callback_method` to send status information on every recording event.
  string status_callback = 14;
  // The HTTP method used to call `status_callback`. Can be: `POST` or `GET`, defaults to `POST`.
  string status_callback_method = 15;
  // The name that was given to the source track of the recording. If no name is given, the `source_sid` is used.
  string track_name = 16;
  // OpenAPI property "type".
  RecordingEnumType type = 17;
  // The absolute URL of the resource.
  string url = 18;
}

// The URLs of related resources.
message VideoV1RecordingLinks {
  // Scalar value wrapper.
  string value = 1;
}

message VideoV1RecordingSettings {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the RecordingSettings resource.
  string account_sid = 1;
  // The SID of the stored Credential resource.
  string aws_credentials_sid = 2;
  // The URL of the AWS S3 bucket where the recordings are stored. We only support DNS-compliant URLs like `https://documentation-example-twilio-bucket/recordings`, where `recordings` is the path in which you want the recordings to be stored. This URL accepts only URI-valid characters, as described in the [RFC 3986](https://tools.ietf.org/html/rfc3986#section-2).
  string aws_s3_url = 3;
  // Whether all recordings are written to the `aws_s3_url`. When `false`, all recordings are stored in our cloud.
  bool aws_storage_enabled = 4;
  // Whether all recordings are stored in an encrypted form. The default is `false`.
  bool encryption_enabled = 5;
  // The SID of the Public Key resource used for encryption.
  string encryption_key_sid = 6;
  // The string that you assigned to describe the resource and show the user in the console
  string friendly_name = 7;
  // The absolute URL of the resource.
  string url = 8;
}

message VideoV1Room {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Room resource.
  string account_sid = 1;
  // When set to true, indicates that the participants in the room will only publish audio. No video tracks will be allowed.
  bool audio_only = 2;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 4;
  // The duration of the room in seconds.
  int64 duration = 5;
  // Specifies how long (in minutes) a room will remain active after last participant leaves. Can be configured when creating a room via REST API. For Ad-Hoc rooms this value cannot be changed.
  int64 empty_room_timeout = 6;
  // Deprecated, now always considered to be true.
  bool enable_turn = 7;
  // The UTC end time of the room in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601#UTC) format.
  string end_time = 8;
  // Indicates if this is a large room.
  bool large_room = 9;
  // The URLs of related resources.
  VideoV1RoomLinks links = 10;
  // The maximum number of published audio, video, and data tracks all participants combined are allowed to publish in the room at the same time. Check [Programmable Video Limits](https://www.twilio.com/docs/video/programmable-video-limits) for more details. If it is set to 0 it means unconstrained.
  int64 max_concurrent_published_tracks = 11;
  // The maximum number of seconds a Participant can be connected to the room. The maximum possible value is 86400 seconds (24 hours). The default is 14400 seconds (4 hours).
  int64 max_participant_duration = 12;
  // The maximum number of concurrent Participants allowed in the room.
  int64 max_participants = 13;
  // The region for the Room's media server.  Can be one of the [available Media Regions](https://www.twilio.com/docs/video/ip-addresses#media-servers).
  string media_region = 14;
  // Whether to start recording when Participants connect.
  bool record_participants_on_connect = 15;
  // The unique string that Twilio created to identify the Room resource.
  string sid = 16;
  // OpenAPI property "status".
  RoomEnumRoomStatus status = 17;
  // The URL Twilio calls using the `status_callback_method` to send status information to your application on every room event. See [Status Callbacks](https://www.twilio.com/docs/video/api/status-callbacks) for more info.
  string status_callback = 18;
  // The HTTP method Twilio uses to call `status_callback`. Can be `POST` or `GET` and defaults to `POST`.
  string status_callback_method = 19;
  // OpenAPI property "type".
  RoomEnumRoomType type = 20;
  // An application-defined string that uniquely identifies the resource. It can be used as a `room_sid` in place of the resource's `sid` in the URL to address the resource, assuming it does not contain any [reserved characters](https://tools.ietf.org/html/rfc3986#section-2.2) that would need to be URL encoded. This value is unique for `in-progress` rooms. SDK clients can use this name to connect to the room. REST API clients can use this name in place of the Room SID to interact with the room as long as the room is `in-progress`.
  string unique_name = 21;
  // Specifies how long (in minutes) a room will remain active if no one joins. Can be configured when creating a room via REST API. For Ad-Hoc rooms this value cannot be changed.
  int64 unused_room_timeout = 22;
  // The absolute URL of the resource.
  string url = 23;
  // An array of the video codecs that are supported when publishing a track in the room.  Can be: `VP8` and `H264`.
  repeated RoomEnumVideoCodec video_codecs = 24;
}

// The URLs of related resources.
message VideoV1RoomLinks {
  // Scalar value wrapper.
  string value = 1;
}

message VideoV1RoomRoomParticipant {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the RoomParticipant resource.
  string account_sid = 1;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 2;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 3;
  // The duration in seconds that the participant was `connected`. Populated only after the participant is `disconnected`.
  int64 duration = 4;
  // The time when the participant disconnected from the room in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601#UTC) format.
  string end_time = 5;
  // The application-defined string that uniquely identifies the resource's User within a Room. If a client joins with an existing Identity, the existing client is disconnected. See [access tokens](https://www.twilio.com/docs/video/tutorials/user-identity-access-tokens) and [limits](https://www.twilio.com/docs/video/programmable-video-limits) for more info.
  string identity = 6;
  // The URLs of related resources.
  VideoV1RoomRoomParticipantLinks links = 7;
  // The SID of the participant's room.
  string room_sid = 8;
  // The unique string that we created to identify the RoomParticipant resource.
  string sid = 9;
  // The time of participant connected to the room in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601#UTC) format.
  string start_time = 10;
  // OpenAPI property "status".
  RoomParticipantEnumStatus status = 11;
  // The absolute URL of the resource.
  string url = 12;
}

// The URLs of related resources.
message VideoV1RoomRoomParticipantLinks {
  // Scalar value wrapper.
  string value = 1;
}

message VideoV1RoomRoomParticipantRoomParticipantAnonymize {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the RoomParticipant resource.
  string account_sid = 1;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 2;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 3;
  // The duration in seconds that the participant was `connected`. Populated only after the participant is `disconnected`.
  int64 duration = 4;
  // The time when the participant disconnected from the room in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601#UTC) format.
  string end_time = 5;
  // The SID of the participant.
  string identity = 6;
  // The SID of the participant's room.
  string room_sid = 7;
  // The unique string that we created to identify the RoomParticipant resource.
  string sid = 8;
  // The time of participant connected to the room in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601#UTC) format.
  string start_time = 9;
  // OpenAPI property "status".
  RoomParticipantAnonymizeEnumStatus status = 10;
  // The absolute URL of the resource.
  string url = 11;
}

message VideoV1RoomRoomParticipantRoomParticipantPublishedTrack {
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 1;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 2;
  // Whether the track is enabled.
  bool enabled = 3;
  // OpenAPI property "kind".
  RoomParticipantPublishedTrackEnumKind kind = 4;
  // The track name. Must be no more than 128 characters, and be unique among the participant's published tracks.
  string name = 5;
  // The SID of the Participant resource with the published track.
  string participant_sid = 6;
  // The SID of the Room resource where the track is published.
  string room_sid = 7;
  // The unique string that we created to identify the RoomParticipantPublishedTrack resource.
  string sid = 8;
  // The absolute URL of the resource.
  string url = 9;
}

message VideoV1RoomRoomParticipantRoomParticipantSubscribeRule {
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 1;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 2;
  // The SID of the Participant resource for the Subscribe Rules.
  string participant_sid = 3;
  // The SID of the Room resource for the Subscribe Rules
  string room_sid = 4;
  // A collection of Subscribe Rules that describe how to include or exclude matching tracks. See the [Specifying Subscribe Rules](https://www.twilio.com/docs/video/api/track-subscriptions#specifying-sr) section for further information.
  repeated VideoV1RoomRoomParticipantRoomParticipantSubscribeRuleRulesItem rules = 5;
}

message VideoV1RoomRoomParticipantRoomParticipantSubscribeRuleRulesItem {
  bool all = 1;
  string kind = 2;
  string priority = 3;
  string publisher = 4;
  string track = 5;
  string type = 6;
}

message VideoV1RoomRoomParticipantRoomParticipantSubscribedTrack {
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 1;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 2;
  // Whether the track is enabled.
  bool enabled = 3;
  // OpenAPI property "kind".
  RoomParticipantSubscribedTrackEnumKind kind = 4;
  // The track name. Must have no more than 128 characters and be unique among the participant's published tracks.
  string name = 5;
  // The SID of the participant that subscribes to the track.
  string participant_sid = 6;
  // The SID of the participant that publishes the track.
  string publisher_sid = 7;
  // The SID of the room where the track is published.
  string room_sid = 8;
  // The unique string that we created to identify the RoomParticipantSubscribedTrack resource.
  string sid = 9;
  // The absolute URL of the resource.
  string url = 10;
}

message VideoV1RoomRoomRecording {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the RoomRecording resource.
  string account_sid = 1;
  // OpenAPI property "codec".
  RoomRecordingEnumCodec codec = 2;
  // OpenAPI property "container_format".
  RoomRecordingEnumFormat container_format = 3;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 4;
  // The duration of the recording rounded to the nearest second. Sub-second duration tracks have a `duration` of 1 second
  int64 duration = 5;
  // A list of SIDs related to the Recording. Includes the `room_sid` and `participant_sid`.
  string grouping_sids = 6;
  // The URLs of related resources.
  VideoV1RoomRoomRecordingLinks links = 7;
  // The URL of the media file associated with the recording when stored externally. See [External S3 Recordings](/docs/video/api/external-s3-recordings) for more details.
  string media_external_location = 8;
  // The time in milliseconds elapsed between an arbitrary point in time, common to all group rooms, and the moment when the source room of this track started. This information provides a synchronization mechanism for recordings belonging to the same room.
  int64 offset = 9;
  // The SID of the Room resource the recording is associated with.
  string room_sid = 10;
  // The unique string that we created to identify the RoomRecording resource.
  string sid = 11;
  // The size of the recorded track in bytes.
  int64 size = 12;
  // The SID of the recording source. For a Room Recording, this value is a `track_sid`.
  string source_sid = 13;
  // OpenAPI property "status".
  RoomRecordingEnumStatus status = 14;
  // The name that was given to the source track of the recording. If no name is given, the `source_sid` is used.
  string track_name = 15;
  // OpenAPI property "type".
  RoomRecordingEnumType type = 16;
  // The absolute URL of the resource.
  string url = 17;
}

// The URLs of related resources.
message VideoV1RoomRoomRecordingLinks {
  // Scalar value wrapper.
  string value = 1;
}

message VideoV1RoomRoomRecordingRule {
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 1;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 2;
  // The SID of the Room resource for the Recording Rules
  string room_sid = 3;
  // A collection of Recording Rules that describe how to include or exclude matching tracks for recording
  repeated VideoV1RoomRoomRecordingRuleRulesItem rules = 4;
}

message VideoV1RoomRoomRecordingRuleRulesItem {
  bool all = 1;
  string kind = 2;
  string publisher = 3;
  string track = 4;
  string type = 5;
}

message VideoV1RoomRoomTranscriptions {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Room resource.
  string account_sid = 1;
  // An JSON object that describes the video layout of the composition in terms of regions. See [Specifying Video Layouts](https://www.twilio.com/docs/video/api/compositions-resource#specifying-video-layouts) for more info.
  VideoV1RoomRoomTranscriptionsConfiguration configuration = 2;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 4;
  // The duration in seconds that the transcriptions were `connected`. Populated only after the transcriptions is `stopped`.
  int64 duration = 5;
  // The time when the transcriptions disconnected from the room in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601#UTC) format.
  string end_time = 6;
  // The SID of the transcriptions's room.
  string room_sid = 7;
  // The SID of the transcriptions's associated call.
  string source_sid = 8;
  // The time of transcriptions connected to the room in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601#UTC) format.
  string start_time = 9;
  // OpenAPI property "status".
  RoomTranscriptionsEnumStatus status = 10;
  // The unique string that we created to identify the transcriptions resource.
  string ttid = 11;
  // The absolute URL of the resource.
  string url = 12;
}

// An JSON object that describes the video layout of the composition in terms of regions. See [Specifying Video Layouts](https://www.twilio.com/docs/video/api/compositions-resource#specifying-video-layouts) for more info.
message VideoV1RoomRoomTranscriptionsConfiguration {
  // Scalar value wrapper.
  string value = 1;
}

// ListCompositionHookResponse
message ListCompositionHookResponseBody {
  repeated VideoV1CompositionHook composition_hooks = 1;
  ListCompositionHookResponseBodyMeta meta = 2;
}

message ListCompositionHookResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// List of all Recording CompositionHook resources.
message ListCompositionHookRequest {
  // Read only CompositionHook resources with an `enabled` value that matches this parameter.
  bool enabled = 1;
  // Read only CompositionHook resources created on or after this [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) datetime with time zone.
  string date_created_after = 2;
  // Read only CompositionHook resources created before this [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) datetime with time zone.
  string date_created_before = 3;
  // Read only CompositionHook resources with friendly names that match this string. The match is not case sensitive and can include asterisk `*` characters as wildcard match.
  string friendly_name = 4;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 5;
  // The page index. This value is simply for client state.
  int64 page = 6;
  // The page token. This is provided by the API.
  string page_token = 7;
}

// Canonical success response.
message ListCompositionHookResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListCompositionHookResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateCompositionHookRequest
message CreateCompositionHookRequestBody {
  // An array of track names from the same group room to merge into the compositions created by the composition hook. Can include zero or more track names. A composition triggered by the composition hook includes all audio sources specified in `audio_sources` except those specified in `audio_sources_excluded`. The track names in this parameter can include an asterisk as a wild card character, which matches zero or more characters in a track name. For example, `student*` includes tracks named `student` as well as `studentTeam`.
  repeated string audio_sources = 1;
  // An array of track names to exclude. A composition triggered by the composition hook includes all audio sources specified in `audio_sources` except for those specified in `audio_sources_excluded`. The track names in this parameter can include an asterisk as a wild card character, which matches zero or more characters in a track name. For example, `student*` excludes `student` as well as `studentTeam`. This parameter can also be empty.
  repeated string audio_sources_excluded = 2;
  // Whether the composition hook is active. When `true`, the composition hook will be triggered for every completed Group Room in the account. When `false`, the composition hook will never be triggered.
  bool enabled = 3;
  // OpenAPI property "Format".
  CompositionHookEnumFormat format = 4;
  // A descriptive string that you create to describe the resource. It can be up to  100 characters long and it must be unique within the account.
  string friendly_name = 5;
  // A string that describes the columns (width) and rows (height) of the generated composed video in pixels. Defaults to `640x480`.
  // The string's format is `{width}x{height}` where:
  // * 16 <= `{width}` <= 1280
  // * 16 <= `{height}` <= 1280
  // * `{width}` * `{height}` <= 921,600
  // Typical values are:
  // * HD = `1280x720`
  // * PAL = `1024x576`
  // * VGA = `640x480`
  // * CIF = `320x240`
  // Note that the `resolution` imposes an aspect ratio to the resulting composition. When the original video tracks are constrained by the aspect ratio, they are scaled to fit. See [Specifying Video Layouts](https://www.twilio.com/docs/video/api/compositions-resource#specifying-video-layouts) for more info.
  string resolution = 6;
  // The URL we should call using the `status_callback_method` to send status information to your application on every composition event. If not provided, status callback events will not be dispatched.
  string status_callback = 7;
  // The HTTP method we should use to call `status_callback`. Can be: `POST` or `GET` and the default is `POST`.
  string status_callback_method = 8;
  // Whether to clip the intervals where there is no active media in the Compositions triggered by the composition hook. The default is `true`. Compositions with `trim` enabled are shorter when the Room is created and no Participant joins for a while as well as if all the Participants leave the room and join later, because those gaps will be removed. See [Specifying Video Layouts](https://www.twilio.com/docs/video/api/compositions-resource#specifying-video-layouts) for more info.
  bool trim = 9;
  // An object that describes the video layout of the composition hook in terms of regions. See [Specifying Video Layouts](https://www.twilio.com/docs/video/api/compositions-resource#specifying-video-layouts) for more info.
  string video_layout = 10;
}

message CreateCompositionHookRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateCompositionHookRequestBody body = 1;
}

// Canonical success response.
message CreateCompositionHookResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1CompositionHook body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Returns a single CompositionHook resource identified by a CompositionHook SID.
message FetchCompositionHookRequest {
  // The SID of the CompositionHook resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchCompositionHookResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1CompositionHook body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateCompositionHookRequest
message UpdateCompositionHookRequestBody {
  // An array of track names from the same group room to merge into the compositions created by the composition hook. Can include zero or more track names. A composition triggered by the composition hook includes all audio sources specified in `audio_sources` except those specified in `audio_sources_excluded`. The track names in this parameter can include an asterisk as a wild card character, which matches zero or more characters in a track name. For example, `student*` includes tracks named `student` as well as `studentTeam`.
  repeated string audio_sources = 1;
  // An array of track names to exclude. A composition triggered by the composition hook includes all audio sources specified in `audio_sources` except for those specified in `audio_sources_excluded`. The track names in this parameter can include an asterisk as a wild card character, which matches zero or more characters in a track name. For example, `student*` excludes `student` as well as `studentTeam`. This parameter can also be empty.
  repeated string audio_sources_excluded = 2;
  // Whether the composition hook is active. When `true`, the composition hook will be triggered for every completed Group Room in the account. When `false`, the composition hook never triggers.
  bool enabled = 3;
  // OpenAPI property "Format".
  CompositionHookEnumFormat format = 4;
  // A descriptive string that you create to describe the resource. It can be up to  100 characters long and it must be unique within the account.
  string friendly_name = 5;
  // A string that describes the columns (width) and rows (height) of the generated composed video in pixels. Defaults to `640x480`.
  // The string's format is `{width}x{height}` where:
  // * 16 <= `{width}` <= 1280
  // * 16 <= `{height}` <= 1280
  // * `{width}` * `{height}` <= 921,600
  // Typical values are:
  // * HD = `1280x720`
  // * PAL = `1024x576`
  // * VGA = `640x480`
  // * CIF = `320x240`
  // Note that the `resolution` imposes an aspect ratio to the resulting composition. When the original video tracks are constrained by the aspect ratio, they are scaled to fit. See [Specifying Video Layouts](https://www.twilio.com/docs/video/api/compositions-resource#specifying-video-layouts) for more info.
  string resolution = 6;
  // The URL we should call using the `status_callback_method` to send status information to your application on every composition event. If not provided, status callback events will not be dispatched.
  string status_callback = 7;
  // The HTTP method we should use to call `status_callback`. Can be: `POST` or `GET` and the default is `POST`.
  string status_callback_method = 8;
  // Whether to clip the intervals where there is no active media in the compositions triggered by the composition hook. The default is `true`. Compositions with `trim` enabled are shorter when the Room is created and no Participant joins for a while as well as if all the Participants leave the room and join later, because those gaps will be removed. See [Specifying Video Layouts](https://www.twilio.com/docs/video/api/compositions-resource#specifying-video-layouts) for more info.
  bool trim = 9;
  // A JSON object that describes the video layout of the composition hook in terms of regions. See [Specifying Video Layouts](https://www.twilio.com/docs/video/api/compositions-resource#specifying-video-layouts) for more info.
  string video_layout = 10;
}

message UpdateCompositionHookRequest {
  // The SID of the CompositionHook resource to update. Required.
  string sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateCompositionHookRequestBody body = 2;
}

// Canonical success response.
message UpdateCompositionHookResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1CompositionHook body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete a Recording CompositionHook resource identified by a `CompositionHook SID`.
message DeleteCompositionHookRequest {
  // The SID of the CompositionHook resource to delete. Required.
  string sid = 1;
}

// Canonical success response.
message DeleteCompositionHookResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

message FetchCompositionSettingsRequest {
}

// Canonical success response.
message FetchCompositionSettingsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1CompositionSettings body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateCompositionSettingsRequest
message CreateCompositionSettingsRequestBody {
  // The SID of the stored Credential resource.
  string aws_credentials_sid = 1;
  // The URL of the AWS S3 bucket where the compositions should be stored. We only support DNS-compliant URLs like `https://documentation-example-twilio-bucket/compositions`, where `compositions` is the path in which you want the compositions to be stored. This URL accepts only URI-valid characters, as described in the [RFC 3986](https://tools.ietf.org/html/rfc3986#section-2).
  string aws_s3_url = 2;
  // Whether all compositions should be written to the `aws_s3_url`. When `false`, all compositions are stored in our cloud.
  bool aws_storage_enabled = 3;
  // Whether all compositions should be stored in an encrypted form. The default is `false`.
  bool encryption_enabled = 4;
  // The SID of the Public Key resource to use for encryption.
  string encryption_key_sid = 5;
  // A descriptive string that you create to describe the resource and show to the user in the console
  string friendly_name = 6;
}

message CreateCompositionSettingsRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateCompositionSettingsRequestBody body = 1;
}

// Canonical success response.
message CreateCompositionSettingsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1CompositionSettings body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListCompositionResponse
message ListCompositionResponseBody {
  repeated VideoV1Composition compositions = 1;
  ListCompositionResponseBodyMeta meta = 2;
}

message ListCompositionResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// List of all Recording compositions.
message ListCompositionRequest {
  // Read only Composition resources with this status. Can be: `enqueued`, `processing`, `completed`, `deleted`, or `failed`.
  CompositionEnumStatus status = 1;
  // Read only Composition resources created on or after this [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date-time with time zone.
  string date_created_after = 2;
  // Read only Composition resources created before this ISO 8601 date-time with time zone.
  string date_created_before = 3;
  // Read only Composition resources with this Room SID.
  string room_sid = 4;
  // How many resources to return in each list page.
  int64 page_size = 5;
  // The page index. This value is simply for client state.
  int64 page = 6;
  // The page token. This is provided by the API.
  string page_token = 7;
}

// Canonical success response.
message ListCompositionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListCompositionResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateCompositionRequest
message CreateCompositionRequestBody {
  // An array of track names from the same group room to merge into the new composition. Can include zero or more track names. The new composition includes all audio sources specified in `audio_sources` except for those specified in `audio_sources_excluded`. The track names in this parameter can include an asterisk as a wild card character, which will match zero or more characters in a track name. For example, `student*` includes `student` as well as `studentTeam`. Please, be aware that either video_layout or audio_sources have to be provided to get a valid creation request
  repeated string audio_sources = 1;
  // An array of track names to exclude. The new composition includes all audio sources specified in `audio_sources` except for those specified in `audio_sources_excluded`. The track names in this parameter can include an asterisk as a wild card character, which will match zero or more characters in a track name. For example, `student*` excludes `student` as well as `studentTeam`. This parameter can also be empty.
  repeated string audio_sources_excluded = 2;
  // OpenAPI property "Format".
  CompositionEnumFormat format = 3;
  // A string that describes the columns (width) and rows (height) of the generated composed video in pixels. Defaults to `640x480`.
  // The string's format is `{width}x{height}` where:
  // * 16 <= `{width}` <= 1280
  // * 16 <= `{height}` <= 1280
  // * `{width}` * `{height}` <= 921,600
  // Typical values are:
  // * HD = `1280x720`
  // * PAL = `1024x576`
  // * VGA = `640x480`
  // * CIF = `320x240`
  // Note that the `resolution` imposes an aspect ratio to the resulting composition. When the original video tracks are constrained by the aspect ratio, they are scaled to fit. See [Specifying Video Layouts](https://www.twilio.com/docs/video/api/compositions-resource#specifying-video-layouts) for more info.
  string resolution = 4;
  // The SID of the Group Room with the media tracks to be used as composition sources.
  string room_sid = 5;
  // The URL we should call using the `status_callback_method` to send status information to your application on every composition event. If not provided, status callback events will not be dispatched.
  string status_callback = 6;
  // The HTTP method we should use to call `status_callback`. Can be: `POST` or `GET` and the default is `POST`.
  string status_callback_method = 7;
  // Whether to clip the intervals where there is no active media in the composition. The default is `true`. Compositions with `trim` enabled are shorter when the Room is created and no Participant joins for a while as well as if all the Participants leave the room and join later, because those gaps will be removed. See [Specifying Video Layouts](https://www.twilio.com/docs/video/api/compositions-resource#specifying-video-layouts) for more info.
  bool trim = 8;
  // An object that describes the video layout of the composition in terms of regions. See [Specifying Video Layouts](https://www.twilio.com/docs/video/api/compositions-resource#specifying-video-layouts) for more info. Please, be aware that either video_layout or audio_sources have to be provided to get a valid creation request
  string video_layout = 9;
}

message CreateCompositionRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateCompositionRequestBody body = 1;
}

// Canonical success response.
message CreateCompositionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1Composition body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Returns a single Composition resource identified by a Composition SID.
message FetchCompositionRequest {
  // The SID of the Composition resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchCompositionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1Composition body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete a Recording Composition resource identified by a Composition SID.
message DeleteCompositionRequest {
  // The SID of the Composition resource to delete. Required.
  string sid = 1;
}

// Canonical success response.
message DeleteCompositionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

message FetchRecordingSettingsRequest {
}

// Canonical success response.
message FetchRecordingSettingsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1RecordingSettings body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateRecordingSettingsRequest
message CreateRecordingSettingsRequestBody {
  // The SID of the stored Credential resource.
  string aws_credentials_sid = 1;
  // The URL of the AWS S3 bucket where the recordings should be stored. We only support DNS-compliant URLs like `https://documentation-example-twilio-bucket/recordings`, where `recordings` is the path in which you want the recordings to be stored. This URL accepts only URI-valid characters, as described in the [RFC 3986](https://tools.ietf.org/html/rfc3986#section-2).
  string aws_s3_url = 2;
  // Whether all recordings should be written to the `aws_s3_url`. When `false`, all recordings are stored in our cloud.
  bool aws_storage_enabled = 3;
  // Whether all recordings should be stored in an encrypted form. The default is `false`.
  bool encryption_enabled = 4;
  // The SID of the Public Key resource to use for encryption.
  string encryption_key_sid = 5;
  // A descriptive string that you create to describe the resource and be shown to users in the console
  string friendly_name = 6;
}

message CreateRecordingSettingsRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateRecordingSettingsRequestBody body = 1;
}

// Canonical success response.
message CreateRecordingSettingsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1RecordingSettings body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListRecordingResponse
message ListRecordingResponseBody {
  ListRecordingResponseBodyMeta meta = 1;
  repeated VideoV1Recording recordings = 2;
}

message ListRecordingResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// List of all Track recordings.
message ListRecordingRequest {
  // Read only the recordings that have this status. Can be: `processing`, `completed`, or `deleted`.
  RecordingEnumStatus status = 1;
  // Read only the recordings that have this `source_sid`.
  string source_sid = 2;
  // Read only recordings with this `grouping_sid`, which may include a `participant_sid` and/or a `room_sid`.
  repeated string grouping_sid = 3;
  // Read only recordings that started on or after this [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date-time with time zone.
  string date_created_after = 4;
  // Read only recordings that started before this [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date-time with time zone, given as `YYYY-MM-DDThh:mm:ss+|-hh:mm` or `YYYY-MM-DDThh:mm:ssZ`.
  string date_created_before = 5;
  // Read only recordings that have this media type. Can be either `audio` or `video`.
  RecordingEnumType media_type = 6;
  // How many resources to return in each list page.
  int64 page_size = 7;
  // The page index. This value is simply for client state.
  int64 page = 8;
  // The page token. This is provided by the API.
  string page_token = 9;
}

// Canonical success response.
message ListRecordingResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListRecordingResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Returns a single Recording resource identified by a Recording SID.
message FetchRecordingRequest {
  // The SID of the Recording resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchRecordingResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1Recording body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete a Recording resource identified by a Recording SID.
message DeleteRecordingRequest {
  // The SID of the Recording resource to delete. Required.
  string sid = 1;
}

// Canonical success response.
message DeleteRecordingResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListRoomResponse
message ListRoomResponseBody {
  ListRoomResponseBodyMeta meta = 1;
  repeated VideoV1Room rooms = 2;
}

message ListRoomResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListRoomRequest {
  // Read only the rooms with this status. Can be: `in-progress` (default) or `completed`
  RoomEnumRoomStatus status = 1;
  // Read only rooms with the this `unique_name`.
  string unique_name = 2;
  // Read only rooms that started on or after this date, given as `YYYY-MM-DD`.
  string date_created_after = 3;
  // Read only rooms that started before this date, given as `YYYY-MM-DD`.
  string date_created_before = 4;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 5;
  // The page index. This value is simply for client state.
  int64 page = 6;
  // The page token. This is provided by the API.
  string page_token = 7;
}

// Canonical success response.
message ListRoomResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListRoomResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateRoomRequest
message CreateRoomRequestBody {
  // When set to true, indicates that the participants in the room will only publish audio. No video tracks will be allowed.
  bool audio_only = 1;
  // Configures how long (in minutes) a room will remain active after last participant leaves. Valid values range from 1 to 60 minutes (no fractions).
  int64 empty_room_timeout = 2;
  // Deprecated, now always considered to be true.
  bool enable_turn = 3;
  // When set to true, indicated that this is the large room.
  bool large_room = 4;
  // The maximum number of seconds a Participant can be connected to the room. The maximum possible value is 86400 seconds (24 hours). The default is 14400 seconds (4 hours).
  int64 max_participant_duration = 5;
  // The maximum number of concurrent Participants allowed in the room. The maximum allowed value is 50.
  int64 max_participants = 6;
  // The region for the Room's media server.  Can be one of the [available Media Regions](https://www.twilio.com/docs/video/ip-addresses#group-rooms-media-servers).
  string media_region = 7;
  // Whether to start recording when Participants connect.
  bool record_participants_on_connect = 8;
  // A collection of Recording Rules that describe how to include or exclude matching tracks for recording
  string recording_rules = 9;
  // The URL Twilio should call using the `status_callback_method` to send status information to your application on every room event. See [Status Callbacks](https://www.twilio.com/docs/video/api/status-callbacks) for more info.
  string status_callback = 10;
  // The HTTP method Twilio should use to call `status_callback`. Can be `POST` or `GET`.
  string status_callback_method = 11;
  // Whether to start transcriptions when Participants connect. If TranscriptionsConfiguration is not provided, default settings will be used.
  bool transcribe_participants_on_connect = 12;
  // A collection of properties that describe transcription behaviour. If TranscribeParticipantsOnConnect is set to true and TranscriptionsConfiguration is not provided, default settings will be used.
  CreateRoomRequestBodyTranscriptionsConfiguration transcriptions_configuration = 13;
  // OpenAPI property "Type".
  RoomEnumRoomType type = 14;
  // An application-defined string that uniquely identifies the resource. It can be used as a `room_sid` in place of the resource's `sid` in the URL to address the resource, assuming it does not contain any [reserved characters](https://tools.ietf.org/html/rfc3986#section-2.2) that would need to be URL encoded. This value is unique for `in-progress` rooms. SDK clients can use this name to connect to the room. REST API clients can use this name in place of the Room SID to interact with the room as long as the room is `in-progress`.
  string unique_name = 15;
  // Configures how long (in minutes) a room will remain active if no one joins. Valid values range from 1 to 60 minutes (no fractions).
  int64 unused_room_timeout = 16;
  // An array of the video codecs that are supported when publishing a track in the room.  Can be: `VP8` and `H264`.
  repeated RoomEnumVideoCodec video_codecs = 17;
}

// A collection of properties that describe transcription behaviour. If TranscribeParticipantsOnConnect is set to true and TranscriptionsConfiguration is not provided, default settings will be used.
message CreateRoomRequestBodyTranscriptionsConfiguration {
  // Scalar value wrapper.
  string value = 1;
}

message CreateRoomRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateRoomRequestBody body = 1;
}

// Canonical success response.
message CreateRoomResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1Room body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListRoomParticipantResponse
message ListRoomParticipantResponseBody {
  ListRoomParticipantResponseBodyMeta meta = 1;
  repeated VideoV1RoomRoomParticipant participants = 2;
}

message ListRoomParticipantResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListRoomParticipantRequest {
  // The SID of the room with the Participant resources to read. Required.
  string room_sid = 1;
  // Read only the participants with this status. Can be: `connected` or `disconnected`. For `in-progress` Rooms the default Status is `connected`, for `completed` Rooms only `disconnected` Participants are returned.
  RoomParticipantEnumStatus status = 2;
  // Read only the Participants with this [User](https://www.twilio.com/docs/chat/rest/user-resource) `identity` value.
  string identity = 3;
  // Read only Participants that started after this date in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601#UTC) format.
  string date_created_after = 4;
  // Read only Participants that started before this date in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601#UTC) format.
  string date_created_before = 5;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 6;
  // The page index. This value is simply for client state.
  int64 page = 7;
  // The page token. This is provided by the API.
  string page_token = 8;
}

// Canonical success response.
message ListRoomParticipantResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListRoomParticipantResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListRoomParticipantPublishedTrackResponse
message ListRoomParticipantPublishedTrackResponseBody {
  ListRoomParticipantPublishedTrackResponseBodyMeta meta = 1;
  repeated VideoV1RoomRoomParticipantRoomParticipantPublishedTrack published_tracks = 2;
}

message ListRoomParticipantPublishedTrackResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Returns a list of tracks associated with a given Participant. Only `currently` Published Tracks are in the list resource.
message ListRoomParticipantPublishedTrackRequest {
  // The SID of the Room resource where the Track resources to read are published. Required.
  string room_sid = 1;
  // The SID of the Participant resource with the published tracks to read. Required.
  string participant_sid = 2;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 3;
  // The page index. This value is simply for client state.
  int64 page = 4;
  // The page token. This is provided by the API.
  string page_token = 5;
}

// Canonical success response.
message ListRoomParticipantPublishedTrackResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListRoomParticipantPublishedTrackResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Returns a single Track resource represented by TrackName or SID.
message FetchRoomParticipantPublishedTrackRequest {
  // The SID of the Room resource where the Track resource to fetch is published. Required.
  string room_sid = 1;
  // The SID of the Participant resource with the published track to fetch. Required.
  string participant_sid = 2;
  // The SID of the RoomParticipantPublishedTrack resource to fetch. Required.
  string sid = 3;
}

// Canonical success response.
message FetchRoomParticipantPublishedTrackResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1RoomRoomParticipantRoomParticipantPublishedTrack body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Returns a list of Subscribe Rules for the Participant.
message FetchRoomParticipantSubscribeRuleRequest {
  // The SID of the Room resource where the subscribe rules to fetch apply. Required.
  string room_sid = 1;
  // The SID of the Participant resource with the subscribe rules to fetch. Required.
  string participant_sid = 2;
}

// Canonical success response.
message FetchRoomParticipantSubscribeRuleResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1RoomRoomParticipantRoomParticipantSubscribeRule body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateRoomParticipantSubscribeRuleRequest
message UpdateRoomParticipantSubscribeRuleRequestBody {
  // A JSON-encoded array of subscribe rules. See the [Specifying Subscribe Rules](https://www.twilio.com/docs/video/api/track-subscriptions#specifying-sr) section for further information.
  string rules = 1;
}

// Update the Subscribe Rules for the Participant
message UpdateRoomParticipantSubscribeRuleRequest {
  // The SID of the Room resource where the subscribe rules to update apply. Required.
  string room_sid = 1;
  // The SID of the Participant resource to update the Subscribe Rules. Required.
  string participant_sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateRoomParticipantSubscribeRuleRequestBody body = 3;
}

// Canonical success response.
message UpdateRoomParticipantSubscribeRuleResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1RoomRoomParticipantRoomParticipantSubscribeRule body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListRoomParticipantSubscribedTrackResponse
message ListRoomParticipantSubscribedTrackResponseBody {
  ListRoomParticipantSubscribedTrackResponseBodyMeta meta = 1;
  repeated VideoV1RoomRoomParticipantRoomParticipantSubscribedTrack subscribed_tracks = 2;
}

message ListRoomParticipantSubscribedTrackResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Returns a list of tracks that are subscribed for the participant.
message ListRoomParticipantSubscribedTrackRequest {
  // The SID of the Room resource with the Track resources to read. Required.
  string room_sid = 1;
  // The SID of the participant that subscribes to the Track resources to read. Required.
  string participant_sid = 2;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 3;
  // The page index. This value is simply for client state.
  int64 page = 4;
  // The page token. This is provided by the API.
  string page_token = 5;
}

// Canonical success response.
message ListRoomParticipantSubscribedTrackResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListRoomParticipantSubscribedTrackResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Returns a single Track resource represented by `track_sid`.  Note: This is one resource with the Video API that requires a SID, be Track Name on the subscriber side is not guaranteed to be unique.
message FetchRoomParticipantSubscribedTrackRequest {
  // The SID of the Room where the Track resource to fetch is subscribed. Required.
  string room_sid = 1;
  // The SID of the participant that subscribes to the Track resource to fetch. Required.
  string participant_sid = 2;
  // The SID of the RoomParticipantSubscribedTrack resource to fetch. Required.
  string sid = 3;
}

// Canonical success response.
message FetchRoomParticipantSubscribedTrackResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1RoomRoomParticipantRoomParticipantSubscribedTrack body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchRoomParticipantRequest {
  // The SID of the room with the Participant resource to fetch. Required.
  string room_sid = 1;
  // The SID of the RoomParticipant resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchRoomParticipantResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1RoomRoomParticipant body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateRoomParticipantRequest
message UpdateRoomParticipantRequestBody {
  // OpenAPI property "Status".
  RoomParticipantEnumStatus status = 1;
}

message UpdateRoomParticipantRequest {
  // The SID of the room with the participant to update. Required.
  string room_sid = 1;
  // The SID of the RoomParticipant resource to update. Required.
  string sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateRoomParticipantRequestBody body = 3;
}

// Canonical success response.
message UpdateRoomParticipantResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1RoomRoomParticipant body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message UpdateRoomParticipantAnonymizeRequest {
  // The SID of the room with the participant to update. Required.
  string room_sid = 1;
  // The SID of the RoomParticipant resource to update. Required.
  string sid = 2;
}

// Canonical success response.
message UpdateRoomParticipantAnonymizeResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1RoomRoomParticipantRoomParticipantAnonymize body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Returns a list of Recording Rules for the Room.
message FetchRoomRecordingRuleRequest {
  // The SID of the Room resource where the recording rules to fetch apply. Required.
  string room_sid = 1;
}

// Canonical success response.
message FetchRoomRecordingRuleResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1RoomRoomRecordingRule body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateRoomRecordingRuleRequest
message UpdateRoomRecordingRuleRequestBody {
  // A JSON-encoded array of recording rules.
  string rules = 1;
}

// Update the Recording Rules for the Room
message UpdateRoomRecordingRuleRequest {
  // The SID of the Room resource where the recording rules to update apply. Required.
  string room_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateRoomRecordingRuleRequestBody body = 2;
}

// Canonical success response.
message UpdateRoomRecordingRuleResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1RoomRoomRecordingRule body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListRoomRecordingResponse
message ListRoomRecordingResponseBody {
  ListRoomRecordingResponseBodyMeta meta = 1;
  repeated VideoV1RoomRoomRecording recordings = 2;
}

message ListRoomRecordingResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListRoomRecordingRequest {
  // The SID of the room with the RoomRecording resources to read. Required.
  string room_sid = 1;
  // Read only the recordings with this status. Can be: `processing`, `completed`, or `deleted`.
  RoomRecordingEnumStatus status = 2;
  // Read only the recordings that have this `source_sid`.
  string source_sid = 3;
  // Read only recordings that started on or after this [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) datetime with time zone.
  string date_created_after = 4;
  // Read only Recordings that started before this [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) datetime with time zone.
  string date_created_before = 5;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 6;
  // The page index. This value is simply for client state.
  int64 page = 7;
  // The page token. This is provided by the API.
  string page_token = 8;
}

// Canonical success response.
message ListRoomRecordingResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListRoomRecordingResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchRoomRecordingRequest {
  // The SID of the Room resource with the recording to fetch. Required.
  string room_sid = 1;
  // The SID of the RoomRecording resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchRoomRecordingResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1RoomRoomRecording body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeleteRoomRecordingRequest {
  // The SID of the room with the RoomRecording resource to delete. Required.
  string room_sid = 1;
  // The SID of the RoomRecording resource to delete. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteRoomRecordingResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListRoomTranscriptionsResponse
message ListRoomTranscriptionsResponseBody {
  ListRoomTranscriptionsResponseBodyMeta meta = 1;
  repeated VideoV1RoomRoomTranscriptions transcriptions = 2;
}

message ListRoomTranscriptionsResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListRoomTranscriptionsRequest {
  // The SID of the room with the transcriptions resources to read. Required.
  string room_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListRoomTranscriptionsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListRoomTranscriptionsResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateRoomTranscriptionsRequest
message CreateRoomTranscriptionsRequestBody {
  // A collection of properties that describe transcription behaviour.
  CreateRoomTranscriptionsRequestBodyConfiguration configuration = 1;
}

// A collection of properties that describe transcription behaviour.
message CreateRoomTranscriptionsRequestBodyConfiguration {
  // Scalar value wrapper.
  string value = 1;
}

message CreateRoomTranscriptionsRequest {
  // The SID of the room new transcriptions resource to be created. Required.
  string room_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateRoomTranscriptionsRequestBody body = 2;
}

// Canonical success response.
message CreateRoomTranscriptionsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1RoomRoomTranscriptions body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchRoomTranscriptionsRequest {
  // The SID of the room with the transcriptions resource to fetch. Required.
  string room_sid = 1;
  // The Twilio type id of the transcriptions resource to fetch. Required.
  string ttid = 2;
}

// Canonical success response.
message FetchRoomTranscriptionsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1RoomRoomTranscriptions body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateRoomTranscriptionsRequest
message UpdateRoomTranscriptionsRequestBody {
  // A collection of properties that describe transcription behaviour.
  UpdateRoomTranscriptionsRequestBodyConfiguration configuration = 1;
  // OpenAPI property "Status".
  RoomTranscriptionsEnumStatus status = 2;
}

// A collection of properties that describe transcription behaviour.
message UpdateRoomTranscriptionsRequestBodyConfiguration {
  // Scalar value wrapper.
  string value = 1;
}

message UpdateRoomTranscriptionsRequest {
  // The SID of the room with the transcriptions resource to update. Required.
  string room_sid = 1;
  // The Twilio type id of the transcriptions resource to update. Required.
  string ttid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateRoomTranscriptionsRequestBody body = 3;
}

// Canonical success response.
message UpdateRoomTranscriptionsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1RoomRoomTranscriptions body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchRoomRequest {
  // The SID of the Room resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchRoomResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1Room body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateRoomRequest
message UpdateRoomRequestBody {
  // OpenAPI property "Status".
  RoomEnumRoomStatus status = 1;
}

message UpdateRoomRequest {
  // The SID of the Room resource to update. Required.
  string sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateRoomRequestBody body = 2;
}

// Canonical success response.
message UpdateRoomResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VideoV1Room body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Generated from VideoV1CompositionHook operations.
service VideoV1CompositionHookService {
  // List of all Recording CompositionHook resources.
  rpc ListCompositionHook(ListCompositionHookRequest) returns (ListCompositionHookResponse) {
    option (google.api.http) = {
      get: "/v1/CompositionHooks"
      response_body: "body"
    };
  }
  rpc CreateCompositionHook(CreateCompositionHookRequest) returns (CreateCompositionHookResponse) {
    option (google.api.http) = {
      post: "/v1/CompositionHooks"
      body: "body"
      response_body: "body"
    };
  }
  // Returns a single CompositionHook resource identified by a CompositionHook SID.
  rpc FetchCompositionHook(FetchCompositionHookRequest) returns (FetchCompositionHookResponse) {
    option (google.api.http) = {
      get: "/v1/CompositionHooks/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateCompositionHook(UpdateCompositionHookRequest) returns (UpdateCompositionHookResponse) {
    option (google.api.http) = {
      post: "/v1/CompositionHooks/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete a Recording CompositionHook resource identified by a `CompositionHook SID`.
  rpc DeleteCompositionHook(DeleteCompositionHookRequest) returns (DeleteCompositionHookResponse) {
    option (google.api.http) = {
      delete: "/v1/CompositionHooks/{sid}"
    };
  }
}

// Generated from VideoV1CompositionSettings operations.
service VideoV1CompositionSettingsService {
  rpc FetchCompositionSettings(FetchCompositionSettingsRequest) returns (FetchCompositionSettingsResponse) {
    option (google.api.http) = {
      get: "/v1/CompositionSettings/Default"
      response_body: "body"
    };
  }
  rpc CreateCompositionSettings(CreateCompositionSettingsRequest) returns (CreateCompositionSettingsResponse) {
    option (google.api.http) = {
      post: "/v1/CompositionSettings/Default"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from VideoV1Composition operations.
service VideoV1CompositionService {
  // List of all Recording compositions.
  rpc ListComposition(ListCompositionRequest) returns (ListCompositionResponse) {
    option (google.api.http) = {
      get: "/v1/Compositions"
      response_body: "body"
    };
  }
  rpc CreateComposition(CreateCompositionRequest) returns (CreateCompositionResponse) {
    option (google.api.http) = {
      post: "/v1/Compositions"
      body: "body"
      response_body: "body"
    };
  }
  // Returns a single Composition resource identified by a Composition SID.
  rpc FetchComposition(FetchCompositionRequest) returns (FetchCompositionResponse) {
    option (google.api.http) = {
      get: "/v1/Compositions/{sid}"
      response_body: "body"
    };
  }
  // Delete a Recording Composition resource identified by a Composition SID.
  rpc DeleteComposition(DeleteCompositionRequest) returns (DeleteCompositionResponse) {
    option (google.api.http) = {
      delete: "/v1/Compositions/{sid}"
    };
  }
}

// Generated from VideoV1RecordingSettings operations.
service VideoV1RecordingSettingsService {
  rpc FetchRecordingSettings(FetchRecordingSettingsRequest) returns (FetchRecordingSettingsResponse) {
    option (google.api.http) = {
      get: "/v1/RecordingSettings/Default"
      response_body: "body"
    };
  }
  rpc CreateRecordingSettings(CreateRecordingSettingsRequest) returns (CreateRecordingSettingsResponse) {
    option (google.api.http) = {
      post: "/v1/RecordingSettings/Default"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from VideoV1Recording operations.
service VideoV1RecordingService {
  // List of all Track recordings.
  rpc ListRecording(ListRecordingRequest) returns (ListRecordingResponse) {
    option (google.api.http) = {
      get: "/v1/Recordings"
      response_body: "body"
    };
  }
  // Returns a single Recording resource identified by a Recording SID.
  rpc FetchRecording(FetchRecordingRequest) returns (FetchRecordingResponse) {
    option (google.api.http) = {
      get: "/v1/Recordings/{sid}"
      response_body: "body"
    };
  }
  // Delete a Recording resource identified by a Recording SID.
  rpc DeleteRecording(DeleteRecordingRequest) returns (DeleteRecordingResponse) {
    option (google.api.http) = {
      delete: "/v1/Recordings/{sid}"
    };
  }
}

// Generated from VideoV1Room operations.
service VideoV1RoomService {
  rpc ListRoom(ListRoomRequest) returns (ListRoomResponse) {
    option (google.api.http) = {
      get: "/v1/Rooms"
      response_body: "body"
    };
  }
  rpc CreateRoom(CreateRoomRequest) returns (CreateRoomResponse) {
    option (google.api.http) = {
      post: "/v1/Rooms"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchRoom(FetchRoomRequest) returns (FetchRoomResponse) {
    option (google.api.http) = {
      get: "/v1/Rooms/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateRoom(UpdateRoomRequest) returns (UpdateRoomResponse) {
    option (google.api.http) = {
      post: "/v1/Rooms/{sid}"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from VideoV1Participant operations.
service VideoV1ParticipantService {
  rpc ListRoomParticipant(ListRoomParticipantRequest) returns (ListRoomParticipantResponse) {
    option (google.api.http) = {
      get: "/v1/Rooms/{room_sid}/Participants"
      response_body: "body"
    };
  }
  rpc FetchRoomParticipant(FetchRoomParticipantRequest) returns (FetchRoomParticipantResponse) {
    option (google.api.http) = {
      get: "/v1/Rooms/{room_sid}/Participants/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateRoomParticipant(UpdateRoomParticipantRequest) returns (UpdateRoomParticipantResponse) {
    option (google.api.http) = {
      post: "/v1/Rooms/{room_sid}/Participants/{sid}"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from VideoV1PublishedTrack operations.
service VideoV1PublishedTrackService {
  // Returns a list of tracks associated with a given Participant. Only `currently` Published Tracks are in the list resource.
  rpc ListRoomParticipantPublishedTrack(ListRoomParticipantPublishedTrackRequest) returns (ListRoomParticipantPublishedTrackResponse) {
    option (google.api.http) = {
      get: "/v1/Rooms/{room_sid}/Participants/{participant_sid}/PublishedTracks"
      response_body: "body"
    };
  }
  // Returns a single Track resource represented by TrackName or SID.
  rpc FetchRoomParticipantPublishedTrack(FetchRoomParticipantPublishedTrackRequest) returns (FetchRoomParticipantPublishedTrackResponse) {
    option (google.api.http) = {
      get: "/v1/Rooms/{room_sid}/Participants/{participant_sid}/PublishedTracks/{sid}"
      response_body: "body"
    };
  }
}

// Generated from VideoV1SubscribeRules operations.
service VideoV1SubscribeRulesService {
  // Returns a list of Subscribe Rules for the Participant.
  rpc FetchRoomParticipantSubscribeRule(FetchRoomParticipantSubscribeRuleRequest) returns (FetchRoomParticipantSubscribeRuleResponse) {
    option (google.api.http) = {
      get: "/v1/Rooms/{room_sid}/Participants/{participant_sid}/SubscribeRules"
      response_body: "body"
    };
  }
  // Update the Subscribe Rules for the Participant
  rpc UpdateRoomParticipantSubscribeRule(UpdateRoomParticipantSubscribeRuleRequest) returns (UpdateRoomParticipantSubscribeRuleResponse) {
    option (google.api.http) = {
      post: "/v1/Rooms/{room_sid}/Participants/{participant_sid}/SubscribeRules"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from VideoV1SubscribedTrack operations.
service VideoV1SubscribedTrackService {
  // Returns a list of tracks that are subscribed for the participant.
  rpc ListRoomParticipantSubscribedTrack(ListRoomParticipantSubscribedTrackRequest) returns (ListRoomParticipantSubscribedTrackResponse) {
    option (google.api.http) = {
      get: "/v1/Rooms/{room_sid}/Participants/{participant_sid}/SubscribedTracks"
      response_body: "body"
    };
  }
  // Returns a single Track resource represented by `track_sid`.  Note: This is one resource with the Video API that requires a SID, be Track Name on the subscriber side is not guaranteed to be unique.
  rpc FetchRoomParticipantSubscribedTrack(FetchRoomParticipantSubscribedTrackRequest) returns (FetchRoomParticipantSubscribedTrackResponse) {
    option (google.api.http) = {
      get: "/v1/Rooms/{room_sid}/Participants/{participant_sid}/SubscribedTracks/{sid}"
      response_body: "body"
    };
  }
}

// Generated from VideoV1Anonymize operations.
service VideoV1AnonymizeService {
  rpc UpdateRoomParticipantAnonymize(UpdateRoomParticipantAnonymizeRequest) returns (UpdateRoomParticipantAnonymizeResponse) {
    option (google.api.http) = {
      post: "/v1/Rooms/{room_sid}/Participants/{sid}/Anonymize"
      response_body: "body"
    };
  }
}

// Generated from VideoV1RecordingRules operations.
service VideoV1RecordingRulesService {
  // Returns a list of Recording Rules for the Room.
  rpc FetchRoomRecordingRule(FetchRoomRecordingRuleRequest) returns (FetchRoomRecordingRuleResponse) {
    option (google.api.http) = {
      get: "/v1/Rooms/{room_sid}/RecordingRules"
      response_body: "body"
    };
  }
  // Update the Recording Rules for the Room
  rpc UpdateRoomRecordingRule(UpdateRoomRecordingRuleRequest) returns (UpdateRoomRecordingRuleResponse) {
    option (google.api.http) = {
      post: "/v1/Rooms/{room_sid}/RecordingRules"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from VideoV1RoomRecording operations.
service VideoV1RoomRecordingService {
  rpc ListRoomRecording(ListRoomRecordingRequest) returns (ListRoomRecordingResponse) {
    option (google.api.http) = {
      get: "/v1/Rooms/{room_sid}/Recordings"
      response_body: "body"
    };
  }
  rpc FetchRoomRecording(FetchRoomRecordingRequest) returns (FetchRoomRecordingResponse) {
    option (google.api.http) = {
      get: "/v1/Rooms/{room_sid}/Recordings/{sid}"
      response_body: "body"
    };
  }
  rpc DeleteRoomRecording(DeleteRoomRecordingRequest) returns (DeleteRoomRecordingResponse) {
    option (google.api.http) = {
      delete: "/v1/Rooms/{room_sid}/Recordings/{sid}"
    };
  }
}

// Generated from VideoV1Transcriptions operations.
service VideoV1TranscriptionsService {
  rpc ListRoomTranscriptions(ListRoomTranscriptionsRequest) returns (ListRoomTranscriptionsResponse) {
    option (google.api.http) = {
      get: "/v1/Rooms/{room_sid}/Transcriptions"
      response_body: "body"
    };
  }
  rpc CreateRoomTranscriptions(CreateRoomTranscriptionsRequest) returns (CreateRoomTranscriptionsResponse) {
    option (google.api.http) = {
      post: "/v1/Rooms/{room_sid}/Transcriptions"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchRoomTranscriptions(FetchRoomTranscriptionsRequest) returns (FetchRoomTranscriptionsResponse) {
    option (google.api.http) = {
      get: "/v1/Rooms/{room_sid}/Transcriptions/{ttid}"
      response_body: "body"
    };
  }
  rpc UpdateRoomTranscriptions(UpdateRoomTranscriptionsRequest) returns (UpdateRoomTranscriptionsResponse) {
    option (google.api.http) = {
      post: "/v1/Rooms/{room_sid}/Transcriptions/{ttid}"
      body: "body"
      response_body: "body"
    };
  }
}

