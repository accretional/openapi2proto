// Code generated by openapi2proto. DO NOT EDIT.
// Source: twilio_messaging_v1.json

syntax = "proto3";

package twilio.messaging.v1;

option go_package = "twilio/messaging/v1;twiliomessagingv1";

import "google/api/annotations.proto";

// DEPRECATED. Feedback on how to improve brand score
message BrandRegistrationsEnumBrandFeedback {
  // Scalar value wrapper.
  string value = 1;
}

// When a brand is registered, TCR will attempt to verify the identity of the brand based on the supplied information.
message BrandRegistrationsEnumIdentityStatus {
  // Scalar value wrapper.
  string value = 1;
}

// Brand Registration status. One of "PENDING", "APPROVED", "FAILED", "IN_REVIEW", "DELETION_PENDING", "DELETION_FAILED", "SUSPENDED".
message BrandRegistrationsEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

// The third-party provider that has conducted the vetting. One of “CampaignVerify” (Campaign Verify tokens) or “AEGIS” (Secondary Vetting).
message BrandVettingEnumVettingProvider {
  // Scalar value wrapper.
  string value = 1;
}

message MessagingV1BrandRegistrations {
  // A2P Messaging Profile Bundle BundleSid.
  string a2p_profile_bundle_sid = 1;
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Brand Registration resource.
  string account_sid = 2;
  // DEPRECATED. Feedback on how to improve brand score
  repeated BrandRegistrationsEnumBrandFeedback brand_feedback = 3;
  // The secondary vetting score if it was done. Otherwise, it will be the brand score if it's returned from TCR. It may be null if no score is available.
  int64 brand_score = 4;
  // Type of brand. One of: "STANDARD", "SOLE_PROPRIETOR". SOLE_PROPRIETOR is for the low volume, SOLE_PROPRIETOR campaign use case. There can only be one SOLE_PROPRIETOR campaign created per SOLE_PROPRIETOR brand. STANDARD is for all other campaign use cases. Multiple campaign use cases can be created per STANDARD brand.
  string brand_type = 5;
  // A2P Messaging Profile Bundle BundleSid.
  string customer_profile_bundle_sid = 6;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 7;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 8;
  // A list of errors that occurred during the brand registration process.
  repeated string errors = 9;
  // DEPRECATED. A reason why brand registration has failed. Only applicable when status is FAILED.
  string failure_reason = 10;
  // Identified as a government entity
  bool government_entity = 11;
  // OpenAPI property "identity_status".
  BrandRegistrationsEnumIdentityStatus identity_status = 12;
  MessagingV1BrandRegistrationsLinks links = 13;
  // A boolean that specifies whether brand should be a mock or not. If true, brand will be registered as a mock brand. Defaults to false if no value is provided.
  bool mock = 14;
  // Publicly traded company identified in the Russell 3000 Index
  bool russell_3000 = 15;
  // The unique string to identify Brand Registration.
  string sid = 16;
  // A flag to disable automatic secondary vetting for brands which it would otherwise be done.
  bool skip_automatic_sec_vet = 17;
  // OpenAPI property "status".
  BrandRegistrationsEnumStatus status = 18;
  // Nonprofit organization tax-exempt status per section 501 of the U.S. tax code.
  string tax_exempt_status = 19;
  // Campaign Registry (TCR) Brand ID. Assigned only after successful brand registration.
  string tcr_id = 20;
  // The absolute URL of the Brand Registration resource.
  string url = 21;
}

message MessagingV1BrandRegistrationsLinks {
  // Scalar value wrapper.
  string value = 1;
}

message MessagingV1BrandRegistrationsBrandRegistrationOtp {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Brand Registration resource.
  string account_sid = 1;
  // The unique string to identify Brand Registration of Sole Proprietor Brand
  string brand_registration_sid = 2;
}

message MessagingV1BrandRegistrationsBrandVetting {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the vetting record.
  string account_sid = 1;
  // The unique string to identify Brand Registration.
  string brand_sid = 2;
  // The Twilio SID of the third-party vetting record.
  string brand_vetting_sid = 3;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 4;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 5;
  // The absolute URL of the Brand Vetting resource.
  string url = 6;
  // The type of vetting that has been conducted. One of “STANDARD” (Aegis) or “POLITICAL” (Campaign Verify).
  string vetting_class = 7;
  // The unique identifier of the vetting from the third-party provider.
  string vetting_id = 8;
  // OpenAPI property "vetting_provider".
  BrandVettingEnumVettingProvider vetting_provider = 9;
  // The status of the import vetting attempt. One of “PENDING,” “SUCCESS,” or “FAILED”.
  string vetting_status = 10;
}

message MessagingV1Deactivation {
  // Returns an authenticated url that redirects to a file containing the deactivated numbers for the requested day. This url is valid for up to two minutes.
  string redirect_to = 1;
}

message MessagingV1DomainCertV4 {
  // Optional JSON field describing the status and upload date of a new certificate in the process of validation
  string cert_in_validation = 1;
  // The unique string that we created to identify this Certificate resource.
  string certificate_sid = 2;
  // Date that this Domain was registered to the Twilio platform to create a new Domain object.
  string date_created = 3;
  // Date that the private certificate associated with this domain expires. You will need to update the certificate before that date to ensure your shortened links will continue to work.
  string date_expires = 4;
  // Date that this Domain was last updated.
  string date_updated = 5;
  // Full url path for this domain.
  string domain_name = 6;
  // The unique string that we created to identify the Domain resource.
  string domain_sid = 7;
  string url = 8;
}

message MessagingV1DomainConfig {
  // URL to receive click events to your webhook whenever the recipients click on the shortened links.
  string callback_url = 1;
  // The unique string that we created to identify the Domain config (prefix ZK).
  string config_sid = 2;
  // Boolean field to set customer delivery preference when there is a failure in linkShortening service
  bool continue_on_failure = 3;
  // Date this Domain Config was created.
  string date_created = 4;
  // Date that this Domain Config was last updated.
  string date_updated = 5;
  // Customer's choice to send links with/without "https://" attached to shortened url. If true, messages will not be sent with https:// at the beginning of the url. If false, messages will be sent with https:// at the beginning of the url. False is the default behavior if it is not specified.
  bool disable_https = 6;
  // The unique string that we created to identify the Domain resource.
  string domain_sid = 7;
  // Any requests we receive to this domain that do not match an existing shortened message will be redirected to the fallback url. These will likely be either expired messages, random misdirected traffic, or intentional scraping.
  string fallback_url = 8;
  string url = 9;
}

message MessagingV1DomainConfigMessagingService {
  // URL to receive click events to your webhook whenever the recipients click on the shortened links.
  string callback_url = 1;
  // The unique string that we created to identify the Domain config (prefix ZK).
  string config_sid = 2;
  // Boolean field to set customer delivery preference when there is a failure in linkShortening service
  bool continue_on_failure = 3;
  // Date this Domain Config was created.
  string date_created = 4;
  // Date that this Domain Config was last updated.
  string date_updated = 5;
  // The unique string that we created to identify the Domain resource.
  string domain_sid = 6;
  // Any requests we receive to this domain that do not match an existing shortened message will be redirected to the fallback url. These will likely be either expired messages, random misdirected traffic, or intentional scraping.
  string fallback_url = 7;
  // The unique string that identifies the messaging service
  string messaging_service_sid = 8;
  string url = 9;
}

message MessagingV1DomainDnsValidation {
  // The unique string that we created to identify the Domain resource.
  string domain_sid = 1;
  bool is_valid = 2;
  string reason = 3;
  string url = 4;
}

message MessagingV1ExternalCampaign {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that the Campaign belongs to.
  string account_sid = 1;
  // ID of the preregistered campaign.
  string campaign_id = 2;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The SID of the [Messaging Service](https://www.twilio.com/docs/messaging/api/service-resource) that the resource is associated with.
  string messaging_service_sid = 4;
  // The unique string that identifies a US A2P Compliance resource `QE2c6890da8086d771620e9b13fadeba0b`.
  string sid = 5;
}

message MessagingV1LinkshorteningMessagingService {
  // The unique string identifies the domain resource
  string domain_sid = 1;
  // The unique string that identifies the messaging service
  string messaging_service_sid = 2;
  string url = 3;
}

message MessagingV1LinkshorteningMessagingServiceDomainAssociation {
  // The unique string that we created to identify the Domain resource.
  string domain_sid = 1;
  // The unique string that identifies the messaging service
  string messaging_service_sid = 2;
  string url = 3;
}

message MessagingV1RequestManagedCert {
  // The unique string that we created to identify this Certificate resource.
  string certificate_sid = 1;
  // Date that this Domain was registered to the Twilio platform to create a new Domain object.
  string date_created = 2;
  // Date that the private certificate associated with this domain expires. This is the expiration date of your existing cert.
  string date_expires = 3;
  // Date that this Domain was last updated.
  string date_updated = 4;
  // Full url path for this domain.
  string domain_name = 5;
  // The unique string that we created to identify the Domain resource.
  string domain_sid = 6;
  // A boolean flag indicating if the certificate is managed by Twilio.
  bool managed = 7;
  // A boolean flag indicating if a managed certificate needs to be fulfilled by Twilio.
  bool requesting = 8;
  string url = 9;
}

message MessagingV1Service {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Service resource.
  string account_sid = 1;
  // Whether to enable [Area Code Geomatch](https://www.twilio.com/docs/messaging/services#area-code-geomatch) on the Service Instance.
  bool area_code_geomatch = 2;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 4;
  // The HTTP method we use to call `fallback_url`. Can be: `GET` or `POST`.
  string fallback_method = 5;
  // [OBSOLETE] Former feature used to fallback to long code sender after certain short code message failures.
  bool fallback_to_long_code = 6;
  // The URL that we call using `fallback_method` if an error occurs while retrieving or executing the TwiML from the Inbound Request URL. If the `use_inbound_webhook_on_number` field is enabled then the webhook url defined on the phone number will override the `fallback_url` defined for the Messaging Service.
  string fallback_url = 7;
  // The string that you assigned to describe the resource.
  string friendly_name = 8;
  // The HTTP method we use to call `inbound_request_url`. Can be `GET` or `POST`.
  string inbound_method = 9;
  // The URL we call using `inbound_method` when a message is received by any phone number or short code in the Service. When this property is `null`, receiving inbound messages is disabled. All messages sent to the Twilio phone number or short code will not be logged and received on the Account. If the `use_inbound_webhook_on_number` field is enabled then the webhook url defined on the phone number will override the `inbound_request_url` defined for the Messaging Service.
  string inbound_request_url = 10;
  // The absolute URLs of related resources.
  MessagingV1ServiceLinks links = 11;
  // Whether to enable the [MMS Converter](https://www.twilio.com/docs/messaging/services#mms-converter) for messages sent through the Service instance.
  bool mms_converter = 12;
  // OpenAPI property "scan_message_content".
  ServiceEnumScanMessageContent scan_message_content = 13;
  // The unique string that we created to identify the Service resource.
  string sid = 14;
  // Whether to enable [Smart Encoding](https://www.twilio.com/docs/messaging/services#smart-encoding) for messages sent through the Service instance.
  bool smart_encoding = 15;
  // The URL we call to [pass status updates](https://www.twilio.com/docs/sms/api/message-resource#message-status-values) about message delivery.
  string status_callback = 16;
  // Whether to enable [Sticky Sender](https://www.twilio.com/docs/messaging/services#sticky-sender) on the Service instance.
  bool sticky_sender = 17;
  // Reserved.
  bool synchronous_validation = 18;
  // The absolute URL of the Service resource.
  string url = 19;
  // Whether US A2P campaign is registered for this Service.
  bool us_app_to_person_registered = 20;
  // A boolean value that indicates either the webhook url configured on the phone number will be used or `inbound_request_url`/`fallback_url` url will be called when a message is received from the phone number. If this field is enabled then the webhook url defined on the phone number will override the `inbound_request_url`/`fallback_url` defined for the Messaging Service.
  bool use_inbound_webhook_on_number = 21;
  // A string that describes the scenario in which the Messaging Service will be used. Possible values are `notifications`, `marketing`, `verification`, `discussion`, `poll`, `undeclared`.
  string usecase = 22;
  // How long, in seconds, messages sent from the Service are valid. Can be an integer from `1` to `36,000`. Default value is `36,000`.
  int64 validity_period = 23;
}

// The absolute URLs of related resources.
message MessagingV1ServiceLinks {
  // Scalar value wrapper.
  string value = 1;
}

// Reserved.
message ServiceEnumScanMessageContent {
  // Scalar value wrapper.
  string value = 1;
}

message MessagingV1ServiceAddons {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the add on resource.
  string account_sid = 1;
  // The config of the add on in JSON string format.
  string add_on_config = 2;
  // The SID that identifies the add on type.
  string add_on_type_sid = 3;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 4;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 5;
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) the resource is associated with.
  string service_sid = 6;
  // The unique string that we created to identify the add on resource.
  string sid = 7;
  // The absolute URL of the add on resource.
  string url = 8;
}

message MessagingV1ServiceAlphaSender {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the AlphaSender resource.
  string account_sid = 1;
  // The Alphanumeric Sender ID string.
  string alpha_sender = 2;
  // An array of values that describe whether the number can receive calls or messages. Can be: `SMS`.
  repeated string capabilities = 3;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 4;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 5;
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) the resource is associated with.
  string service_sid = 6;
  // The unique string that we created to identify the AlphaSender resource.
  string sid = 7;
  // The absolute URL of the AlphaSender resource.
  string url = 8;
}

message MessagingV1ServiceChannelSender {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the ChannelSender resource.
  string account_sid = 1;
  // The 2-character [ISO Country Code](https://www.iso.org/iso-3166-country-codes.html) of the number.
  string country_code = 2;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 4;
  // The SID of the [Service](https://www.twilio.com/docs/messaging/services) the resource is associated with.
  string messaging_service_sid = 5;
  // The unique string that identifies the sender e.g whatsapp:+123456XXXX.
  string sender = 6;
  // A string value that identifies the sender type e.g WhatsApp, Messenger.
  string sender_type = 7;
  // The unique string that we created to identify the ChannelSender resource.
  string sid = 8;
  // The absolute URL of the ChannelSender resource.
  string url = 9;
}

message MessagingV1ServiceDestinationAlphaSender {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the AlphaSender resource.
  string account_sid = 1;
  // The Alphanumeric Sender ID string.
  string alpha_sender = 2;
  // An array of values that describe whether the number can receive calls or messages. Can be: `SMS`.
  repeated string capabilities = 3;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 4;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 5;
  // The Two Character ISO Country Code the Alphanumeric Sender ID will be used for. For Default Alpha Senders that work across countries, this value will be an empty string
  string iso_country_code = 6;
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) the resource is associated with.
  string service_sid = 7;
  // The unique string that we created to identify the AlphaSender resource.
  string sid = 8;
  // The absolute URL of the AlphaSender resource.
  string url = 9;
}

message MessagingV1ServiceGenericSender {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the number or channel sender resource.
  string account_sid = 1;
  // The 2-character [ISO Country Code](https://www.iso.org/iso-3166-country-codes.html) of the number or channel sender.
  string country_code = 2;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 4;
  // The unique string that identifies the number in [E.164](https://www.twilio.com/docs/glossary/what-e164) format or the channel sender e.g whatsapp:+123456XXXX.
  string sender = 5;
  // A string value that identifies the number or channel sender type e.g AlphaSenderId, LongCode, ShortCode, Whatsapp, RCS.
  string sender_type = 6;
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) the resource is associated with.
  string service_sid = 7;
  // The SID to identify the number or channel sender resource.
  string sid = 8;
}

message MessagingV1ServicePhoneNumber {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the PhoneNumber resource.
  string account_sid = 1;
  // An array of values that describe whether the number can receive calls or messages. Can be: `Voice`, `SMS`, and `MMS`.
  repeated string capabilities = 2;
  // The 2-character [ISO Country Code](https://www.iso.org/iso-3166-country-codes.html) of the number.
  string country_code = 3;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 4;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 5;
  // The phone number in [E.164](https://www.twilio.com/docs/glossary/what-e164) format, which consists of a + followed by the country code and subscriber number.
  string phone_number = 6;
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) the resource is associated with.
  string service_sid = 7;
  // The unique string that we created to identify the PhoneNumber resource.
  string sid = 8;
  // The absolute URL of the PhoneNumber resource.
  string url = 9;
}

message MessagingV1ServiceServiceForNumber {
  // The string that you assigned to describe the messaging service resource.
  string friendly_name = 1;
  // The SID to identify the number resource.
  string number_sid = 2;
  // The SID of the messaging service that the phone number is in.
  string sid = 3;
  // The absolute URL of the Service resource.
  string url = 4;
}

message MessagingV1ServiceShortCode {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the ShortCode resource.
  string account_sid = 1;
  // An array of values that describe whether the number can receive calls or messages. Can be: `SMS` and `MMS`.
  repeated string capabilities = 2;
  // The 2-character [ISO Country Code](https://www.iso.org/iso-3166-country-codes.html) of the number.
  string country_code = 3;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 4;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 5;
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) the resource is associated with.
  string service_sid = 6;
  // The [E.164](https://www.twilio.com/docs/glossary/what-e164) format of the short code.
  string short_code = 7;
  // The unique string that we created to identify the ShortCode resource.
  string sid = 8;
  // The absolute URL of the ShortCode resource.
  string url = 9;
}

message MessagingV1ServiceUsAppToPerson {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that the Campaign belongs to.
  string account_sid = 1;
  // A boolean that specifies whether campaign is age gated or not.
  bool age_gated = 2;
  // The unique string to identify the A2P brand.
  string brand_registration_sid = 3;
  // The Campaign Registry (TCR) Campaign ID.
  string campaign_id = 4;
  // Campaign status. Examples: IN_PROGRESS, VERIFIED, FAILED.
  string campaign_status = 5;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 6;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 7;
  // A short description of what this SMS campaign does. Min length: 40 characters. Max length: 4096 characters.
  string description = 8;
  // A boolean that specifies whether campaign allows direct lending or not.
  bool direct_lending = 9;
  // Details indicating why a campaign registration failed. These errors can indicate one or more fields that were incorrect or did not meet review requirements.
  repeated string errors = 10;
  // Indicate that this SMS campaign will send messages that contain links.
  bool has_embedded_links = 11;
  // Indicates that this SMS campaign will send messages that contain phone numbers.
  bool has_embedded_phone = 12;
  // End users should be able to text in a keyword to receive help. Those keywords must be provided as part of the campaign registration request. This field is required if managing help keywords yourself (i.e. not using Twilio's Default or Advanced Opt Out features). Values must be alphanumeric. 255 character maximum.
  repeated string help_keywords = 13;
  // When customers receive the help keywords from their end users, Twilio customers are expected to send back an auto-generated response; this may include the brand name and additional support contact information. This field is required if managing help keywords yourself (i.e. not using Twilio's Default or Advanced Opt Out features). 20 character minimum. 320 character maximum.
  string help_message = 14;
  // Indicates whether the campaign was registered externally or not.
  bool is_externally_registered = 15;
  // Details around how a consumer opts-in to their campaign, therefore giving consent to receive their messages. If multiple opt-in methods can be used for the same campaign, they must all be listed. 40 character minimum. 2048 character maximum.
  string message_flow = 16;
  // An array of sample message strings, min two and max five. Min length for each sample: 20 chars. Max length for each sample: 1024 chars.
  repeated string message_samples = 17;
  // The SID of the [Messaging Service](https://www.twilio.com/docs/messaging/api/service-resource) that the resource is associated with.
  string messaging_service_sid = 18;
  // A boolean that specifies whether campaign is a mock or not. Mock campaigns will be automatically created if using a mock brand. Mock campaigns should only be used for testing purposes.
  bool mock = 19;
  // If end users can text in a keyword to start receiving messages from this campaign, those keywords must be provided. This field is required if end users can text in a keyword to start receiving messages from this campaign. Values must be alphanumeric. 255 character maximum.
  repeated string opt_in_keywords = 20;
  // If end users can text in a keyword to start receiving messages from this campaign, the auto-reply messages sent to the end users must be provided. The opt-in response should include the Brand name, confirmation of opt-in enrollment to a recurring message campaign, how to get help, and clear description of how to opt-out. This field is required if end users can text in a keyword to start receiving messages from this campaign. 20 character minimum. 320 character maximum.
  string opt_in_message = 21;
  // End users should be able to text in a keyword to stop receiving messages from this campaign. Those keywords must be provided. This field is required if managing opt out keywords yourself (i.e. not using Twilio's Default or Advanced Opt Out features). Values must be alphanumeric. 255 character maximum.
  repeated string opt_out_keywords = 22;
  // Upon receiving the opt-out keywords from the end users, Twilio customers are expected to send back an auto-generated response, which must provide acknowledgment of the opt-out request and confirmation that no further messages will be sent. It is also recommended that these opt-out messages include the brand name. This field is required if managing opt out keywords yourself (i.e. not using Twilio's Default or Advanced Opt Out features). 20 character minimum. 320 character maximum.
  string opt_out_message = 23;
  // Rate limit and/or classification set by each carrier, Ex. AT&T or T-Mobile.
  string rate_limits = 24;
  // The unique string that identifies a US A2P Compliance resource `QE2c6890da8086d771620e9b13fadeba0b`.
  string sid = 25;
  // A boolean that specifies whether campaign has Subscriber Optin or not.
  bool subscriber_opt_in = 26;
  // The absolute URL of the US App to Person resource.
  string url = 27;
  // A2P Campaign Use Case. Examples: [ 2FA, EMERGENCY, MARKETING, SOLE_PROPRIETOR...]. SOLE_PROPRIETOR campaign use cases can only be created by SOLE_PROPRIETOR Brands, and there can only be one SOLE_PROPRIETOR campaign created per SOLE_PROPRIETOR Brand.
  string us_app_to_person_usecase = 28;
}

message MessagingV1ServiceUsAppToPersonResponse {
  // Scalar value wrapper.
  string value = 1;
}

message MessagingV1ServiceUsAppToPersonUsecase {
  // Human readable name, code, description and post_approval_required (indicates whether or not post approval is required for this Use Case) of A2P Campaign Use Cases.
  repeated string us_app_to_person_usecases = 1;
}

message MessagingV1ServiceUsAppToPersonV2 {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that the Campaign belongs to.
  string account_sid = 1;
  // A boolean that specifies whether campaign is age gated or not.
  bool age_gated = 2;
  // The unique string to identify the A2P brand.
  string brand_registration_sid = 3;
  // The Campaign Registry (TCR) Campaign ID.
  string campaign_id = 4;
  // Campaign status. Examples: IN_PROGRESS, VERIFIED, FAILED.
  string campaign_status = 5;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 6;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 7;
  // A short description of what this SMS campaign does. Min length: 40 characters. Max length: 4096 characters.
  string description = 8;
  // A boolean that specifies whether campaign allows direct lending or not.
  bool direct_lending = 9;
  // Details indicating why a campaign registration failed. These errors can indicate one or more fields that were incorrect or did not meet review requirements.
  repeated string errors = 10;
  // Indicate that this SMS campaign will send messages that contain links.
  bool has_embedded_links = 11;
  // Indicates that this SMS campaign will send messages that contain phone numbers.
  bool has_embedded_phone = 12;
  // End users should be able to text in a keyword to receive help. Those keywords must be provided as part of the campaign registration request. This field is required if managing help keywords yourself (i.e. not using Twilio's Default or Advanced Opt Out features). Values must be alphanumeric. 255 character maximum.
  repeated string help_keywords = 13;
  // When customers receive the help keywords from their end users, Twilio customers are expected to send back an auto-generated response; this may include the brand name and additional support contact information. This field is required if managing help keywords yourself (i.e. not using Twilio's Default or Advanced Opt Out features). 20 character minimum. 320 character maximum.
  string help_message = 14;
  // Indicates whether the campaign was registered externally or not.
  bool is_externally_registered = 15;
  // Details around how a consumer opts-in to their campaign, therefore giving consent to receive their messages. If multiple opt-in methods can be used for the same campaign, they must all be listed. 40 character minimum. 2048 character maximum.
  string message_flow = 16;
  // An array of sample message strings, min two and max five. Min length for each sample: 20 chars. Max length for each sample: 1024 chars.
  repeated string message_samples = 17;
  // The SID of the [Messaging Service](https://www.twilio.com/docs/messaging/api/service-resource) that the resource is associated with.
  string messaging_service_sid = 18;
  // A boolean that specifies whether campaign is a mock or not. Mock campaigns will be automatically created if using a mock brand. Mock campaigns should only be used for testing purposes.
  bool mock = 19;
  // If end users can text in a keyword to start receiving messages from this campaign, those keywords must be provided. This field is required if end users can text in a keyword to start receiving messages from this campaign. Values must be alphanumeric. 255 character maximum.
  repeated string opt_in_keywords = 20;
  // If end users can text in a keyword to start receiving messages from this campaign, the auto-reply messages sent to the end users must be provided. The opt-in response should include the Brand name, confirmation of opt-in enrollment to a recurring message campaign, how to get help, and clear description of how to opt-out. This field is required if end users can text in a keyword to start receiving messages from this campaign. 20 character minimum. 320 character maximum.
  string opt_in_message = 21;
  // End users should be able to text in a keyword to stop receiving messages from this campaign. Those keywords must be provided. This field is required if managing opt out keywords yourself (i.e. not using Twilio's Default or Advanced Opt Out features). Values must be alphanumeric. 255 character maximum.
  repeated string opt_out_keywords = 22;
  // Upon receiving the opt-out keywords from the end users, Twilio customers are expected to send back an auto-generated response, which must provide acknowledgment of the opt-out request and confirmation that no further messages will be sent. It is also recommended that these opt-out messages include the brand name. This field is required if managing opt out keywords yourself (i.e. not using Twilio's Default or Advanced Opt Out features). 20 character minimum. 320 character maximum.
  string opt_out_message = 23;
  // The URL of the privacy policy for the campaign.
  string privacy_policy_url = 24;
  // Rate limit and/or classification set by each carrier, Ex. AT&T or T-Mobile.
  string rate_limits = 25;
  // The unique string that identifies a US A2P Compliance resource `QE2c6890da8086d771620e9b13fadeba0b`.
  string sid = 26;
  // A boolean that specifies whether campaign has Subscriber Optin or not.
  bool subscriber_opt_in = 27;
  // The URL of the terms and conditions for the campaign.
  string terms_and_conditions_url = 28;
  // The absolute URL of the US App to Person resource.
  string url = 29;
  // A2P Campaign Use Case. Examples: [ 2FA, EMERGENCY, MARKETING, SOLE_PROPRIETOR...]. SOLE_PROPRIETOR campaign use cases can only be created by SOLE_PROPRIETOR Brands, and there can only be one SOLE_PROPRIETOR campaign created per SOLE_PROPRIETOR Brand.
  string us_app_to_person_usecase = 30;
}

message MessagingV1TollfreeVerification {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Tollfree Verification resource.
  string account_sid = 1;
  // Additional information to be provided for verification.
  string additional_information = 2;
  // Indicates if the content is age gated.
  bool age_gated_content = 3;
  // The city of the business or organization using the Tollfree number.
  string business_city = 4;
  // The email address of the contact for the business or organization using the Tollfree number.
  string business_contact_email = 5;
  // The first name of the contact for the business or organization using the Tollfree number.
  string business_contact_first_name = 6;
  // The last name of the contact for the business or organization using the Tollfree number.
  string business_contact_last_name = 7;
  // The E.164 formatted phone number of the contact for the business or organization using the Tollfree number.
  string business_contact_phone = 8;
  // The country of the business or organization using the Tollfree number.
  string business_country = 9;
  // The name of the business or organization using the Tollfree number.
  string business_name = 10;
  // The postal code of the business or organization using the Tollfree number.
  string business_postal_code = 11;
  // OpenAPI property "business_registration_authority".
  TollfreeVerificationEnumBusinessRegistrationAuthority business_registration_authority = 12;
  // Country business is registered in
  string business_registration_country = 13;
  // A legally recognized business registration number
  string business_registration_number = 14;
  // The E.164 formatted number associated with the business.
  string business_registration_phone_number = 15;
  // The state/province/region of the business or organization using the Tollfree number.
  string business_state_province_region = 16;
  // The address of the business or organization using the Tollfree number.
  string business_street_address = 17;
  // The address of the business or organization using the Tollfree number.
  string business_street_address2 = 18;
  // OpenAPI property "business_type".
  TollfreeVerificationEnumBusinessType business_type = 19;
  // The website of the business or organization using the Tollfree number.
  string business_website = 20;
  // Customer's Profile Bundle BundleSid.
  string customer_profile_sid = 21;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 22;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 23;
  // Trade name, sub entity, or downstream business name of business being submitted for verification
  string doing_business_as = 24;
  // If a rejected verification is allowed to be edited/resubmitted. Some rejection reasons allow editing and some do not.
  bool edit_allowed = 25;
  // The date and time when the ability to edit a rejected verification expires.
  string edit_expiration = 26;
  // The error code given when a Tollfree Verification has been rejected.
  int64 error_code = 27;
  // An optional external reference ID supplied by customer and echoed back on status retrieval.
  string external_reference_id = 28;
  // A sample help message provided to users.
  string help_message_sample = 29;
  // Estimate monthly volume of messages from the Tollfree Number.
  string message_volume = 30;
  // The email address to receive the notification about the verification result. .
  string notification_email = 31;
  // The confirmation message sent to users when they opt in to receive messages.
  string opt_in_confirmation_message = 32;
  // Link to an image that shows the opt-in workflow. Multiple images allowed and must be a publicly hosted URL.
  repeated string opt_in_image_urls = 33;
  // List of keywords that users can send to opt in or out of messages.
  repeated string opt_in_keywords = 34;
  // OpenAPI property "opt_in_type".
  TollfreeVerificationEnumOptInType opt_in_type = 35;
  // The URL to the privacy policy for the business or organization.
  string privacy_policy_url = 36;
  // An example of message content, i.e. a sample message.
  string production_message_sample = 37;
  // The SID of the Regulated Item.
  string regulated_item_sid = 38;
  // The rejection reason given when a Tollfree Verification has been rejected.
  string rejection_reason = 39;
  // A list of rejection reasons and codes describing why a Tollfree Verification has been rejected.
  repeated string rejection_reasons = 40;
  // The URLs of the documents associated with the Tollfree Verification resource.
  string resource_links = 41;
  // The unique string to identify Tollfree Verification.
  string sid = 42;
  // OpenAPI property "status".
  TollfreeVerificationEnumStatus status = 43;
  // The URL of the terms and conditions for the business or organization.
  string terms_and_conditions_url = 44;
  // The E.164 formatted toll-free phone number associated with the verification.
  string tollfree_phone_number = 45;
  // The SID of the Phone Number associated with the Tollfree Verification.
  string tollfree_phone_number_sid = 46;
  // Tollfree TrustProduct Bundle BundleSid.
  string trust_product_sid = 47;
  // The absolute URL of the Tollfree Verification resource.
  string url = 48;
  // OpenAPI property "use_case_categories".
  TollfreeVerificationEnumUseCaseCategories use_case_categories = 49;
  // Use this to further explain how messaging is used by the business or organization.
  string use_case_summary = 50;
  string vetting_id = 51;
  string vetting_id_expiration = 52;
  // OpenAPI property "vetting_provider".
  TollfreeVerificationEnumVettingProvider vetting_provider = 53;
}

// The organizational authority for business registrations. Required for all business types except SOLE_PROPRIETOR.
message TollfreeVerificationEnumBusinessRegistrationAuthority {
  // Scalar value wrapper.
  string value = 1;
}

// The type of business, valid values are PRIVATE_PROFIT, PUBLIC_PROFIT, NON_PROFIT, SOLE_PROPRIETOR, GOVERNMENT. Required field.
message TollfreeVerificationEnumBusinessType {
  // Scalar value wrapper.
  string value = 1;
}

// Describe how a user opts-in to text messages.
message TollfreeVerificationEnumOptInType {
  // Scalar value wrapper.
  string value = 1;
}

// The compliance status of the Tollfree Verification record.
message TollfreeVerificationEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

// The category of the use case for the Tollfree Number. List as many as are applicable.
message TollfreeVerificationEnumUseCaseCategories {
  // Array values.
  repeated string items = 1;
}

// The third-party political vetting provider.
message TollfreeVerificationEnumVettingProvider {
  // Scalar value wrapper.
  string value = 1;
}

message MessagingV1Usecase {
  // Human readable use case details (usecase, description and purpose) of Messaging Service Use Cases.
  repeated string usecases = 1;
}

// The status of the sender. Configuring: We are in the process of registering the sender. If your sender stays in this state for a long period of time it is possible that there is an issue with parameters you provided; PendingVerification: We have successfully registered the sender with WhatsApp and you should receive a code from their services; Configured: The sender has been successfully verified with WhatsApp and is all set to start sending messages; ConfigurationError - If configuration fails due to below possibilities: parameters provided were incorrect, Twilio account suspended or deleted, whatsapp api failed, Twilio internal error. VerificationError - If verification api fails, please check error_message for more details
message WhatsappSenderEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

// The Category of this WhatsApp Template. One of `ACCOUNT_UPDATE`, `ALERT_UPDATE`, `APPOINTMENT_UPDATE`, `AUTO_REPLY`, `ISSUE_RESOLUTION`, `PAYMENT_UPDATE`, `PERSONAL_FINANCE_UPDATE`, `RESERVATION_UPDATE`, `SHIPPING_UPDATE`, `TICKET_UPDATE`, `TRANSPORTATION_UPDATE`, `MARKETING`, `AUTHENTICATION`, `UTILITY`, `OTP` or `TRANSACTIONAL`.
message WhatsappTemplateEnumCategory {
  // Scalar value wrapper.
  string value = 1;
}

// Fetch a list of all United States numbers that have been deactivated on a specific date.
message FetchDeactivationRequest {
  // The request will return a list of all United States Phone Numbers that were deactivated on the day specified by this parameter. This date should be specified in YYYY-MM-DD format.
  string date = 1;
}

// Canonical success response.
message FetchDeactivationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1Deactivation body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchDomainCertV4Request {
  // Unique string used to identify the domain that this certificate should be associated with. Required.
  string domain_sid = 1;
}

// Canonical success response.
message FetchDomainCertV4Response {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1DomainCertV4 body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateDomainCertV4Request
message UpdateDomainCertV4RequestBody {
  // Contains the full TLS certificate and private for this domain in PEM format: https://en.wikipedia.org/wiki/Privacy-Enhanced_Mail. Twilio uses this information to process HTTPS traffic sent to your domain.
  string tls_cert = 1;
}

message UpdateDomainCertV4Request {
  // Unique string used to identify the domain that this certificate should be associated with. Required.
  string domain_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateDomainCertV4RequestBody body = 2;
}

// Canonical success response.
message UpdateDomainCertV4Response {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1DomainCertV4 body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeleteDomainCertV4Request {
  // Unique string used to identify the domain that this certificate should be associated with. Required.
  string domain_sid = 1;
}

// Canonical success response.
message DeleteDomainCertV4Response {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

message FetchDomainConfigRequest {
  // Unique string used to identify the domain that this config should be associated with. Required.
  string domain_sid = 1;
}

// Canonical success response.
message FetchDomainConfigResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1DomainConfig body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateDomainConfigRequest
message UpdateDomainConfigRequestBody {
  // URL to receive click events to your webhook whenever the recipients click on the shortened links
  string callback_url = 1;
  // Boolean field to set customer delivery preference when there is a failure in linkShortening service
  bool continue_on_failure = 2;
  // Customer's choice to send links with/without "https://" attached to shortened url. If true, messages will not be sent with https:// at the beginning of the url. If false, messages will be sent with https:// at the beginning of the url. False is the default behavior if it is not specified.
  bool disable_https = 3;
  // Any requests we receive to this domain that do not match an existing shortened message will be redirected to the fallback url. These will likely be either expired messages, random misdirected traffic, or intentional scraping.
  string fallback_url = 4;
}

message UpdateDomainConfigRequest {
  // Unique string used to identify the domain that this config should be associated with. Required.
  string domain_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateDomainConfigRequestBody body = 2;
}

// Canonical success response.
message UpdateDomainConfigResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1DomainConfig body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message CreateLinkshorteningMessagingServiceRequest {
  // The domain SID to associate with a messaging service. With URL shortening enabled, links in messages sent with the associated messaging service will be shortened to the provided domain Required.
  string domain_sid = 1;
  // A messaging service SID to associate with a domain. With URL shortening enabled, links in messages sent with the provided messaging service will be shortened to the associated domain Required.
  string messaging_service_sid = 2;
}

// Canonical success response.
message CreateLinkshorteningMessagingServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1LinkshorteningMessagingService body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeleteLinkshorteningMessagingServiceRequest {
  // The domain SID to dissociate from a messaging service. With URL shortening enabled, links in messages sent with the associated messaging service will be shortened to the provided domain Required.
  string domain_sid = 1;
  // A messaging service SID to dissociate from a domain. With URL shortening enabled, links in messages sent with the provided messaging service will be shortened to the associated domain Required.
  string messaging_service_sid = 2;
}

// Canonical success response.
message DeleteLinkshorteningMessagingServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

message UpdateRequestManagedCertRequest {
  // Unique string used to identify the domain that this certificate should be associated with. Required.
  string domain_sid = 1;
}

// Canonical success response.
message UpdateRequestManagedCertResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1RequestManagedCert body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchDomainDnsValidationRequest {
  // Unique string used to identify the domain. Required.
  string domain_sid = 1;
}

// Canonical success response.
message FetchDomainDnsValidationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1DomainDnsValidation body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchDomainConfigMessagingServiceRequest {
  // Unique string used to identify the Messaging service that this domain should be associated with. Required.
  string messaging_service_sid = 1;
}

// Canonical success response.
message FetchDomainConfigMessagingServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1DomainConfigMessagingService body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchLinkshorteningMessagingServiceDomainAssociationRequest {
  // Unique string used to identify the Messaging service that this domain should be associated with. Required.
  string messaging_service_sid = 1;
}

// Canonical success response.
message FetchLinkshorteningMessagingServiceDomainAssociationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1LinkshorteningMessagingServiceDomainAssociation body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListServiceResponse
message ListServiceResponseBody {
  ListServiceResponseBodyMeta meta = 1;
  repeated MessagingV1Service services = 2;
}

message ListServiceResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListServiceRequest {
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 1;
  // The page index. This value is simply for client state.
  int64 page = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
}

// Canonical success response.
message ListServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListServiceResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateServiceRequest
message CreateServiceRequestBody {
  // Whether to enable [Area Code Geomatch](https://www.twilio.com/docs/messaging/services#area-code-geomatch) on the Service Instance.
  bool area_code_geomatch = 1;
  // The HTTP method we should use to call `fallback_url`. Can be: `GET` or `POST`.
  string fallback_method = 2;
  // [OBSOLETE] Former feature used to fallback to long code sender after certain short code message failures.
  bool fallback_to_long_code = 3;
  // The URL that we call using `fallback_method` if an error occurs while retrieving or executing the TwiML from the Inbound Request URL. If the `use_inbound_webhook_on_number` field is enabled then the webhook url defined on the phone number will override the `fallback_url` defined for the Messaging Service.
  string fallback_url = 4;
  // A descriptive string that you create to describe the resource. It can be up to 64 characters long.
  string friendly_name = 5;
  // The HTTP method we should use to call `inbound_request_url`. Can be `GET` or `POST` and the default is `POST`.
  string inbound_method = 6;
  // The URL we call using `inbound_method` when a message is received by any phone number or short code in the Service. When this property is `null`, receiving inbound messages is disabled. All messages sent to the Twilio phone number or short code will not be logged and received on the Account. If the `use_inbound_webhook_on_number` field is enabled then the webhook url defined on the phone number will override the `inbound_request_url` defined for the Messaging Service.
  string inbound_request_url = 7;
  // Whether to enable the [MMS Converter](https://www.twilio.com/docs/messaging/services#mms-converter) for messages sent through the Service instance.
  bool mms_converter = 8;
  // OpenAPI property "ScanMessageContent".
  ServiceEnumScanMessageContent scan_message_content = 9;
  // Whether to enable [Smart Encoding](https://www.twilio.com/docs/messaging/services#smart-encoding) for messages sent through the Service instance.
  bool smart_encoding = 10;
  // The URL we should call to [pass status updates](https://www.twilio.com/docs/sms/api/message-resource#message-status-values) about message delivery.
  string status_callback = 11;
  // Whether to enable [Sticky Sender](https://www.twilio.com/docs/messaging/services#sticky-sender) on the Service instance.
  bool sticky_sender = 12;
  // Reserved.
  bool synchronous_validation = 13;
  // A boolean value that indicates either the webhook url configured on the phone number will be used or `inbound_request_url`/`fallback_url` url will be called when a message is received from the phone number. If this field is enabled then the webhook url defined on the phone number will override the `inbound_request_url`/`fallback_url` defined for the Messaging Service.
  bool use_inbound_webhook_on_number = 14;
  // A string that describes the scenario in which the Messaging Service will be used. Possible values are `notifications`, `marketing`, `verification`, `discussion`, `poll`, `undeclared`.
  string usecase = 15;
  // How long, in seconds, messages sent from the Service are valid. Can be an integer from `1` to `36,000`. Default value is `36,000`.
  int64 validity_period = 16;
}

message CreateServiceRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateServiceRequestBody body = 1;
}

// Canonical success response.
message CreateServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1Service body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateExternalCampaignRequest
message CreateExternalCampaignRequestBody {
  // ID of the preregistered campaign.
  string campaign_id = 1;
  // Customers should use this flag during the ERC registration process to indicate to Twilio that the campaign being registered is undergoing CNP migration. It is important for the user to first trigger the CNP migration process for said campaign in their CSP portal and have Twilio accept the sharing request, before making this api call.
  bool cnp_migration = 2;
  // The SID of the [Messaging Service](https://www.twilio.com/docs/messaging/api/service-resource) that the resource is associated with.
  string messaging_service_sid = 3;
}

message CreateExternalCampaignRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateExternalCampaignRequestBody body = 1;
}

// Canonical success response.
message CreateExternalCampaignResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1ExternalCampaign body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchUsecaseRequest {
}

// Canonical success response.
message FetchUsecaseResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1Usecase body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListChannelSenderResponse
message ListChannelSenderResponseBody {
  ListChannelSenderResponseBodyMeta meta = 1;
  repeated MessagingV1ServiceChannelSender senders = 2;
}

message ListChannelSenderResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListChannelSenderRequest {
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) to read the resources from. Required.
  string messaging_service_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListChannelSenderResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListChannelSenderResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateChannelSenderRequest
message CreateChannelSenderRequestBody {
  // The SID of the Channel Sender being added to the Service.
  string sid = 1;
}

message CreateChannelSenderRequest {
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) to create the resource under. Required.
  string messaging_service_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateChannelSenderRequestBody body = 2;
}

// Canonical success response.
message CreateChannelSenderResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1ServiceChannelSender body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchChannelSenderRequest {
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) to fetch the resource from. Required.
  string messaging_service_sid = 1;
  // The SID of the ChannelSender resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchChannelSenderResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1ServiceChannelSender body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeleteChannelSenderRequest {
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) to delete the resource from. Required.
  string messaging_service_sid = 1;
  // The SID of the Channel Sender resource to delete. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteChannelSenderResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListUsAppToPersonResponse
message ListUsAppToPersonResponseBody {
  repeated MessagingV1ServiceUsAppToPersonResponse compliance = 1;
  ListUsAppToPersonResponseBodyMeta meta = 2;
}

message ListUsAppToPersonResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListUsAppToPersonRequest {
  // The SID of the [Messaging Service](https://www.twilio.com/docs/messaging/api/service-resource) to fetch the resource from. Required.
  string messaging_service_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
  // The version of the Messaging API to use for this request
  string header_x_twilio_api_version = 5;
}

// Canonical success response.
message ListUsAppToPersonResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListUsAppToPersonResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateUsAppToPersonRequest
message CreateUsAppToPersonRequestBody {
  // A boolean that specifies whether campaign is age gated or not.
  bool age_gated = 1;
  // A2P Brand Registration SID
  string brand_registration_sid = 2;
  // A short description of what this SMS campaign does. Min length: 40 characters. Max length: 4096 characters.
  string description = 3;
  // A boolean that specifies whether campaign allows direct lending or not.
  bool direct_lending = 4;
  // Indicates that this SMS campaign will send messages that contain links.
  bool has_embedded_links = 5;
  // Indicates that this SMS campaign will send messages that contain phone numbers.
  bool has_embedded_phone = 6;
  // End users should be able to text in a keyword to receive help. Those keywords must be provided as part of the campaign registration request. This field is required if managing help keywords yourself (i.e. not using Twilio's Default or Advanced Opt Out features). Values must be alphanumeric. 255 character maximum.
  repeated string help_keywords = 7;
  // When customers receive the help keywords from their end users, Twilio customers are expected to send back an auto-generated response; this may include the brand name and additional support contact information. This field is required if managing help keywords yourself (i.e. not using Twilio's Default or Advanced Opt Out features). 20 character minimum. 320 character maximum.
  string help_message = 8;
  // Required for all Campaigns. Details around how a consumer opts-in to their campaign, therefore giving consent to receive their messages. If multiple opt-in methods can be used for the same campaign, they must all be listed. 40 character minimum. 2048 character maximum.
  string message_flow = 9;
  // An array of sample message strings, min two and max five. Min length for each sample: 20 chars. Max length for each sample: 1024 chars.
  repeated string message_samples = 10;
  // If end users can text in a keyword to start receiving messages from this campaign, those keywords must be provided. This field is required if end users can text in a keyword to start receiving messages from this campaign. Values must be alphanumeric. 255 character maximum.
  repeated string opt_in_keywords = 11;
  // If end users can text in a keyword to start receiving messages from this campaign, the auto-reply messages sent to the end users must be provided. The opt-in response should include the Brand name, confirmation of opt-in enrollment to a recurring message campaign, how to get help, and clear description of how to opt-out. This field is required if end users can text in a keyword to start receiving messages from this campaign. 20 character minimum. 320 character maximum.
  string opt_in_message = 12;
  // End users should be able to text in a keyword to stop receiving messages from this campaign. Those keywords must be provided. This field is required if managing opt out keywords yourself (i.e. not using Twilio's Default or Advanced Opt Out features). Values must be alphanumeric. 255 character maximum.
  repeated string opt_out_keywords = 13;
  // Upon receiving the opt-out keywords from the end users, Twilio customers are expected to send back an auto-generated response, which must provide acknowledgment of the opt-out request and confirmation that no further messages will be sent. It is also recommended that these opt-out messages include the brand name. This field is required if managing opt out keywords yourself (i.e. not using Twilio's Default or Advanced Opt Out features). 20 character minimum. 320 character maximum.
  string opt_out_message = 14;
  // The URL of the privacy policy for the campaign.
  string privacy_policy_url = 15;
  // A boolean that specifies whether campaign has Subscriber Optin or not.
  bool subscriber_opt_in = 16;
  // The URL of the terms and conditions for the campaign.
  string terms_and_conditions_url = 17;
  // A2P Campaign Use Case. Examples: [ 2FA, EMERGENCY, MARKETING..]
  string us_app_to_person_usecase = 18;
}

message CreateUsAppToPersonRequest {
  // The SID of the [Messaging Service](https://www.twilio.com/docs/messaging/api/service-resource) to create the resources from. Required.
  string messaging_service_sid = 1;
  // The version of the Messaging API to use for this request
  string header_x_twilio_api_version = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateUsAppToPersonRequestBody body = 3;
}

// Canonical success response.
message CreateUsAppToPersonResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1ServiceUsAppToPersonResponse body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchUsAppToPersonUsecaseRequest {
  // The SID of the [Messaging Service](https://www.twilio.com/docs/messaging/api/service-resource) to fetch the resource from. Required.
  string messaging_service_sid = 1;
  // The unique string to identify the A2P brand.
  string brand_registration_sid = 2;
}

// Canonical success response.
message FetchUsAppToPersonUsecaseResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1ServiceUsAppToPersonUsecase body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchUsAppToPersonRequest {
  // The SID of the [Messaging Service](https://www.twilio.com/docs/messaging/api/service-resource) to fetch the resource from. Required.
  string messaging_service_sid = 1;
  // The SID of the US A2P Compliance resource to fetch `QE2c6890da8086d771620e9b13fadeba0b`. Required.
  string sid = 2;
  // The version of the Messaging API to use for this request
  string header_x_twilio_api_version = 3;
}

// Canonical success response.
message FetchUsAppToPersonResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1ServiceUsAppToPersonResponse body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateUsAppToPersonRequest
message UpdateUsAppToPersonRequestBody {
  // A boolean that specifies whether campaign requires age gate for federally legal content.
  bool age_gated = 1;
  // A short description of what this SMS campaign does. Min length: 40 characters. Max length: 4096 characters.
  string description = 2;
  // A boolean that specifies whether campaign allows direct lending or not.
  bool direct_lending = 3;
  // Indicates that this SMS campaign will send messages that contain links.
  bool has_embedded_links = 4;
  // Indicates that this SMS campaign will send messages that contain phone numbers.
  bool has_embedded_phone = 5;
  // Required for all Campaigns. Details around how a consumer opts-in to their campaign, therefore giving consent to receive their messages. If multiple opt-in methods can be used for the same campaign, they must all be listed. 40 character minimum. 2048 character maximum.
  string message_flow = 6;
  // An array of sample message strings, min two and max five. Min length for each sample: 20 chars. Max length for each sample: 1024 chars.
  repeated string message_samples = 7;
  // The URL of the privacy policy for the campaign.
  string privacy_policy_url = 8;
  // The URL of the terms and conditions for the campaign.
  string terms_and_conditions_url = 9;
}

message UpdateUsAppToPersonRequest {
  // The SID of the [Messaging Service](https://www.twilio.com/docs/messaging/services/api) to update the resource from. Required.
  string messaging_service_sid = 1;
  // The SID of the US A2P Compliance resource to update `QE2c6890da8086d771620e9b13fadeba0b`. Required.
  string sid = 2;
  // The version of the Messaging API to use for this request
  string header_x_twilio_api_version = 3;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateUsAppToPersonRequestBody body = 4;
}

// Canonical success response.
message UpdateUsAppToPersonResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1ServiceUsAppToPersonResponse body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeleteUsAppToPersonRequest {
  // The SID of the [Messaging Service](https://www.twilio.com/docs/messaging/api/service-resource) to delete the resource from. Required.
  string messaging_service_sid = 1;
  // The SID of the US A2P Compliance resource to delete `QE2c6890da8086d771620e9b13fadeba0b`. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteUsAppToPersonResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListAlphaSenderResponse
message ListAlphaSenderResponseBody {
  repeated MessagingV1ServiceAlphaSender alpha_senders = 1;
  ListAlphaSenderResponseBodyMeta meta = 2;
}

message ListAlphaSenderResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListAlphaSenderRequest {
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) to read the resources from. Required.
  string service_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListAlphaSenderResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListAlphaSenderResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateAlphaSenderRequest
message CreateAlphaSenderRequestBody {
  // The Alphanumeric Sender ID string. Can be up to 11 characters long. Valid characters are A-Z, a-z, 0-9, space, hyphen `-`, plus `+`, underscore `_` and ampersand `&`. This value cannot contain only numbers.
  string alpha_sender = 1;
}

message CreateAlphaSenderRequest {
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) to create the resource under. Required.
  string service_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateAlphaSenderRequestBody body = 2;
}

// Canonical success response.
message CreateAlphaSenderResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1ServiceAlphaSender body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchAlphaSenderRequest {
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) to fetch the resource from. Required.
  string service_sid = 1;
  // The SID of the AlphaSender resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchAlphaSenderResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1ServiceAlphaSender body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeleteAlphaSenderRequest {
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) to delete the resource from. Required.
  string service_sid = 1;
  // The SID of the AlphaSender resource to delete. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteAlphaSenderResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListDestinationAlphaSenderResponse
message ListDestinationAlphaSenderResponseBody {
  repeated MessagingV1ServiceDestinationAlphaSender alpha_senders = 1;
  ListDestinationAlphaSenderResponseBodyMeta meta = 2;
}

message ListDestinationAlphaSenderResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListDestinationAlphaSenderRequest {
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) to read the resources from. Required.
  string service_sid = 1;
  // Optional filter to return only alphanumeric sender IDs associated with the specified two-character ISO country code.
  string iso_country_code = 2;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 3;
  // The page index. This value is simply for client state.
  int64 page = 4;
  // The page token. This is provided by the API.
  string page_token = 5;
}

// Canonical success response.
message ListDestinationAlphaSenderResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListDestinationAlphaSenderResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateDestinationAlphaSenderRequest
message CreateDestinationAlphaSenderRequestBody {
  // The Alphanumeric Sender ID string. Can be up to 11 characters long. Valid characters are A-Z, a-z, 0-9, space, hyphen `-`, plus `+`, underscore `_` and ampersand `&`. This value cannot contain only numbers.
  string alpha_sender = 1;
  // The Optional Two Character ISO Country Code the Alphanumeric Sender ID will be used for. If the IsoCountryCode is not provided, a default Alpha Sender will be created that can be used across all countries.
  string iso_country_code = 2;
}

message CreateDestinationAlphaSenderRequest {
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) to create the resource under. Required.
  string service_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateDestinationAlphaSenderRequestBody body = 2;
}

// Canonical success response.
message CreateDestinationAlphaSenderResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1ServiceDestinationAlphaSender body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchDestinationAlphaSenderRequest {
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) to fetch the resource from. Required.
  string service_sid = 1;
  // The SID of the AlphaSender resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchDestinationAlphaSenderResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1ServiceDestinationAlphaSender body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeleteDestinationAlphaSenderRequest {
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) to delete the resource from. Required.
  string service_sid = 1;
  // The SID of the AlphaSender resource to delete. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteDestinationAlphaSenderResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListPhoneNumberResponse
message ListPhoneNumberResponseBody {
  ListPhoneNumberResponseBodyMeta meta = 1;
  repeated MessagingV1ServicePhoneNumber phone_numbers = 2;
}

message ListPhoneNumberResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListPhoneNumberRequest {
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) to read the resources from. Required.
  string service_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListPhoneNumberResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListPhoneNumberResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreatePhoneNumberRequest
message CreatePhoneNumberRequestBody {
  // The SID of the Phone Number being added to the Service.
  string phone_number_sid = 1;
}

message CreatePhoneNumberRequest {
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) to create the resource under. Required.
  string service_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreatePhoneNumberRequestBody body = 2;
}

// Canonical success response.
message CreatePhoneNumberResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1ServicePhoneNumber body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchPhoneNumberRequest {
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) to fetch the resource from. Required.
  string service_sid = 1;
  // The SID of the PhoneNumber resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchPhoneNumberResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1ServicePhoneNumber body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeletePhoneNumberRequest {
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) to delete the resource from. Required.
  string service_sid = 1;
  // The SID of the PhoneNumber resource to delete. Required.
  string sid = 2;
}

// Canonical success response.
message DeletePhoneNumberResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListShortCodeResponse
message ListShortCodeResponseBody {
  ListShortCodeResponseBodyMeta meta = 1;
  repeated MessagingV1ServiceShortCode short_codes = 2;
}

message ListShortCodeResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListShortCodeRequest {
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) to read the resources from. Required.
  string service_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListShortCodeResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListShortCodeResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateShortCodeRequest
message CreateShortCodeRequestBody {
  // The SID of the ShortCode resource being added to the Service.
  string short_code_sid = 1;
}

message CreateShortCodeRequest {
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) to create the resource under. Required.
  string service_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateShortCodeRequestBody body = 2;
}

// Canonical success response.
message CreateShortCodeResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1ServiceShortCode body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchShortCodeRequest {
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) to fetch the resource from. Required.
  string service_sid = 1;
  // The SID of the ShortCode resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchShortCodeResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1ServiceShortCode body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeleteShortCodeRequest {
  // The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) to delete the resource from. Required.
  string service_sid = 1;
  // The SID of the ShortCode resource to delete. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteShortCodeResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

message FetchServiceRequest {
  // The SID of the Service resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1Service body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateServiceRequest
message UpdateServiceRequestBody {
  // Whether to enable [Area Code Geomatch](https://www.twilio.com/docs/messaging/services#area-code-geomatch) on the Service Instance.
  bool area_code_geomatch = 1;
  // The HTTP method we should use to call `fallback_url`. Can be: `GET` or `POST`.
  string fallback_method = 2;
  // [OBSOLETE] Former feature used to fallback to long code sender after certain short code message failures.
  bool fallback_to_long_code = 3;
  // The URL that we call using `fallback_method` if an error occurs while retrieving or executing the TwiML from the Inbound Request URL. If the `use_inbound_webhook_on_number` field is enabled then the webhook url defined on the phone number will override the `fallback_url` defined for the Messaging Service.
  string fallback_url = 4;
  // A descriptive string that you create to describe the resource. It can be up to 64 characters long.
  string friendly_name = 5;
  // The HTTP method we should use to call `inbound_request_url`. Can be `GET` or `POST` and the default is `POST`.
  string inbound_method = 6;
  // The URL we call using `inbound_method` when a message is received by any phone number or short code in the Service. When this property is `null`, receiving inbound messages is disabled. All messages sent to the Twilio phone number or short code will not be logged and received on the Account. If the `use_inbound_webhook_on_number` field is enabled then the webhook url defined on the phone number will override the `inbound_request_url` defined for the Messaging Service.
  string inbound_request_url = 7;
  // Whether to enable the [MMS Converter](https://www.twilio.com/docs/messaging/services#mms-converter) for messages sent through the Service instance.
  bool mms_converter = 8;
  // OpenAPI property "ScanMessageContent".
  ServiceEnumScanMessageContent scan_message_content = 9;
  // Whether to enable [Smart Encoding](https://www.twilio.com/docs/messaging/services#smart-encoding) for messages sent through the Service instance.
  bool smart_encoding = 10;
  // The URL we should call to [pass status updates](https://www.twilio.com/docs/sms/api/message-resource#message-status-values) about message delivery.
  string status_callback = 11;
  // Whether to enable [Sticky Sender](https://www.twilio.com/docs/messaging/services#sticky-sender) on the Service instance.
  bool sticky_sender = 12;
  // Reserved.
  bool synchronous_validation = 13;
  // A boolean value that indicates either the webhook url configured on the phone number will be used or `inbound_request_url`/`fallback_url` url will be called when a message is received from the phone number. If this field is enabled then the webhook url defined on the phone number will override the `inbound_request_url`/`fallback_url` defined for the Messaging Service.
  bool use_inbound_webhook_on_number = 14;
  // A string that describes the scenario in which the Messaging Service will be used. Possible values are `notifications`, `marketing`, `verification`, `discussion`, `poll`, `undeclared`.
  string usecase = 15;
  // How long, in seconds, messages sent from the Service are valid. Can be an integer from `1` to `36,000`. Default value is `36,000`.
  int64 validity_period = 16;
}

message UpdateServiceRequest {
  // The SID of the Service resource to update. Required.
  string sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateServiceRequestBody body = 2;
}

// Canonical success response.
message UpdateServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1Service body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeleteServiceRequest {
  // The SID of the Service resource to delete. Required.
  string sid = 1;
}

// Canonical success response.
message DeleteServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListTollfreeVerificationResponse
message ListTollfreeVerificationResponseBody {
  ListTollfreeVerificationResponseBodyMeta meta = 1;
  repeated MessagingV1TollfreeVerification verifications = 2;
}

message ListTollfreeVerificationResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// List tollfree verifications
message ListTollfreeVerificationRequest {
  // The SID of the Phone Number associated with the Tollfree Verification.
  string tollfree_phone_number_sid = 1;
  // The compliance status of the Tollfree Verification record.
  TollfreeVerificationEnumStatus status = 2;
  // Customer supplied reference id for the Tollfree Verification record.
  string external_reference_id = 3;
  // Whether to include Tollfree Verifications from sub accounts in list response.
  bool include_sub_accounts = 4;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 5;
  // The page index. This value is simply for client state.
  int64 page = 6;
  // The page token. This is provided by the API.
  string page_token = 7;
  // The trust product sids / tollfree bundle sids of tollfree verifications
  repeated string trust_product_sid = 8;
}

// Canonical success response.
message ListTollfreeVerificationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListTollfreeVerificationResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateTollfreeVerificationRequest
message CreateTollfreeVerificationRequestBody {
  // Additional information to be provided for verification.
  string additional_information = 1;
  // Indicates if the content is age gated.
  bool age_gated_content = 2;
  // The city of the business or organization using the Tollfree number.
  string business_city = 3;
  // The email address of the contact for the business or organization using the Tollfree number.
  string business_contact_email = 4;
  // The first name of the contact for the business or organization using the Tollfree number.
  string business_contact_first_name = 5;
  // The last name of the contact for the business or organization using the Tollfree number.
  string business_contact_last_name = 6;
  // The E.164 formatted phone number of the contact for the business or organization using the Tollfree number.
  string business_contact_phone = 7;
  // The country of the business or organization using the Tollfree number.
  string business_country = 8;
  // The name of the business or organization using the Tollfree number.
  string business_name = 9;
  // The postal code of the business or organization using the Tollfree number.
  string business_postal_code = 10;
  // OpenAPI property "BusinessRegistrationAuthority".
  TollfreeVerificationEnumBusinessRegistrationAuthority business_registration_authority = 11;
  // The country where the business is registered. Required for all business types except SOLE_PROPRIETOR.
  string business_registration_country = 12;
  // A legally recognized business registration number. Required for all business types except SOLE_PROPRIETOR.
  string business_registration_number = 13;
  // The E.164 formatted number associated with the business.
  string business_registration_phone_number = 14;
  // The state/province/region of the business or organization using the Tollfree number.
  string business_state_province_region = 15;
  // The address of the business or organization using the Tollfree number.
  string business_street_address = 16;
  // The address of the business or organization using the Tollfree number.
  string business_street_address2 = 17;
  // OpenAPI property "BusinessType".
  TollfreeVerificationEnumBusinessType business_type = 18;
  // The website of the business or organization using the Tollfree number.
  string business_website = 19;
  // Customer's Profile Bundle BundleSid.
  string customer_profile_sid = 20;
  // Trade name, sub entity, or downstream business name of business being submitted for verification
  string doing_business_as = 21;
  // An optional external reference ID supplied by customer and echoed back on status retrieval.
  string external_reference_id = 22;
  // A sample help message provided to users.
  string help_message_sample = 23;
  // Estimate monthly volume of messages from the Tollfree Number.
  string message_volume = 24;
  // The email address to receive the notification about the verification result. .
  string notification_email = 25;
  // The confirmation message sent to users when they opt in to receive messages.
  string opt_in_confirmation_message = 26;
  // Link to an image that shows the opt-in workflow. Multiple images allowed and must be a publicly hosted URL.
  repeated string opt_in_image_urls = 27;
  // List of keywords that users can text in to opt in to receive messages.
  repeated string opt_in_keywords = 28;
  // OpenAPI property "OptInType".
  TollfreeVerificationEnumOptInType opt_in_type = 29;
  // The URL to the privacy policy for the business or organization.
  string privacy_policy_url = 30;
  // An example of message content, i.e. a sample message.
  string production_message_sample = 31;
  // The URL to the terms and conditions for the business or organization.
  string terms_and_conditions_url = 32;
  // The SID of the Phone Number associated with the Tollfree Verification.
  string tollfree_phone_number_sid = 33;
  // OpenAPI property "UseCaseCategories".
  TollfreeVerificationEnumUseCaseCategories use_case_categories = 34;
  // Use this to further explain how messaging is used by the business or organization.
  string use_case_summary = 35;
  // The unique ID of the vetting
  string vetting_id = 36;
  // OpenAPI property "VettingProvider".
  TollfreeVerificationEnumVettingProvider vetting_provider = 37;
}

// Create a tollfree verification
message CreateTollfreeVerificationRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateTollfreeVerificationRequestBody body = 1;
}

// Canonical success response.
message CreateTollfreeVerificationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1TollfreeVerification body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Retrieve a tollfree verification
message FetchTollfreeVerificationRequest {
  // A unique string identifying a Tollfree Verification. Required.
  string sid = 1;
}

// Canonical success response.
message FetchTollfreeVerificationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1TollfreeVerification body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateTollfreeVerificationRequest
message UpdateTollfreeVerificationRequestBody {
  // Additional information to be provided for verification.
  string additional_information = 1;
  // Indicates if the content is age gated.
  bool age_gated_content = 2;
  // The city of the business or organization using the Tollfree number.
  string business_city = 3;
  // The email address of the contact for the business or organization using the Tollfree number.
  string business_contact_email = 4;
  // The first name of the contact for the business or organization using the Tollfree number.
  string business_contact_first_name = 5;
  // The last name of the contact for the business or organization using the Tollfree number.
  string business_contact_last_name = 6;
  // The E.164 formatted phone number of the contact for the business or organization using the Tollfree number.
  string business_contact_phone = 7;
  // The country of the business or organization using the Tollfree number.
  string business_country = 8;
  // The name of the business or organization using the Tollfree number.
  string business_name = 9;
  // The postal code of the business or organization using the Tollfree number.
  string business_postal_code = 10;
  // OpenAPI property "BusinessRegistrationAuthority".
  TollfreeVerificationEnumBusinessRegistrationAuthority business_registration_authority = 11;
  // Country business is registered in
  string business_registration_country = 12;
  // A legally recognized business registration number
  string business_registration_number = 13;
  // The E.164 formatted number associated with the business.
  string business_registration_phone_number = 14;
  // The state/province/region of the business or organization using the Tollfree number.
  string business_state_province_region = 15;
  // The address of the business or organization using the Tollfree number.
  string business_street_address = 16;
  // The address of the business or organization using the Tollfree number.
  string business_street_address2 = 17;
  // OpenAPI property "BusinessType".
  TollfreeVerificationEnumBusinessType business_type = 18;
  // The website of the business or organization using the Tollfree number.
  string business_website = 19;
  // Trade name, sub entity, or downstream business name of business being submitted for verification
  string doing_business_as = 20;
  // Describe why the verification is being edited. If the verification was rejected because of a technical issue, such as the website being down, and the issue has been resolved this parameter should be set to something similar to 'Website fixed'.
  string edit_reason = 21;
  // A sample help message provided to users.
  string help_message_sample = 22;
  // Estimate monthly volume of messages from the Tollfree Number.
  string message_volume = 23;
  // The email address to receive the notification about the verification result. .
  string notification_email = 24;
  // The confirmation message sent to users when they opt in to receive messages.
  string opt_in_confirmation_message = 25;
  // Link to an image that shows the opt-in workflow. Multiple images allowed and must be a publicly hosted URL.
  repeated string opt_in_image_urls = 26;
  // List of keywords that users can text in to opt in to receive messages.
  repeated string opt_in_keywords = 27;
  // OpenAPI property "OptInType".
  TollfreeVerificationEnumOptInType opt_in_type = 28;
  // The URL to the privacy policy for the business or organization.
  string privacy_policy_url = 29;
  // An example of message content, i.e. a sample message.
  string production_message_sample = 30;
  // The URL to the terms and conditions for the business or organization.
  string terms_and_conditions_url = 31;
  // OpenAPI property "UseCaseCategories".
  TollfreeVerificationEnumUseCaseCategories use_case_categories = 32;
  // Use this to further explain how messaging is used by the business or organization.
  string use_case_summary = 33;
  // The unique ID of the vetting
  string vetting_id = 34;
  // OpenAPI property "VettingProvider".
  TollfreeVerificationEnumVettingProvider vetting_provider = 35;
}

// Edit a tollfree verification
message UpdateTollfreeVerificationRequest {
  // The unique string to identify Tollfree Verification. Required.
  string sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateTollfreeVerificationRequestBody body = 2;
}

// Canonical success response.
message UpdateTollfreeVerificationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1TollfreeVerification body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete a tollfree verification
message DeleteTollfreeVerificationRequest {
  // The unique string to identify Tollfree Verification. Required.
  string sid = 1;
}

// Canonical success response.
message DeleteTollfreeVerificationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListBrandRegistrationsResponse
message ListBrandRegistrationsResponseBody {
  repeated MessagingV1BrandRegistrations data = 1;
  ListBrandRegistrationsResponseBodyMeta meta = 2;
}

message ListBrandRegistrationsResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListBrandRegistrationsRequest {
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 1;
  // The page index. This value is simply for client state.
  int64 page = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
}

// Canonical success response.
message ListBrandRegistrationsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListBrandRegistrationsResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateBrandRegistrationsRequest
message CreateBrandRegistrationsRequestBody {
  // A2P Messaging Profile Bundle Sid.
  string a2_pprofile_bundle_sid = 1;
  // Type of brand being created. One of: "STANDARD", "SOLE_PROPRIETOR". SOLE_PROPRIETOR is for low volume, SOLE_PROPRIETOR use cases. STANDARD is for all other use cases.
  string brand_type = 2;
  // Customer Profile Bundle Sid.
  string customer_profile_bundle_sid = 3;
  // A boolean that specifies whether brand should be a mock or not. If true, brand will be registered as a mock brand. Defaults to false if no value is provided.
  bool mock = 4;
  // A flag to disable automatic secondary vetting for brands which it would otherwise be done.
  bool skip_automatic_sec_vet = 5;
}

message CreateBrandRegistrationsRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateBrandRegistrationsRequestBody body = 1;
}

// Canonical success response.
message CreateBrandRegistrationsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1BrandRegistrations body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message CreateBrandRegistrationOtpRequest {
  // Brand Registration Sid of Sole Proprietor Brand. Required.
  string brand_registration_sid = 1;
}

// Canonical success response.
message CreateBrandRegistrationOtpResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1BrandRegistrationsBrandRegistrationOtp body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListBrandVettingResponse
message ListBrandVettingResponseBody {
  repeated MessagingV1BrandRegistrationsBrandVetting data = 1;
  ListBrandVettingResponseBodyMeta meta = 2;
}

message ListBrandVettingResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListBrandVettingRequest {
  // The SID of the Brand Registration resource of the vettings to read . Required.
  string brand_sid = 1;
  // The third-party provider of the vettings to read
  BrandVettingEnumVettingProvider vetting_provider = 2;
}

// Canonical success response.
message ListBrandVettingResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListBrandVettingResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateBrandVettingRequest
message CreateBrandVettingRequestBody {
  // The unique ID of the vetting
  string vetting_id = 1;
  // OpenAPI property "VettingProvider".
  BrandVettingEnumVettingProvider vetting_provider = 2;
}

message CreateBrandVettingRequest {
  // The SID of the Brand Registration resource of the vettings to create . Required.
  string brand_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateBrandVettingRequestBody body = 2;
}

// Canonical success response.
message CreateBrandVettingResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1BrandRegistrationsBrandVetting body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchBrandVettingRequest {
  // The SID of the Brand Registration resource of the vettings to read . Required.
  string brand_sid = 1;
  // The Twilio SID of the third-party vetting record. Required.
  string brand_vetting_sid = 2;
}

// Canonical success response.
message FetchBrandVettingResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1BrandRegistrationsBrandVetting body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchBrandRegistrationsRequest {
  // The SID of the Brand Registration resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchBrandRegistrationsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1BrandRegistrations body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message UpdateBrandRegistrationsRequest {
  // The SID of the Brand Registration resource to update. Required.
  string sid = 1;
}

// Canonical success response.
message UpdateBrandRegistrationsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV1BrandRegistrations body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Generated from MessagingV1Deactivations operations.
service MessagingV1DeactivationsService {
  // Fetch a list of all United States numbers that have been deactivated on a specific date.
  rpc FetchDeactivation(FetchDeactivationRequest) returns (FetchDeactivationResponse) {
    option (google.api.http) = {
      get: "/v1/Deactivations"
      response_body: "body"
    };
  }
}

// Generated from MessagingV1DomainCerts operations.
service MessagingV1DomainCertsService {
  rpc FetchDomainCertV4(FetchDomainCertV4Request) returns (FetchDomainCertV4Response) {
    option (google.api.http) = {
      get: "/v1/LinkShortening/Domains/{domain_sid}/Certificate"
      response_body: "body"
    };
  }
  rpc UpdateDomainCertV4(UpdateDomainCertV4Request) returns (UpdateDomainCertV4Response) {
    option (google.api.http) = {
      post: "/v1/LinkShortening/Domains/{domain_sid}/Certificate"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteDomainCertV4(DeleteDomainCertV4Request) returns (DeleteDomainCertV4Response) {
    option (google.api.http) = {
      delete: "/v1/LinkShortening/Domains/{domain_sid}/Certificate"
    };
  }
}

// Generated from MessagingV1DomainConfig operations.
service MessagingV1DomainConfigService {
  rpc FetchDomainConfig(FetchDomainConfigRequest) returns (FetchDomainConfigResponse) {
    option (google.api.http) = {
      get: "/v1/LinkShortening/Domains/{domain_sid}/Config"
      response_body: "body"
    };
  }
  rpc UpdateDomainConfig(UpdateDomainConfigRequest) returns (UpdateDomainConfigResponse) {
    option (google.api.http) = {
      post: "/v1/LinkShortening/Domains/{domain_sid}/Config"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from MessagingV1LinkshorteningMessagingService operations.
service MessagingV1LinkshorteningMessagingServiceService {
  rpc CreateLinkshorteningMessagingService(CreateLinkshorteningMessagingServiceRequest) returns (CreateLinkshorteningMessagingServiceResponse) {
    option (google.api.http) = {
      post: "/v1/LinkShortening/Domains/{domain_sid}/MessagingServices/{messaging_service_sid}"
      response_body: "body"
    };
  }
  rpc DeleteLinkshorteningMessagingService(DeleteLinkshorteningMessagingServiceRequest) returns (DeleteLinkshorteningMessagingServiceResponse) {
    option (google.api.http) = {
      delete: "/v1/LinkShortening/Domains/{domain_sid}/MessagingServices/{messaging_service_sid}"
    };
  }
}

// Generated from MessagingV1RequestManagedCert operations.
service MessagingV1RequestManagedCertService {
  rpc UpdateRequestManagedCert(UpdateRequestManagedCertRequest) returns (UpdateRequestManagedCertResponse) {
    option (google.api.http) = {
      post: "/v1/LinkShortening/Domains/{domain_sid}/RequestManagedCert"
      response_body: "body"
    };
  }
}

// Generated from MessagingV1DomainValidateDns operations.
service MessagingV1DomainValidateDnsService {
  rpc FetchDomainDnsValidation(FetchDomainDnsValidationRequest) returns (FetchDomainDnsValidationResponse) {
    option (google.api.http) = {
      get: "/v1/LinkShortening/Domains/{domain_sid}/ValidateDns"
      response_body: "body"
    };
  }
}

// Generated from MessagingV1DomainConfigMessagingService operations.
service MessagingV1DomainConfigMessagingServiceService {
  rpc FetchDomainConfigMessagingService(FetchDomainConfigMessagingServiceRequest) returns (FetchDomainConfigMessagingServiceResponse) {
    option (google.api.http) = {
      get: "/v1/LinkShortening/MessagingService/{messaging_service_sid}/DomainConfig"
      response_body: "body"
    };
  }
}

// Generated from MessagingV1LinkshorteningMessagingServiceDomainAssociation operations.
service MessagingV1LinkshorteningMessagingServiceDomainAssociationService {
  rpc FetchLinkshorteningMessagingServiceDomainAssociation(FetchLinkshorteningMessagingServiceDomainAssociationRequest) returns (FetchLinkshorteningMessagingServiceDomainAssociationResponse) {
    option (google.api.http) = {
      get: "/v1/LinkShortening/MessagingServices/{messaging_service_sid}/Domain"
      response_body: "body"
    };
  }
}

// Generated from MessagingV1Service operations.
service MessagingV1ServiceService {
  rpc ListService(ListServiceRequest) returns (ListServiceResponse) {
    option (google.api.http) = {
      get: "/v1/Services"
      response_body: "body"
    };
  }
  rpc CreateService(CreateServiceRequest) returns (CreateServiceResponse) {
    option (google.api.http) = {
      post: "/v1/Services"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchService(FetchServiceRequest) returns (FetchServiceResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateService(UpdateServiceRequest) returns (UpdateServiceResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteService(DeleteServiceRequest) returns (DeleteServiceResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{sid}"
    };
  }
}

// Generated from MessagingV1ExternalCampaign operations.
service MessagingV1ExternalCampaignService {
  rpc CreateExternalCampaign(CreateExternalCampaignRequest) returns (CreateExternalCampaignResponse) {
    option (google.api.http) = {
      post: "/v1/Services/PreregisteredUsa2p"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from MessagingV1Usecase operations.
service MessagingV1UsecaseService {
  rpc FetchUsecase(FetchUsecaseRequest) returns (FetchUsecaseResponse) {
    option (google.api.http) = {
      get: "/v1/Services/Usecases"
      response_body: "body"
    };
  }
}

// Generated from MessagingV1ChannelSender operations.
service MessagingV1ChannelSenderService {
  rpc ListChannelSender(ListChannelSenderRequest) returns (ListChannelSenderResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{messaging_service_sid}/ChannelSenders"
      response_body: "body"
    };
  }
  rpc CreateChannelSender(CreateChannelSenderRequest) returns (CreateChannelSenderResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{messaging_service_sid}/ChannelSenders"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchChannelSender(FetchChannelSenderRequest) returns (FetchChannelSenderResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{messaging_service_sid}/ChannelSenders/{sid}"
      response_body: "body"
    };
  }
  rpc DeleteChannelSender(DeleteChannelSenderRequest) returns (DeleteChannelSenderResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{messaging_service_sid}/ChannelSenders/{sid}"
    };
  }
}

// Generated from MessagingV1UsAppToPerson operations.
service MessagingV1UsAppToPersonService {
  rpc ListUsAppToPerson(ListUsAppToPersonRequest) returns (ListUsAppToPersonResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{messaging_service_sid}/Compliance/Usa2p"
      response_body: "body"
    };
  }
  rpc CreateUsAppToPerson(CreateUsAppToPersonRequest) returns (CreateUsAppToPersonResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{messaging_service_sid}/Compliance/Usa2p"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchUsAppToPerson(FetchUsAppToPersonRequest) returns (FetchUsAppToPersonResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{messaging_service_sid}/Compliance/Usa2p/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateUsAppToPerson(UpdateUsAppToPersonRequest) returns (UpdateUsAppToPersonResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{messaging_service_sid}/Compliance/Usa2p/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteUsAppToPerson(DeleteUsAppToPersonRequest) returns (DeleteUsAppToPersonResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{messaging_service_sid}/Compliance/Usa2p/{sid}"
    };
  }
}

// Generated from MessagingV1UsAppToPersonUsecase operations.
service MessagingV1UsAppToPersonUsecaseService {
  rpc FetchUsAppToPersonUsecase(FetchUsAppToPersonUsecaseRequest) returns (FetchUsAppToPersonUsecaseResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{messaging_service_sid}/Compliance/Usa2p/Usecases"
      response_body: "body"
    };
  }
}

// Generated from MessagingV1AlphaSender operations.
service MessagingV1AlphaSenderService {
  rpc ListAlphaSender(ListAlphaSenderRequest) returns (ListAlphaSenderResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/AlphaSenders"
      response_body: "body"
    };
  }
  rpc CreateAlphaSender(CreateAlphaSenderRequest) returns (CreateAlphaSenderResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/AlphaSenders"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchAlphaSender(FetchAlphaSenderRequest) returns (FetchAlphaSenderResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/AlphaSenders/{sid}"
      response_body: "body"
    };
  }
  rpc DeleteAlphaSender(DeleteAlphaSenderRequest) returns (DeleteAlphaSenderResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{service_sid}/AlphaSenders/{sid}"
    };
  }
}

// Generated from MessagingV1DestinationAlphaSender operations.
service MessagingV1DestinationAlphaSenderService {
  rpc ListDestinationAlphaSender(ListDestinationAlphaSenderRequest) returns (ListDestinationAlphaSenderResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/DestinationAlphaSenders"
      response_body: "body"
    };
  }
  rpc CreateDestinationAlphaSender(CreateDestinationAlphaSenderRequest) returns (CreateDestinationAlphaSenderResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/DestinationAlphaSenders"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchDestinationAlphaSender(FetchDestinationAlphaSenderRequest) returns (FetchDestinationAlphaSenderResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/DestinationAlphaSenders/{sid}"
      response_body: "body"
    };
  }
  rpc DeleteDestinationAlphaSender(DeleteDestinationAlphaSenderRequest) returns (DeleteDestinationAlphaSenderResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{service_sid}/DestinationAlphaSenders/{sid}"
    };
  }
}

// Generated from MessagingV1PhoneNumber operations.
service MessagingV1PhoneNumberService {
  rpc ListPhoneNumber(ListPhoneNumberRequest) returns (ListPhoneNumberResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/PhoneNumbers"
      response_body: "body"
    };
  }
  rpc CreatePhoneNumber(CreatePhoneNumberRequest) returns (CreatePhoneNumberResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/PhoneNumbers"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchPhoneNumber(FetchPhoneNumberRequest) returns (FetchPhoneNumberResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/PhoneNumbers/{sid}"
      response_body: "body"
    };
  }
  rpc DeletePhoneNumber(DeletePhoneNumberRequest) returns (DeletePhoneNumberResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{service_sid}/PhoneNumbers/{sid}"
    };
  }
}

// Generated from MessagingV1ShortCode operations.
service MessagingV1ShortCodeService {
  rpc ListShortCode(ListShortCodeRequest) returns (ListShortCodeResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/ShortCodes"
      response_body: "body"
    };
  }
  rpc CreateShortCode(CreateShortCodeRequest) returns (CreateShortCodeResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/ShortCodes"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchShortCode(FetchShortCodeRequest) returns (FetchShortCodeResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/ShortCodes/{sid}"
      response_body: "body"
    };
  }
  rpc DeleteShortCode(DeleteShortCodeRequest) returns (DeleteShortCodeResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{service_sid}/ShortCodes/{sid}"
    };
  }
}

// Generated from MessagingV1TollfreeVerification operations.
service MessagingV1TollfreeVerificationService {
  // List tollfree verifications
  rpc ListTollfreeVerification(ListTollfreeVerificationRequest) returns (ListTollfreeVerificationResponse) {
    option (google.api.http) = {
      get: "/v1/Tollfree/Verifications"
      response_body: "body"
    };
  }
  // Create a tollfree verification
  rpc CreateTollfreeVerification(CreateTollfreeVerificationRequest) returns (CreateTollfreeVerificationResponse) {
    option (google.api.http) = {
      post: "/v1/Tollfree/Verifications"
      body: "body"
      response_body: "body"
    };
  }
  // Retrieve a tollfree verification
  rpc FetchTollfreeVerification(FetchTollfreeVerificationRequest) returns (FetchTollfreeVerificationResponse) {
    option (google.api.http) = {
      get: "/v1/Tollfree/Verifications/{sid}"
      response_body: "body"
    };
  }
  // Edit a tollfree verification
  rpc UpdateTollfreeVerification(UpdateTollfreeVerificationRequest) returns (UpdateTollfreeVerificationResponse) {
    option (google.api.http) = {
      post: "/v1/Tollfree/Verifications/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete a tollfree verification
  rpc DeleteTollfreeVerification(DeleteTollfreeVerificationRequest) returns (DeleteTollfreeVerificationResponse) {
    option (google.api.http) = {
      delete: "/v1/Tollfree/Verifications/{sid}"
    };
  }
}

// Generated from MessagingV1BrandRegistration operations.
service MessagingV1BrandRegistrationService {
  rpc ListBrandRegistrations(ListBrandRegistrationsRequest) returns (ListBrandRegistrationsResponse) {
    option (google.api.http) = {
      get: "/v1/a2p/BrandRegistrations"
      response_body: "body"
    };
  }
  rpc CreateBrandRegistrations(CreateBrandRegistrationsRequest) returns (CreateBrandRegistrationsResponse) {
    option (google.api.http) = {
      post: "/v1/a2p/BrandRegistrations"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchBrandRegistrations(FetchBrandRegistrationsRequest) returns (FetchBrandRegistrationsResponse) {
    option (google.api.http) = {
      get: "/v1/a2p/BrandRegistrations/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateBrandRegistrations(UpdateBrandRegistrationsRequest) returns (UpdateBrandRegistrationsResponse) {
    option (google.api.http) = {
      post: "/v1/a2p/BrandRegistrations/{sid}"
      response_body: "body"
    };
  }
}

// Generated from MessagingV1BrandRegistrationOtp operations.
service MessagingV1BrandRegistrationOtpService {
  rpc CreateBrandRegistrationOtp(CreateBrandRegistrationOtpRequest) returns (CreateBrandRegistrationOtpResponse) {
    option (google.api.http) = {
      post: "/v1/a2p/BrandRegistrations/{brand_registration_sid}/SmsOtp"
      response_body: "body"
    };
  }
}

// Generated from MessagingV1BrandVetting operations.
service MessagingV1BrandVettingService {
  rpc ListBrandVetting(ListBrandVettingRequest) returns (ListBrandVettingResponse) {
    option (google.api.http) = {
      get: "/v1/a2p/BrandRegistrations/{brand_sid}/Vettings"
      response_body: "body"
    };
  }
  rpc CreateBrandVetting(CreateBrandVettingRequest) returns (CreateBrandVettingResponse) {
    option (google.api.http) = {
      post: "/v1/a2p/BrandRegistrations/{brand_sid}/Vettings"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchBrandVetting(FetchBrandVettingRequest) returns (FetchBrandVettingResponse) {
    option (google.api.http) = {
      get: "/v1/a2p/BrandRegistrations/{brand_sid}/Vettings/{brand_vetting_sid}"
      response_body: "body"
    };
  }
}

