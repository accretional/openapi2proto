// Code generated by openapi2proto. DO NOT EDIT.
// Source: twilio_supersim_v1.json

syntax = "proto3";

package twilio.supersim.v1;

option go_package = "twilio/supersim/v1;twiliosupersimv1";

import "google/api/annotations.proto";

// The type of Billing Period. Can be `ready` or `active`.
message BillingPeriodEnumBpType {
  // Scalar value wrapper.
  string value = 1;
}

// The status of the eSIM Profile. Can be: `new`, `reserving`, `available`, `downloaded`, `installed` or `failed`. See the [eSIM Profile Status Values](https://www.twilio.com/docs/iot/supersim/api/esimprofile-resource#status-values) for a description of each.
message EsimProfileEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

// The model by which a SIMs usage is metered and billed. Defaults to `payg`.
message FleetEnumDataMetering {
  // Scalar value wrapper.
  string value = 1;
}

// Either `to_sim` or `from_sim`. Indicates whether the IP Command resource was sent from or to the Super SIM.
message IpCommandEnumDirection {
  // Scalar value wrapper.
  string value = 1;
}

// Either “text” or “binary”. For an IP Command sent to a Super SIM, `payload_type` is configurable. For an IP Command sent from a Super SIM, `payload_type` is always “binary”.
message IpCommandEnumPayloadType {
  // Scalar value wrapper.
  string value = 1;
}

// The delivery status of the IP Command. This is one of the following: “queued”, “sent”, “failed” or “received”.
message IpCommandEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

// The Status of this Settings Update. One of `scheduled`, `in-progress`, `successful` or `failed`.
message SettingsUpdateEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

// The status of the Super SIM. Can be `new`, `ready`, `active`, `inactive`, or `scheduled`. See the [Super SIM Status Values](https://www.twilio.com/docs/iot/supersim/api/sim-resource#status-values) for a description of each.
message SimEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

message SimEnumStatusUpdate {
  // Scalar value wrapper.
  string value = 1;
}

// IP address version of the corresponding IP address
message SimIpAddressEnumIpAddressVersion {
  // Scalar value wrapper.
  string value = 1;
}

// The direction of the SMS Command. Can be `to_sim` or `from_sim`. The value of `to_sim` is synonymous with the term `mobile terminated`, and `from_sim` is synonymous with the term `mobile originated`.
message SmsCommandEnumDirection {
  // Scalar value wrapper.
  string value = 1;
}

// The status of the SMS Command. Can be: `queued`, `sent`, `delivered`, `received` or `failed`. See the [SMS Command Status Values](https://www.twilio.com/docs/iot/supersim/api/smscommand-resource#status-values) for a description of each.
message SmsCommandEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

message SupersimV1EsimProfile {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) to which the eSIM Profile resource belongs.
  string account_sid = 1;
  // Combined machine-readable activation code for acquiring an eSIM Profile with the Activation Code download method. Can be used in a QR code to download an eSIM profile.
  string activation_code = 2;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 4;
  // Identifier of the eUICC that can claim the eSIM Profile.
  string eid = 5;
  // Code indicating the failure if the download of the SIM Profile failed and the eSIM Profile is in `failed` state.
  string error_code = 6;
  // Error message describing the failure if the download of the SIM Profile failed and the eSIM Profile is in `failed` state.
  string error_message = 7;
  // The [ICCID](https://en.wikipedia.org/wiki/Subscriber_identity_module#ICCID) associated with the Sim resource.
  string iccid = 8;
  // Unique identifier of the eSIM profile that can be used to identify and download the eSIM profile from the SM-DP+ server. Populated if `generate_matching_id` is set to `true` when creating the eSIM profile reservation.
  string matching_id = 9;
  // The unique string that we created to identify the eSIM Profile resource.
  string sid = 10;
  // The SID of the [Sim](https://www.twilio.com/docs/iot/supersim/api/sim-resource) resource that this eSIM Profile controls.
  string sim_sid = 11;
  // Address of the SM-DP+ server from which the Profile will be downloaded. The URL will appear once the eSIM Profile reaches the status `available`.
  string smdp_plus_address = 12;
  // OpenAPI property "status".
  EsimProfileEnumStatus status = 13;
  // The absolute URL of the eSIM Profile resource.
  string url = 14;
}

message SupersimV1Fleet {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Fleet resource.
  string account_sid = 1;
  // Defines whether SIMs in the Fleet are capable of using 2G/3G/4G/LTE/CAT-M data connectivity. Defaults to `true`.
  bool data_enabled = 2;
  // The total data usage (download and upload combined) in Megabytes that each Super SIM assigned to the Fleet can consume during a billing period (normally one month). Value must be between 1MB (1) and 2TB (2,000,000). Defaults to 250MB.
  int64 data_limit = 3;
  // OpenAPI property "data_metering".
  FleetEnumDataMetering data_metering = 4;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 5;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 6;
  // A string representing the HTTP method to use when making a request to `ip_commands_url`. Can be one of `POST` or `GET`. Defaults to `POST`.
  string ip_commands_method = 7;
  // The URL that will receive a webhook when a Super SIM in the Fleet is used to send an IP Command from your device to a special IP address. Your server should respond with an HTTP status code in the 200 range; any response body will be ignored.
  string ip_commands_url = 8;
  // The SID of the Network Access Profile that controls which cellular networks the Fleet's SIMs can connect to.
  string network_access_profile_sid = 9;
  // The unique string that we created to identify the Fleet resource.
  string sid = 10;
  // Defines whether SIMs in the Fleet are capable of sending and receiving machine-to-machine SMS via Commands. Defaults to `false`.
  bool sms_commands_enabled = 11;
  // A string representing the HTTP method to use when making a request to `sms_commands_url`. Can be one of `POST` or `GET`. Defaults to `POST`.
  string sms_commands_method = 12;
  // The URL that will receive a webhook when a Super SIM in the Fleet is used to send an SMS from your device to the SMS Commands number. Your server should respond with an HTTP status code in the 200 range; any response body will be ignored.
  string sms_commands_url = 13;
  // An application-defined string that uniquely identifies the resource. It can be used in place of the resource's `sid` in the URL to address the resource.
  string unique_name = 14;
  // The absolute URL of the Fleet resource.
  string url = 15;
}

message SupersimV1IpCommand {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the IP Command resource.
  string account_sid = 1;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 2;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 3;
  // The IP address of the device that the IP Command was sent to or received from. For an IP Command sent to a Super SIM, `device_ip` starts out as `null`, and once the IP Command is “sent”, the `device_ip` will be filled out. An IP Command sent from a Super SIM have its `device_ip` always set.
  string device_ip = 4;
  // For an IP Command sent to a Super SIM, it would be the destination port of the IP message. For an IP Command sent from a Super SIM, it would be the source port of the IP message.
  int64 device_port = 5;
  // OpenAPI property "direction".
  IpCommandEnumDirection direction = 6;
  // The payload that is carried in the IP/UDP message. The payload can be encoded in either text or binary format. For text payload, UTF-8 encoding must be used.
  // For an IP Command sent to a Super SIM, the payload is appended to the IP/UDP message “as is”. The payload should not exceed 1300 bytes.
  // For an IP Command sent from a Super SIM, the payload from the received IP/UDP message is extracted and sent in binary encoding. For an IP Command sent from a Super SIM, the payload should not exceed 1300 bytes. If it is larger than 1300 bytes, there might be fragmentation on the upstream and the message may appear truncated.
  string payload = 7;
  // OpenAPI property "payload_type".
  IpCommandEnumPayloadType payload_type = 8;
  // The unique string that we created to identify the IP Command resource.
  string sid = 9;
  // The [ICCID](https://en.wikipedia.org/wiki/Subscriber_identity_module#ICCID) of the [Super SIM](https://www.twilio.com/docs/iot/supersim/api/sim-resource) that this IP Command was sent to or from.
  string sim_iccid = 10;
  // The SID of the [Super SIM](https://www.twilio.com/docs/iot/supersim/api/sim-resource) that this IP Command was sent to or from.
  string sim_sid = 11;
  // OpenAPI property "status".
  IpCommandEnumStatus status = 12;
  // The absolute URL of the IP Command resource.
  string url = 13;
}

message SupersimV1Network {
  // A human readable identifier of this resource.
  string friendly_name = 1;
  // Array of objects identifying the [MCC-MNCs](https://en.wikipedia.org/wiki/Mobile_country_code) that are included in the Network resource.
  repeated string identifiers = 2;
  // The [ISO country code](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) of the Network resource.
  string iso_country = 3;
  // The unique string that we created to identify the Network resource.
  string sid = 4;
  // The absolute URL of the Network resource.
  string url = 5;
}

message SupersimV1NetworkAccessProfile {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that the Network Access Profile belongs to.
  string account_sid = 1;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 2;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 3;
  SupersimV1NetworkAccessProfileLinks links = 4;
  // The unique string that identifies the Network Access Profile resource.
  string sid = 5;
  // An application-defined string that uniquely identifies the resource. It can be used in place of the resource's `sid` in the URL to address the resource.
  string unique_name = 6;
  // The absolute URL of the Network Access Profile resource.
  string url = 7;
}

message SupersimV1NetworkAccessProfileLinks {
  // Scalar value wrapper.
  string value = 1;
}

message SupersimV1NetworkAccessProfileNetworkAccessProfileNetwork {
  // A human readable identifier of the Network this resource refers to.
  string friendly_name = 1;
  // Array of objects identifying the [MCC-MNCs](https://en.wikipedia.org/wiki/Mobile_country_code) that are included in the Network resource.
  repeated string identifiers = 2;
  // The [ISO country code](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) of the Network resource.
  string iso_country = 3;
  // The unique string that identifies the Network resource's Network Access Profile resource.
  string network_access_profile_sid = 4;
  // The unique string that identifies the Network resource.
  string sid = 5;
  // The absolute URL of the Network resource.
  string url = 6;
}

message SupersimV1SettingsUpdate {
  // The time, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format, when the update successfully completed and the new settings were applied to the SIM.
  string date_completed = 1;
  // The date that this Settings Update was created, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 2;
  // The date that this Settings Update was updated, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 3;
  // The [ICCID](https://en.wikipedia.org/wiki/SIM_card#ICCID) associated with the SIM.
  string iccid = 4;
  // Array containing the different Settings Packages that will be applied to the SIM after the update completes. Each object within the array indicates the name and the version of the Settings Package that will be on the SIM if the update is successful.
  repeated string packages = 5;
  // The unique identifier of this Settings Update.
  string sid = 6;
  // The SID of the Super SIM to which this Settings Update was applied.
  string sim_sid = 7;
  // OpenAPI property "status".
  SettingsUpdateEnumStatus status = 8;
}

message SupersimV1Sim {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that the Super SIM belongs to.
  string account_sid = 1;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 2;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 3;
  // The unique ID of the Fleet configured for this SIM.
  string fleet_sid = 4;
  // The [ICCID](https://en.wikipedia.org/wiki/Subscriber_identity_module#ICCID) associated with the SIM.
  string iccid = 5;
  SupersimV1SimLinks links = 6;
  // The unique string that identifies the Sim resource.
  string sid = 7;
  // OpenAPI property "status".
  SimEnumStatus status = 8;
  // An application-defined string that uniquely identifies the resource. It can be used in place of the resource's `sid` in the URL to address the resource.
  string unique_name = 9;
  // The absolute URL of the Sim Resource.
  string url = 10;
}

message SupersimV1SimLinks {
  // Scalar value wrapper.
  string value = 1;
}

message SupersimV1SimBillingPeriod {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) the Super SIM belongs to.
  string account_sid = 1;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 2;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 3;
  // The end time of the Billing Period specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string end_time = 4;
  // OpenAPI property "period_type".
  BillingPeriodEnumBpType period_type = 5;
  // The SID of the Billing Period.
  string sid = 6;
  // The SID of the Super SIM the Billing Period belongs to.
  string sim_sid = 7;
  // The start time of the Billing Period specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string start_time = 8;
}

message SupersimV1SimSimIpAddress {
  // IP address assigned to the given Super SIM
  string ip_address = 1;
  // OpenAPI property "ip_address_version".
  SimIpAddressEnumIpAddressVersion ip_address_version = 2;
}

message SupersimV1SmsCommand {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the SMS Command resource.
  string account_sid = 1;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 2;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 3;
  // OpenAPI property "direction".
  SmsCommandEnumDirection direction = 4;
  // The message body of the SMS Command sent to or from the SIM. For text mode messages, this can be up to 160 characters.
  string payload = 5;
  // The unique string that we created to identify the SMS Command resource.
  string sid = 6;
  // The SID of the [SIM](https://www.twilio.com/docs/iot/supersim/api/sim-resource) that this SMS Command was sent to or from.
  string sim_sid = 7;
  // OpenAPI property "status".
  SmsCommandEnumStatus status = 8;
  // The absolute URL of the SMS Command resource.
  string url = 9;
}

message SupersimV1UsageRecord {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that incurred the usage.
  string account_sid = 1;
  // The currency in which the billed amounts are measured, specified in the 3 letter ISO 4127 format (e.g. `USD`, `EUR`, `JPY`). This can be null when data_toal_billed is 0 and we do not yet have billing information for the corresponding data usage. Refer to [Data Usage Processing](https://www.twilio.com/docs/iot/supersim/api/usage-record-resource#data-usage-processing) for more details.
  string billed_unit = 2;
  // Total data downloaded in bytes, aggregated by the query parameters.
  int64 data_download = 3;
  // Total of data_upload and data_download.
  int64 data_total = 4;
  // Total amount in the `billed_unit` that was charged for the data uploaded or downloaded. Will return 0 for usage prior to February 1, 2022. Value may be 0 despite `data_total` being greater than 0 if the data usage is still being processed by Twilio's billing system. Refer to [Data Usage Processing](https://www.twilio.com/docs/iot/supersim/api/usage-record-resource#data-usage-processing) for more details.
  double data_total_billed = 5;
  // Total data uploaded in bytes, aggregated by the query parameters.
  int64 data_upload = 6;
  // SID of the Fleet resource the usage occurred on. Value will only be present when either a value for the `Fleet` query parameter is provided or when UsageRecords are grouped by `fleet`. Otherwise, the value will be `null`.
  string fleet_sid = 7;
  // Alpha-2 ISO Country Code that the usage occurred in. Value will only be present when either a value for the `IsoCountry` query parameter is provided or when UsageRecords are grouped by `isoCountry`. Otherwise, the value will be `null`.
  string iso_country = 8;
  // SID of the Network resource the usage occurred on. Value will only be present when either a value for the `Network` query parameter is provided or when UsageRecords are grouped by `network`. Otherwise, the value will be `null`.
  string network_sid = 9;
  // The time period for which the usage is reported. The period is represented as a pair of `start_time` and `end_time` timestamps specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string period = 10;
  // SID of a Sim resource to which the UsageRecord belongs. Value will only be present when either a value for the `Sim` query parameter is provided or when UsageRecords are grouped by `sim`. Otherwise, the value will be `null`.
  string sim_sid = 11;
}

// Time-based grouping that UsageRecords should be aggregated by. Can be: `hour`, `day`, or `all`. Default is `all`. `all` returns one UsageRecord that describes the usage for the entire period.
message UsageRecordEnumGranularity {
  // Scalar value wrapper.
  string value = 1;
}

// Dimension over which to aggregate usage records. Can be: `sim`, `fleet`, `network`, `isoCountry`. Default is to not aggregate across any of these dimensions, UsageRecords will be aggregated into the time buckets described by the `Granularity` parameter.
message UsageRecordEnumGroup {
  // Scalar value wrapper.
  string value = 1;
}

message UsageRecordEnumSortBy {
  // Scalar value wrapper.
  string value = 1;
}

// ListEsimProfileResponse
message ListEsimProfileResponseBody {
  repeated SupersimV1EsimProfile esim_profiles = 1;
  ListEsimProfileResponseBodyMeta meta = 2;
}

message ListEsimProfileResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of eSIM Profiles.
message ListEsimProfileRequest {
  // List the eSIM Profiles that have been associated with an EId.
  string eid = 1;
  // Find the eSIM Profile resource related to a [Sim](https://www.twilio.com/docs/iot/supersim/api/sim-resource) resource by providing the SIM SID. Will always return an array with either 1 or 0 records.
  string sim_sid = 2;
  // List the eSIM Profiles that are in a given status.
  EsimProfileEnumStatus status = 3;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 4;
  // The page index. This value is simply for client state.
  int64 page = 5;
  // The page token. This is provided by the API.
  string page_token = 6;
}

// Canonical success response.
message ListEsimProfileResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListEsimProfileResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateEsimProfileRequest
message CreateEsimProfileRequestBody {
  // The HTTP method we should use to call `callback_url`. Can be: `GET` or `POST` and the default is POST.
  string callback_method = 1;
  // The URL we should call using the `callback_method` when the status of the eSIM Profile changes. At this stage of the eSIM Profile pilot, the a request to the URL will only be called when the ESimProfile resource changes from `reserving` to `available`.
  string callback_url = 2;
  // Identifier of the eUICC that will claim the eSIM Profile.
  string eid = 3;
  // When set to `true`, a value for `Eid` does not need to be provided. Instead, when the eSIM profile is reserved, a matching ID will be generated and returned via the `matching_id` property. This identifies the specific eSIM profile that can be used by any capable device to claim and download the profile.
  bool generate_matching_id = 4;
}

// Order an eSIM Profile.
message CreateEsimProfileRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateEsimProfileRequestBody body = 1;
}

// Canonical success response.
message CreateEsimProfileResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SupersimV1EsimProfile body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch an eSIM Profile.
message FetchEsimProfileRequest {
  // The SID of the eSIM Profile resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchEsimProfileResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SupersimV1EsimProfile body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListFleetResponse
message ListFleetResponseBody {
  repeated SupersimV1Fleet fleets = 1;
  ListFleetResponseBodyMeta meta = 2;
}

message ListFleetResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of Fleets from your account.
message ListFleetRequest {
  // The SID or unique name of the Network Access Profile that controls which cellular networks the Fleet's SIMs can connect to.
  string network_access_profile = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListFleetResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListFleetResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateFleetRequest
message CreateFleetRequestBody {
  // Defines whether SIMs in the Fleet are capable of using 2G/3G/4G/LTE/CAT-M data connectivity. Defaults to `true`.
  bool data_enabled = 1;
  // The total data usage (download and upload combined) in Megabytes that each Super SIM assigned to the Fleet can consume during a billing period (normally one month). Value must be between 1MB (1) and 2TB (2,000,000). Defaults to 1GB (1,000).
  int64 data_limit = 2;
  // A string representing the HTTP method to use when making a request to `ip_commands_url`. Can be one of `POST` or `GET`. Defaults to `POST`.
  string ip_commands_method = 3;
  // The URL that will receive a webhook when a Super SIM in the Fleet is used to send an IP Command from your device to a special IP address. Your server should respond with an HTTP status code in the 200 range; any response body will be ignored.
  string ip_commands_url = 4;
  // The SID or unique name of the Network Access Profile that will control which cellular networks the Fleet's SIMs can connect to.
  string network_access_profile = 5;
  // Defines whether SIMs in the Fleet are capable of sending and receiving machine-to-machine SMS via Commands. Defaults to `true`.
  bool sms_commands_enabled = 6;
  // A string representing the HTTP method to use when making a request to `sms_commands_url`. Can be one of `POST` or `GET`. Defaults to `POST`.
  string sms_commands_method = 7;
  // The URL that will receive a webhook when a Super SIM in the Fleet is used to send an SMS from your device to the SMS Commands number. Your server should respond with an HTTP status code in the 200 range; any response body will be ignored.
  string sms_commands_url = 8;
  // An application-defined string that uniquely identifies the resource. It can be used in place of the resource's `sid` in the URL to address the resource.
  string unique_name = 9;
}

// Create a Fleet
message CreateFleetRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateFleetRequestBody body = 1;
}

// Canonical success response.
message CreateFleetResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SupersimV1Fleet body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch a Fleet instance from your account.
message FetchFleetRequest {
  // The SID of the Fleet resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchFleetResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SupersimV1Fleet body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateFleetRequest
message UpdateFleetRequestBody {
  // The total data usage (download and upload combined) in Megabytes that each Super SIM assigned to the Fleet can consume during a billing period (normally one month). Value must be between 1MB (1) and 2TB (2,000,000). Defaults to 1GB (1,000).
  int64 data_limit = 1;
  // A string representing the HTTP method to use when making a request to `ip_commands_url`. Can be one of `POST` or `GET`. Defaults to `POST`.
  string ip_commands_method = 2;
  // The URL that will receive a webhook when a Super SIM in the Fleet is used to send an IP Command from your device to a special IP address. Your server should respond with an HTTP status code in the 200 range; any response body will be ignored.
  string ip_commands_url = 3;
  // The SID or unique name of the Network Access Profile that will control which cellular networks the Fleet's SIMs can connect to.
  string network_access_profile = 4;
  // A string representing the HTTP method to use when making a request to `sms_commands_url`. Can be one of `POST` or `GET`. Defaults to `POST`.
  string sms_commands_method = 5;
  // The URL that will receive a webhook when a Super SIM in the Fleet is used to send an SMS from your device to the SMS Commands number. Your server should respond with an HTTP status code in the 200 range; any response body will be ignored.
  string sms_commands_url = 6;
  // An application-defined string that uniquely identifies the resource. It can be used in place of the resource's `sid` in the URL to address the resource.
  string unique_name = 7;
}

// Updates the given properties of a Super SIM Fleet instance from your account.
message UpdateFleetRequest {
  // The SID of the Fleet resource to update. Required.
  string sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateFleetRequestBody body = 2;
}

// Canonical success response.
message UpdateFleetResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SupersimV1Fleet body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListIpCommandResponse
message ListIpCommandResponseBody {
  repeated SupersimV1IpCommand ip_commands = 1;
  ListIpCommandResponseBodyMeta meta = 2;
}

message ListIpCommandResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of IP Commands from your account.
message ListIpCommandRequest {
  // The SID or unique name of the Sim resource that IP Command was sent to or from.
  string sim = 1;
  // The ICCID of the Sim resource that IP Command was sent to or from.
  string sim_iccid = 2;
  // The status of the IP Command. Can be: `queued`, `sent`, `received` or `failed`. See the [IP Command Status Values](https://www.twilio.com/docs/iot/supersim/api/ipcommand-resource#status-values) for a description of each.
  IpCommandEnumStatus status = 3;
  // The direction of the IP Command. Can be `to_sim` or `from_sim`. The value of `to_sim` is synonymous with the term `mobile terminated`, and `from_sim` is synonymous with the term `mobile originated`.
  IpCommandEnumDirection direction = 4;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 5;
  // The page index. This value is simply for client state.
  int64 page = 6;
  // The page token. This is provided by the API.
  string page_token = 7;
}

// Canonical success response.
message ListIpCommandResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListIpCommandResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateIpCommandRequest
message CreateIpCommandRequestBody {
  // The HTTP method we should use to call `callback_url`. Can be `GET` or `POST`, and the default is `POST`.
  string callback_method = 1;
  // The URL we should call using the `callback_method` after we have sent the IP Command.
  string callback_url = 2;
  // The device port to which the IP Command will be sent.
  int64 device_port = 3;
  // The data that will be sent to the device. The payload cannot exceed 1300 bytes. If the PayloadType is set to text, the payload is encoded in UTF-8. If PayloadType is set to binary, the payload is encoded in Base64.
  string payload = 4;
  // OpenAPI property "PayloadType".
  IpCommandEnumPayloadType payload_type = 5;
  // The `sid` or `unique_name` of the [Super SIM](https://www.twilio.com/docs/iot/supersim/api/sim-resource) to send the IP Command to.
  string sim = 6;
}

// Send an IP Command to a Super SIM.
message CreateIpCommandRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateIpCommandRequestBody body = 1;
}

// Canonical success response.
message CreateIpCommandResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SupersimV1IpCommand body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch IP Command instance from your account.
message FetchIpCommandRequest {
  // The SID of the IP Command resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchIpCommandResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SupersimV1IpCommand body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListNetworkAccessProfileResponse
message ListNetworkAccessProfileResponseBody {
  ListNetworkAccessProfileResponseBodyMeta meta = 1;
  repeated SupersimV1NetworkAccessProfile network_access_profiles = 2;
}

message ListNetworkAccessProfileResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of Network Access Profiles from your account.
message ListNetworkAccessProfileRequest {
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 1;
  // The page index. This value is simply for client state.
  int64 page = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
}

// Canonical success response.
message ListNetworkAccessProfileResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListNetworkAccessProfileResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateNetworkAccessProfileRequest
message CreateNetworkAccessProfileRequestBody {
  // List of Network SIDs that this Network Access Profile will allow connections to.
  repeated string networks = 1;
  // An application-defined string that uniquely identifies the resource. It can be used in place of the resource's `sid` in the URL to address the resource.
  string unique_name = 2;
}

// Create a new Network Access Profile
message CreateNetworkAccessProfileRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateNetworkAccessProfileRequestBody body = 1;
}

// Canonical success response.
message CreateNetworkAccessProfileResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SupersimV1NetworkAccessProfile body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListNetworkAccessProfileNetworkResponse
message ListNetworkAccessProfileNetworkResponseBody {
  ListNetworkAccessProfileNetworkResponseBodyMeta meta = 1;
  repeated SupersimV1NetworkAccessProfileNetworkAccessProfileNetwork networks = 2;
}

message ListNetworkAccessProfileNetworkResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of Network Access Profile resource's Network resource.
message ListNetworkAccessProfileNetworkRequest {
  // The unique string that identifies the Network Access Profile resource. Required.
  string network_access_profile_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListNetworkAccessProfileNetworkResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListNetworkAccessProfileNetworkResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateNetworkAccessProfileNetworkRequest
message CreateNetworkAccessProfileNetworkRequestBody {
  // The SID of the Network resource to be added to the Network Access Profile resource.
  string network = 1;
}

// Add a Network resource to the Network Access Profile resource.
message CreateNetworkAccessProfileNetworkRequest {
  // The unique string that identifies the Network Access Profile resource. Required.
  string network_access_profile_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateNetworkAccessProfileNetworkRequestBody body = 2;
}

// Canonical success response.
message CreateNetworkAccessProfileNetworkResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SupersimV1NetworkAccessProfileNetworkAccessProfileNetwork body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch a Network Access Profile resource's Network resource.
message FetchNetworkAccessProfileNetworkRequest {
  // The unique string that identifies the Network Access Profile resource. Required.
  string network_access_profile_sid = 1;
  // The SID of the Network resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchNetworkAccessProfileNetworkResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SupersimV1NetworkAccessProfileNetworkAccessProfileNetwork body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Remove a Network resource from the Network Access Profile resource's.
message DeleteNetworkAccessProfileNetworkRequest {
  // The unique string that identifies the Network Access Profile resource. Required.
  string network_access_profile_sid = 1;
  // The SID of the Network resource to be removed from the Network Access Profile resource. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteNetworkAccessProfileNetworkResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// Fetch a Network Access Profile instance from your account.
message FetchNetworkAccessProfileRequest {
  // The SID of the Network Access Profile resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchNetworkAccessProfileResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SupersimV1NetworkAccessProfile body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateNetworkAccessProfileRequest
message UpdateNetworkAccessProfileRequestBody {
  // The new unique name of the Network Access Profile.
  string unique_name = 1;
}

// Updates the given properties of a Network Access Profile in your account.
message UpdateNetworkAccessProfileRequest {
  // The SID of the Network Access Profile to update. Required.
  string sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateNetworkAccessProfileRequestBody body = 2;
}

// Canonical success response.
message UpdateNetworkAccessProfileResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SupersimV1NetworkAccessProfile body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListNetworkResponse
message ListNetworkResponseBody {
  ListNetworkResponseBodyMeta meta = 1;
  repeated SupersimV1Network networks = 2;
}

message ListNetworkResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of Network resources.
message ListNetworkRequest {
  // The [ISO country code](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) of the Network resources to read.
  string iso_country = 1;
  // The 'mobile country code' of a country. Network resources with this `mcc` in their `identifiers` will be read.
  string mcc = 2;
  // The 'mobile network code' of a mobile operator network. Network resources with this `mnc` in their `identifiers` will be read.
  string mnc = 3;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 4;
  // The page index. This value is simply for client state.
  int64 page = 5;
  // The page token. This is provided by the API.
  string page_token = 6;
}

// Canonical success response.
message ListNetworkResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListNetworkResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch a Network resource.
message FetchNetworkRequest {
  // The SID of the Network resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchNetworkResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SupersimV1Network body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListSettingsUpdateResponse
message ListSettingsUpdateResponseBody {
  ListSettingsUpdateResponseBodyMeta meta = 1;
  repeated SupersimV1SettingsUpdate settings_updates = 2;
}

message ListSettingsUpdateResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of Settings Updates.
message ListSettingsUpdateRequest {
  // Filter the Settings Updates by a Super SIM's SID or UniqueName.
  string sim = 1;
  // Filter the Settings Updates by status. Can be `scheduled`, `in-progress`, `successful`, or `failed`.
  SettingsUpdateEnumStatus status = 2;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 3;
  // The page index. This value is simply for client state.
  int64 page = 4;
  // The page token. This is provided by the API.
  string page_token = 5;
}

// Canonical success response.
message ListSettingsUpdateResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListSettingsUpdateResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListSimResponse
message ListSimResponseBody {
  ListSimResponseBodyMeta meta = 1;
  repeated SupersimV1Sim sims = 2;
}

message ListSimResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of Super SIMs from your account.
message ListSimRequest {
  // The status of the Sim resources to read. Can be `new`, `ready`, `active`, `inactive`, or `scheduled`.
  SimEnumStatus status = 1;
  // The SID or unique name of the Fleet to which a list of Sims are assigned.
  string fleet = 2;
  // The [ICCID](https://en.wikipedia.org/wiki/Subscriber_identity_module#ICCID) associated with a Super SIM to filter the list by. Passing this parameter will always return a list containing zero or one SIMs.
  string iccid = 3;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 4;
  // The page index. This value is simply for client state.
  int64 page = 5;
  // The page token. This is provided by the API.
  string page_token = 6;
}

// Canonical success response.
message ListSimResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListSimResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateSimRequest
message CreateSimRequestBody {
  // The [ICCID](https://en.wikipedia.org/wiki/Subscriber_identity_module#ICCID) of the Super SIM to be added to your Account.
  string iccid = 1;
  // The 10-digit code required to claim the Super SIM for your Account.
  string registration_code = 2;
}

// Register a Super SIM to your Account
message CreateSimRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateSimRequestBody body = 1;
}

// Canonical success response.
message CreateSimResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SupersimV1Sim body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch a Super SIM instance from your account.
message FetchSimRequest {
  // The SID of the Sim resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchSimResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SupersimV1Sim body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateSimRequest
message UpdateSimRequestBody {
  // The SID of the Account to which the Sim resource should belong. The Account SID can only be that of the requesting Account or that of a Subaccount of the requesting Account. Only valid when the Sim resource's status is new.
  string account_sid = 1;
  // The HTTP method we should use to call `callback_url`. Can be: `GET` or `POST` and the default is POST.
  string callback_method = 2;
  // The URL we should call using the `callback_method` after an asynchronous update has finished.
  string callback_url = 3;
  // The SID or unique name of the Fleet to which the SIM resource should be assigned.
  string fleet = 4;
  // OpenAPI property "Status".
  SimEnumStatusUpdate status = 5;
  // An application-defined string that uniquely identifies the resource. It can be used in place of the resource's `sid` in the URL to address the resource.
  string unique_name = 6;
}

// Updates the given properties of a Super SIM instance from your account.
message UpdateSimRequest {
  // The SID of the Sim resource to update. Required.
  string sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateSimRequestBody body = 2;
}

// Canonical success response.
message UpdateSimResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SupersimV1Sim body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListBillingPeriodResponse
message ListBillingPeriodResponseBody {
  repeated SupersimV1SimBillingPeriod billing_periods = 1;
  ListBillingPeriodResponseBodyMeta meta = 2;
}

message ListBillingPeriodResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of Billing Periods for a Super SIM.
message ListBillingPeriodRequest {
  // The SID of the Super SIM to list Billing Periods for. Required.
  string sim_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListBillingPeriodResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListBillingPeriodResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListSimIpAddressResponse
message ListSimIpAddressResponseBody {
  repeated SupersimV1SimSimIpAddress ip_addresses = 1;
  ListSimIpAddressResponseBodyMeta meta = 2;
}

message ListSimIpAddressResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of IP Addresses for the given Super SIM.
message ListSimIpAddressRequest {
  // The SID of the Super SIM to list IP Addresses for. Required.
  string sim_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListSimIpAddressResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListSimIpAddressResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListSmsCommandResponse
message ListSmsCommandResponseBody {
  ListSmsCommandResponseBodyMeta meta = 1;
  repeated SupersimV1SmsCommand sms_commands = 2;
}

message ListSmsCommandResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of SMS Commands from your account.
message ListSmsCommandRequest {
  // The SID or unique name of the Sim resource that SMS Command was sent to or from.
  string sim = 1;
  // The status of the SMS Command. Can be: `queued`, `sent`, `delivered`, `received` or `failed`. See the [SMS Command Status Values](https://www.twilio.com/docs/iot/supersim/api/smscommand-resource#status-values) for a description of each.
  SmsCommandEnumStatus status = 2;
  // The direction of the SMS Command. Can be `to_sim` or `from_sim`. The value of `to_sim` is synonymous with the term `mobile terminated`, and `from_sim` is synonymous with the term `mobile originated`.
  SmsCommandEnumDirection direction = 3;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 4;
  // The page index. This value is simply for client state.
  int64 page = 5;
  // The page token. This is provided by the API.
  string page_token = 6;
}

// Canonical success response.
message ListSmsCommandResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListSmsCommandResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateSmsCommandRequest
message CreateSmsCommandRequestBody {
  // The HTTP method we should use to call `callback_url`. Can be: `GET` or `POST` and the default is POST.
  string callback_method = 1;
  // The URL we should call using the `callback_method` after we have sent the command.
  string callback_url = 2;
  // The message body of the SMS Command.
  string payload = 3;
  // The `sid` or `unique_name` of the [SIM](https://www.twilio.com/docs/iot/supersim/api/sim-resource) to send the SMS Command to.
  string sim = 4;
}

// Send SMS Command to a Sim.
message CreateSmsCommandRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateSmsCommandRequestBody body = 1;
}

// Canonical success response.
message CreateSmsCommandResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SupersimV1SmsCommand body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch SMS Command instance from your account.
message FetchSmsCommandRequest {
  // The SID of the SMS Command resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchSmsCommandResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SupersimV1SmsCommand body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListUsageRecordResponse
message ListUsageRecordResponseBody {
  ListUsageRecordResponseBodyMeta meta = 1;
  repeated SupersimV1UsageRecord usage_records = 2;
}

message ListUsageRecordResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// List UsageRecords
message ListUsageRecordRequest {
  // SID or unique name of a Sim resource. Only show UsageRecords representing usage incurred by this Super SIM.
  string sim = 1;
  // SID or unique name of a Fleet resource. Only show UsageRecords representing usage for Super SIMs belonging to this Fleet resource at the time the usage occurred.
  string fleet = 2;
  // SID of a Network resource. Only show UsageRecords representing usage on this network.
  string network = 3;
  // Alpha-2 ISO Country Code. Only show UsageRecords representing usage in this country.
  string iso_country = 4;
  // Dimension over which to aggregate usage records. Can be: `sim`, `fleet`, `network`, `isoCountry`. Default is to not aggregate across any of these dimensions, UsageRecords will be aggregated into the time buckets described by the `Granularity` parameter.
  UsageRecordEnumGroup group_field = 5;
  // Time-based grouping that UsageRecords should be aggregated by. Can be: `hour`, `day`, or `all`. Default is `all`. `all` returns one UsageRecord that describes the usage for the entire period.
  UsageRecordEnumGranularity granularity = 6;
  // Only include usage that occurred at or after this time, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format. Default is one month before the `end_time`.
  string start_time = 7;
  // Only include usage that occurred before this time (exclusive), specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format. Default is the current time.
  string end_time = 8;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 9;
  // The page index. This value is simply for client state.
  int64 page = 10;
  // The page token. This is provided by the API.
  string page_token = 11;
}

// Canonical success response.
message ListUsageRecordResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListUsageRecordResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Generated from SupersimV1EsimProfile operations.
service SupersimV1EsimProfileService {
  // Retrieve a list of eSIM Profiles.
  rpc ListEsimProfile(ListEsimProfileRequest) returns (ListEsimProfileResponse) {
    option (google.api.http) = {
      get: "/v1/ESimProfiles"
      response_body: "body"
    };
  }
  // Order an eSIM Profile.
  rpc CreateEsimProfile(CreateEsimProfileRequest) returns (CreateEsimProfileResponse) {
    option (google.api.http) = {
      post: "/v1/ESimProfiles"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch an eSIM Profile.
  rpc FetchEsimProfile(FetchEsimProfileRequest) returns (FetchEsimProfileResponse) {
    option (google.api.http) = {
      get: "/v1/ESimProfiles/{sid}"
      response_body: "body"
    };
  }
}

// Generated from SupersimV1Fleet operations.
service SupersimV1FleetService {
  // Retrieve a list of Fleets from your account.
  rpc ListFleet(ListFleetRequest) returns (ListFleetResponse) {
    option (google.api.http) = {
      get: "/v1/Fleets"
      response_body: "body"
    };
  }
  // Create a Fleet
  rpc CreateFleet(CreateFleetRequest) returns (CreateFleetResponse) {
    option (google.api.http) = {
      post: "/v1/Fleets"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch a Fleet instance from your account.
  rpc FetchFleet(FetchFleetRequest) returns (FetchFleetResponse) {
    option (google.api.http) = {
      get: "/v1/Fleets/{sid}"
      response_body: "body"
    };
  }
  // Updates the given properties of a Super SIM Fleet instance from your account.
  rpc UpdateFleet(UpdateFleetRequest) returns (UpdateFleetResponse) {
    option (google.api.http) = {
      post: "/v1/Fleets/{sid}"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from SupersimV1IpCommand operations.
service SupersimV1IpCommandService {
  // Retrieve a list of IP Commands from your account.
  rpc ListIpCommand(ListIpCommandRequest) returns (ListIpCommandResponse) {
    option (google.api.http) = {
      get: "/v1/IpCommands"
      response_body: "body"
    };
  }
  // Send an IP Command to a Super SIM.
  rpc CreateIpCommand(CreateIpCommandRequest) returns (CreateIpCommandResponse) {
    option (google.api.http) = {
      post: "/v1/IpCommands"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch IP Command instance from your account.
  rpc FetchIpCommand(FetchIpCommandRequest) returns (FetchIpCommandResponse) {
    option (google.api.http) = {
      get: "/v1/IpCommands/{sid}"
      response_body: "body"
    };
  }
}

// Generated from SupersimV1NetworkAccessProfile operations.
service SupersimV1NetworkAccessProfileService {
  // Retrieve a list of Network Access Profiles from your account.
  rpc ListNetworkAccessProfile(ListNetworkAccessProfileRequest) returns (ListNetworkAccessProfileResponse) {
    option (google.api.http) = {
      get: "/v1/NetworkAccessProfiles"
      response_body: "body"
    };
  }
  // Create a new Network Access Profile
  rpc CreateNetworkAccessProfile(CreateNetworkAccessProfileRequest) returns (CreateNetworkAccessProfileResponse) {
    option (google.api.http) = {
      post: "/v1/NetworkAccessProfiles"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch a Network Access Profile instance from your account.
  rpc FetchNetworkAccessProfile(FetchNetworkAccessProfileRequest) returns (FetchNetworkAccessProfileResponse) {
    option (google.api.http) = {
      get: "/v1/NetworkAccessProfiles/{sid}"
      response_body: "body"
    };
  }
  // Updates the given properties of a Network Access Profile in your account.
  rpc UpdateNetworkAccessProfile(UpdateNetworkAccessProfileRequest) returns (UpdateNetworkAccessProfileResponse) {
    option (google.api.http) = {
      post: "/v1/NetworkAccessProfiles/{sid}"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from SupersimV1NetworkAccessProfileNetwork operations.
service SupersimV1NetworkAccessProfileNetworkService {
  // Retrieve a list of Network Access Profile resource's Network resource.
  rpc ListNetworkAccessProfileNetwork(ListNetworkAccessProfileNetworkRequest) returns (ListNetworkAccessProfileNetworkResponse) {
    option (google.api.http) = {
      get: "/v1/NetworkAccessProfiles/{network_access_profile_sid}/Networks"
      response_body: "body"
    };
  }
  // Add a Network resource to the Network Access Profile resource.
  rpc CreateNetworkAccessProfileNetwork(CreateNetworkAccessProfileNetworkRequest) returns (CreateNetworkAccessProfileNetworkResponse) {
    option (google.api.http) = {
      post: "/v1/NetworkAccessProfiles/{network_access_profile_sid}/Networks"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch a Network Access Profile resource's Network resource.
  rpc FetchNetworkAccessProfileNetwork(FetchNetworkAccessProfileNetworkRequest) returns (FetchNetworkAccessProfileNetworkResponse) {
    option (google.api.http) = {
      get: "/v1/NetworkAccessProfiles/{network_access_profile_sid}/Networks/{sid}"
      response_body: "body"
    };
  }
  // Remove a Network resource from the Network Access Profile resource's.
  rpc DeleteNetworkAccessProfileNetwork(DeleteNetworkAccessProfileNetworkRequest) returns (DeleteNetworkAccessProfileNetworkResponse) {
    option (google.api.http) = {
      delete: "/v1/NetworkAccessProfiles/{network_access_profile_sid}/Networks/{sid}"
    };
  }
}

// Generated from SupersimV1Network operations.
service SupersimV1NetworkService {
  // Retrieve a list of Network resources.
  rpc ListNetwork(ListNetworkRequest) returns (ListNetworkResponse) {
    option (google.api.http) = {
      get: "/v1/Networks"
      response_body: "body"
    };
  }
  // Fetch a Network resource.
  rpc FetchNetwork(FetchNetworkRequest) returns (FetchNetworkResponse) {
    option (google.api.http) = {
      get: "/v1/Networks/{sid}"
      response_body: "body"
    };
  }
}

// Generated from SupersimV1SettingsUpdate operations.
service SupersimV1SettingsUpdateService {
  // Retrieve a list of Settings Updates.
  rpc ListSettingsUpdate(ListSettingsUpdateRequest) returns (ListSettingsUpdateResponse) {
    option (google.api.http) = {
      get: "/v1/SettingsUpdates"
      response_body: "body"
    };
  }
}

// Generated from SupersimV1Sim operations.
service SupersimV1SimService {
  // Retrieve a list of Super SIMs from your account.
  rpc ListSim(ListSimRequest) returns (ListSimResponse) {
    option (google.api.http) = {
      get: "/v1/Sims"
      response_body: "body"
    };
  }
  // Register a Super SIM to your Account
  rpc CreateSim(CreateSimRequest) returns (CreateSimResponse) {
    option (google.api.http) = {
      post: "/v1/Sims"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch a Super SIM instance from your account.
  rpc FetchSim(FetchSimRequest) returns (FetchSimResponse) {
    option (google.api.http) = {
      get: "/v1/Sims/{sid}"
      response_body: "body"
    };
  }
  // Updates the given properties of a Super SIM instance from your account.
  rpc UpdateSim(UpdateSimRequest) returns (UpdateSimResponse) {
    option (google.api.http) = {
      post: "/v1/Sims/{sid}"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from SupersimV1BillingPeriod operations.
service SupersimV1BillingPeriodService {
  // Retrieve a list of Billing Periods for a Super SIM.
  rpc ListBillingPeriod(ListBillingPeriodRequest) returns (ListBillingPeriodResponse) {
    option (google.api.http) = {
      get: "/v1/Sims/{sim_sid}/BillingPeriods"
      response_body: "body"
    };
  }
}

// Generated from SupersimV1SimIpAddress operations.
service SupersimV1SimIpAddressService {
  // Retrieve a list of IP Addresses for the given Super SIM.
  rpc ListSimIpAddress(ListSimIpAddressRequest) returns (ListSimIpAddressResponse) {
    option (google.api.http) = {
      get: "/v1/Sims/{sim_sid}/IpAddresses"
      response_body: "body"
    };
  }
}

// Generated from SupersimV1SmsCommand operations.
service SupersimV1SmsCommandService {
  // Retrieve a list of SMS Commands from your account.
  rpc ListSmsCommand(ListSmsCommandRequest) returns (ListSmsCommandResponse) {
    option (google.api.http) = {
      get: "/v1/SmsCommands"
      response_body: "body"
    };
  }
  // Send SMS Command to a Sim.
  rpc CreateSmsCommand(CreateSmsCommandRequest) returns (CreateSmsCommandResponse) {
    option (google.api.http) = {
      post: "/v1/SmsCommands"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch SMS Command instance from your account.
  rpc FetchSmsCommand(FetchSmsCommandRequest) returns (FetchSmsCommandResponse) {
    option (google.api.http) = {
      get: "/v1/SmsCommands/{sid}"
      response_body: "body"
    };
  }
}

// Generated from SupersimV1UsageRecord operations.
service SupersimV1UsageRecordService {
  // List UsageRecords
  rpc ListUsageRecord(ListUsageRecordRequest) returns (ListUsageRecordResponse) {
    option (google.api.http) = {
      get: "/v1/UsageRecords"
      response_body: "body"
    };
  }
}

