// Code generated by openapi2proto. DO NOT EDIT.
// Source: twilio_bulkexports_v1.json

syntax = "proto3";

package twilio.bulkexports.v1;

option go_package = "twilio/bulkexports/v1;twiliobulkexportsv1";

import "google/api/annotations.proto";

message BulkexportsV1Export {
  // Contains a dictionary of URL links to nested resources of this Export.
  BulkexportsV1ExportLinks links = 1;
  // The type of communication – Messages, Calls, Conferences, and Participants
  string resource_type = 2;
  // The URL of this resource.
  string url = 3;
}

// Contains a dictionary of URL links to nested resources of this Export.
message BulkexportsV1ExportLinks {
  // Scalar value wrapper.
  string value = 1;
}

message BulkexportsV1ExportDay {
  // The ISO 8601 format date when resources is created
  string create_date = 1;
  // The ISO 8601 format date of the resources in the file, for a UTC day
  string day = 2;
  // The friendly name specified when creating the job
  string friendly_name = 3;
  // The type of communication – Messages, Calls, Conferences, and Participants
  string resource_type = 4;
  // The size of the day's data file in bytes
  int64 size = 5;
}

message BulkexportsV1ExportDayInstance {
  string redirect_to = 1;
}

message BulkexportsV1ExportExportCustomJob {
  // The details of a job which is an object that contains an array of status grouped by `status` state.  Each `status` object has a `status` string, a count which is the number of days in that `status`, and list of days in that `status`. The day strings are in the format yyyy-MM-dd. As an example, a currently running job may have a status object for COMPLETED and a `status` object for SUBMITTED each with its own count and list of days.
  repeated string details = 1;
  // The optional email to send the completion notification to
  string email = 2;
  // The end day for the export specified when creating the job
  string end_day = 3;
  // this is the time estimated until your job is complete. This is calculated each time you request the job list. The time is calculated based on the current rate of job completion (which may vary) and your job queue position
  string estimated_completion_time = 4;
  // The friendly name specified when creating the job
  string friendly_name = 5;
  // This is the job position from the 1st in line. Your queue position will never increase. As jobs ahead of yours in the queue are processed, the queue position number will decrease
  string job_queue_position = 6;
  // The unique job_sid returned when the custom export was created
  string job_sid = 7;
  // The type of communication – Messages, Calls, Conferences, and Participants
  string resource_type = 8;
  // The start day for the custom export specified when creating the job
  string start_day = 9;
  // This is the method used to call the webhook on completion of the job. If this is supplied, `WebhookUrl` must also be supplied.
  string webhook_method = 10;
  // The optional webhook url called on completion of the job. If this is supplied, `WebhookMethod` must also be supplied.
  string webhook_url = 11;
}

message BulkexportsV1ExportJob {
  // The details of a job which is an object that contains an array of status grouped by `status` state.  Each `status` object has a `status` string, a count which is the number of days in that `status`, and list of days in that `status`. The day strings are in the format yyyy-MM-dd. As an example, a currently running job may have a status object for COMPLETED and a `status` object for SUBMITTED each with its own count and list of days.
  repeated string details = 1;
  // The optional email to send the completion notification to
  string email = 2;
  // The end time for the export specified when creating the job
  string end_day = 3;
  // this is the time estimated until your job is complete. This is calculated each time you request the job list. The time is calculated based on the current rate of job completion (which may vary) and your job queue position
  string estimated_completion_time = 4;
  // The friendly name specified when creating the job
  string friendly_name = 5;
  // This is the job position from the 1st in line. Your queue position will never increase. As jobs ahead of yours in the queue are processed, the queue position number will decrease
  string job_queue_position = 6;
  // The job_sid returned when the export was created
  string job_sid = 7;
  // The type of communication – Messages, Calls, Conferences, and Participants
  string resource_type = 8;
  // The start time for the export specified when creating the job
  string start_day = 9;
  string url = 10;
  // This is the method used to call the webhook
  string webhook_method = 11;
  // The optional webhook url called on completion
  string webhook_url = 12;
}

message BulkexportsV1ExportConfiguration {
  // If true, Twilio will automatically generate every day's file when the day is over.
  bool enabled = 1;
  // The type of communication – Messages, Calls, Conferences, and Participants
  string resource_type = 2;
  // The URL of this resource.
  string url = 3;
  // Sets whether Twilio should call a webhook URL when the automatic generation is complete, using GET or POST. The actual destination is set in the webhook_url
  string webhook_method = 4;
  // Stores the URL destination for the method specified in webhook_method.
  string webhook_url = 5;
}

message ExportCustomJobEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

message JobEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

message FetchJobRequest {
  // The unique string that that we created to identify the Bulk Export job Required.
  string job_sid = 1;
}

// Canonical success response.
message FetchJobResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  BulkexportsV1ExportJob body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeleteJobRequest {
  // The unique string that that we created to identify the Bulk Export job Required.
  string job_sid = 1;
}

// Canonical success response.
message DeleteJobResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// Fetch a specific Export.
message FetchExportRequest {
  // The type of communication – Messages, Calls, Conferences, and Participants Required.
  string resource_type = 1;
}

// Canonical success response.
message FetchExportResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  BulkexportsV1Export body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch a specific Export Configuration.
message FetchExportConfigurationRequest {
  // The type of communication – Messages, Calls, Conferences, and Participants Required.
  string resource_type = 1;
}

// Canonical success response.
message FetchExportConfigurationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  BulkexportsV1ExportConfiguration body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateExportConfigurationRequest
message UpdateExportConfigurationRequestBody {
  // If true, Twilio will automatically generate every day's file when the day is over.
  bool enabled = 1;
  // Sets whether Twilio should call a webhook URL when the automatic generation is complete, using GET or POST. The actual destination is set in the webhook_url
  string webhook_method = 2;
  // Stores the URL destination for the method specified in webhook_method.
  string webhook_url = 3;
}

// Update a specific Export Configuration.
message UpdateExportConfigurationRequest {
  // The type of communication – Messages, Calls, Conferences, and Participants Required.
  string resource_type = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateExportConfigurationRequestBody body = 2;
}

// Canonical success response.
message UpdateExportConfigurationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  BulkexportsV1ExportConfiguration body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListDayResponse
message ListDayResponseBody {
  repeated BulkexportsV1ExportDay days = 1;
  ListDayResponseBodyMeta meta = 2;
}

message ListDayResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Days for a resource.
message ListDayRequest {
  // The type of communication – Messages, Calls, Conferences, and Participants Required.
  string resource_type = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 400.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListDayResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListDayResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch a specific Day.
message FetchDayRequest {
  // The type of communication – Messages, Calls, Conferences, and Participants Required.
  string resource_type = 1;
  // The ISO 8601 format date of the resources in the file, for a UTC day Required.
  string day = 2;
}

// Canonical success response.
message FetchDayResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  BulkexportsV1ExportDayInstance body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListExportCustomJobResponse
message ListExportCustomJobResponseBody {
  repeated BulkexportsV1ExportExportCustomJob jobs = 1;
  ListExportCustomJobResponseBodyMeta meta = 2;
}

message ListExportCustomJobResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListExportCustomJobRequest {
  // The type of communication – Messages, Calls, Conferences, and Participants Required.
  string resource_type = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListExportCustomJobResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListExportCustomJobResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateExportCustomJobRequest
message CreateExportCustomJobRequestBody {
  // The optional email to send the completion notification to. You can set both webhook, and email, or one or the other. If you set neither, the job will run but you will have to query to determine your job's status.
  string email = 1;
  // The end day for the custom export specified as a string in the format of yyyy-mm-dd. End day is inclusive and must be 2 days earlier than the current UTC day.
  string end_day = 2;
  // The friendly name specified when creating the job
  string friendly_name = 3;
  // The start day for the custom export specified as a string in the format of yyyy-mm-dd
  string start_day = 4;
  // This is the method used to call the webhook on completion of the job. If this is supplied, `WebhookUrl` must also be supplied.
  string webhook_method = 5;
  // The optional webhook url called on completion of the job. If this is supplied, `WebhookMethod` must also be supplied. If you set neither webhook nor email, you will have to check your job's status manually.
  string webhook_url = 6;
}

message CreateExportCustomJobRequest {
  // The type of communication – Messages or Calls, Conferences, and Participants Required.
  string resource_type = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateExportCustomJobRequestBody body = 2;
}

// Canonical success response.
message CreateExportCustomJobResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  BulkexportsV1ExportExportCustomJob body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Generated from BulkexportsV1Job operations.
service BulkexportsV1JobService {
  rpc FetchJob(FetchJobRequest) returns (FetchJobResponse) {
    option (google.api.http) = {
      get: "/v1/Exports/Jobs/{job_sid}"
      response_body: "body"
    };
  }
  rpc DeleteJob(DeleteJobRequest) returns (DeleteJobResponse) {
    option (google.api.http) = {
      delete: "/v1/Exports/Jobs/{job_sid}"
    };
  }
}

// Generated from BulkexportsV1Export operations.
service BulkexportsV1ExportService {
  // Fetch a specific Export.
  rpc FetchExport(FetchExportRequest) returns (FetchExportResponse) {
    option (google.api.http) = {
      get: "/v1/Exports/{resource_type}"
      response_body: "body"
    };
  }
}

// Generated from BulkexportsV1ExportConfiguration operations.
service BulkexportsV1ExportConfigurationService {
  // Fetch a specific Export Configuration.
  rpc FetchExportConfiguration(FetchExportConfigurationRequest) returns (FetchExportConfigurationResponse) {
    option (google.api.http) = {
      get: "/v1/Exports/{resource_type}/Configuration"
      response_body: "body"
    };
  }
  // Update a specific Export Configuration.
  rpc UpdateExportConfiguration(UpdateExportConfigurationRequest) returns (UpdateExportConfigurationResponse) {
    option (google.api.http) = {
      post: "/v1/Exports/{resource_type}/Configuration"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from BulkexportsV1Day operations.
service BulkexportsV1DayService {
  // Retrieve a list of all Days for a resource.
  rpc ListDay(ListDayRequest) returns (ListDayResponse) {
    option (google.api.http) = {
      get: "/v1/Exports/{resource_type}/Days"
      response_body: "body"
    };
  }
  // Fetch a specific Day.
  rpc FetchDay(FetchDayRequest) returns (FetchDayResponse) {
    option (google.api.http) = {
      get: "/v1/Exports/{resource_type}/Days/{day}"
      response_body: "body"
    };
  }
}

// Generated from BulkexportsV1ExportCustomJob operations.
service BulkexportsV1ExportCustomJobService {
  rpc ListExportCustomJob(ListExportCustomJobRequest) returns (ListExportCustomJobResponse) {
    option (google.api.http) = {
      get: "/v1/Exports/{resource_type}/Jobs"
      response_body: "body"
    };
  }
  rpc CreateExportCustomJob(CreateExportCustomJobRequest) returns (CreateExportCustomJobResponse) {
    option (google.api.http) = {
      post: "/v1/Exports/{resource_type}/Jobs"
      body: "body"
      response_body: "body"
    };
  }
}

