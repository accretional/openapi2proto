// Code generated by openapi2proto. DO NOT EDIT.
// Source: twilio_verify_v2.json

syntax = "proto3";

package twilio.verify.v2;

option go_package = "twilio/verify/v2;twilioverifyv2";

import "google/api/annotations.proto";

// The Type of the Factor. Currently only `push` is supported.
message AccessTokenEnumFactorTypes {
  // Scalar value wrapper.
  string value = 1;
}

// Reason for the Challenge to be in certain `status`. One of `none`, `not_needed` or `not_requested`.
message ChallengeEnumChallengeReasons {
  // Scalar value wrapper.
  string value = 1;
}

// The Status of this Challenge. One of `pending`, `expired`, `approved` or `denied`.
message ChallengeEnumChallengeStatuses {
  // Scalar value wrapper.
  string value = 1;
}

// The Factor Type of this Challenge. Currently `push` and `totp` are supported.
message ChallengeEnumFactorTypes {
  // Scalar value wrapper.
  string value = 1;
}

message ChallengeEnumListOrders {
  // Scalar value wrapper.
  string value = 1;
}

// The Status of this Factor. One of `unverified` or `verified`.
message FactorEnumFactorStatuses {
  // Scalar value wrapper.
  string value = 1;
}

// The Type of this Factor. Currently `push` and `totp` are supported.
message FactorEnumFactorTypes {
  // Scalar value wrapper.
  string value = 1;
}

message FactorEnumNotificationPlatforms {
  // Scalar value wrapper.
  string value = 1;
}

message FactorEnumTotpAlgorithms {
  // Scalar value wrapper.
  string value = 1;
}

// The Type of this Form. Currently only `form-push` is supported.
message FormEnumFormTypes {
  // Scalar value wrapper.
  string value = 1;
}

// The Status of this Factor. One of `unverified` or `verified`.
message NewFactorEnumFactorStatuses {
  // Scalar value wrapper.
  string value = 1;
}

// The Type of this Factor. Currently `push` and `totp` are supported.
message NewFactorEnumFactorTypes {
  // Scalar value wrapper.
  string value = 1;
}

message NewFactorEnumNotificationPlatforms {
  // Scalar value wrapper.
  string value = 1;
}

message NewFactorEnumTotpAlgorithms {
  // Scalar value wrapper.
  string value = 1;
}

message VerificationAttemptEnumAttemptStatus {
  // Scalar value wrapper.
  string value = 1;
}

message VerificationAttemptEnumCallStatus {
  // Scalar value wrapper.
  string value = 1;
}

// A string specifying the communication channel used for the verification attempt.
message VerificationAttemptEnumChannels {
  // Scalar value wrapper.
  string value = 1;
}

// A string specifying the conversion status of the verification. A conversion happens when the user is able to provide the correct code. Possible values are `CONVERTED` and `UNCONVERTED`.
message VerificationAttemptEnumConversionStatus {
  // Scalar value wrapper.
  string value = 1;
}

message VerificationAttemptEnumMessageStatus {
  // Scalar value wrapper.
  string value = 1;
}

message VerificationAttemptsSummaryEnumChannels {
  // Scalar value wrapper.
  string value = 1;
}

// The verification method to use. One of: [`email`](https://www.twilio.com/docs/verify/email), `sms`, `whatsapp`, `call`, or `sna`.
message VerificationCheckEnumChannel {
  // Scalar value wrapper.
  string value = 1;
}

// The verification method used. One of: [`email`](https://www.twilio.com/docs/verify/email), `sms`, `whatsapp`, `call`, `sna`, or `rcs`.
message VerificationEnumChannel {
  // Scalar value wrapper.
  string value = 1;
}

// Risk_check overrides Fraud Prevention measures like Fraud Guard, Geo Permissions etc per verification attempt basis, allowing Verify to block traffic considered fraudulent if enabled or bypass active protections if disabled. Can be: `enable`(default) or `disable`. For SMS channel only.
message VerificationEnumRiskCheck {
  // Scalar value wrapper.
  string value = 1;
}

// The status of the verification. Can be: `pending`, `approved`, `canceled`, `max_attempts_reached`, `deleted`, `failed` or `expired`.
message VerificationEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

message VerifyV2Form {
  // Additional information for the available forms for this type. E.g. The separator string used for `binding` in a Factor push.
  string form_meta = 1;
  // OpenAPI property "form_type".
  FormEnumFormTypes form_type = 2;
  // Object that contains the available forms for this type. This available forms are given in the standard [JSON Schema](https://json-schema.org/) format
  string forms = 3;
  // The URL to access the forms for this type.
  string url = 4;
}

message VerifyV2Safelist {
  // The phone number in SafeList.
  string phone_number = 1;
  // The unique string that we created to identify the SafeList resource.
  string sid = 2;
  // The absolute URL of the SafeList resource.
  string url = 3;
}

message VerifyV2Service {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Service resource.
  string account_sid = 1;
  // The length of the verification code to generate.
  int64 code_length = 2;
  // Whether to allow sending verifications with a custom code instead of a randomly generated one.
  bool custom_code_enabled = 3;
  // The date and time in GMT when the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 4;
  // The date and time in GMT when the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 5;
  string default_template_sid = 6;
  // Whether to add a security warning at the end of an SMS verification body. Disabled by default and applies only to SMS. Example SMS body: `Your AppName verification code is: 1234. Don’t share this code with anyone; our employees will never ask for the code`
  bool do_not_share_warning_enabled = 7;
  // Whether to ask the user to press a number before delivering the verify code in a phone call.
  bool dtmf_input_required = 8;
  // The name that appears in the body of your verification messages. It can be up to 30 characters long and can include letters, numbers, spaces, dashes, underscores. Phone numbers, special characters or links are NOT allowed. It cannot contain more than 4 (consecutive or non-consecutive) digits. **This value should not contain PII.**
  string friendly_name = 9;
  // The URLs of related resources.
  VerifyV2ServiceLinks links = 10;
  // Whether to perform a lookup with each verification started and return info about the phone number.
  bool lookup_enabled = 11;
  string passkeys = 12;
  // Whether to pass PSD2 transaction parameters when starting a verification.
  bool psd2_enabled = 13;
  // Configurations for the Push factors (channel) created under this Service.
  string push = 14;
  // The unique string that we created to identify the Service resource.
  string sid = 15;
  // Whether to skip sending SMS verifications to landlines. Requires `lookup_enabled`.
  bool skip_sms_to_landlines = 16;
  // Configurations for the TOTP factors (channel) created under this Service.
  string totp = 17;
  // The name of an alternative text-to-speech service to use in phone calls. Applies only to TTS languages.
  string tts_name = 18;
  // The absolute URL of the resource.
  string url = 19;
  // Whether to allow verifications from the service to reach the stream-events sinks if configured
  bool verify_event_subscription_enabled = 20;
  string whatsapp = 21;
}

// The URLs of related resources.
message VerifyV2ServiceLinks {
  // Scalar value wrapper.
  string value = 1;
}

message VerifyV2ServiceAccessToken {
  // The unique SID identifier of the Account.
  string account_sid = 1;
  // The date that this access token was created, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 2;
  // The unique external identifier for the Entity of the Service.
  string entity_identity = 3;
  // A human readable description of this factor, up to 64 characters. For a push factor, this can be the device's name.
  string factor_friendly_name = 4;
  // OpenAPI property "factor_type".
  AccessTokenEnumFactorTypes factor_type = 5;
  // The unique SID identifier of the Verify Service.
  string service_sid = 6;
  // A 34 character string that uniquely identifies this Access Token.
  string sid = 7;
  // The access token generated for enrollment, this is an encrypted json web token.
  string token = 8;
  // How long, in seconds, the access token is valid. Max: 5 minutes
  int64 ttl = 9;
  // The URL of this resource.
  string url = 10;
}

message VerifyV2ServiceEntity {
  // The unique SID identifier of the Account.
  string account_sid = 1;
  // The date that this Entity was created, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 2;
  // The date that this Entity was updated, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 3;
  // The unique external identifier for the Entity of the Service. This identifier should be immutable, not PII, length between 8 and 64 characters, and generated by your external system, such as your user's UUID, GUID, or SID. It can only contain dash (-) separated alphanumeric characters.
  string identity = 4;
  // Contains a dictionary of URL links to nested resources of this Entity.
  VerifyV2ServiceEntityLinks links = 5;
  // The unique SID identifier of the Service.
  string service_sid = 6;
  // A 34 character string that uniquely identifies this Entity.
  string sid = 7;
  // The URL of this resource.
  string url = 8;
}

// Contains a dictionary of URL links to nested resources of this Entity.
message VerifyV2ServiceEntityLinks {
  // Scalar value wrapper.
  string value = 1;
}

message VerifyV2ServiceEntityChallenge {
  // The unique SID identifier of the Account.
  string account_sid = 1;
  // The date that this Challenge was created, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 2;
  // The date that this Challenge was responded, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_responded = 3;
  // The date that this Challenge was updated, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 4;
  // Details provided to give context about the Challenge. Intended to be shown to the end user.
  string details = 5;
  // The unique SID identifier of the Entity.
  string entity_sid = 6;
  // The date-time when this Challenge expires, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format. The default value is five (5) minutes after Challenge creation. The max value is sixty (60) minutes after creation.
  string expiration_date = 7;
  // The unique SID identifier of the Factor.
  string factor_sid = 8;
  // OpenAPI property "factor_type".
  ChallengeEnumFactorTypes factor_type = 9;
  // Details provided to give context about the Challenge. Intended to be hidden from the end user. It must be a stringified JSON with only strings values eg. `{"ip": "172.168.1.234"}`
  string hidden_details = 10;
  // Customer unique identity for the Entity owner of the Challenge. This identifier should be immutable, not PII, length between 8 and 64 characters, and generated by your external system, such as your user's UUID, GUID, or SID. It can only contain dash (-) separated alphanumeric characters.
  string identity = 11;
  // Contains a dictionary of URL links to nested resources of this Challenge.
  VerifyV2ServiceEntityChallengeLinks links = 12;
  // Custom metadata associated with the challenge. This is added by the Device/SDK directly to allow for the inclusion of device information. It must be a stringified JSON with only strings values eg. `{"os": "Android"}`. Can be up to 1024 characters in length.
  string metadata = 13;
  // OpenAPI property "responded_reason".
  ChallengeEnumChallengeReasons responded_reason = 14;
  // The unique SID identifier of the Service.
  string service_sid = 15;
  // A 34 character string that uniquely identifies this Challenge.
  string sid = 16;
  // OpenAPI property "status".
  ChallengeEnumChallengeStatuses status = 17;
  // The URL of this resource.
  string url = 18;
}

// Contains a dictionary of URL links to nested resources of this Challenge.
message VerifyV2ServiceEntityChallengeLinks {
  // Scalar value wrapper.
  string value = 1;
}

message VerifyV2ServiceEntityChallengeNotification {
  // The unique SID identifier of the Account.
  string account_sid = 1;
  // The unique SID identifier of the Challenge.
  string challenge_sid = 2;
  // The date that this Notification was created, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The unique SID identifier of the Entity.
  string entity_sid = 4;
  // Customer unique identity for the Entity owner of the Challenge. This identifier should be immutable, not PII, length between 8 and 64 characters, and generated by your external system, such as your user's UUID, GUID, or SID. It can only contain dash (-) separated alphanumeric characters.
  string identity = 5;
  // The priority of the notification. For `push` Challenges it's always `high` which sends the notification immediately, and can wake up a sleeping device.
  string priority = 6;
  // The unique SID identifier of the Service.
  string service_sid = 7;
  // A 34 character string that uniquely identifies this Notification.
  string sid = 8;
  // How long, in seconds, the notification is valid. Max: 5 minutes
  int64 ttl = 9;
}

message VerifyV2ServiceEntityFactor {
  // The unique SID identifier of the Account.
  string account_sid = 1;
  // An object that contains configurations specific to a `factor_type`.
  string config = 2;
  // The date that this Factor was created, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The date that this Factor was updated, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 4;
  // The unique SID identifier of the Entity.
  string entity_sid = 5;
  // OpenAPI property "factor_type".
  FactorEnumFactorTypes factor_type = 6;
  // A human readable description of this resource, up to 64 characters. For a push factor, this can be the device's name.
  string friendly_name = 7;
  // Customer unique identity for the Entity owner of the Factor. This identifier should be immutable, not PII, length between 8 and 64 characters, and generated by your external system, such as your user's UUID, GUID, or SID. It can only contain dash (-) separated alphanumeric characters.
  string identity = 8;
  // Custom metadata associated with the factor. This is added by the Device/SDK directly to allow for the inclusion of device information. It must be a stringified JSON with only strings values eg. `{"os": "Android"}`. Can be up to 1024 characters in length.
  string metadata = 9;
  // The unique SID identifier of the Service.
  string service_sid = 10;
  // A 34 character string that uniquely identifies this Factor.
  string sid = 11;
  // OpenAPI property "status".
  FactorEnumFactorStatuses status = 12;
  // The URL of this resource.
  string url = 13;
}

message VerifyV2ServiceEntityNewFactor {
  // The unique SID identifier of the Account.
  string account_sid = 1;
  // Contains the `factor_type` specific secret and metadata.
  // For push, this is `binding.public_key` and `binding.alg`.
  // For totp, this is `binding.secret` and `binding.uri`. The `binding.uri` property is generated following the [google authenticator key URI format](https://github.com/google/google-authenticator/wiki/Key-Uri-Format), and `Factor.friendly_name` is used for the “accountname” value and `Service.friendly_name` or `Service.totp.issuer` is used for the `issuer` value.
  // The Binding property is ONLY returned upon Factor creation.
  string binding = 2;
  // An object that contains configurations specific to a `factor_type`.
  string config = 3;
  // The date that this Factor was created, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 4;
  // The date that this Factor was updated, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 5;
  // The unique SID identifier of the Entity.
  string entity_sid = 6;
  // OpenAPI property "factor_type".
  NewFactorEnumFactorTypes factor_type = 7;
  // The friendly name of this Factor. This can be any string up to 64 characters, meant for humans to distinguish between Factors.
  // For `factor_type` `push`, this could be a device name.
  // For `factor_type` `totp`, this value is used as the “account name” in constructing the `binding.uri` property.
  // At the same time, we recommend avoiding providing PII.
  string friendly_name = 8;
  // Customer unique identity for the Entity owner of the Factor. This identifier should be immutable, not PII, length between 8 and 64 characters, and generated by your external system, such as your user's UUID, GUID, or SID. It can only contain dash (-) separated alphanumeric characters.
  string identity = 9;
  // Custom metadata associated with the factor. This is added by the Device/SDK directly to allow for the inclusion of device information. It must be a stringified JSON with only strings values eg. `{"os": "Android"}`. Can be up to 1024 characters in length.
  string metadata = 10;
  string options = 11;
  // The unique SID identifier of the Service.
  string service_sid = 12;
  // A 34 character string that uniquely identifies this Factor.
  string sid = 13;
  // OpenAPI property "status".
  NewFactorEnumFactorStatuses status = 14;
  // The URL of this resource.
  string url = 15;
}

message VerifyV2ServiceMessagingConfiguration {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Service resource.
  string account_sid = 1;
  // The [ISO-3166-1](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) country code of the country this configuration will be applied to. If this is a global configuration, Country will take the value `all`.
  string country = 2;
  // The date and time in GMT when the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 3;
  // The date and time in GMT when the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 4;
  // The SID of the [Messaging Service](https://www.twilio.com/docs/messaging/api/service-resource) to be used to send SMS to the country of this configuration.
  string messaging_service_sid = 5;
  // The SID of the [Service](https://www.twilio.com/docs/verify/api/service) that the resource is associated with.
  string service_sid = 6;
  // The URL of this resource.
  string url = 7;
}

message VerifyV2ServiceRateLimit {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Rate Limit resource.
  string account_sid = 1;
  // The date and time in GMT when the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 2;
  // The date and time in GMT when the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 3;
  // Description of this Rate Limit
  string description = 4;
  // The URLs of related resources.
  VerifyV2ServiceRateLimitLinks links = 5;
  // The SID of the [Service](https://www.twilio.com/docs/verify/api/service) the resource is associated with.
  string service_sid = 6;
  // A 34 character string that uniquely identifies this Rate Limit.
  string sid = 7;
  // Provides a unique and addressable name to be assigned to this Rate Limit, assigned by the developer, to be optionally used in addition to SID. **This value should not contain PII.**
  string unique_name = 8;
  // The URL of this resource.
  string url = 9;
}

// The URLs of related resources.
message VerifyV2ServiceRateLimitLinks {
  // Scalar value wrapper.
  string value = 1;
}

message VerifyV2ServiceRateLimitBucket {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Rate Limit resource.
  string account_sid = 1;
  // The date and time in GMT when the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 2;
  // The date and time in GMT when the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 3;
  // Number of seconds that the rate limit will be enforced over.
  int64 interval = 4;
  // Maximum number of requests permitted in during the interval.
  int64 max_field = 5;
  // The Twilio-provided string that uniquely identifies the Rate Limit resource.
  string rate_limit_sid = 6;
  // The SID of the [Service](https://www.twilio.com/docs/verify/api/service) the resource is associated with.
  string service_sid = 7;
  // A 34 character string that uniquely identifies this Bucket.
  string sid = 8;
  // The URL of this resource.
  string url = 9;
}

message VerifyV2ServiceVerification {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Verification resource.
  string account_sid = 1;
  // The amount of the associated PSD2 compliant transaction. Requires the PSD2 Service flag enabled.
  string amount = 2;
  // OpenAPI property "channel".
  VerificationEnumChannel channel = 3;
  // The date and time in GMT when the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 4;
  // The date and time in GMT when the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 5;
  // Information about the phone number being verified.
  string lookup = 6;
  // The payee of the associated PSD2 compliant transaction. Requires the PSD2 Service flag enabled.
  string payee = 7;
  // An array of verification attempt objects containing the channel attempted and the channel-specific transaction SID.
  repeated string send_code_attempts = 8;
  // The SID of the [Service](https://www.twilio.com/docs/verify/api/service) the resource is associated with.
  string service_sid = 9;
  // The unique string that we created to identify the Verification resource.
  string sid = 10;
  // The set of fields used for a silent network auth (`sna`) verification. Contains a single field with the URL to be invoked to verify the phone number.
  string sna = 11;
  // The status of the verification. Can be: `pending`, `approved`, `canceled`, `max_attempts_reached`, `deleted`, `failed` or `expired`.
  string status = 12;
  // The phone number or [email](https://www.twilio.com/docs/verify/email) being verified. Phone numbers must be in [E.164 format](https://www.twilio.com/docs/glossary/what-e164).
  string to_field = 13;
  // The absolute URL of the Verification resource.
  string url = 14;
  // Use "status" instead. Legacy property indicating whether the verification was successful.
  bool valid = 15;
}

message VerifyV2ServiceVerificationCheck {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the VerificationCheck resource.
  string account_sid = 1;
  // The amount of the associated PSD2 compliant transaction. Requires the PSD2 Service flag enabled.
  string amount = 2;
  // OpenAPI property "channel".
  VerificationCheckEnumChannel channel = 3;
  // The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date and time in GMT when the Verification Check resource was created.
  string date_created = 4;
  // The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date and time in GMT when the Verification Check resource was last updated.
  string date_updated = 5;
  // The payee of the associated PSD2 compliant transaction. Requires the PSD2 Service flag enabled.
  string payee = 6;
  // The SID of the [Service](https://www.twilio.com/docs/verify/api/service) the resource is associated with.
  string service_sid = 7;
  // The unique string that we created to identify the VerificationCheck resource.
  string sid = 8;
  // List of error codes as a result of attempting a verification using the `sna` channel. The error codes are chronologically ordered, from the first attempt to the latest attempt. This will be an empty list if no errors occured or `null` if the last channel used wasn't `sna`.
  repeated string sna_attempts_error_codes = 9;
  // The status of the verification. Can be: `pending`, `approved`, `canceled`, `max_attempts_reached`, `deleted`, `failed` or `expired`.
  string status = 10;
  // The phone number or [email](https://www.twilio.com/docs/verify/email) being verified. Phone numbers must be in [E.164 format](https://www.twilio.com/docs/glossary/what-e164).
  string to_field = 11;
  // Use "status" instead. Legacy property indicating whether the verification was successful.
  bool valid = 12;
}

message VerifyV2ServiceWebhook {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Service resource.
  string account_sid = 1;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 2;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 3;
  // The array of events that this Webhook is subscribed to. Possible event types: `*, factor.deleted, factor.created, factor.verified, challenge.approved, challenge.denied`
  repeated string event_types = 4;
  // The string that you assigned to describe the webhook. **This value should not contain PII.**
  string friendly_name = 5;
  // The unique SID identifier of the Service.
  string service_sid = 6;
  // The unique string that we created to identify the Webhook resource.
  string sid = 7;
  // OpenAPI property "status".
  WebhookEnumStatus status = 8;
  // The absolute URL of the Webhook resource.
  string url = 9;
  // OpenAPI property "version".
  WebhookEnumVersion version = 10;
  // OpenAPI property "webhook_method".
  WebhookEnumMethods webhook_method = 11;
  // The URL associated with this Webhook.
  string webhook_url = 12;
}

// The webhook status. Default value is `enabled`. One of: `enabled` or `disabled`
message WebhookEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

// The webhook version. Default value is `v2` which includes all the latest fields. Version `v1` is legacy and may be removed in the future.
message WebhookEnumVersion {
  // Scalar value wrapper.
  string value = 1;
}

// The method to be used when calling the webhook's URL.
message WebhookEnumMethods {
  // Scalar value wrapper.
  string value = 1;
}

message VerifyV2VerificationAttempt {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Verification resource.
  string account_sid = 1;
  // OpenAPI property "channel".
  VerificationAttemptEnumChannels channel = 2;
  // An object containing the channel specific information for an attempt.
  string channel_data = 3;
  // OpenAPI property "conversion_status".
  VerificationAttemptEnumConversionStatus conversion_status = 4;
  // The date that this Attempt was created, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 5;
  // The date that this Attempt was updated, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 6;
  // An object containing the charge for this verification attempt related to the channel costs and the currency used. The costs related to the succeeded verifications are not included. May not be immediately available. More information on pricing is available [here](https://www.twilio.com/en-us/verify/pricing).
  string price = 7;
  // The SID of the [Service](https://www.twilio.com/docs/verify/api/service) used to generate the attempt.
  string service_sid = 8;
  // The SID that uniquely identifies the verification attempt resource.
  string sid = 9;
  string url = 10;
  // The SID of the [Verification](https://www.twilio.com/docs/verify/api/verification) that generated the attempt.
  string verification_sid = 11;
}

message VerifyV2VerificationAttemptsSummary {
  // Percentage of the confirmed messages over the total, defined by (total_converted/total_attempts)*100.
  string conversion_rate_percentage = 1;
  // Total of attempts made according to the provided filters
  int64 total_attempts = 2;
  // Total of  attempts made that were confirmed by the end user, according to the provided filters.
  int64 total_converted = 3;
  // Total of attempts made that were not confirmed by the end user, according to the provided filters.
  int64 total_unconverted = 4;
  string url = 5;
}

message VerifyV2VerificationTemplate {
  // The unique SID identifier of the Account.
  string account_sid = 1;
  // A list of channels that support the Template. Can include: sms, voice.
  repeated string channels = 2;
  // A descriptive string that you create to describe a Template. It can be up to 32 characters long.
  string friendly_name = 3;
  // A 34 character string that uniquely identifies a Verification Template.
  string sid = 4;
  // An object that contains the different translations of the template. Every translation is identified by the language short name and contains its respective information as the approval status, text and created/modified date.
  string translations = 5;
}

// ListVerificationAttemptResponse
message ListVerificationAttemptResponseBody {
  repeated VerifyV2VerificationAttempt attempts = 1;
  ListVerificationAttemptResponseBodyMeta meta = 2;
}

message ListVerificationAttemptResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// List all the verification attempts for a given Account.
message ListVerificationAttemptRequest {
  // Datetime filter used to consider only Verification Attempts created after this datetime on the summary aggregation. Given as GMT in ISO 8601 formatted datetime string: yyyy-MM-dd'T'HH:mm:ss'Z.
  string date_created_after = 1;
  // Datetime filter used to consider only Verification Attempts created before this datetime on the summary aggregation. Given as GMT in ISO 8601 formatted datetime string: yyyy-MM-dd'T'HH:mm:ss'Z.
  string date_created_before = 2;
  // Destination of a verification. It is phone number in E.164 format.
  string channel_data_to = 3;
  // Filter used to query Verification Attempts sent to the specified destination country.
  string country = 4;
  // Filter used to query Verification Attempts by communication channel.
  VerificationAttemptEnumChannels channel = 5;
  // Filter used to query Verification Attempts by verify service. Only attempts of the provided SID will be returned.
  string verify_service_sid = 6;
  // Filter used to return all the Verification Attempts of a single verification. Only attempts of the provided verification SID will be returned.
  string verification_sid = 7;
  // Filter used to query Verification Attempts by conversion status. Valid values are `UNCONVERTED`, for attempts that were not converted, and `CONVERTED`, for attempts that were confirmed.
  VerificationAttemptEnumConversionStatus status = 8;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 9;
  // The page index. This value is simply for client state.
  int64 page = 10;
  // The page token. This is provided by the API.
  string page_token = 11;
}

// Canonical success response.
message ListVerificationAttemptResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListVerificationAttemptResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Get a summary of how many attempts were made and how many were converted.
message FetchVerificationAttemptsSummaryRequest {
  // Filter used to consider only Verification Attempts of the given verify service on the summary aggregation.
  string verify_service_sid = 1;
  // Datetime filter used to consider only Verification Attempts created after this datetime on the summary aggregation. Given as GMT in ISO 8601 formatted datetime string: yyyy-MM-dd'T'HH:mm:ss'Z.
  string date_created_after = 2;
  // Datetime filter used to consider only Verification Attempts created before this datetime on the summary aggregation. Given as GMT in ISO 8601 formatted datetime string: yyyy-MM-dd'T'HH:mm:ss'Z.
  string date_created_before = 3;
  // Filter used to consider only Verification Attempts sent to the specified destination country on the summary aggregation.
  string country = 4;
  // Filter Verification Attempts considered on the summary aggregation by communication channel.
  VerificationAttemptsSummaryEnumChannels channel = 5;
  // Filter the Verification Attempts considered on the summary aggregation by Destination prefix. It is the prefix of a phone number in E.164 format.
  string destination_prefix = 6;
}

// Canonical success response.
message FetchVerificationAttemptsSummaryResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2VerificationAttemptsSummary body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch a specific verification attempt.
message FetchVerificationAttemptRequest {
  // The unique SID identifier of a Verification Attempt Required.
  string sid = 1;
}

// Canonical success response.
message FetchVerificationAttemptResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2VerificationAttempt body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch the forms for a specific Form Type.
message FetchFormRequest {
  // The Type of this Form. Currently only `form-push` is supported. Required.
  FormEnumFormTypes form_type = 1;
}

// Canonical success response.
message FetchFormResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2Form body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateSafelistRequest
message CreateSafelistRequestBody {
  // The phone number to be added in SafeList. Phone numbers must be in [E.164 format](https://www.twilio.com/docs/glossary/what-e164).
  string phone_number = 1;
}

// Add a new phone number to SafeList.
message CreateSafelistRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateSafelistRequestBody body = 1;
}

// Canonical success response.
message CreateSafelistResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2Safelist body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Check if a phone number exists in SafeList.
message FetchSafelistRequest {
  // The phone number to be fetched from SafeList. Phone numbers must be in [E.164 format](https://www.twilio.com/docs/glossary/what-e164). Required.
  string phone_number = 1;
}

// Canonical success response.
message FetchSafelistResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2Safelist body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Remove a phone number from SafeList.
message DeleteSafelistRequest {
  // The phone number to be removed from SafeList. Phone numbers must be in [E.164 format](https://www.twilio.com/docs/glossary/what-e164). Required.
  string phone_number = 1;
}

// Canonical success response.
message DeleteSafelistResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListServiceResponse
message ListServiceResponseBody {
  ListServiceResponseBodyMeta meta = 1;
  repeated VerifyV2Service services = 2;
}

message ListServiceResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Verification Services for an account.
message ListServiceRequest {
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 1;
  // The page index. This value is simply for client state.
  int64 page = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
}

// Canonical success response.
message ListServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListServiceResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateServiceRequest
message CreateServiceRequestBody {
  // The length of the verification code to generate. Must be an integer value between 4 and 10, inclusive.
  int64 code_length = 1;
  // Whether to allow sending verifications with a custom code instead of a randomly generated one.
  bool custom_code_enabled = 2;
  // The default message [template](https://www.twilio.com/docs/verify/api/templates). Will be used for all SMS verifications unless explicitly overriden. SMS channel only.
  string default_template_sid = 3;
  // Whether to add a security warning at the end of an SMS verification body. Disabled by default and applies only to SMS. Example SMS body: `Your AppName verification code is: 1234. Don’t share this code with anyone; our employees will never ask for the code`
  bool do_not_share_warning_enabled = 4;
  // Whether to ask the user to press a number before delivering the verify code in a phone call.
  bool dtmf_input_required = 5;
  // A descriptive string that you create to describe the verification service. It can be up to 32 characters long. **This value should not contain PII.**
  string friendly_name = 6;
  // Whether to perform a lookup with each verification started and return info about the phone number.
  bool lookup_enabled = 7;
  // The Authenticator Attachment for Passkeys. This is the type of authenticator that will be used to create Passkeys. It can be empty or it can have the values `platform`, `cross-platform` or `any`.
  string passkeys_authenticator_attachment = 8;
  // Indicates whether credentials must be discoverable by the authenticator. It can be empty or it can have the values `required`, `preferred` or `discouraged`.
  string passkeys_discoverable_credentials = 9;
  // The Relying Party ID for Passkeys. This is the domain of your application, e.g. `example.com`. It is used to identify your application when creating Passkeys.
  string passkeys_relying_party_id = 10;
  // The Relying Party Name for Passkeys. This is the name of your application, e.g. `Example App`. It is used to identify your application when creating Passkeys.
  string passkeys_relying_party_name = 11;
  // The Relying Party Origins for Passkeys. This is the origin of your application, e.g. `login.example.com,www.example.com`. It is used to identify your application when creating Passkeys, it can have multiple origins split by `,`.
  string passkeys_relying_party_origins = 12;
  // The User Verification for Passkeys. This is the type of user verification that will be used to create Passkeys. It can be empty or it can have the values `required`, `preferred` or `discouraged`.
  string passkeys_user_verification = 13;
  // Whether to pass PSD2 transaction parameters when starting a verification.
  bool psd2_enabled = 14;
  // Optional configuration for the Push factors. Set the APN Credential for this service. This will allow to send push notifications to iOS devices. See [Credential Resource](https://www.twilio.com/docs/notify/api/credential-resource)
  string push_apn_credential_sid = 15;
  // Optional configuration for the Push factors. Set the FCM Credential for this service. This will allow to send push notifications to Android devices. See [Credential Resource](https://www.twilio.com/docs/notify/api/credential-resource)
  string push_fcm_credential_sid = 16;
  // Optional configuration for the Push factors. If true, include the date in the Challenge's response. Otherwise, the date is omitted from the response. See [Challenge](https://www.twilio.com/docs/verify/api/challenge) resource’s details parameter for more info. Default: false. **Deprecated** do not use this parameter. This timestamp value is the same one as the one found in `date_created`, please use that one instead.
  bool push_include_date = 17;
  // Whether to skip sending SMS verifications to landlines. Requires `lookup_enabled`.
  bool skip_sms_to_landlines = 18;
  // Optional configuration for the TOTP factors. Number of digits for generated TOTP codes. Must be between 3 and 8, inclusive. Defaults to 6
  int64 totp_code_length = 19;
  // Optional configuration for the TOTP factors. Set TOTP Issuer for this service. This will allow to configure the issuer of the TOTP URI. Defaults to the service friendly name if not provided.
  string totp_issuer = 20;
  // Optional configuration for the TOTP factors. The number of time-steps, past and future, that are valid for validation of TOTP codes. Must be between 0 and 2, inclusive. Defaults to 1
  int64 totp_skew = 21;
  // Optional configuration for the TOTP factors. Defines how often, in seconds, are TOTP codes generated. i.e, a new TOTP code is generated every time_step seconds. Must be between 20 and 60 seconds, inclusive. Defaults to 30 seconds
  int64 totp_time_step = 22;
  // The name of an alternative text-to-speech service to use in phone calls. Applies only to TTS languages.
  string tts_name = 23;
  // Whether to allow verifications from the service to reach the stream-events sinks if configured
  bool verify_event_subscription_enabled = 24;
  // The number to use as the WhatsApp Sender that Verify will use to send WhatsApp messages to your users.This WhatsApp Sender must be associated with a Messaging Service SID.
  string whatsapp_from = 25;
  // The SID of the Messaging Service containing WhatsApp Sender(s) that Verify will use to send WhatsApp messages to your users.
  string whatsapp_msg_service_sid = 26;
}

// Create a new Verification Service.
message CreateServiceRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateServiceRequestBody body = 1;
}

// Canonical success response.
message CreateServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2Service body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateAccessTokenRequest
message CreateAccessTokenRequestBody {
  // The friendly name of the factor that is going to be created with this access token
  string factor_friendly_name = 1;
  // OpenAPI property "FactorType".
  AccessTokenEnumFactorTypes factor_type = 2;
  // The unique external identifier for the Entity of the Service. This identifier should be immutable, not PII, and generated by your external system, such as your user's UUID, GUID, or SID.
  string identity = 3;
  // How long, in seconds, the access token is valid. Can be an integer between 60 and 300. Default is 60.
  int64 ttl = 4;
}

// Create a new enrollment Access Token for the Entity
message CreateAccessTokenRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateAccessTokenRequestBody body = 2;
}

// Canonical success response.
message CreateAccessTokenResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceAccessToken body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch an Access Token for the Entity
message FetchAccessTokenRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // A 34 character string that uniquely identifies this Access Token. Required.
  string sid = 2;
}

// Canonical success response.
message FetchAccessTokenResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceAccessToken body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListEntityResponse
message ListEntityResponseBody {
  repeated VerifyV2ServiceEntity entities = 1;
  ListEntityResponseBodyMeta meta = 2;
}

message ListEntityResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Entities for a Service.
message ListEntityRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListEntityResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListEntityResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateEntityRequest
message CreateEntityRequestBody {
  // The unique external identifier for the Entity of the Service. This identifier should be immutable, not PII, length between 8 and 64 characters, and generated by your external system, such as your user's UUID, GUID, or SID. It can only contain dash (-) separated alphanumeric characters.
  string identity = 1;
}

// Create a new Entity for the Service
message CreateEntityRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateEntityRequestBody body = 2;
}

// Canonical success response.
message CreateEntityResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceEntity body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch a specific Entity.
message FetchEntityRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // The unique external identifier for the Entity of the Service. This identifier should be immutable, not PII, length between 8 and 64 characters, and generated by your external system, such as your user's UUID, GUID, or SID. It can only contain dash (-) separated alphanumeric characters. Required.
  string identity = 2;
}

// Canonical success response.
message FetchEntityResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceEntity body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete a specific Entity.
message DeleteEntityRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // The unique external identifier for the Entity of the Service. This identifier should be immutable, not PII, length between 8 and 64 characters, and generated by your external system, such as your user's UUID, GUID, or SID. It can only contain dash (-) separated alphanumeric characters. Required.
  string identity = 2;
}

// Canonical success response.
message DeleteEntityResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListChallengeResponse
message ListChallengeResponseBody {
  repeated VerifyV2ServiceEntityChallenge challenges = 1;
  ListChallengeResponseBodyMeta meta = 2;
}

message ListChallengeResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Challenges for a Factor.
message ListChallengeRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // Customer unique identity for the Entity owner of the Challenge. This identifier should be immutable, not PII, length between 8 and 64 characters, and generated by your external system, such as your user's UUID, GUID, or SID. It can only contain dash (-) separated alphanumeric characters. Required.
  string identity = 2;
  // The unique SID identifier of the Factor.
  string factor_sid = 3;
  // The Status of the Challenges to fetch. One of `pending`, `expired`, `approved` or `denied`.
  ChallengeEnumChallengeStatuses status = 4;
  // The desired sort order of the Challenges list. One of `asc` or `desc` for ascending and descending respectively. Defaults to `asc`.
  ChallengeEnumListOrders order = 5;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 6;
  // The page index. This value is simply for client state.
  int64 page = 7;
  // The page token. This is provided by the API.
  string page_token = 8;
}

// Canonical success response.
message ListChallengeResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListChallengeResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateChallengeRequest
message CreateChallengeRequestBody {
  // Optional payload used to verify the Challenge upon creation. Only used with a Factor of type `totp` to carry the TOTP code that needs to be verified. For `TOTP` this value must be between 3 and 8 characters long.
  string auth_payload = 1;
  // A list of objects that describe the Fields included in the Challenge. Each object contains the label and value of the field, the label can be up to 36 characters in length and the value can be up to 128 characters in length. Used when `factor_type` is `push`. There can be up to 20 details fields.
  repeated string details_fields = 2;
  // Shown to the user when the push notification arrives. Required when `factor_type` is `push`. Can be up to 256 characters in length
  string details_message = 3;
  // The date-time when this Challenge expires, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format. The default value is five (5) minutes after Challenge creation. The max value is sixty (60) minutes after creation.
  string expiration_date = 4;
  // The unique SID identifier of the Factor.
  string factor_sid = 5;
  // Details provided to give context about the Challenge. Not shown to the end user. It must be a stringified JSON with only strings values eg. `{"ip": "172.168.1.234"}`. Can be up to 1024 characters in length
  string hidden_details = 6;
}

// Create a new Challenge for the Factor
message CreateChallengeRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // Customer unique identity for the Entity owner of the Challenge. This identifier should be immutable, not PII, length between 8 and 64 characters, and generated by your external system, such as your user's UUID, GUID, or SID. It can only contain dash (-) separated alphanumeric characters. Required.
  string identity = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateChallengeRequestBody body = 3;
}

// Canonical success response.
message CreateChallengeResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceEntityChallenge body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateNotificationRequest
message CreateNotificationRequestBody {
  // How long, in seconds, the notification is valid. Can be an integer between 0 and 300. Default is 300. Delivery is attempted until the TTL elapses, even if the device is offline. 0 means that the notification delivery is attempted immediately, only once, and is not stored for future delivery.
  int64 ttl = 1;
}

// Create a new Notification for the corresponding Challenge
message CreateNotificationRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // Customer unique identity for the Entity owner of the Challenge. This identifier should be immutable, not PII, length between 8 and 64 characters, and generated by your external system, such as your user's UUID, GUID, or SID. It can only contain dash (-) separated alphanumeric characters. Required.
  string identity = 2;
  // The unique SID identifier of the Challenge. Required.
  string challenge_sid = 3;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateNotificationRequestBody body = 4;
}

// Canonical success response.
message CreateNotificationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceEntityChallengeNotification body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch a specific Challenge.
message FetchChallengeRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // Customer unique identity for the Entity owner of the Challenges. This identifier should be immutable, not PII, length between 8 and 64 characters, and generated by your external system, such as your user's UUID, GUID, or SID. It can only contain dash (-) separated alphanumeric characters. Required.
  string identity = 2;
  // A 34 character string that uniquely identifies this Challenge. Required.
  string sid = 3;
}

// Canonical success response.
message FetchChallengeResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceEntityChallenge body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "Twilio-Verify-Signature-Fields".
  string header_twilio_verify_signature_fields = 8;
}

// UpdateChallengeRequest
message UpdateChallengeRequestBody {
  // The optional payload needed to verify the Challenge. E.g., a TOTP would use the numeric code. For `TOTP` this value must be between 3 and 8 characters long. For `Push` this value can be up to 5456 characters in length
  string auth_payload = 1;
  // Custom metadata associated with the challenge. This is added by the Device/SDK directly to allow for the inclusion of device information. It must be a stringified JSON with only strings values eg. `{"os": "Android"}`. Can be up to 1024 characters in length.
  string metadata = 2;
}

// Verify a specific Challenge.
message UpdateChallengeRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // Customer unique identity for the Entity owner of the Challenge. This identifier should be immutable, not PII, length between 8 and 64 characters, and generated by your external system, such as your user's UUID, GUID, or SID. It can only contain dash (-) separated alphanumeric characters. Required.
  string identity = 2;
  // A 34 character string that uniquely identifies this Challenge. Required.
  string sid = 3;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateChallengeRequestBody body = 4;
}

// Canonical success response.
message UpdateChallengeResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceEntityChallenge body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "Twilio-Verify-Challenge-Signature".
  string header_twilio_verify_challenge_signature = 8;
}

// ListFactorResponse
message ListFactorResponseBody {
  repeated VerifyV2ServiceEntityFactor factors = 1;
  ListFactorResponseBodyMeta meta = 2;
}

message ListFactorResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Factors for an Entity.
message ListFactorRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // Customer unique identity for the Entity owner of the Factors. This identifier should be immutable, not PII, length between 8 and 64 characters, and generated by your external system, such as your user's UUID, GUID, or SID. It can only contain dash (-) separated alphanumeric characters. Required.
  string identity = 2;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 3;
  // The page index. This value is simply for client state.
  int64 page = 4;
  // The page token. This is provided by the API.
  string page_token = 5;
}

// Canonical success response.
message ListFactorResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListFactorResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateNewFactorRequest
message CreateNewFactorRequestBody {
  // The algorithm used when `factor_type` is `push`. Algorithm supported: `ES256`
  string binding_alg = 1;
  // The Ecdsa public key in PKIX, ASN.1 DER format encoded in Base64.
  // Required when `factor_type` is `push`
  string binding_public_key = 2;
  // The shared secret for TOTP factors encoded in Base32. This can be provided when creating the Factor, otherwise it will be generated.
  // Used when `factor_type` is `totp`
  string binding_secret = 3;
  // OpenAPI property "Config.Alg".
  NewFactorEnumTotpAlgorithms config_alg = 4;
  // The ID that uniquely identifies your app in the Google or Apple store, such as `com.example.myapp`. It can be up to 100 characters long.
  // Required when `factor_type` is `push`.
  string config_app_id = 5;
  // Number of digits for generated TOTP codes. Must be between 3 and 8, inclusive. The default value is defined at the service level in the property `totp.code_length`. If not configured defaults to 6.
  // Used when `factor_type` is `totp`
  int64 config_code_length = 6;
  // OpenAPI property "Config.NotificationPlatform".
  NewFactorEnumNotificationPlatforms config_notification_platform = 7;
  // For APN, the device token. For FCM, the registration token. It is used to send the push notifications. Must be between 32 and 255 characters long.
  // Required when `factor_type` is `push`.
  string config_notification_token = 8;
  // The Verify Push SDK version used to configure the factor
  // Required when `factor_type` is `push`
  string config_sdk_version = 9;
  // The number of time-steps, past and future, that are valid for validation of TOTP codes. Must be between 0 and 2, inclusive. The default value is defined at the service level in the property `totp.skew`. If not configured defaults to 1.
  // Used when `factor_type` is `totp`
  int64 config_skew = 10;
  // Defines how often, in seconds, are TOTP codes generated. i.e, a new TOTP code is generated every time_step seconds. Must be between 20 and 60 seconds, inclusive. The default value is defined at the service level in the property `totp.time_step`. Defaults to 30 seconds if not configured.
  // Used when `factor_type` is `totp`
  int64 config_time_step = 11;
  // OpenAPI property "FactorType".
  NewFactorEnumFactorTypes factor_type = 12;
  // The friendly name of this Factor. This can be any string up to 64 characters, meant for humans to distinguish between Factors.
  // For `factor_type` `push`, this could be a device name.
  // For `factor_type` `totp`, this value is used as the “account name” in constructing the `binding.uri` property.
  // At the same time, we recommend avoiding providing PII.
  string friendly_name = 13;
  // Custom metadata associated with the factor. This is added by the Device/SDK directly to allow for the inclusion of device information. It must be a stringified JSON with only strings values eg. `{"os": "Android"}`. Can be up to 1024 characters in length.
  string metadata = 14;
}

// Create a new Factor for the Entity
message CreateNewFactorRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // Customer unique identity for the Entity owner of the Factor. This identifier should be immutable, not PII, length between 8 and 64 characters, and generated by your external system, such as your user's UUID, GUID, or SID. It can only contain dash (-) separated alphanumeric characters. Required.
  string identity = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateNewFactorRequestBody body = 3;
}

// Canonical success response.
message CreateNewFactorResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceEntityNewFactor body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch a specific Factor.
message FetchFactorRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // Customer unique identity for the Entity owner of the Factor. This identifier should be immutable, not PII, length between 8 and 64 characters, and generated by your external system, such as your user's UUID, GUID, or SID. It can only contain dash (-) separated alphanumeric characters. Required.
  string identity = 2;
  // A 34 character string that uniquely identifies this Factor. Required.
  string sid = 3;
}

// Canonical success response.
message FetchFactorResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceEntityFactor body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateFactorRequest
message UpdateFactorRequestBody {
  // The optional payload needed to verify the Factor for the first time. E.g. for a TOTP, the numeric code.
  string auth_payload = 1;
  // OpenAPI property "Config.Alg".
  FactorEnumTotpAlgorithms config_alg = 2;
  // Number of digits for generated TOTP codes. Must be between 3 and 8, inclusive
  int64 config_code_length = 3;
  // The transport technology used to generate the Notification Token. Can be `apn`, `fcm` or `none`.
  // Required when `factor_type` is `push`.
  string config_notification_platform = 4;
  // For APN, the device token. For FCM, the registration token. It is used to send the push notifications. Required when `factor_type` is `push`. If specified, this value must be between 32 and 255 characters long.
  string config_notification_token = 5;
  // The Verify Push SDK version used to configure the factor
  string config_sdk_version = 6;
  // The number of time-steps, past and future, that are valid for validation of TOTP codes. Must be between 0 and 2, inclusive
  int64 config_skew = 7;
  // Defines how often, in seconds, are TOTP codes generated. i.e, a new TOTP code is generated every time_step seconds. Must be between 20 and 60 seconds, inclusive
  int64 config_time_step = 8;
  // The new friendly name of this Factor. It can be up to 64 characters.
  string friendly_name = 9;
}

// Update a specific Factor. This endpoint can be used to Verify a Factor if passed an `AuthPayload` param.
message UpdateFactorRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // Customer unique identity for the Entity owner of the Factor. This identifier should be immutable, not PII, length between 8 and 64 characters, and generated by your external system, such as your user's UUID, GUID, or SID. It can only contain dash (-) separated alphanumeric characters. Required.
  string identity = 2;
  // A 34 character string that uniquely identifies this Factor. Required.
  string sid = 3;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateFactorRequestBody body = 4;
}

// Canonical success response.
message UpdateFactorResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceEntityFactor body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "Twilio-Verify-Factor-Signature".
  string header_twilio_verify_factor_signature = 8;
}

// Delete a specific Factor.
message DeleteFactorRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // Customer unique identity for the Entity owner of the Factor. This identifier should be immutable, not PII, length between 8 and 64 characters, and generated by your external system, such as your user's UUID, GUID, or SID. It can only contain dash (-) separated alphanumeric characters. Required.
  string identity = 2;
  // A 34 character string that uniquely identifies this Factor. Required.
  string sid = 3;
}

// Canonical success response.
message DeleteFactorResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListMessagingConfigurationResponse
message ListMessagingConfigurationResponseBody {
  repeated VerifyV2ServiceMessagingConfiguration messaging_configurations = 1;
  ListMessagingConfigurationResponseBodyMeta meta = 2;
}

message ListMessagingConfigurationResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Messaging Configurations for a Service.
message ListMessagingConfigurationRequest {
  // The SID of the [Service](https://www.twilio.com/docs/verify/api/service) that the resource is associated with. Required.
  string service_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListMessagingConfigurationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListMessagingConfigurationResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateMessagingConfigurationRequest
message CreateMessagingConfigurationRequestBody {
  // The [ISO-3166-1](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) country code of the country this configuration will be applied to. If this is a global configuration, Country will take the value `all`.
  string country = 1;
  // The SID of the [Messaging Service](https://www.twilio.com/docs/messaging/api/service-resource) to be used to send SMS to the country of this configuration.
  string messaging_service_sid = 2;
}

// Create a new MessagingConfiguration for a service.
message CreateMessagingConfigurationRequest {
  // The SID of the [Service](https://www.twilio.com/docs/verify/api/service) that the resource is associated with. Required.
  string service_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateMessagingConfigurationRequestBody body = 2;
}

// Canonical success response.
message CreateMessagingConfigurationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceMessagingConfiguration body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch a specific MessagingConfiguration.
message FetchMessagingConfigurationRequest {
  // The SID of the [Service](https://www.twilio.com/docs/verify/api/service) that the resource is associated with. Required.
  string service_sid = 1;
  // The [ISO-3166-1](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) country code of the country this configuration will be applied to. If this is a global configuration, Country will take the value `all`. Required.
  string country = 2;
}

// Canonical success response.
message FetchMessagingConfigurationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceMessagingConfiguration body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateMessagingConfigurationRequest
message UpdateMessagingConfigurationRequestBody {
  // The SID of the [Messaging Service](https://www.twilio.com/docs/messaging/api/service-resource) to be used to send SMS to the country of this configuration.
  string messaging_service_sid = 1;
}

// Update a specific MessagingConfiguration
message UpdateMessagingConfigurationRequest {
  // The SID of the [Service](https://www.twilio.com/docs/verify/api/service) that the resource is associated with. Required.
  string service_sid = 1;
  // The [ISO-3166-1](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) country code of the country this configuration will be applied to. If this is a global configuration, Country will take the value `all`. Required.
  string country = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateMessagingConfigurationRequestBody body = 3;
}

// Canonical success response.
message UpdateMessagingConfigurationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceMessagingConfiguration body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete a specific MessagingConfiguration.
message DeleteMessagingConfigurationRequest {
  // The SID of the [Service](https://www.twilio.com/docs/verify/api/service) that the resource is associated with. Required.
  string service_sid = 1;
  // The [ISO-3166-1](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) country code of the country this configuration will be applied to. If this is a global configuration, Country will take the value `all`. Required.
  string country = 2;
}

// Canonical success response.
message DeleteMessagingConfigurationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ApprovePasskeysChallengeRequest
message UpdateChallengePasskeysRequestBody {
  // A string that indicates the mechanism by which the WebAuthn implementation is attached to the authenticator at the time the associated `navigator.credentials.create()` or `navigator.credentials.get()` call completes.
  string authenticator_attachment = 1;
  // A [base64url](https://base64.guru/standards/base64url) encoded representation of `rawId`.
  string id = 2;
  // The globally unique identifier for this `PublicKeyCredential`.
  string raw_id = 3;
  // The result of a WebAuthn authentication via a `navigator.credentials.get()` request, as specified in [AuthenticatorAttestationResponse](https://developer.mozilla.org/en-US/docs/Web/API/AuthenticatorAttestationResponse).
  UpdateChallengePasskeysRequestBodyResponse response = 4;
  // The valid credential types supported by the API. The values of this enumeration are used for versioning the `AuthenticatorAssertion` and `AuthenticatorAttestation` structures according to the type of the authenticator.
  string type = 5;
}

// The result of a WebAuthn authentication via a `navigator.credentials.get()` request, as specified in [AuthenticatorAttestationResponse](https://developer.mozilla.org/en-US/docs/Web/API/AuthenticatorAttestationResponse).
message UpdateChallengePasskeysRequestBodyResponse {
  // The [authenticator data](https://developer.mozilla.org/en-US/docs/Web/API/Web_Authentication_API/Authenticator_data) structure contains information from the authenticator about the processing of a credential creation or authentication request.
  string authenticator_data = 1;
  // This property contains the JSON-compatible serialization of the data passed from the browser to the authenticator in order to generate this credential.
  string client_data_json = 2;
  // An assertion signature over `authenticatorData` and `clientDataJSON`. The assertion signature is created with the private key of the key pair that was created during the originating `navigator.credentials.create()` call and verified using the public key of that same key pair.
  string signature = 3;
  // The user handle stored in the authenticator, specified as `user.id` in the options passed to the originating `navigator.credentials.create()` call. This property should contain a base64url-encoded entity SID.
  string user_handle = 4;
}

message UpdateChallengePasskeysResponseBody {
  // The unique SID identifier of the Account.
  string account_sid = 1;
  // The date that this Challenge was created, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 2;
  // The date that this Challenge was responded, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_responded = 3;
  // The date that this Challenge was updated, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 4;
  // Details provided to give context about the Challenge.
  string details = 5;
  // The unique SID identifier of the Entity.
  string entity_sid = 6;
  // The date-time when this Challenge expires, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string expiration_date = 7;
  // The unique SID identifier of the Factor.
  string factor_sid = 8;
  // The Factor Type of this Challenge. Currently `push` and `totp` are supported.
  string factor_type = 9;
  // Details provided to give context about the Challenge.
  string hidden_details = 10;
  // Customer unique identity for the Entity owner of the Challenge.
  string identity = 11;
  // Contains a dictionary of URL links to nested resources of this Challenge.
  UpdateChallengePasskeysResponseBodyLinks links = 12;
  // Custom metadata associated with the challenge.
  string metadata = 13;
  // An object that contains challenge options. Currently only used for `passkeys`.
  UpdateChallengePasskeysResponseBodyOptions options = 14;
  // Reason for the Challenge to be in certain `status`. One of `none`, `not_needed` or `not_requested`.
  string responded_reason = 15;
  // The unique SID identifier of the Service.
  string service_sid = 16;
  // A 34 character string that uniquely identifies this Challenge.
  string sid = 17;
  // The Status of this Challenge. One of `pending`, `expired`, `approved` or `denied`.
  string status = 18;
  // The URL of this resource.
  string url = 19;
}

// Contains a dictionary of URL links to nested resources of this Challenge.
message UpdateChallengePasskeysResponseBodyLinks {
  // Scalar value wrapper.
  string value = 1;
}

// An object that contains challenge options. Currently only used for `passkeys`.
message UpdateChallengePasskeysResponseBodyOptions {
  // Scalar value wrapper.
  string value = 1;
}

// Approve a Passkeys Challenge
message UpdateChallengePasskeysRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // Request body from media type "application/json".
  UpdateChallengePasskeysRequestBody body = 2;
}

// Canonical success response.
message UpdateChallengePasskeysResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  UpdateChallengePasskeysResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreatePasskeysChallengeRequest
message CreateChallengePasskeysRequestBody {
  string factor_sid = 1;
  string identity = 2;
}

message CreateChallengePasskeysResponseBody {
  // The unique SID identifier of the Account.
  string account_sid = 1;
  // The date that this Challenge was created, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 2;
  // The date that this Challenge was responded, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_responded = 3;
  // The date that this Challenge was updated, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 4;
  // Details provided to give context about the Challenge.
  string details = 5;
  // The unique SID identifier of the Entity.
  string entity_sid = 6;
  // The date-time when this Challenge expires, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string expiration_date = 7;
  // The unique SID identifier of the Factor.
  string factor_sid = 8;
  // The Factor Type of this Challenge. Currently `push` and `totp` are supported.
  string factor_type = 9;
  // Details provided to give context about the Challenge.
  string hidden_details = 10;
  // Customer unique identity for the Entity owner of the Challenge.
  string identity = 11;
  // Contains a dictionary of URL links to nested resources of this Challenge.
  CreateChallengePasskeysResponseBodyLinks links = 12;
  // Custom metadata associated with the challenge.
  string metadata = 13;
  // An object that contains challenge options. Currently only used for `passkeys`.
  CreateChallengePasskeysResponseBodyOptions options = 14;
  // Reason for the Challenge to be in certain `status`. One of `none`, `not_needed` or `not_requested`.
  string responded_reason = 15;
  // The unique SID identifier of the Service.
  string service_sid = 16;
  // A 34 character string that uniquely identifies this Challenge.
  string sid = 17;
  // The Status of this Challenge. One of `pending`, `expired`, `approved` or `denied`.
  string status = 18;
  // The URL of this resource.
  string url = 19;
}

// Contains a dictionary of URL links to nested resources of this Challenge.
message CreateChallengePasskeysResponseBodyLinks {
  // Scalar value wrapper.
  string value = 1;
}

// An object that contains challenge options. Currently only used for `passkeys`.
message CreateChallengePasskeysResponseBodyOptions {
  // Scalar value wrapper.
  string value = 1;
}

// Create a Passkeys Challenge
message CreateChallengePasskeysRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // Request body from media type "application/json".
  CreateChallengePasskeysRequestBody body = 2;
}

// Canonical success response.
message CreateChallengePasskeysResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  CreateChallengePasskeysResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateNewPasskeysFactorRequest
message CreateNewFactorPasskeyRequestBody {
  CreateNewFactorPasskeyRequestBodyConfig config = 1;
  string friendly_name = 2;
  string identity = 3;
}

message CreateNewFactorPasskeyRequestBodyConfig {
  string authenticator_attachment = 1;
  string discoverable_credentials = 2;
  // Contains the information of the party requesting the user for authentication
  CreateNewFactorPasskeyRequestBodyConfigRelyingParty relying_party = 3;
  string user_verification = 4;
}

// Contains the information of the party requesting the user for authentication
message CreateNewFactorPasskeyRequestBodyConfigRelyingParty {
  string id = 1;
  string name = 2;
  repeated string origins = 3;
}

message CreateNewFactorPasskeyResponseBody {
  // The unique SID identifier of the Account.
  string account_sid = 1;
  // Contains the `factor_type` specific secret and metadata. The Binding property is ONLY returned upon Factor creation.
  string binding = 2;
  // An object that contains configurations specific to a `factor_type`.
  string config = 3;
  // The date that this Factor was created, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 4;
  // The date that this Factor was updated, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 5;
  // The unique SID identifier of the Entity.
  string entity_sid = 6;
  // The Type of this Factor. Currently `push` and `totp` are supported.
  string factor_type = 7;
  // The friendly name of this Factor. This can be any string up to 64 characters, meant for humans to distinguish between Factors.
  string friendly_name = 8;
  // Customer unique identity for the Entity owner of the Factor.
  string identity = 9;
  // Custom metadata associated with the factor.
  string metadata = 10;
  string options = 11;
  // The unique SID identifier of the Service.
  string service_sid = 12;
  // A 34 character string that uniquely identifies this Factor.
  string sid = 13;
  // The Status of this Factor. One of `unverified` or `verified`.
  string status = 14;
  // The URL of this resource.
  string url = 15;
}

// Create a new Passkeys Factor for the Entity
message CreateNewFactorPasskeyRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // Request body from media type "application/json".
  CreateNewFactorPasskeyRequestBody body = 2;
}

// Canonical success response.
message CreateNewFactorPasskeyResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  CreateNewFactorPasskeyResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// VerifyPasskeysFactorRequest
message UpdatePasskeysFactorRequestBody {
  // A string that indicates the mechanism by which the WebAuthn implementation is attached to the authenticator at the time the associated
  // `navigator.credentials.create()` or `navigator.credentials.get()` call completes.
  string authenticator_attachment = 1;
  // A [base64url](https://base64.guru/standards/base64url) encoded representation of `rawId`.
  string id = 2;
  // The globally unique identifier for this `PublicKeyCredential`.
  string raw_id = 3;
  // The result of a WebAuthn credential registration via `navigator.credentials.create()`, as specified in [AuthenticatorAttestationResponse](https://developer.mozilla.org/en-US/docs/Web/API/AuthenticatorAttestationResponse).
  UpdatePasskeysFactorRequestBodyResponse response = 4;
  // The valid credential types supported by the API.
  // The values of this enumeration are used for versioning the `AuthenticatorAssertion` and `AuthenticatorAttestation` structures according to the type of the authenticator.
  string type = 5;
}

// The result of a WebAuthn credential registration via `navigator.credentials.create()`, as specified in [AuthenticatorAttestationResponse](https://developer.mozilla.org/en-US/docs/Web/API/AuthenticatorAttestationResponse).
message UpdatePasskeysFactorRequestBodyResponse {
  // The authenticator data and an attestation statement for a new key pair generated by the authenticator.
  string attestation_object = 1;
  // This property contains the JSON-compatible serialization of the data passed from the browser to the authenticator in order to generate this credential.
  string client_data_json = 2;
  // An array of strings providing hints as to the methods the client could use to communicate with the relevant authenticator of the public key credential to retrieve.
  repeated string transports = 3;
}

message UpdatePasskeysFactorResponseBody {
  // The unique SID identifier of the Account.
  string account_sid = 1;
  // An object that contains configurations specific to a `factor_type`.
  string config = 2;
  // The date that this Factor was created, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The date that this Factor was updated, given in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 4;
  // The unique SID identifier of the Entity.
  string entity_sid = 5;
  // The Type of this Factor. Currently `push` and `totp` are supported.
  string factor_type = 6;
  // A human readable description of this resource, up to 64 characters.
  string friendly_name = 7;
  // Customer unique identity for the Entity owner of the Factor.
  string identity = 8;
  // Custom metadata associated with the factor.
  string metadata = 9;
  // The unique SID identifier of the Service.
  string service_sid = 10;
  // A 34 character string that uniquely identifies this Factor.
  string sid = 11;
  // The Status of this Factor. One of `unverified` or `verified`.
  string status = 12;
  // The URL of this resource.
  string url = 13;
}

// Verify a Passkeys Factor
message UpdatePasskeysFactorRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // Request body from media type "application/json".
  UpdatePasskeysFactorRequestBody body = 2;
}

// Canonical success response.
message UpdatePasskeysFactorResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  UpdatePasskeysFactorResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListRateLimitResponse
message ListRateLimitResponseBody {
  ListRateLimitResponseBodyMeta meta = 1;
  repeated VerifyV2ServiceRateLimit rate_limits = 2;
}

message ListRateLimitResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Rate Limits for a service.
message ListRateLimitRequest {
  // The SID of the [Service](https://www.twilio.com/docs/verify/api/service) the resource is associated with. Required.
  string service_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListRateLimitResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListRateLimitResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateRateLimitRequest
message CreateRateLimitRequestBody {
  // Description of this Rate Limit
  string description = 1;
  // Provides a unique and addressable name to be assigned to this Rate Limit, assigned by the developer, to be optionally used in addition to SID. **This value should not contain PII.**
  string unique_name = 2;
}

// Create a new Rate Limit for a Service
message CreateRateLimitRequest {
  // The SID of the [Service](https://www.twilio.com/docs/verify/api/service) the resource is associated with. Required.
  string service_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateRateLimitRequestBody body = 2;
}

// Canonical success response.
message CreateRateLimitResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceRateLimit body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListBucketResponse
message ListBucketResponseBody {
  repeated VerifyV2ServiceRateLimitBucket buckets = 1;
  ListBucketResponseBodyMeta meta = 2;
}

message ListBucketResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Buckets for a Rate Limit.
message ListBucketRequest {
  // The SID of the [Service](https://www.twilio.com/docs/verify/api/service) the resource is associated with. Required.
  string service_sid = 1;
  // The Twilio-provided string that uniquely identifies the Rate Limit resource. Required.
  string rate_limit_sid = 2;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 3;
  // The page index. This value is simply for client state.
  int64 page = 4;
  // The page token. This is provided by the API.
  string page_token = 5;
}

// Canonical success response.
message ListBucketResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListBucketResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateBucketRequest
message CreateBucketRequestBody {
  // Number of seconds that the rate limit will be enforced over.
  int64 interval = 1;
  // Maximum number of requests permitted in during the interval.
  int64 max_field = 2;
}

// Create a new Bucket for a Rate Limit
message CreateBucketRequest {
  // The SID of the [Service](https://www.twilio.com/docs/verify/api/service) the resource is associated with. Required.
  string service_sid = 1;
  // The Twilio-provided string that uniquely identifies the Rate Limit resource. Required.
  string rate_limit_sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateBucketRequestBody body = 3;
}

// Canonical success response.
message CreateBucketResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceRateLimitBucket body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch a specific Bucket.
message FetchBucketRequest {
  // The SID of the [Service](https://www.twilio.com/docs/verify/api/service) the resource is associated with. Required.
  string service_sid = 1;
  // The Twilio-provided string that uniquely identifies the Rate Limit resource. Required.
  string rate_limit_sid = 2;
  // A 34 character string that uniquely identifies this Bucket. Required.
  string sid = 3;
}

// Canonical success response.
message FetchBucketResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceRateLimitBucket body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateBucketRequest
message UpdateBucketRequestBody {
  // Number of seconds that the rate limit will be enforced over.
  int64 interval = 1;
  // Maximum number of requests permitted in during the interval.
  int64 max_field = 2;
}

// Update a specific Bucket.
message UpdateBucketRequest {
  // The SID of the [Service](https://www.twilio.com/docs/verify/api/service) the resource is associated with. Required.
  string service_sid = 1;
  // The Twilio-provided string that uniquely identifies the Rate Limit resource. Required.
  string rate_limit_sid = 2;
  // A 34 character string that uniquely identifies this Bucket. Required.
  string sid = 3;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateBucketRequestBody body = 4;
}

// Canonical success response.
message UpdateBucketResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceRateLimitBucket body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete a specific Bucket.
message DeleteBucketRequest {
  // The SID of the [Service](https://www.twilio.com/docs/verify/api/service) the resource is associated with. Required.
  string service_sid = 1;
  // The Twilio-provided string that uniquely identifies the Rate Limit resource. Required.
  string rate_limit_sid = 2;
  // A 34 character string that uniquely identifies this Bucket. Required.
  string sid = 3;
}

// Canonical success response.
message DeleteBucketResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// Fetch a specific Rate Limit.
message FetchRateLimitRequest {
  // The SID of the [Service](https://www.twilio.com/docs/verify/api/service) the resource is associated with. Required.
  string service_sid = 1;
  // The Twilio-provided string that uniquely identifies the Rate Limit resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchRateLimitResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceRateLimit body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateRateLimitRequest
message UpdateRateLimitRequestBody {
  // Description of this Rate Limit
  string description = 1;
}

// Update a specific Rate Limit.
message UpdateRateLimitRequest {
  // The SID of the [Service](https://www.twilio.com/docs/verify/api/service) the resource is associated with. Required.
  string service_sid = 1;
  // The Twilio-provided string that uniquely identifies the Rate Limit resource to fetch. Required.
  string sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateRateLimitRequestBody body = 3;
}

// Canonical success response.
message UpdateRateLimitResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceRateLimit body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete a specific Rate Limit.
message DeleteRateLimitRequest {
  // The SID of the [Service](https://www.twilio.com/docs/verify/api/service) the resource is associated with. Required.
  string service_sid = 1;
  // The Twilio-provided string that uniquely identifies the Rate Limit resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteRateLimitResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// CreateVerificationCheckRequest
message CreateVerificationCheckRequestBody {
  // The amount of the associated PSD2 compliant transaction. Requires the PSD2 Service flag enabled.
  string amount = 1;
  // The 4-10 character string being verified.
  string code = 2;
  // The payee of the associated PSD2 compliant transaction. Requires the PSD2 Service flag enabled.
  string payee = 3;
  // A sna client token received in sna url invocation response needs to be passed in Verification Check request and should match to get successful response.
  string sna_client_token = 4;
  // The phone number or [email](https://www.twilio.com/docs/verify/email) to verify. Either this parameter or the `verification_sid` must be specified. Phone numbers must be in [E.164 format](https://www.twilio.com/docs/glossary/what-e164).
  string to_field = 5;
  // A SID that uniquely identifies the Verification Check. Either this parameter or the `to` phone number/[email](https://www.twilio.com/docs/verify/email) must be specified.
  string verification_sid = 6;
}

// challenge a specific Verification Check.
message CreateVerificationCheckRequest {
  // The SID of the verification [Service](https://www.twilio.com/docs/verify/api/service) to create the resource under. Required.
  string service_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateVerificationCheckRequestBody body = 2;
}

// Canonical success response.
message CreateVerificationCheckResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceVerificationCheck body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateVerificationRequest
message CreateVerificationRequestBody {
  // The amount of the associated PSD2 compliant transaction. Requires the PSD2 Service flag enabled.
  string amount = 1;
  // Your [App Hash](https://developers.google.com/identity/sms-retriever/verify#computing_your_apps_hash_string) to be appended at the end of your verification SMS body. Applies only to SMS. Example SMS body: `<#> Your AppName verification code is: 1234 He42w354ol9`.
  string app_hash = 2;
  // The verification method to use. One of: [`email`](https://www.twilio.com/docs/verify/email), `sms`, `whatsapp`, `call`, `sna` or `auto`.
  string channel = 3;
  // [`email`](https://www.twilio.com/docs/verify/email) channel configuration in json format. The fields 'from' and 'from_name' are optional but if included the 'from' field must have a valid email address.
  string channel_configuration = 4;
  // A pre-generated code to use for verification. The code can be between 4 and 10 characters, inclusive.
  string custom_code = 5;
  // A custom user defined friendly name that overwrites the existing one in the verification message
  string custom_friendly_name = 6;
  // The text of a custom message to use for the verification [DEPRECATED].
  string custom_message = 7;
  // Strongly encouraged if using the auto channel. The IP address of the client's device. If provided, it has to be a valid IPv4 or IPv6 address.
  string device_ip = 8;
  // An optional Boolean value to indicate the requirement of sna client token in the SNA URL invocation response for added security. This token must match in the Verification Check request to confirm phone number verification.
  bool enable_sna_client_token = 9;
  // Locale will automatically resolve based on phone number country code for SMS, WhatsApp, and call channel verifications. It will fallback to English or the template’s default translation if the selected translation is not available. This parameter will override the automatic locale resolution. [See supported languages and more information here](https://www.twilio.com/docs/verify/supported-languages).
  string locale = 10;
  // The payee of the associated PSD2 compliant transaction. Requires the PSD2 Service flag enabled.
  string payee = 11;
  // The custom key-value pairs of Programmable Rate Limits. Keys correspond to `unique_name` fields defined when [creating your Rate Limit](https://www.twilio.com/docs/verify/api/service-rate-limits). Associated value pairs represent values in the request that you are rate limiting on. You may include multiple Rate Limit values in each request.
  string rate_limits = 12;
  // OpenAPI property "RiskCheck".
  VerificationEnumRiskCheck risk_check = 13;
  // The digits to send after a phone call is answered, for example, to dial an extension. For more information, see the Programmable Voice documentation of [sendDigits](https://www.twilio.com/docs/voice/twiml/number#attributes-sendDigits).
  string send_digits = 14;
  // A string containing a JSON map of key value pairs of tags to be recorded as metadata for the message. The object may contain up to 10 tags. Keys and values can each be up to 128 characters in length.
  string tags = 15;
  // A stringified JSON object in which the keys are the template's special variables and the values are the variables substitutions.
  string template_custom_substitutions = 16;
  // The message [template](https://www.twilio.com/docs/verify/api/templates). If provided, will override the default template for the Service. SMS and Voice channels only.
  string template_sid = 17;
  // The phone number or [email](https://www.twilio.com/docs/verify/email) to verify. Phone numbers must be in [E.164 format](https://www.twilio.com/docs/glossary/what-e164).
  string to_field = 18;
}

// Create a new Verification using a Service
message CreateVerificationRequest {
  // The SID of the verification [Service](https://www.twilio.com/docs/verify/api/service) to create the resource under. Required.
  string service_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateVerificationRequestBody body = 2;
}

// Canonical success response.
message CreateVerificationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceVerification body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "Retry-After".
  string header_retry_after = 8;
  // Response header "Twilio-Reached-Rate-Limit".
  string header_twilio_reached_rate_limit = 9;
}

// Fetch a specific Verification
message FetchVerificationRequest {
  // The SID of the verification [Service](https://www.twilio.com/docs/verify/api/service) to fetch the resource from. Required.
  string service_sid = 1;
  // The Twilio-provided string that uniquely identifies the Verification resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchVerificationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceVerification body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateVerificationRequest
message UpdateVerificationRequestBody {
  // OpenAPI property "Status".
  VerificationEnumStatus status = 1;
}

// Update a Verification status
message UpdateVerificationRequest {
  // The SID of the verification [Service](https://www.twilio.com/docs/verify/api/service) to update the resource from. Required.
  string service_sid = 1;
  // The Twilio-provided string that uniquely identifies the Verification resource to update. Required.
  string sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateVerificationRequestBody body = 3;
}

// Canonical success response.
message UpdateVerificationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceVerification body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListWebhookResponse
message ListWebhookResponseBody {
  ListWebhookResponseBodyMeta meta = 1;
  repeated VerifyV2ServiceWebhook webhooks = 2;
}

message ListWebhookResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Webhooks for a Service.
message ListWebhookRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListWebhookResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListWebhookResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateWebhookRequest
message CreateWebhookRequestBody {
  // The array of events that this Webhook is subscribed to. Possible event types: `*, factor.deleted, factor.created, factor.verified, challenge.approved, challenge.denied`
  repeated string event_types = 1;
  // The string that you assigned to describe the webhook. **This value should not contain PII.**
  string friendly_name = 2;
  // OpenAPI property "Status".
  WebhookEnumStatus status = 3;
  // OpenAPI property "Version".
  WebhookEnumVersion version = 4;
  // The URL associated with this Webhook.
  string webhook_url = 5;
}

// Create a new Webhook for the Service
message CreateWebhookRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateWebhookRequestBody body = 2;
}

// Canonical success response.
message CreateWebhookResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceWebhook body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch a specific Webhook.
message FetchWebhookRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // The Twilio-provided string that uniquely identifies the Webhook resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchWebhookResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceWebhook body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateWebhookRequest
message UpdateWebhookRequestBody {
  // The array of events that this Webhook is subscribed to. Possible event types: `*, factor.deleted, factor.created, factor.verified, challenge.approved, challenge.denied`
  repeated string event_types = 1;
  // The string that you assigned to describe the webhook. **This value should not contain PII.**
  string friendly_name = 2;
  // OpenAPI property "Status".
  WebhookEnumStatus status = 3;
  // OpenAPI property "Version".
  WebhookEnumVersion version = 4;
  // The URL associated with this Webhook.
  string webhook_url = 5;
}

message UpdateWebhookRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // The Twilio-provided string that uniquely identifies the Webhook resource to update. Required.
  string sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateWebhookRequestBody body = 3;
}

// Canonical success response.
message UpdateWebhookResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2ServiceWebhook body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete a specific Webhook.
message DeleteWebhookRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // The Twilio-provided string that uniquely identifies the Webhook resource to delete. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteWebhookResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// Fetch specific Verification Service Instance.
message FetchServiceRequest {
  // The Twilio-provided string that uniquely identifies the Verification Service resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2Service body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateServiceRequest
message UpdateServiceRequestBody {
  // The length of the verification code to generate. Must be an integer value between 4 and 10, inclusive.
  int64 code_length = 1;
  // Whether to allow sending verifications with a custom code instead of a randomly generated one.
  bool custom_code_enabled = 2;
  // The default message [template](https://www.twilio.com/docs/verify/api/templates). Will be used for all SMS verifications unless explicitly overriden. SMS channel only.
  string default_template_sid = 3;
  // Whether to add a privacy warning at the end of an SMS. **Disabled by default and applies only for SMS.**
  bool do_not_share_warning_enabled = 4;
  // Whether to ask the user to press a number before delivering the verify code in a phone call.
  bool dtmf_input_required = 5;
  // A descriptive string that you create to describe the verification service. It can be up to 32 characters long. **This value should not contain PII.**
  string friendly_name = 6;
  // Whether to perform a lookup with each verification started and return info about the phone number.
  bool lookup_enabled = 7;
  // The Authenticator Attachment for Passkeys. This is the type of authenticator that will be used to create Passkeys. It can be empty or it can have the values `platform`, `cross-platform` or `any`.
  string passkeys_authenticator_attachment = 8;
  // Indicates whether credentials must be discoverable by the authenticator. It can be empty or it can have the values `required`, `preferred` or `discouraged`.
  string passkeys_discoverable_credentials = 9;
  // The Relying Party ID for Passkeys. This is the domain of your application, e.g. `example.com`. It is used to identify your application when creating Passkeys.
  string passkeys_relying_party_id = 10;
  // The Relying Party Name for Passkeys. This is the name of your application, e.g. `Example App`. It is used to identify your application when creating Passkeys.
  string passkeys_relying_party_name = 11;
  // The Relying Party Origins for Passkeys. This is the origin of your application, e.g. `login.example.com,www.example.com`. It is used to identify your application when creating Passkeys, it can have multiple origins split by `,`.
  string passkeys_relying_party_origins = 12;
  // The User Verification for Passkeys. This is the type of user verification that will be used to create Passkeys. It can be empty or it can have the values `required`, `preferred` or `discouraged`.
  string passkeys_user_verification = 13;
  // Whether to pass PSD2 transaction parameters when starting a verification.
  bool psd2_enabled = 14;
  // Optional configuration for the Push factors. Set the APN Credential for this service. This will allow to send push notifications to iOS devices. See [Credential Resource](https://www.twilio.com/docs/notify/api/credential-resource)
  string push_apn_credential_sid = 15;
  // Optional configuration for the Push factors. Set the FCM Credential for this service. This will allow to send push notifications to Android devices. See [Credential Resource](https://www.twilio.com/docs/notify/api/credential-resource)
  string push_fcm_credential_sid = 16;
  // Optional configuration for the Push factors. If true, include the date in the Challenge's response. Otherwise, the date is omitted from the response. See [Challenge](https://www.twilio.com/docs/verify/api/challenge) resource’s details parameter for more info. Default: false. **Deprecated** do not use this parameter.
  bool push_include_date = 17;
  // Whether to skip sending SMS verifications to landlines. Requires `lookup_enabled`.
  bool skip_sms_to_landlines = 18;
  // Optional configuration for the TOTP factors. Number of digits for generated TOTP codes. Must be between 3 and 8, inclusive. Defaults to 6
  int64 totp_code_length = 19;
  // Optional configuration for the TOTP factors. Set TOTP Issuer for this service. This will allow to configure the issuer of the TOTP URI.
  string totp_issuer = 20;
  // Optional configuration for the TOTP factors. The number of time-steps, past and future, that are valid for validation of TOTP codes. Must be between 0 and 2, inclusive. Defaults to 1
  int64 totp_skew = 21;
  // Optional configuration for the TOTP factors. Defines how often, in seconds, are TOTP codes generated. i.e, a new TOTP code is generated every time_step seconds. Must be between 20 and 60 seconds, inclusive. Defaults to 30 seconds
  int64 totp_time_step = 22;
  // The name of an alternative text-to-speech service to use in phone calls. Applies only to TTS languages.
  string tts_name = 23;
  // Whether to allow verifications from the service to reach the stream-events sinks if configured
  bool verify_event_subscription_enabled = 24;
  // The WhatsApp number to use as the sender of the verification messages. This number must be associated with the WhatsApp Message Service.
  string whatsapp_from = 25;
  // The SID of the [Messaging Service](https://www.twilio.com/docs/messaging/services) to associate with the Verification Service.
  string whatsapp_msg_service_sid = 26;
}

// Update a specific Verification Service.
message UpdateServiceRequest {
  // The Twilio-provided string that uniquely identifies the Service resource to update. Required.
  string sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateServiceRequestBody body = 2;
}

// Canonical success response.
message UpdateServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VerifyV2Service body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete a specific Verification Service Instance.
message DeleteServiceRequest {
  // The Twilio-provided string that uniquely identifies the Verification Service resource to delete. Required.
  string sid = 1;
}

// Canonical success response.
message DeleteServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListVerificationTemplateResponse
message ListVerificationTemplateResponseBody {
  ListVerificationTemplateResponseBodyMeta meta = 1;
  repeated VerifyV2VerificationTemplate templates = 2;
}

message ListVerificationTemplateResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// List all the available templates for a given Account.
message ListVerificationTemplateRequest {
  // String filter used to query templates with a given friendly name.
  string friendly_name = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListVerificationTemplateResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListVerificationTemplateResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Generated from VerifyV2VerificationAttempt operations.
service VerifyV2VerificationAttemptService {
  // List all the verification attempts for a given Account.
  rpc ListVerificationAttempt(ListVerificationAttemptRequest) returns (ListVerificationAttemptResponse) {
    option (google.api.http) = {
      get: "/v2/Attempts"
      response_body: "body"
    };
  }
  // Fetch a specific verification attempt.
  rpc FetchVerificationAttempt(FetchVerificationAttemptRequest) returns (FetchVerificationAttemptResponse) {
    option (google.api.http) = {
      get: "/v2/Attempts/{sid}"
      response_body: "body"
    };
  }
}

// Generated from VerifyV2VerificationAttemptsSummary operations.
service VerifyV2VerificationAttemptsSummaryService {
  // Get a summary of how many attempts were made and how many were converted.
  rpc FetchVerificationAttemptsSummary(FetchVerificationAttemptsSummaryRequest) returns (FetchVerificationAttemptsSummaryResponse) {
    option (google.api.http) = {
      get: "/v2/Attempts/Summary"
      response_body: "body"
    };
  }
}

// Generated from VerifyV2Form operations.
service VerifyV2FormService {
  // Fetch the forms for a specific Form Type.
  rpc FetchForm(FetchFormRequest) returns (FetchFormResponse) {
    option (google.api.http) = {
      get: "/v2/Forms/{form_type}"
      response_body: "body"
    };
  }
}

// Generated from VerifyV2Safelist operations.
service VerifyV2SafelistService {
  // Add a new phone number to SafeList.
  rpc CreateSafelist(CreateSafelistRequest) returns (CreateSafelistResponse) {
    option (google.api.http) = {
      post: "/v2/SafeList/Numbers"
      body: "body"
      response_body: "body"
    };
  }
  // Check if a phone number exists in SafeList.
  rpc FetchSafelist(FetchSafelistRequest) returns (FetchSafelistResponse) {
    option (google.api.http) = {
      get: "/v2/SafeList/Numbers/{phone_number}"
      response_body: "body"
    };
  }
  // Remove a phone number from SafeList.
  rpc DeleteSafelist(DeleteSafelistRequest) returns (DeleteSafelistResponse) {
    option (google.api.http) = {
      delete: "/v2/SafeList/Numbers/{phone_number}"
    };
  }
}

// Generated from VerifyV2Service operations.
service VerifyV2ServiceService {
  // Retrieve a list of all Verification Services for an account.
  rpc ListService(ListServiceRequest) returns (ListServiceResponse) {
    option (google.api.http) = {
      get: "/v2/Services"
      response_body: "body"
    };
  }
  // Create a new Verification Service.
  rpc CreateService(CreateServiceRequest) returns (CreateServiceResponse) {
    option (google.api.http) = {
      post: "/v2/Services"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch specific Verification Service Instance.
  rpc FetchService(FetchServiceRequest) returns (FetchServiceResponse) {
    option (google.api.http) = {
      get: "/v2/Services/{sid}"
      response_body: "body"
    };
  }
  // Update a specific Verification Service.
  rpc UpdateService(UpdateServiceRequest) returns (UpdateServiceResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete a specific Verification Service Instance.
  rpc DeleteService(DeleteServiceRequest) returns (DeleteServiceResponse) {
    option (google.api.http) = {
      delete: "/v2/Services/{sid}"
    };
  }
}

// Generated from VerifyV2AccessToken operations.
service VerifyV2AccessTokenService {
  // Create a new enrollment Access Token for the Entity
  rpc CreateAccessToken(CreateAccessTokenRequest) returns (CreateAccessTokenResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/AccessTokens"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch an Access Token for the Entity
  rpc FetchAccessToken(FetchAccessTokenRequest) returns (FetchAccessTokenResponse) {
    option (google.api.http) = {
      get: "/v2/Services/{service_sid}/AccessTokens/{sid}"
      response_body: "body"
    };
  }
}

// Generated from VerifyV2Entity operations.
service VerifyV2EntityService {
  // Retrieve a list of all Entities for a Service.
  rpc ListEntity(ListEntityRequest) returns (ListEntityResponse) {
    option (google.api.http) = {
      get: "/v2/Services/{service_sid}/Entities"
      response_body: "body"
    };
  }
  // Create a new Entity for the Service
  rpc CreateEntity(CreateEntityRequest) returns (CreateEntityResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/Entities"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch a specific Entity.
  rpc FetchEntity(FetchEntityRequest) returns (FetchEntityResponse) {
    option (google.api.http) = {
      get: "/v2/Services/{service_sid}/Entities/{identity}"
      response_body: "body"
    };
  }
  // Delete a specific Entity.
  rpc DeleteEntity(DeleteEntityRequest) returns (DeleteEntityResponse) {
    option (google.api.http) = {
      delete: "/v2/Services/{service_sid}/Entities/{identity}"
    };
  }
}

// Generated from VerifyV2Challenge operations.
service VerifyV2ChallengeService {
  // Retrieve a list of all Challenges for a Factor.
  rpc ListChallenge(ListChallengeRequest) returns (ListChallengeResponse) {
    option (google.api.http) = {
      get: "/v2/Services/{service_sid}/Entities/{identity}/Challenges"
      response_body: "body"
    };
  }
  // Create a new Challenge for the Factor
  rpc CreateChallenge(CreateChallengeRequest) returns (CreateChallengeResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/Entities/{identity}/Challenges"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch a specific Challenge.
  rpc FetchChallenge(FetchChallengeRequest) returns (FetchChallengeResponse) {
    option (google.api.http) = {
      get: "/v2/Services/{service_sid}/Entities/{identity}/Challenges/{sid}"
      response_body: "body"
    };
  }
  // Verify a specific Challenge.
  rpc UpdateChallenge(UpdateChallengeRequest) returns (UpdateChallengeResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/Entities/{identity}/Challenges/{sid}"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from VerifyV2Notification operations.
service VerifyV2NotificationService {
  // Create a new Notification for the corresponding Challenge
  rpc CreateNotification(CreateNotificationRequest) returns (CreateNotificationResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/Entities/{identity}/Challenges/{challenge_sid}/Notifications"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from VerifyV2Factor operations.
service VerifyV2FactorService {
  // Retrieve a list of all Factors for an Entity.
  rpc ListFactor(ListFactorRequest) returns (ListFactorResponse) {
    option (google.api.http) = {
      get: "/v2/Services/{service_sid}/Entities/{identity}/Factors"
      response_body: "body"
    };
  }
  // Fetch a specific Factor.
  rpc FetchFactor(FetchFactorRequest) returns (FetchFactorResponse) {
    option (google.api.http) = {
      get: "/v2/Services/{service_sid}/Entities/{identity}/Factors/{sid}"
      response_body: "body"
    };
  }
  // Update a specific Factor. This endpoint can be used to Verify a Factor if passed an `AuthPayload` param.
  rpc UpdateFactor(UpdateFactorRequest) returns (UpdateFactorResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/Entities/{identity}/Factors/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete a specific Factor.
  rpc DeleteFactor(DeleteFactorRequest) returns (DeleteFactorResponse) {
    option (google.api.http) = {
      delete: "/v2/Services/{service_sid}/Entities/{identity}/Factors/{sid}"
    };
  }
}

// Generated from VerifyV2NewFactor operations.
service VerifyV2NewFactorService {
  // Create a new Factor for the Entity
  rpc CreateNewFactor(CreateNewFactorRequest) returns (CreateNewFactorResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/Entities/{identity}/Factors"
      body: "body"
      response_body: "body"
    };
  }
  // Create a new Passkeys Factor for the Entity
  rpc CreateNewFactorPasskey(CreateNewFactorPasskeyRequest) returns (CreateNewFactorPasskeyResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/Passkeys/Factors"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from VerifyV2MessagingConfiguration operations.
service VerifyV2MessagingConfigurationService {
  // Retrieve a list of all Messaging Configurations for a Service.
  rpc ListMessagingConfiguration(ListMessagingConfigurationRequest) returns (ListMessagingConfigurationResponse) {
    option (google.api.http) = {
      get: "/v2/Services/{service_sid}/MessagingConfigurations"
      response_body: "body"
    };
  }
  // Create a new MessagingConfiguration for a service.
  rpc CreateMessagingConfiguration(CreateMessagingConfigurationRequest) returns (CreateMessagingConfigurationResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/MessagingConfigurations"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch a specific MessagingConfiguration.
  rpc FetchMessagingConfiguration(FetchMessagingConfigurationRequest) returns (FetchMessagingConfigurationResponse) {
    option (google.api.http) = {
      get: "/v2/Services/{service_sid}/MessagingConfigurations/{country}"
      response_body: "body"
    };
  }
  // Update a specific MessagingConfiguration
  rpc UpdateMessagingConfiguration(UpdateMessagingConfigurationRequest) returns (UpdateMessagingConfigurationResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/MessagingConfigurations/{country}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete a specific MessagingConfiguration.
  rpc DeleteMessagingConfiguration(DeleteMessagingConfigurationRequest) returns (DeleteMessagingConfigurationResponse) {
    option (google.api.http) = {
      delete: "/v2/Services/{service_sid}/MessagingConfigurations/{country}"
    };
  }
}

// Generated from twilio.verify.v2 operations.
service V2Service {
  // Approve a Passkeys Challenge
  rpc UpdateChallengePasskeys(UpdateChallengePasskeysRequest) returns (UpdateChallengePasskeysResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/Passkeys/ApproveChallenge"
      body: "body"
      response_body: "body"
    };
  }
  // Verify a Passkeys Factor
  rpc UpdatePasskeysFactor(UpdatePasskeysFactorRequest) returns (UpdatePasskeysFactorResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/Passkeys/VerifyFactor"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from VerifyV2NewChallenge operations.
service VerifyV2NewChallengeService {
  // Create a Passkeys Challenge
  rpc CreateChallengePasskeys(CreateChallengePasskeysRequest) returns (CreateChallengePasskeysResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/Passkeys/Challenges"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from VerifyV2RateLimit operations.
service VerifyV2RateLimitService {
  // Retrieve a list of all Rate Limits for a service.
  rpc ListRateLimit(ListRateLimitRequest) returns (ListRateLimitResponse) {
    option (google.api.http) = {
      get: "/v2/Services/{service_sid}/RateLimits"
      response_body: "body"
    };
  }
  // Create a new Rate Limit for a Service
  rpc CreateRateLimit(CreateRateLimitRequest) returns (CreateRateLimitResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/RateLimits"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch a specific Rate Limit.
  rpc FetchRateLimit(FetchRateLimitRequest) returns (FetchRateLimitResponse) {
    option (google.api.http) = {
      get: "/v2/Services/{service_sid}/RateLimits/{sid}"
      response_body: "body"
    };
  }
  // Update a specific Rate Limit.
  rpc UpdateRateLimit(UpdateRateLimitRequest) returns (UpdateRateLimitResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/RateLimits/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete a specific Rate Limit.
  rpc DeleteRateLimit(DeleteRateLimitRequest) returns (DeleteRateLimitResponse) {
    option (google.api.http) = {
      delete: "/v2/Services/{service_sid}/RateLimits/{sid}"
    };
  }
}

// Generated from VerifyV2Bucket operations.
service VerifyV2BucketService {
  // Retrieve a list of all Buckets for a Rate Limit.
  rpc ListBucket(ListBucketRequest) returns (ListBucketResponse) {
    option (google.api.http) = {
      get: "/v2/Services/{service_sid}/RateLimits/{rate_limit_sid}/Buckets"
      response_body: "body"
    };
  }
  // Create a new Bucket for a Rate Limit
  rpc CreateBucket(CreateBucketRequest) returns (CreateBucketResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/RateLimits/{rate_limit_sid}/Buckets"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch a specific Bucket.
  rpc FetchBucket(FetchBucketRequest) returns (FetchBucketResponse) {
    option (google.api.http) = {
      get: "/v2/Services/{service_sid}/RateLimits/{rate_limit_sid}/Buckets/{sid}"
      response_body: "body"
    };
  }
  // Update a specific Bucket.
  rpc UpdateBucket(UpdateBucketRequest) returns (UpdateBucketResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/RateLimits/{rate_limit_sid}/Buckets/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete a specific Bucket.
  rpc DeleteBucket(DeleteBucketRequest) returns (DeleteBucketResponse) {
    option (google.api.http) = {
      delete: "/v2/Services/{service_sid}/RateLimits/{rate_limit_sid}/Buckets/{sid}"
    };
  }
}

// Generated from VerifyV2VerificationCheck operations.
service VerifyV2VerificationCheckService {
  // challenge a specific Verification Check.
  rpc CreateVerificationCheck(CreateVerificationCheckRequest) returns (CreateVerificationCheckResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/VerificationCheck"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from VerifyV2Verification operations.
service VerifyV2VerificationService {
  // Create a new Verification using a Service
  rpc CreateVerification(CreateVerificationRequest) returns (CreateVerificationResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/Verifications"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch a specific Verification
  rpc FetchVerification(FetchVerificationRequest) returns (FetchVerificationResponse) {
    option (google.api.http) = {
      get: "/v2/Services/{service_sid}/Verifications/{sid}"
      response_body: "body"
    };
  }
  // Update a Verification status
  rpc UpdateVerification(UpdateVerificationRequest) returns (UpdateVerificationResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/Verifications/{sid}"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from VerifyV2Webhook operations.
service VerifyV2WebhookService {
  // Retrieve a list of all Webhooks for a Service.
  rpc ListWebhook(ListWebhookRequest) returns (ListWebhookResponse) {
    option (google.api.http) = {
      get: "/v2/Services/{service_sid}/Webhooks"
      response_body: "body"
    };
  }
  // Create a new Webhook for the Service
  rpc CreateWebhook(CreateWebhookRequest) returns (CreateWebhookResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/Webhooks"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch a specific Webhook.
  rpc FetchWebhook(FetchWebhookRequest) returns (FetchWebhookResponse) {
    option (google.api.http) = {
      get: "/v2/Services/{service_sid}/Webhooks/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateWebhook(UpdateWebhookRequest) returns (UpdateWebhookResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/Webhooks/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete a specific Webhook.
  rpc DeleteWebhook(DeleteWebhookRequest) returns (DeleteWebhookResponse) {
    option (google.api.http) = {
      delete: "/v2/Services/{service_sid}/Webhooks/{sid}"
    };
  }
}

// Generated from VerifyV2Template operations.
service VerifyV2TemplateService {
  // List all the available templates for a given Account.
  rpc ListVerificationTemplate(ListVerificationTemplateRequest) returns (ListVerificationTemplateResponse) {
    option (google.api.http) = {
      get: "/v2/Templates"
      response_body: "body"
    };
  }
}

