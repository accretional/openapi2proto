// Code generated by openapi2proto. DO NOT EDIT.
// Source: twilio_iam_v1.json

syntax = "proto3";

package twilio.iam.v1;

option go_package = "twilio/iam/v1;twilioiamv1";

import "google/api/annotations.proto";

message AccountsEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

message AccountsEnumType {
  // Scalar value wrapper.
  string value = 1;
}

message IamV1AccountsChangeAccountOwnerRequest {
  // New owner's email address
  string email_address = 1;
}

message IamV1AccountsChangeAccountOwnerResponse {
  // The SID of the workflow that will handle the account ownership change
  string execution_sid = 1;
}

message IamV1AccountsCreationStatusResponse {
  // The ID of the provisioned account (only available when status is SUCCEEDED)
  string account_id = 1;
  IamV1AccountsCreationStatusResponseError error = 2;
  // ID of the operation
  string operation_id = 3;
  // The owner of the account to be provisioned.
  string owner_id = 4;
  // The current status of the account provisioning operation
  string status = 5;
}

message IamV1AccountsCreationStatusResponseError {
  // A human-readable error message describing the error, if applicable.
  string message_field = 1;
  // The reason for the failure of the operation, if applicable.
  string reason = 2;
}

// Account user invitation
message IamV1AccountsUserInvitation {
  // Unique Twilio sid of the account that the user was invited to
  string account_sid = 1;
  // The date and time when the invitation was created
  string date_created = 2;
  // The date and time when the invitation was last sent
  string date_last_sent = 3;
  // The recipient's email address
  string email = 4;
  // Friendly name of the account
  string friendly_name = 5;
  // The sender's email address
  string invited_by = 6;
  // The roles assigned to the user in the account
  repeated IamV1AccountsUserRole roles = 7;
  // Unique Twilio notification sid of the invitation
  string sid = 8;
  // The status of the invitation
  string status = 9;
  // Indicates whether the invitation token has expired
  bool token_expired = 10;
  // Type of the account
  string type = 11;
}

// Account user role
message IamV1AccountsUserRole {
  // The name of the role
  string name = 1;
  // Unique Twilio sid of the account role
  string sid = 2;
}

// The method used to challenge the user (e.g. “SMS”, “TOTP”).
message IamV1ChallengeMethod {
  // Scalar value wrapper.
  string value = 1;
}

message IamV1ChangeAccountOwnerRequest {
  // The SID of the new owner to transfer the account to
  string new_owner_sid = 1;
}

message IamV1ChangeAccountOwnerResponse {
  // The SID of the workflow that will handle the account ownership change
  string execution_sid = 1;
}

message IamV1CreateAccountRequest {
  string friendly_name = 1;
  // The owner of the account to be provisioned.
  string owner_id = 2;
  map<string, string> tags = 3;
}

message IamV1CreateDomainResponse {
  // Unique Twilio domain sid
  string sid = 1;
  // Verification token for the domain
  string token = 2;
}

message IamV1CreateSubaccountRequest {
  string friendly_name = 1;
  // The parent account ID of the subaccount to be provisioned.
  string parent_account_id = 2;
  map<string, string> tags = 3;
}

message IamV1Domain {
  // Creation date of the domain
  string date_created = 1;
  // Date where the domain was last verified
  string date_last_verification = 2;
  // Days left to re-verify until this domain gets back into 'unverified' state. Only applicable to domains in 'verified-uncertain' state
  int64 days_left_to_reverify = 3;
  // Name of the domain
  string name = 4;
  // Unique Twilio domain sid
  string sid = 5;
  // Status of the domain
  string status = 6;
  // Verification token for the domain
  string token = 7;
  // Type of verification used for the domain
  string verification_type = 8;
  // Whether the domain is verified by another resource. Only applicable to domains in 'verified' state
  bool verified_by_another_resource = 9;
}

message IamV1EmailChangeInitiateRequest {
  // The new email address
  string new_email = 1;
}

message IamV1EmailChangeInitiateResponse {
  // Unique Twilio notification sid
  string notification_sid = 1;
}

message IamV1EmailChangeResendRequest {
  // Unique Twilio notification sid
  string notification_sid = 1;
}

message IamV1EmailChangeVerifyRequest {
  // The verification code sent to the new email address
  string code = 1;
  // Unique Twilio notification sid
  string notification_sid = 2;
}

message IamV1ExternalUser {
  // User email address
  string email = 1;
  // User first name
  string first_name = 2;
  // User last name
  string last_name = 3;
  // Unique Twilio organization sid
  string organization_sid = 4;
  string status = 5;
  // Unique Twilio user sid
  string user_sid = 6;
}

message IamV1ImportUsersRun {
  // Finish date and time of the import run
  string finish_date = 1;
  // Unique identifier for the import run
  string id = 2;
  // Number of users successfully imported
  int64 num_users_imported = 3;
  // Number of users that were not imported
  int64 num_users_not_imported = 4;
  // Unique Twilio organization sid
  string organization_sid = 5;
  // Start date and time of the import run
  string start_date = 6;
  // Status of the import run
  string status = 7;
}

message IamV1ImportableUsersCount {
  // The name of the organization domain
  string domain_name = 1;
  // The number of users that can be imported into the organization
  int32 user_count = 2;
}

message IamV1InvitedAccount {
  // Unique Twilio account sid
  string account_sid = 1;
  // Whether the invitation can be resent
  bool can_resend = 2;
  // Reason why the invitation cannot be resent, if applicable
  string can_resend_reason = 3;
  // The date and time of the invitation creation
  string date_created = 4;
  // The date and time of the last invitation sent
  string date_last_sent = 5;
  // Friendly name of the account
  string friendly_name = 6;
  // Unique Twilio notification sid
  string notification_sid = 7;
  // Email of the sender who invited the account
  string sender_email = 8;
  // First name of the sender who invited the account
  string sender_first_name = 9;
  // Last name of the sender who invited the account
  string sender_last_name = 10;
  // Whether the invitation token has expired
  bool token_expired = 11;
  // Total number of invitations sent
  int32 total_sends = 12;
}

message IamV1InvitedUser {
  bool can_resend = 1;
  string can_resend_reason = 2;
  string date_created = 3;
  string date_last_sent = 4;
  string email = 5;
  string notification_sid = 6;
  repeated IamV1InvitedUserRoleAssignmentsItem role_assignments = 7;
  string sender_email = 8;
  string sender_first_name = 9;
  string sender_last_name = 10;
  bool token_expired = 11;
  int64 total_sends = 12;
}

message IamV1InvitedUserRoleAssignmentsItem {
  // Name of this role
  string role_name = 1;
  // List of role SIDs assigned for the scope
  string role_sid = 2;
  // The SID of the entity that the role is scoped to (AC…, OR…, etc)
  string scope = 3;
  // Scope Name e.g. Account or Organization friendly name
  string scope_name = 4;
  // Type of the scope entity
  string scope_type = 5;
}

message IamV1Limit {
  // Limit status
  string status = 1;
}

message IamV1ManagedAccountOrganizationResponse {
  // Unique Twilio organization sid
  string organization_sid = 1;
}

message IamV1ManagedUser {
  // User email address
  string email = 1;
  // User first name
  string first_name = 2;
  // User last name
  string last_name = 3;
  // Unique Twilio organization sid
  string organization_sid = 4;
  // Whether the user has SSO enabled
  bool sso = 5;
  string status = 6;
  // Unique Twilio user sid
  string user_sid = 7;
}

message IamV1ManagedUserWithRoleAssignments {
  // User email address
  string email = 1;
  // User name
  string name = 2;
  // Unique Twilio organization sid
  string organization_sid = 3;
  // List of role assignments for the user
  repeated IamV1ManagedUserWithRoleAssignmentsRoleAssignmentsItem role_assignments = 4;
  // Whether the user has SSO enabled
  bool sso_enabled = 5;
  string status = 6;
  // Unique Twilio user sid
  string user_sid = 7;
}

message IamV1ManagedUserWithRoleAssignmentsRoleAssignmentsItem {
  // Date and time when the role assignment was created
  string date_created = 1;
  // Name of this role
  string role_name = 2;
  // Unique identifier for the role
  string role_sid = 3;
  // SID of the entity that the role is scoped to (AC…, OR…, etc)
  string scope = 4;
  // Scope Name e.g. Account or Organization friendly name
  string scope_name = 5;
  // Type of the scope entity
  string scope_type = 6;
  // Unique identifier for the role assignment
  string sid = 7;
}

message IamV1MfaInfo {
  // Whether the user is allowed to enroll in TOTP.
  bool can_enroll_in_totp = 1;
  // OpenAPI property "challengeMethod".
  IamV1ChallengeMethod challenge_method = 2;
  // Whether the user is enrolled in any MFA.
  bool enrolled_in_mfa = 3;
  // Whether MFA is disabled for this user.
  bool is_mfa_disabled = 4;
  // Whether a recovery code has been generated for the user.
  bool is_recovery_code_generated = 5;
  // Whether the user authenticated via SSO.
  bool is_sso = 6;
  // The user’s phone number.
  string phone_number = 7;
  // Whether the phone number has been verified.
  bool phone_verified = 8;
  // Whether TOTP setup has been verified.
  bool totp_verified = 9;
}

message IamV1MfaRegenerateRecoveryTokenResponse {
  string recovery_token = 1;
}

message IamV1MfaTotpQrCodeUrlResponse {
  // The QrCodeUrl of the TOTP factor
  string otp_auth_qr_code_url = 1;
}

message IamV1MfaTotpToken {
  // A TOTP token generated by an authenticator app.
  string token = 1;
}

message IamV1OneConsoleProvisioningStatus {
  string status = 1;
}

message IamV1Operation {
  // The date that the Operation was completed, if applicable.
  string date_completed = 1;
  // The date that this Operation was created, given in RFC 2822 format.
  string date_created = 2;
  IamV1OperationError error = 3;
  // ID of the operation
  string id = 4;
  // A polymorphic input object. Fields vary depending on the operation.
  map<string, string> input = 5;
  IamV1OperationMeta meta = 6;
  // ID of the operation's parent operation, if applicable
  string parent_id = 7;
  // A polymorphic result object. Fields vary depending on the operation.
  // Common fields include:
  // - `resourceId`: string
  map<string, string> result = 8;
  string state = 9;
  // The type of the operation.
  string type = 10;
}

message IamV1OperationError {
  // A human-readable error message describing the error, if applicable.
  string message_field = 1;
  // The reason for the failure of the operation, if applicable.
  string reason = 2;
}

message IamV1OperationMeta {
  // Links to resources related to the operation.
  // Common links include:
  // - `self`: link to this operation
  // - `childOperations`:  link to child operations of this operation
  map<string, string> links = 1;
}

message IamV1Organization {
  // Creation date of the organization
  string date_created = 1;
  // User sign-up type for Organization domains
  string domain_signup_type = 2;
  // Organization friendly name
  string friendly_name = 3;
  // Unique Twilio organization sid
  string organization_sid = 4;
}

message IamV1OrganizationFeatures {
  // List of features enabled for the organization
  repeated string enabled_features = 1;
}

message IamV1OrganizationUpdateRequest {
  // User sign-up type for Organization domains
  string domain_signup_type = 1;
  // Organization friendly name
  string friendly_name = 2;
}

message IamV1SocialConnection {
  // The creation date and time of the social connection
  string date_created = 1;
  // Social Connection Name (e.g. google, github etc)
  string name = 2;
}

message IamV1User {
  // User email address
  string email = 1;
  // Whether the user email is verified or not
  bool email_verified = 2;
  // User first name
  string first_name = 3;
  // User last name
  string last_name = 4;
  // Organization friendly name
  string organization_friendly_name = 5;
  // Unique Twilio organization sid
  string organization_sid = 6;
  // List of social connections associated with the user
  repeated IamV1SocialConnection social_connections = 7;
  // Whether the user has SSO enabled
  bool sso = 8;
  // User time zone
  string time_zone = 9;
  // Unique Twilio user sid
  string user_sid = 10;
}

message IamV1UserPatchRequest {
  // User first name
  string first_name = 1;
  // User last name
  string last_name = 2;
  // The user's timezone in IANA Time Zone Database format
  string time_zone = 3;
}

message IamV1UserPatchResponse {
  // User first name
  string first_name = 1;
  // User last name
  string last_name = 2;
  // The user's timezone in IANA Time Zone Database format
  string time_zone = 3;
  // Unique Twilio user sid
  string user_sid = 4;
}

message IamV1AccountVendorOauthAppCreateRequest {
  int64 access_token_ttl = 1;
  string client_sid = 2;
  string description = 3;
  string friendly_name = 4;
  string owner_sid = 5;
  IamV1AccountVendorOauthAppCreateRequestPolicy policy = 6;
  string type = 7;
}

message IamV1AccountVendorOauthAppCreateRequestPolicy {
  // Set of permissions explicitly allowed
  repeated string allow = 1;
  // Set of permissions explicitly denied
  repeated string deny = 2;
}

message IamV1AccountVendorOauthAppCreateUpdateResponse {
  int64 access_token_ttl = 1;
  string created_by = 2;
  string date_created = 3;
  string description = 4;
  string friendly_name = 5;
  IamV1AccountVendorOauthAppCreateUpdateResponsePolicy policy = 6;
  string secret = 7;
  string sid = 8;
  string status = 9;
  string type = 10;
}

message IamV1AccountVendorOauthAppCreateUpdateResponsePolicy {
  // Set of permissions explicitly allowed
  repeated string allow = 1;
  // Set of permissions explicitly denied
  repeated string deny = 2;
}

message IamV1AccountVendorOauthAppUpdateRequest {
  int64 access_token_ttl = 1;
  string description = 2;
  string friendly_name = 3;
  IamV1AccountVendorOauthAppUpdateRequestPolicy policy = 4;
  string type = 5;
}

message IamV1AccountVendorOauthAppUpdateRequestPolicy {
  // Set of permissions explicitly allowed
  repeated string allow = 1;
  // Set of permissions explicitly denied
  repeated string deny = 2;
}

message IamV1AccountVendoroauthapp {
  int64 access_token_ttl = 1;
  string created_by = 2;
  string date_created = 3;
  string description = 4;
  string friendly_name = 5;
  IamV1AccountVendoroauthappPolicy policy = 6;
  string sid = 7;
  string status = 8;
  string type = 9;
}

message IamV1AccountVendoroauthappPolicy {
  // Set of permissions explicitly allowed
  repeated string allow = 1;
  // Set of permissions explicitly denied
  repeated string deny = 2;
}

message IamV1AccountSearchAccount {
  string account_path = 1;
  string account_sid = 2;
  bool dormant = 3;
  string friendly_name = 4;
  string member_platform = 5;
  int64 status = 6;
  bool subaccount = 7;
  bool trial = 8;
}

message IamV1AccountSearchListMeta {
  string next_token = 1;
  int64 page_size = 2;
  string previous_token = 3;
  int64 total_pages = 4;
  int64 total_results = 5;
}

message IamV1AccountSearchRequest {
  bool dormant = 1;
  string parent_account_id = 2;
  string search_string = 3;
  repeated int64 status = 4;
  bool subaccount = 5;
  bool trial = 6;
}

message IamV1AccountSearchRoleResponse {
  // Array values.
  repeated IamV1AccountSearchRoleResponseItem items = 1;
}

message IamV1AccountSearchRoleResponseItem {
  string description = 1;
  string friendly_name = 2;
  string role_sid = 3;
}

message IamV1AccountStatsResponse {
  int64 number_of_accounts = 1;
}

message IamV1CreateConsent {
  string actor_sid = 1;
  repeated string allow = 2;
  string client_sid = 3;
  string csrf_token = 4;
  string csrf_token_session_state = 5;
  string issuer = 6;
  string owner_sid = 7;
  string redirect_uri = 8;
  string response = 9;
  string vendor_client_id = 10;
}

message IamV1GetKeys {
  // The date and time in GMT that the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 1;
  // The date and time in GMT that the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 2;
  repeated string flags = 3;
  // The string that you assigned to describe the resource.
  string friendly_name = 4;
  // The unique string that we created to identify the Key resource.
  string sid = 5;
}

message IamV1Key {
  // The date and time in GMT that the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 1;
  // The date and time in GMT that the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 2;
  // The string that you assigned to describe the resource.
  string friendly_name = 3;
  // The \`Policy\` object is a collection that specifies the allowed Twilio permissions for the restricted key.
  // For more information on the permissions available with restricted API keys, refer to the [Twilio documentation](https://www.twilio.com/docs/iam/api-keys/restricted-api-keys#permissions-available-with-restricted-api-keys).
  IamV1KeyPolicy policy = 4;
  // The unique string that we created to identify the Key resource.
  string sid = 5;
}

// The \`Policy\` object is a collection that specifies the allowed Twilio permissions for the restricted key.
// For more information on the permissions available with restricted API keys, refer to the [Twilio documentation](https://www.twilio.com/docs/iam/api-keys/restricted-api-keys#permissions-available-with-restricted-api-keys).
message IamV1KeyPolicy {
  // Scalar value wrapper.
  string value = 1;
}

message IamV1NewKey {
  // The date and time in GMT that the API Key was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 1;
  // The date and time in GMT that the new API Key was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 2;
  // The string that you assigned to describe the resource.
  string friendly_name = 3;
  // Collection of allow assertions.
  IamV1NewKeyPolicy policy = 4;
  // The secret your application uses to sign Access Tokens and to authenticate to the REST API (you will use this as the basic-auth `password`).  **Note that for security reasons, this field is ONLY returned when the API Key is first created.**
  string secret = 5;
  // The unique string that that we created to identify the NewKey resource. You will use this as the basic-auth `user` when authenticating to the API.
  string sid = 6;
}

// Collection of allow assertions.
message IamV1NewKeyPolicy {
  // Scalar value wrapper.
  string value = 1;
}

message IamV1OrganizationGeneratePresignedImageUploadUrlResponse {
  // Timestamp when the presigned URL expires
  string expires_at = 1;
  // S3 key where the image will be stored.
  string image_key = 2;
  // Presigned S3 URL for uploading the image (PUT request). Valid for 5 minutes.
  string presigned_url = 3;
}

message IamV1OrganizationVendorOauthAppCreateRequest {
  int64 access_token_ttl = 1;
  string client_sid = 2;
  string company_name = 3;
  string description = 4;
  string friendly_name = 5;
  string homepage_url = 6;
  string image_key = 7;
  string image_url = 8;
  string owner_sid = 9;
  IamV1OrganizationVendorOauthAppCreateRequestPolicy policy = 10;
  repeated string redirect_urls = 11;
  string tos_url = 12;
  string type = 13;
}

message IamV1OrganizationVendorOauthAppCreateRequestPolicy {
  // Set of permissions explicitly allowed
  repeated string allow = 1;
  // Set of permissions explicitly denied
  repeated string deny = 2;
}

message IamV1OrganizationVendorOauthAppCreateUpdateResponse {
  int64 access_token_ttl = 1;
  string authorization_url = 2;
  string company_name = 3;
  string created_by = 4;
  string date_created = 5;
  string description = 6;
  string friendly_name = 7;
  string homepage_url = 8;
  string image_url = 9;
  IamV1OrganizationVendorOauthAppCreateUpdateResponsePolicy policy = 10;
  repeated string redirect_urls = 11;
  string secret = 12;
  string sid = 13;
  string status = 14;
  string tos_url = 15;
  string type = 16;
}

message IamV1OrganizationVendorOauthAppCreateUpdateResponsePolicy {
  // Set of permissions explicitly allowed
  repeated string allow = 1;
  // Set of permissions explicitly denied
  repeated string deny = 2;
}

message IamV1OrganizationVendorOauthAppUpdateRequest {
  int64 access_token_ttl = 1;
  string company_name = 2;
  string description = 3;
  string friendly_name = 4;
  string homepage_url = 5;
  string image_key = 6;
  string image_url = 7;
  IamV1OrganizationVendorOauthAppUpdateRequestPolicy policy = 8;
  repeated string redirect_urls = 9;
  string tos_url = 10;
}

message IamV1OrganizationVendorOauthAppUpdateRequestPolicy {
  // Set of permissions explicitly allowed
  repeated string allow = 1;
  // Set of permissions explicitly denied
  repeated string deny = 2;
}

message IamV1OrganizationVendoroauthapp {
  int64 access_token_ttl = 1;
  string authorization_url = 2;
  string company_name = 3;
  string created_by = 4;
  string date_created = 5;
  string description = 6;
  string friendly_name = 7;
  string homepage_url = 8;
  string image_url = 9;
  IamV1OrganizationVendoroauthappPolicy policy = 10;
  // Presigned URL for temporary direct access to the image. Valid for 5 minutes.
  string presigned_image_url = 11;
  // Timestamp when the presigned image URL expires
  string presigned_image_url_expires_at = 12;
  repeated string redirect_urls = 13;
  string sid = 14;
  string status = 15;
  string tos_url = 16;
  string type = 17;
}

message IamV1OrganizationVendoroauthappPolicy {
  // Set of permissions explicitly allowed
  repeated string allow = 1;
  // Set of permissions explicitly denied
  repeated string deny = 2;
}

message NewKeyEnumKeytype {
  // Scalar value wrapper.
  string value = 1;
}

message OauthV1Token {
  // Token which carries the necessary information to access a Twilio resource directly.
  string access_token = 1;
  int64 expires_in = 2;
  // Token which carries the information necessary of user profile.
  string id_token = 3;
  // Token which carries the information necessary to get a new access token.
  string refresh_token = 4;
  // Token type
  string token_type = 5;
}

message RoleEnumScopeType {
  // Scalar value wrapper.
  string value = 1;
}

// Creates an oauth app for the given AccountSid
message CreateOauthAppAccountRequest {
  // Request body from media type "application/json".
  IamV1AccountVendorOauthAppCreateRequest body = 1;
}

// Canonical success response.
message CreateOauthAppAccountResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IamV1AccountVendorOauthAppCreateUpdateResponse body = 2;
}

// Updates an existing OAuth app for the given OauthAppSid
message UpdateOauthAppAccountRequest {
  // Unique ID (sid) of the OAuth app Required.
  string sid = 1;
  // Request body from media type "application/json".
  IamV1AccountVendorOauthAppUpdateRequest body = 2;
}

// Canonical success response.
message UpdateOauthAppAccountResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IamV1AccountVendorOauthAppCreateUpdateResponse body = 2;
}

// Deletes an oauth app for the given AccountSid
message DeleteOauthAppAccountRequest {
  // Unique ID (sid) of the OAuth app Required.
  string sid = 1;
}

// Canonical success response.
message DeleteOauthAppAccountResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListGetKeysResponse
message ListGetKeysResponseBody {
  repeated IamV1GetKeys keys = 1;
  ListGetKeysResponseBodyMeta meta = 2;
}

message ListGetKeysResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Keys for a account.
message ListGetKeysRequest {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Payments resource. Required.
  string account_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListGetKeysResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListGetKeysResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateNewKeyRequest
message CreateNewKeyRequestBody {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Payments resource.
  string account_sid = 1;
  // A descriptive string that you create to describe the resource. It can be up to 64 characters long.
  string friendly_name = 2;
  // OpenAPI property "KeyType".
  NewKeyEnumKeytype key_type = 3;
  // The \`Policy\` object is a collection that specifies the allowed Twilio permissions for the restricted key.
  // For more information on the permissions available with restricted API keys, refer to the [Twilio documentation](https://www.twilio.com/docs/iam/api-keys/restricted-api-keys#permissions-available-with-restricted-api-keys).
  string policy = 4;
}

// Create a new Signing Key for the account making the request.
message CreateNewKeyRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateNewKeyRequestBody body = 1;
}

// Canonical success response.
message CreateNewKeyResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IamV1NewKey body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch a specific Key.
message FetchKeyRequest {
  // The Twilio-provided string that uniquely identifies the Key resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchKeyResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IamV1Key body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateKeyRequest
message UpdateKeyRequestBody {
  // A descriptive string that you create to describe the resource. It can be up to 64 characters long.
  string friendly_name = 1;
  // The \`Policy\` object is a collection that specifies the allowed Twilio permissions for the restricted key.
  // For more information on the permissions available with restricted API keys, refer to the [Twilio documentation](https://www.twilio.com/docs/iam/api-keys/restricted-api-keys#permissions-available-with-restricted-api-keys).
  string policy = 2;
}

// Update a specific Key.
message UpdateKeyRequest {
  // The Twilio-provided string that uniquely identifies the Key resource to update. Required.
  string sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateKeyRequestBody body = 2;
}

// Canonical success response.
message UpdateKeyResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IamV1Key body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete a specific Key.
message DeleteKeyRequest {
  // The Twilio-provided string that uniquely identifies the Key resource to delete. Required.
  string sid = 1;
}

// Canonical success response.
message DeleteKeyResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// CreateTokenRequest
message CreateTokenRequestBody {
  // The targeted audience uri
  string audience = 1;
  // A 34 character string that uniquely identifies this OAuth App.
  string client_id = 2;
  // The credential for confidential OAuth App.
  string client_secret = 3;
  // JWT token related to the authorization code grant type.
  string code = 4;
  // Grant type is a credential representing resource owner's authorization which can be used by client to obtain access token.
  string grant_type = 5;
  // The redirect uri
  string redirect_uri = 6;
  // JWT token related to refresh access token.
  string refresh_token = 7;
  // The scope of token
  string scope = 8;
}

// Issues a new Access token (optionally identity_token & refresh_token) in exchange of Oauth grant
message CreateTokenRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateTokenRequestBody body = 1;
}

// Canonical success response.
message CreateTokenResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  OauthV1Token body = 2;
}

// Generated from Oauth2 operations.
service Oauth2Service {
  // Creates an oauth app for the given AccountSid
  rpc CreateOauthAppAccount(CreateOauthAppAccountRequest) returns (CreateOauthAppAccountResponse) {
    option (google.api.http) = {
      post: "/v1/Account/OAuthApps"
      body: "body"
      response_body: "body"
    };
  }
  // Updates an existing OAuth app for the given OauthAppSid
  rpc UpdateOauthAppAccount(UpdateOauthAppAccountRequest) returns (UpdateOauthAppAccountResponse) {
    option (google.api.http) = {
      put: "/v1/Account/OAuthApps/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  // Deletes an oauth app for the given AccountSid
  rpc DeleteOauthAppAccount(DeleteOauthAppAccountRequest) returns (DeleteOauthAppAccountResponse) {
    option (google.api.http) = {
      delete: "/v1/Account/OAuthApps/{sid}"
    };
  }
}

// Generated from IamV1GetApiKeys operations.
service IamV1GetApiKeysService {
  // Retrieve a list of all Keys for a account.
  rpc ListGetKeys(ListGetKeysRequest) returns (ListGetKeysResponse) {
    option (google.api.http) = {
      get: "/v1/Keys"
      response_body: "body"
    };
  }
}

// Generated from IamV1NewApiKey operations.
service IamV1NewApiKeyService {
  // Create a new Signing Key for the account making the request.
  rpc CreateNewKey(CreateNewKeyRequest) returns (CreateNewKeyResponse) {
    option (google.api.http) = {
      post: "/v1/Keys"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from IamV1ApiKey operations.
service IamV1ApiKeyService {
  // Fetch a specific Key.
  rpc FetchKey(FetchKeyRequest) returns (FetchKeyResponse) {
    option (google.api.http) = {
      get: "/v1/Keys/{sid}"
      response_body: "body"
    };
  }
  // Update a specific Key.
  rpc UpdateKey(UpdateKeyRequest) returns (UpdateKeyResponse) {
    option (google.api.http) = {
      post: "/v1/Keys/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete a specific Key.
  rpc DeleteKey(DeleteKeyRequest) returns (DeleteKeyResponse) {
    option (google.api.http) = {
      delete: "/v1/Keys/{sid}"
    };
  }
}

// Generated from OauthV1Token operations.
service OauthV1TokenService {
  // Issues a new Access token (optionally identity_token & refresh_token) in exchange of Oauth grant
  rpc CreateToken(CreateTokenRequest) returns (CreateTokenResponse) {
    option (google.api.http) = {
      post: "/v1/token"
      body: "body"
      response_body: "body"
    };
  }
}

