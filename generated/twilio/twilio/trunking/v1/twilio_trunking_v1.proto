// Code generated by openapi2proto. DO NOT EDIT.
// Source: twilio_trunking_v1.json

syntax = "proto3";

package twilio.trunking.v1;

option go_package = "twilio/trunking/v1;twiliotrunkingv1";

import "google/api/annotations.proto";

// Whether the phone number requires an [Address](https://www.twilio.com/docs/usage/api/address) registered with Twilio and, if so, what type. Can be: `none`, `any`, `local`, or `foreign`.
message PhoneNumberEnumAddressRequirement {
  // Scalar value wrapper.
  string value = 1;
}

// The recording mode for the trunk. Can be do-not-record (default), record-from-ringing, record-from-answer, record-from-ringing-dual, or record-from-answer-dual.
message RecordingEnumRecordingMode {
  // Scalar value wrapper.
  string value = 1;
}

// The recording trim setting for the trunk. Can be do-not-trim (default) or trim-silence.
message RecordingEnumRecordingTrim {
  // Scalar value wrapper.
  string value = 1;
}

// Caller Id for transfer target. Can be: `from-transferee` (default) or `from-transferor`.
message TrunkEnumTransferCallerId {
  // Scalar value wrapper.
  string value = 1;
}

// The call transfer settings for the trunk. Can be: `enable-all`, `sip-only` and `disable-all`. See [Transfer](https://www.twilio.com/docs/sip-trunking/call-transfer) for more information.
message TrunkEnumTransferSetting {
  // Scalar value wrapper.
  string value = 1;
}

message TrunkingV1Trunk {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Trunk resource.
  string account_sid = 1;
  // The types of authentication mapped to the domain. Can be: `IP_ACL` and `CREDENTIAL_LIST`. If both are mapped, the values are returned in a comma delimited list. If empty, the domain will not receive any traffic.
  string auth_type = 2;
  // Reserved.
  repeated string auth_type_set = 3;
  // Whether Caller ID Name (CNAM) lookup is enabled for the trunk. If enabled, all inbound calls to the SIP Trunk from the United States and Canada automatically perform a CNAM Lookup and display Caller ID data on your phone. See [CNAM Lookups](https://www.twilio.com/docs/sip-trunking#CNAM) for more information.
  bool cnam_lookup_enabled = 4;
  // The date and time in GMT when the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 5;
  // The date and time in GMT when the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 6;
  // The HTTP method we use to call the `disaster_recovery_url`. Can be: `GET` or `POST`.
  string disaster_recovery_method = 7;
  // The URL we call using the `disaster_recovery_method` if an error occurs while sending SIP traffic towards the configured Origination URL. We retrieve TwiML from this URL and execute the instructions like any other normal TwiML call. See [Disaster Recovery](https://www.twilio.com/docs/sip-trunking#disaster-recovery) for more information.
  string disaster_recovery_url = 8;
  // The unique address you reserve on Twilio to which you route your SIP traffic. Domain names can contain letters, digits, and `-` and must end with `pstn.twilio.com`. See [Termination Settings](https://www.twilio.com/docs/sip-trunking#termination) for more information.
  string domain_name = 9;
  // The string that you assigned to describe the resource.
  string friendly_name = 10;
  // The URLs of related resources.
  TrunkingV1TrunkLinks links = 11;
  // The recording settings for the trunk. Can be: `do-not-record`, `record-from-ringing`, `record-from-answer`. If set to `record-from-ringing` or `record-from-answer`, all calls going through the trunk will be recorded. The only way to change recording parameters is on a sub-resource of a Trunk after it has been created. e.g.`/Trunks/[Trunk_SID]/Recording -XPOST -d'Mode=record-from-answer'`. See [Recording](https://www.twilio.com/docs/sip-trunking#recording) for more information.
  string recording = 12;
  // Whether Secure Trunking is enabled for the trunk. If enabled, all calls going through the trunk will be secure using SRTP for media and TLS for signaling. If disabled, then RTP will be used for media. See [Secure Trunking](https://www.twilio.com/docs/sip-trunking#securetrunking) for more information.
  bool secure = 13;
  // The unique string that we created to identify the Trunk resource.
  string sid = 14;
  // Whether Symmetric RTP is enabled for the trunk. When Symmetric RTP is disabled, Twilio will send RTP to the destination negotiated in the SDP. Disabling Symmetric RTP is considered to be more secure and therefore recommended. See [Symmetric RTP](https://www.twilio.com/docs/sip-trunking#symmetric-rtp) for more information.
  bool symmetric_rtp_enabled = 15;
  // OpenAPI property "transfer_caller_id".
  TrunkEnumTransferCallerId transfer_caller_id = 16;
  // OpenAPI property "transfer_mode".
  TrunkEnumTransferSetting transfer_mode = 17;
  // The absolute URL of the resource.
  string url = 18;
}

// The URLs of related resources.
message TrunkingV1TrunkLinks {
  // Scalar value wrapper.
  string value = 1;
}

message TrunkingV1TrunkCredentialList {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the CredentialList resource.
  string account_sid = 1;
  // The date and time in GMT when the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 2;
  // The date and time in GMT when the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 3;
  // The string that you assigned to describe the resource.
  string friendly_name = 4;
  // The unique string that we created to identify the CredentialList resource.
  string sid = 5;
  // The SID of the Trunk the credential list in associated with.
  string trunk_sid = 6;
  // The absolute URL of the resource.
  string url = 7;
}

message TrunkingV1TrunkIpAccessControlList {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the IpAccessControlList resource.
  string account_sid = 1;
  // The date and time in GMT when the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 2;
  // The date and time in GMT when the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 3;
  // The string that you assigned to describe the resource.
  string friendly_name = 4;
  // The unique string that we created to identify the IpAccessControlList resource.
  string sid = 5;
  // The SID of the Trunk the resource is associated with.
  string trunk_sid = 6;
  // The absolute URL of the resource.
  string url = 7;
}

message TrunkingV1TrunkOriginationUrl {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the OriginationUrl resource.
  string account_sid = 1;
  // The date and time in GMT when the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 2;
  // The date and time in GMT when the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 3;
  // Whether the URL is enabled. The default is `true`.
  bool enabled = 4;
  // The string that you assigned to describe the resource.
  string friendly_name = 5;
  // The relative importance of the URI. Can be an integer from 0 to 65535, inclusive, and the default is 10. The lowest number represents the most important URI.
  int64 priority = 6;
  // The unique string that we created to identify the OriginationUrl resource.
  string sid = 7;
  // The SIP address you want Twilio to route your Origination calls to. This must be a `sip:` schema.
  string sip_url = 8;
  // The SID of the Trunk that owns the Origination URL.
  string trunk_sid = 9;
  // The absolute URL of the resource.
  string url = 10;
  // The value that determines the relative share of the load the URI should receive compared to other URIs with the same priority. Can be an integer from 1 to 65535, inclusive, and the default is 10. URLs with higher values receive more load than those with lower ones with the same priority.
  int64 weight = 11;
}

message TrunkingV1TrunkPhoneNumber {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the PhoneNumber resource.
  string account_sid = 1;
  // OpenAPI property "address_requirements".
  PhoneNumberEnumAddressRequirement address_requirements = 2;
  // The API version used to start a new TwiML session.
  string api_version = 3;
  // Whether the phone number is new to the Twilio platform. Can be: `true` or `false`.
  bool beta = 4;
  // The set of Boolean properties that indicate whether a phone number can receive calls or messages.  Capabilities are  `Voice`, `SMS`, and `MMS` and each capability can be: `true` or `false`.
  TrunkingV1TrunkPhoneNumberCapabilities capabilities = 5;
  // The date and time in GMT when the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 6;
  // The date and time in GMT when the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 7;
  // The string that you assigned to describe the resource.
  string friendly_name = 8;
  // The URLs of related resources.
  TrunkingV1TrunkPhoneNumberLinks links = 9;
  // The phone number in [E.164](https://www.twilio.com/docs/glossary/what-e164) format, which consists of a + followed by the country code and subscriber number.
  string phone_number = 10;
  // The unique string that we created to identify the PhoneNumber resource.
  string sid = 11;
  // The SID of the application that handles SMS messages sent to the phone number. If an `sms_application_sid` is present, we ignore all `sms_*_url` values and use those of the application.
  string sms_application_sid = 12;
  // The HTTP method we use to call `sms_fallback_url`. Can be: `GET` or `POST`.
  string sms_fallback_method = 13;
  // The URL that we call using the `sms_fallback_method` when an error occurs while retrieving or executing the TwiML from `sms_url`.
  string sms_fallback_url = 14;
  // The HTTP method we use to call `sms_url`. Can be: `GET` or `POST`.
  string sms_method = 15;
  // The URL we call using the `sms_method` when the phone number receives an incoming SMS message.
  string sms_url = 16;
  // The URL we call using the `status_callback_method` to send status information to your application.
  string status_callback = 17;
  // The HTTP method we use to call `status_callback`. Can be: `GET` or `POST`.
  string status_callback_method = 18;
  // The SID of the Trunk that handles calls to the phone number. If a `trunk_sid` is present, we ignore all of the voice URLs and voice applications and use those set on the Trunk. Setting a `trunk_sid` will automatically delete your `voice_application_sid` and vice versa.
  string trunk_sid = 19;
  // The absolute URL of the resource.
  string url = 20;
  // The SID of the application that handles calls to the phone number. If a `voice_application_sid` is present, we ignore all of the voice URLs and use those set on the application. Setting a `voice_application_sid` will automatically delete your `trunk_sid` and vice versa.
  string voice_application_sid = 21;
  // Whether we look up the caller's caller-ID name from the CNAM database ($0.01 per look up). Can be: `true` or `false`.
  bool voice_caller_id_lookup = 22;
  // The HTTP method that we use to call `voice_fallback_url`. Can be: `GET` or `POST`.
  string voice_fallback_method = 23;
  // The URL that we call using the `voice_fallback_method` when an error occurs retrieving or executing the TwiML requested by `url`.
  string voice_fallback_url = 24;
  // The HTTP method we use to call `voice_url`. Can be: `GET` or `POST`.
  string voice_method = 25;
  // The URL we call using the `voice_method` when the phone number receives a call. The `voice_url` is not be used if a `voice_application_sid` or a `trunk_sid` is set.
  string voice_url = 26;
}

// The set of Boolean properties that indicate whether a phone number can receive calls or messages.  Capabilities are  `Voice`, `SMS`, and `MMS` and each capability can be: `true` or `false`.
message TrunkingV1TrunkPhoneNumberCapabilities {
  bool fax = 1;
  bool mms = 2;
  bool sms = 3;
  bool voice = 4;
}

// The URLs of related resources.
message TrunkingV1TrunkPhoneNumberLinks {
  // Scalar value wrapper.
  string value = 1;
}

message TrunkingV1TrunkRecording {
  // OpenAPI property "mode".
  RecordingEnumRecordingMode mode = 1;
  // OpenAPI property "trim".
  RecordingEnumRecordingTrim trim = 2;
}

// ListTrunkResponse
message ListTrunkResponseBody {
  ListTrunkResponseBodyMeta meta = 1;
  repeated TrunkingV1Trunk trunks = 2;
}

message ListTrunkResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListTrunkRequest {
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 1;
  // The page index. This value is simply for client state.
  int64 page = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
}

// Canonical success response.
message ListTrunkResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListTrunkResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateTrunkRequest
message CreateTrunkRequestBody {
  // Whether Caller ID Name (CNAM) lookup should be enabled for the trunk. If enabled, all inbound calls to the SIP Trunk from the United States and Canada automatically perform a CNAM Lookup and display Caller ID data on your phone. See [CNAM Lookups](https://www.twilio.com/docs/sip-trunking#CNAM) for more information.
  bool cnam_lookup_enabled = 1;
  // The HTTP method we should use to call the `disaster_recovery_url`. Can be: `GET` or `POST`.
  string disaster_recovery_method = 2;
  // The URL we should call using the `disaster_recovery_method` if an error occurs while sending SIP traffic towards the configured Origination URL. We retrieve TwiML from the URL and execute the instructions like any other normal TwiML call. See [Disaster Recovery](https://www.twilio.com/docs/sip-trunking#disaster-recovery) for more information.
  string disaster_recovery_url = 3;
  // The unique address you reserve on Twilio to which you route your SIP traffic. Domain names can contain letters, digits, and `-` and must end with `pstn.twilio.com`. See [Termination Settings](https://www.twilio.com/docs/sip-trunking#termination) for more information.
  string domain_name = 4;
  // A descriptive string that you create to describe the resource. It can be up to 64 characters long.
  string friendly_name = 5;
  // Whether Secure Trunking is enabled for the trunk. If enabled, all calls going through the trunk will be secure using SRTP for media and TLS for signaling. If disabled, then RTP will be used for media. See [Secure Trunking](https://www.twilio.com/docs/sip-trunking#securetrunking) for more information.
  bool secure = 6;
  // OpenAPI property "TransferCallerId".
  TrunkEnumTransferCallerId transfer_caller_id = 7;
  // OpenAPI property "TransferMode".
  TrunkEnumTransferSetting transfer_mode = 8;
}

message CreateTrunkRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateTrunkRequestBody body = 1;
}

// Canonical success response.
message CreateTrunkResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TrunkingV1Trunk body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchTrunkRequest {
  // The unique string that we created to identify the Trunk resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchTrunkResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TrunkingV1Trunk body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateTrunkRequest
message UpdateTrunkRequestBody {
  // Whether Caller ID Name (CNAM) lookup should be enabled for the trunk. If enabled, all inbound calls to the SIP Trunk from the United States and Canada automatically perform a CNAM Lookup and display Caller ID data on your phone. See [CNAM Lookups](https://www.twilio.com/docs/sip-trunking#CNAM) for more information.
  bool cnam_lookup_enabled = 1;
  // The HTTP method we should use to call the `disaster_recovery_url`. Can be: `GET` or `POST`.
  string disaster_recovery_method = 2;
  // The URL we should call using the `disaster_recovery_method` if an error occurs while sending SIP traffic towards the configured Origination URL. We retrieve TwiML from the URL and execute the instructions like any other normal TwiML call. See [Disaster Recovery](https://www.twilio.com/docs/sip-trunking#disaster-recovery) for more information.
  string disaster_recovery_url = 3;
  // The unique address you reserve on Twilio to which you route your SIP traffic. Domain names can contain letters, digits, and `-` and must end with `pstn.twilio.com`. See [Termination Settings](https://www.twilio.com/docs/sip-trunking#termination) for more information.
  string domain_name = 4;
  // A descriptive string that you create to describe the resource. It can be up to 64 characters long.
  string friendly_name = 5;
  // Whether Secure Trunking is enabled for the trunk. If enabled, all calls going through the trunk will be secure using SRTP for media and TLS for signaling. If disabled, then RTP will be used for media. See [Secure Trunking](https://www.twilio.com/docs/sip-trunking#securetrunking) for more information.
  bool secure = 6;
  // OpenAPI property "TransferCallerId".
  TrunkEnumTransferCallerId transfer_caller_id = 7;
  // OpenAPI property "TransferMode".
  TrunkEnumTransferSetting transfer_mode = 8;
}

message UpdateTrunkRequest {
  // The unique string that we created to identify the OriginationUrl resource to update. Required.
  string sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateTrunkRequestBody body = 2;
}

// Canonical success response.
message UpdateTrunkResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TrunkingV1Trunk body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeleteTrunkRequest {
  // The unique string that we created to identify the Trunk resource to delete. Required.
  string sid = 1;
}

// Canonical success response.
message DeleteTrunkResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListCredentialListResponse
message ListCredentialListResponseBody {
  repeated TrunkingV1TrunkCredentialList credential_lists = 1;
  ListCredentialListResponseBodyMeta meta = 2;
}

message ListCredentialListResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListCredentialListRequest {
  // The SID of the Trunk from which to read the credential lists. Required.
  string trunk_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListCredentialListResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListCredentialListResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateCredentialListRequest
message CreateCredentialListRequestBody {
  // The SID of the [Credential List](https://www.twilio.com/docs/voice/sip/api/sip-credentiallist-resource) that you want to associate with the trunk. Once associated, we will authenticate access to the trunk against this list.
  string credential_list_sid = 1;
}

message CreateCredentialListRequest {
  // The SID of the Trunk to associate the credential list with. Required.
  string trunk_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateCredentialListRequestBody body = 2;
}

// Canonical success response.
message CreateCredentialListResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TrunkingV1TrunkCredentialList body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchCredentialListRequest {
  // The SID of the Trunk from which to fetch the credential list. Required.
  string trunk_sid = 1;
  // The unique string that we created to identify the CredentialList resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchCredentialListResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TrunkingV1TrunkCredentialList body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeleteCredentialListRequest {
  // The SID of the Trunk from which to delete the credential list. Required.
  string trunk_sid = 1;
  // The unique string that we created to identify the CredentialList resource to delete. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteCredentialListResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListIpAccessControlListResponse
message ListIpAccessControlListResponseBody {
  repeated TrunkingV1TrunkIpAccessControlList ip_access_control_lists = 1;
  ListIpAccessControlListResponseBodyMeta meta = 2;
}

message ListIpAccessControlListResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// List all IP Access Control Lists for a Trunk
message ListIpAccessControlListRequest {
  // The SID of the Trunk from which to read the IP Access Control Lists. Required.
  string trunk_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListIpAccessControlListResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListIpAccessControlListResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateIpAccessControlListRequest
message CreateIpAccessControlListRequestBody {
  // The SID of the [IP Access Control List](https://www.twilio.com/docs/voice/sip/api/sip-ipaccesscontrollist-resource) that you want to associate with the trunk.
  string ip_access_control_list_sid = 1;
}

// Associate an IP Access Control List with a Trunk
message CreateIpAccessControlListRequest {
  // The SID of the Trunk to associate the IP Access Control List with. Required.
  string trunk_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateIpAccessControlListRequestBody body = 2;
}

// Canonical success response.
message CreateIpAccessControlListResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TrunkingV1TrunkIpAccessControlList body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchIpAccessControlListRequest {
  // The SID of the Trunk from which to fetch the IP Access Control List. Required.
  string trunk_sid = 1;
  // The unique string that we created to identify the IpAccessControlList resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchIpAccessControlListResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TrunkingV1TrunkIpAccessControlList body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Remove an associated IP Access Control List from a Trunk
message DeleteIpAccessControlListRequest {
  // The SID of the Trunk from which to delete the IP Access Control List. Required.
  string trunk_sid = 1;
  // The unique string that we created to identify the IpAccessControlList resource to delete. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteIpAccessControlListResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListOriginationUrlResponse
message ListOriginationUrlResponseBody {
  ListOriginationUrlResponseBodyMeta meta = 1;
  repeated TrunkingV1TrunkOriginationUrl origination_urls = 2;
}

message ListOriginationUrlResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListOriginationUrlRequest {
  // The SID of the Trunk from which to read the OriginationUrl. Required.
  string trunk_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListOriginationUrlResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListOriginationUrlResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateOriginationUrlRequest
message CreateOriginationUrlRequestBody {
  // Whether the URL is enabled. The default is `true`.
  bool enabled = 1;
  // A descriptive string that you create to describe the resource. It can be up to 64 characters long.
  string friendly_name = 2;
  // The relative importance of the URI. Can be an integer from 0 to 65535, inclusive, and the default is 10. The lowest number represents the most important URI.
  int64 priority = 3;
  // The SIP address you want Twilio to route your Origination calls to. This must be a `sip:` schema.
  string sip_url = 4;
  // The value that determines the relative share of the load the URI should receive compared to other URIs with the same priority. Can be an integer from 1 to 65535, inclusive, and the default is 10. URLs with higher values receive more load than those with lower ones with the same priority.
  int64 weight = 5;
}

message CreateOriginationUrlRequest {
  // The SID of the Trunk to associate the resource with. Required.
  string trunk_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateOriginationUrlRequestBody body = 2;
}

// Canonical success response.
message CreateOriginationUrlResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TrunkingV1TrunkOriginationUrl body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchOriginationUrlRequest {
  // The SID of the Trunk from which to fetch the OriginationUrl. Required.
  string trunk_sid = 1;
  // The unique string that we created to identify the OriginationUrl resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchOriginationUrlResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TrunkingV1TrunkOriginationUrl body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateOriginationUrlRequest
message UpdateOriginationUrlRequestBody {
  // Whether the URL is enabled. The default is `true`.
  bool enabled = 1;
  // A descriptive string that you create to describe the resource. It can be up to 64 characters long.
  string friendly_name = 2;
  // The relative importance of the URI. Can be an integer from 0 to 65535, inclusive, and the default is 10. The lowest number represents the most important URI.
  int64 priority = 3;
  // The SIP address you want Twilio to route your Origination calls to. This must be a `sip:` schema. `sips` is NOT supported.
  string sip_url = 4;
  // The value that determines the relative share of the load the URI should receive compared to other URIs with the same priority. Can be an integer from 1 to 65535, inclusive, and the default is 10. URLs with higher values receive more load than those with lower ones with the same priority.
  int64 weight = 5;
}

message UpdateOriginationUrlRequest {
  // The SID of the Trunk from which to update the OriginationUrl. Required.
  string trunk_sid = 1;
  // The unique string that we created to identify the OriginationUrl resource to update. Required.
  string sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateOriginationUrlRequestBody body = 3;
}

// Canonical success response.
message UpdateOriginationUrlResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TrunkingV1TrunkOriginationUrl body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeleteOriginationUrlRequest {
  // The SID of the Trunk from which to delete the OriginationUrl. Required.
  string trunk_sid = 1;
  // The unique string that we created to identify the OriginationUrl resource to delete. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteOriginationUrlResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListPhoneNumberResponse
message ListPhoneNumberResponseBody {
  ListPhoneNumberResponseBodyMeta meta = 1;
  repeated TrunkingV1TrunkPhoneNumber phone_numbers = 2;
}

message ListPhoneNumberResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListPhoneNumberRequest {
  // The SID of the Trunk from which to read the PhoneNumber resources. Required.
  string trunk_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListPhoneNumberResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListPhoneNumberResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreatePhoneNumberRequest
message CreatePhoneNumberRequestBody {
  // The SID of the [Incoming Phone Number](https://www.twilio.com/docs/phone-numbers/api/incomingphonenumber-resource) that you want to associate with the trunk.
  string phone_number_sid = 1;
}

message CreatePhoneNumberRequest {
  // The SID of the Trunk to associate the phone number with. Required.
  string trunk_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreatePhoneNumberRequestBody body = 2;
}

// Canonical success response.
message CreatePhoneNumberResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TrunkingV1TrunkPhoneNumber body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchPhoneNumberRequest {
  // The SID of the Trunk from which to fetch the PhoneNumber resource. Required.
  string trunk_sid = 1;
  // The unique string that we created to identify the PhoneNumber resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchPhoneNumberResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TrunkingV1TrunkPhoneNumber body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeletePhoneNumberRequest {
  // The SID of the Trunk from which to delete the PhoneNumber resource. Required.
  string trunk_sid = 1;
  // The unique string that we created to identify the PhoneNumber resource to delete. Required.
  string sid = 2;
}

// Canonical success response.
message DeletePhoneNumberResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

message FetchRecordingRequest {
  // The SID of the Trunk from which to fetch the recording settings. Required.
  string trunk_sid = 1;
}

// Canonical success response.
message FetchRecordingResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TrunkingV1TrunkRecording body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateRecordingRequest
message UpdateRecordingRequestBody {
  // OpenAPI property "Mode".
  RecordingEnumRecordingMode mode = 1;
  // OpenAPI property "Trim".
  RecordingEnumRecordingTrim trim = 2;
}

message UpdateRecordingRequest {
  // The SID of the Trunk that will have its recording settings updated. Required.
  string trunk_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateRecordingRequestBody body = 2;
}

// Canonical success response.
message UpdateRecordingResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TrunkingV1TrunkRecording body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Generated from TrunkingV1Trunk operations.
service TrunkingV1TrunkService {
  rpc ListTrunk(ListTrunkRequest) returns (ListTrunkResponse) {
    option (google.api.http) = {
      get: "/v1/Trunks"
      response_body: "body"
    };
  }
  rpc CreateTrunk(CreateTrunkRequest) returns (CreateTrunkResponse) {
    option (google.api.http) = {
      post: "/v1/Trunks"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchTrunk(FetchTrunkRequest) returns (FetchTrunkResponse) {
    option (google.api.http) = {
      get: "/v1/Trunks/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateTrunk(UpdateTrunkRequest) returns (UpdateTrunkResponse) {
    option (google.api.http) = {
      post: "/v1/Trunks/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteTrunk(DeleteTrunkRequest) returns (DeleteTrunkResponse) {
    option (google.api.http) = {
      delete: "/v1/Trunks/{sid}"
    };
  }
}

// Generated from TrunkingV1CredentialList operations.
service TrunkingV1CredentialListService {
  rpc ListCredentialList(ListCredentialListRequest) returns (ListCredentialListResponse) {
    option (google.api.http) = {
      get: "/v1/Trunks/{trunk_sid}/CredentialLists"
      response_body: "body"
    };
  }
  rpc CreateCredentialList(CreateCredentialListRequest) returns (CreateCredentialListResponse) {
    option (google.api.http) = {
      post: "/v1/Trunks/{trunk_sid}/CredentialLists"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchCredentialList(FetchCredentialListRequest) returns (FetchCredentialListResponse) {
    option (google.api.http) = {
      get: "/v1/Trunks/{trunk_sid}/CredentialLists/{sid}"
      response_body: "body"
    };
  }
  rpc DeleteCredentialList(DeleteCredentialListRequest) returns (DeleteCredentialListResponse) {
    option (google.api.http) = {
      delete: "/v1/Trunks/{trunk_sid}/CredentialLists/{sid}"
    };
  }
}

// Generated from TrunkingV1IpAccessControlList operations.
service TrunkingV1IpAccessControlListService {
  // List all IP Access Control Lists for a Trunk
  rpc ListIpAccessControlList(ListIpAccessControlListRequest) returns (ListIpAccessControlListResponse) {
    option (google.api.http) = {
      get: "/v1/Trunks/{trunk_sid}/IpAccessControlLists"
      response_body: "body"
    };
  }
  // Associate an IP Access Control List with a Trunk
  rpc CreateIpAccessControlList(CreateIpAccessControlListRequest) returns (CreateIpAccessControlListResponse) {
    option (google.api.http) = {
      post: "/v1/Trunks/{trunk_sid}/IpAccessControlLists"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchIpAccessControlList(FetchIpAccessControlListRequest) returns (FetchIpAccessControlListResponse) {
    option (google.api.http) = {
      get: "/v1/Trunks/{trunk_sid}/IpAccessControlLists/{sid}"
      response_body: "body"
    };
  }
  // Remove an associated IP Access Control List from a Trunk
  rpc DeleteIpAccessControlList(DeleteIpAccessControlListRequest) returns (DeleteIpAccessControlListResponse) {
    option (google.api.http) = {
      delete: "/v1/Trunks/{trunk_sid}/IpAccessControlLists/{sid}"
    };
  }
}

// Generated from TrunkingV1OriginationUrl operations.
service TrunkingV1OriginationUrlService {
  rpc ListOriginationUrl(ListOriginationUrlRequest) returns (ListOriginationUrlResponse) {
    option (google.api.http) = {
      get: "/v1/Trunks/{trunk_sid}/OriginationUrls"
      response_body: "body"
    };
  }
  rpc CreateOriginationUrl(CreateOriginationUrlRequest) returns (CreateOriginationUrlResponse) {
    option (google.api.http) = {
      post: "/v1/Trunks/{trunk_sid}/OriginationUrls"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchOriginationUrl(FetchOriginationUrlRequest) returns (FetchOriginationUrlResponse) {
    option (google.api.http) = {
      get: "/v1/Trunks/{trunk_sid}/OriginationUrls/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateOriginationUrl(UpdateOriginationUrlRequest) returns (UpdateOriginationUrlResponse) {
    option (google.api.http) = {
      post: "/v1/Trunks/{trunk_sid}/OriginationUrls/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteOriginationUrl(DeleteOriginationUrlRequest) returns (DeleteOriginationUrlResponse) {
    option (google.api.http) = {
      delete: "/v1/Trunks/{trunk_sid}/OriginationUrls/{sid}"
    };
  }
}

// Generated from TrunkingV1PhoneNumber operations.
service TrunkingV1PhoneNumberService {
  rpc ListPhoneNumber(ListPhoneNumberRequest) returns (ListPhoneNumberResponse) {
    option (google.api.http) = {
      get: "/v1/Trunks/{trunk_sid}/PhoneNumbers"
      response_body: "body"
    };
  }
  rpc CreatePhoneNumber(CreatePhoneNumberRequest) returns (CreatePhoneNumberResponse) {
    option (google.api.http) = {
      post: "/v1/Trunks/{trunk_sid}/PhoneNumbers"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchPhoneNumber(FetchPhoneNumberRequest) returns (FetchPhoneNumberResponse) {
    option (google.api.http) = {
      get: "/v1/Trunks/{trunk_sid}/PhoneNumbers/{sid}"
      response_body: "body"
    };
  }
  rpc DeletePhoneNumber(DeletePhoneNumberRequest) returns (DeletePhoneNumberResponse) {
    option (google.api.http) = {
      delete: "/v1/Trunks/{trunk_sid}/PhoneNumbers/{sid}"
    };
  }
}

// Generated from TrunkingV1Recording operations.
service TrunkingV1RecordingService {
  rpc FetchRecording(FetchRecordingRequest) returns (FetchRecordingResponse) {
    option (google.api.http) = {
      get: "/v1/Trunks/{trunk_sid}/Recording"
      response_body: "body"
    };
  }
  rpc UpdateRecording(UpdateRecordingRequest) returns (UpdateRecordingResponse) {
    option (google.api.http) = {
      post: "/v1/Trunks/{trunk_sid}/Recording"
      body: "body"
      response_body: "body"
    };
  }
}

