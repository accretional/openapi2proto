// Code generated by openapi2proto. DO NOT EDIT.
// Source: twilio_monitor_v2.json

syntax = "proto3";

package twilio.monitor.v2;

option go_package = "twilio/monitor/v2;twiliomonitorv2";

import "google/api/annotations.proto";

// Twilio error response
message TwilioServiceErrorResponse {
  // Twilio error code
  int64 code = 1;
  // Http error code returned
  int64 http_status_code = 2;
  // Error message
  string message_field = 3;
  // More information link
  string more_info = 4;
  map<string, string> params = 5;
  // HTTP status code
  int64 status = 6;
  // Indicates if it was a user error
  bool user_error = 7;
}

// Basic user friendly view representation of an alarm.
message Alarm {
  // Sid for the alarm.
  string alert_config_sid = 1;
  // Dataset for the alarm.
  string dataset_name = 2;
  // Date Updated epoch for the alarm.
  int64 date_updated_epoch_milli = 3;
  // Description for the alarm.
  string description = 4;
  // Is alarm enbled? Default is true.
  bool enabled = 5;
  // OpenAPI property "friendlyName".
  FriendlyName friendly_name = 6;
  // Product the alarm is related to
  string product = 7;
}

// Friendly name for alarm
message FriendlyName {
  // Scalar value wrapper.
  string value = 1;
}

// Alarm sid
message MonitorV2AlarmSidRepsonse {
  string sid = 1;
}

// Alarm response body
message MonitorV2AlarmBody {
  // Sid for the alarm
  string alert_config_sid = 1;
  // Whether to send console notifications
  bool console_indicator = 2;
  // The date and time in UTC when the alarm was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The date and time in UTC when the alarm was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format
  string date_updated = 4;
  // Description for the alarm.
  string description = 5;
  // Email notifications to send
  repeated string email = 6;
  // Is alarm enbled? Default is true.
  bool enabled = 7;
  // OpenAPI property "friendlyName".
  FriendlyName friendly_name = 8;
  // Product the alarm is related to.
  string product = 9;
  // Value to query for
  string query = 10;
  // OpenAPI property "queryType".
  QueryType query_type = 11;
  // OpenAPI property "timeWindow".
  TimeWindow time_window = 12;
  // Threshold to send customer alarm notification
  int64 trigger_value = 13;
  // Webhook notification to send
  string webhook = 14;
}

// Type of query
message QueryType {
  // Scalar value wrapper.
  string value = 1;
}

// Time window for alarm to trigger
message TimeWindow {
  // Scalar value wrapper.
  string value = 1;
}

// Alarm object
message MonitorV2AlarmCreateObject {
  // Whether to send console notifications
  bool console_indicator = 1;
  // Description for the alarm.
  string description = 2;
  // Email notifications to send
  repeated string email = 3;
  // Is alarm enbled? Default is true.
  bool enabled = 4;
  // OpenAPI property "friendlyName".
  FriendlyName friendly_name = 5;
  // Value to query for
  string query = 6;
  // OpenAPI property "queryType".
  QueryType query_type = 7;
  // OpenAPI property "timeWindow".
  TimeWindow time_window = 8;
  // Threshold to send customer alarm notification
  int64 trigger_value = 9;
  // Webhook notification to send
  string webhook = 10;
}

// Enable or disable alarm
message MonitorV2AlarmStatus {
  // status of alarm
  bool enabled = 1;
}

// ListAlarms
message ListAlarmsResponseBody {
  // List of basic parameters defined for alarms for an account.
  repeated MonitorV2AlarmBody alarms = 1;
  ListAlarmsResponseBodyMeta meta = 2;
}

message ListAlarmsResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Fetch list of Alarms
message ListAlarmsRequest {
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 1;
  // The page index. This value is simply for client state.
  int64 page = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
  // OpenAPI query parameter "SortOrder".
  string sort_order = 4;
}

// Canonical success response.
message ListAlarmsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListAlarmsResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Create an alarm
message CreateAlarmRequest {
  // Request body from media type "application/json".
  MonitorV2AlarmCreateObject body = 1;
}

// Canonical success response.
message CreateAlarmResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MonitorV2AlarmSidRepsonse body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Get alarm
message FetchAlarmRequest {
  // Sid Required.
  string sid = 1;
}

// Canonical success response.
message FetchAlarmResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MonitorV2AlarmBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Update alarm
message UpdateAlarmRequest {
  // Sid Required.
  string sid = 1;
  // Request body from media type "application/json".
  MonitorV2AlarmCreateObject body = 2;
}

// Canonical success response.
message UpdateAlarmResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MonitorV2AlarmSidRepsonse body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete alarm
message DeleteAlarmRequest {
  // Sid Required.
  string sid = 1;
}

// Canonical success response.
message DeleteAlarmResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 2;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 3;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 4;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 5;
  // Headers exposed to the client
  string header_access_control_expose_headers = 6;
}

// Update status of alarm between enabled and disabled
message UpdateAlarmStatusRequest {
  // Sid Required.
  string sid = 1;
  // Request body from media type "application/json".
  MonitorV2AlarmStatus body = 2;
}

// Canonical success response.
message UpdateAlarmStatusResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 2;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 3;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 4;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 5;
  // Headers exposed to the client
  string header_access_control_expose_headers = 6;
}

// Generated from twilio.monitor.v2 operations.
service V2Service {
  // Fetch list of Alarms
  rpc ListAlarms(ListAlarmsRequest) returns (ListAlarmsResponse) {
    option (google.api.http) = {
      get: "/v2/Alarms"
      response_body: "body"
    };
  }
  // Create an alarm
  rpc CreateAlarm(CreateAlarmRequest) returns (CreateAlarmResponse) {
    option (google.api.http) = {
      post: "/v2/Alarms"
      body: "body"
      response_body: "body"
    };
  }
  // Get alarm
  rpc FetchAlarm(FetchAlarmRequest) returns (FetchAlarmResponse) {
    option (google.api.http) = {
      get: "/v2/Alarms/{sid}"
      response_body: "body"
    };
  }
  // Update alarm
  rpc UpdateAlarm(UpdateAlarmRequest) returns (UpdateAlarmResponse) {
    option (google.api.http) = {
      put: "/v2/Alarms/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete alarm
  rpc DeleteAlarm(DeleteAlarmRequest) returns (DeleteAlarmResponse) {
    option (google.api.http) = {
      delete: "/v2/Alarms/{sid}"
    };
  }
  // Update status of alarm between enabled and disabled
  rpc UpdateAlarmStatus(UpdateAlarmStatusRequest) returns (UpdateAlarmStatusResponse) {
    option (google.api.http) = {
      put: "/v2/Alarms/{sid}/status"
      body: "body"
    };
  }
}

