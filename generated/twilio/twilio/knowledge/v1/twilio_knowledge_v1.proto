// Code generated by openapi2proto. DO NOT EDIT.
// Source: twilio_knowledge_v1.json

syntax = "proto3";

package twilio.knowledge.v1;

option go_package = "twilio/knowledge/v1;twilioknowledgev1";

import "google/api/annotations.proto";

message KnowledgeV1ServiceCreateKnowledgeRequest {
  // The description of the knowledge source.
  string description = 1;
  // The embedding model to be used for the knowledge source. It's required for 'Database' type but disallowed for other types.
  string embedding_model = 2;
  // The details of the knowledge source based on the type.
  KnowledgeV1ServiceCreateKnowledgeRequestKnowledgeSourceDetails knowledge_source_details = 3;
  // The name of the tool.
  string name = 4;
  // OpenAPI property "policy".
  KnowledgeV1ServiceCreatePolicyRequest policy = 5;
  // The type of the knowledge source.
  string type = 6;
}

// The details of the knowledge source based on the type.
message KnowledgeV1ServiceCreateKnowledgeRequestKnowledgeSourceDetails {
  // Scalar value wrapper.
  string value = 1;
}

message KnowledgeV1ServiceCreatePolicyRequest {
  // The description of the policy.
  string description = 1;
  // The Policy ID.
  string id = 2;
  // The name of the policy.
  string name = 3;
  string policy_details = 4;
  // The description of the policy.
  string type = 5;
}

message KnowledgeV1ServiceCreateTagRequest {
  // Whether the tag allows multiple values.
  bool allows_multiple = 1;
  // The key of the tag.
  string key = 2;
  // Whether the tag is a Twilio tag.
  bool twilio_tag = 3;
}

message KnowledgeV1ServiceCreateTagValueRequest {
  // Whether the tag value is enabled.
  bool enabled = 1;
  // The ID of the tag.
  string tag_id = 2;
  // The value of the tag.
  string value = 3;
}

message KnowledgeV1ServiceKnowledge {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Knowledge resource.
  string account_sid = 1;
  // The date and time in GMT when the Knowledge was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 2;
  // The date and time in GMT when the Knowledge was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 3;
  // The type of knowledge source.
  string description = 4;
  // The embedding model to be used for the knowledge source.
  string embedding_model = 5;
  // The description of knowledge.
  string id = 6;
  // The details of the knowledge source based on the type.
  KnowledgeV1ServiceKnowledgeKnowledgeSourceDetails knowledge_source_details = 7;
  // The name of the knowledge source.
  string name = 8;
  // The status of processing the knowledge source ('QUEUED', 'PROCESSING', 'COMPLETED', 'FAILED')
  string status = 9;
  // The type of knowledge source ('Web', 'Database', 'Text', 'File')
  string type = 10;
  // The url of the knowledge resource.
  string url = 11;
}

// The details of the knowledge source based on the type.
message KnowledgeV1ServiceKnowledgeKnowledgeSourceDetails {
  // Scalar value wrapper.
  string value = 1;
}

message KnowledgeV1ServiceKnowledgeChunk {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Knowledge resource.
  string account_sid = 1;
  // The chunk content.
  string content = 2;
  // The date and time in GMT when the Chunk was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The date and time in GMT when the Chunk was updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 4;
  // The metadata of the chunk.
  KnowledgeV1ServiceKnowledgeChunkMetadata metadata = 5;
}

// The metadata of the chunk.
message KnowledgeV1ServiceKnowledgeChunkMetadata {
  // Scalar value wrapper.
  string value = 1;
}

message KnowledgeV1ServiceKnowledgeChunkWithScore {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Knowledge resource.
  string account_sid = 1;
  // The chunk content.
  string content = 2;
  // The date and time in GMT when the Chunk was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The date and time in GMT when the Chunk was updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 4;
  // The metadata of the chunk.
  KnowledgeV1ServiceKnowledgeChunkWithScoreMetadata metadata = 5;
  // The knowledge ID associated with the chunk.
  string knowledge_id = 6;
  // The score associated with the chunk.
  float score = 7;
}

// The metadata of the chunk.
message KnowledgeV1ServiceKnowledgeChunkWithScoreMetadata {
  // Scalar value wrapper.
  string value = 1;
}

message KnowledgeV1ServiceKnowledgeStatus {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Knowledge resource.
  string account_sid = 1;
  // The date and time in GMT when the Knowledge was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 2;
  // The last status of processing the knowledge source ('QUEUED', 'PROCESSING', 'COMPLETED', 'FAILED')
  string last_status = 3;
  // The status of processing the knowledge source ('QUEUED', 'PROCESSING', 'COMPLETED', 'FAILED')
  string status = 4;
}

message KnowledgeV1ServiceTag {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Tag resource.
  string account_sid = 1;
  // Whether the tag allows multiple values.
  bool allows_multiple = 2;
  // The date and time in GMT when the Tag was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The date and time in GMT when the Tag was updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 4;
  // The ID of the tag.
  string id = 5;
  // The key of the tag.
  string key = 6;
  // Whether the tag is a Twilio tag.
  bool twilio_tag = 7;
  // The URL of the Tag resource.
  string url = 8;
}

message KnowledgeV1ServiceTagValue {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Tag Value resource.
  string account_sid = 1;
  // The date and time in GMT when the Tag Value was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 2;
  // The date and time in GMT when the Tag Value was updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 3;
  // Whether the tag value is enabled.
  bool enabled = 4;
  // The ID of the tag value.
  string id = 5;
  // The ID of the resource.
  string resource_id = 6;
  // The type of the resource.
  string resource_type = 7;
  // The ID of the tag.
  string tag_id = 8;
  // The URL of the Tag Value resource.
  string url = 9;
  // The value of the tag.
  string value = 10;
}

message KnowledgeV1ServiceUpdateKnowledgeRequest {
  // The description of the knowledge source.
  string description = 1;
  // The embedding model to be used for the knowledge source. It's only applicable to 'Database' type.
  string embedding_model = 2;
  // The details of the knowledge source based on the type.
  KnowledgeV1ServiceUpdateKnowledgeRequestKnowledgeSourceDetails knowledge_source_details = 3;
  // The name of the knowledge source.
  string name = 4;
  // OpenAPI property "policy".
  KnowledgeV1ServiceCreatePolicyRequest policy = 5;
  // The description of the knowledge source.
  string type = 6;
}

// The details of the knowledge source based on the type.
message KnowledgeV1ServiceUpdateKnowledgeRequestKnowledgeSourceDetails {
  // Scalar value wrapper.
  string value = 1;
}

message KnowledgeV1ServiceUpdateTagRequest {
  // Whether the tag allows multiple values.
  bool allows_multiple = 1;
  // The key of the tag.
  string key = 2;
}

message KnowledgeV1ServiceUpdateTagValueRequest {
  // Whether the tag value is enabled.
  bool enabled = 1;
  // The value of the tag.
  string value = 2;
}

// Array of tag and value pairs for tag filtering.
message Tags {
  // Array values.
  repeated TagsItem items = 1;
}

message TagsItem {
  // The tag key
  string key = 1;
  // The tag value
  string value = 2;
}

// ListKnowledgeResponse
message ListKnowledgeResponseBody {
  repeated ListKnowledgeResponseBodyKnowledgeItem knowledge = 1;
  ListKnowledgeResponseBodyMeta meta = 2;
}

message ListKnowledgeResponseBodyKnowledgeItem {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Knowledge resource.
  string account_sid = 1;
  // The date and time in GMT when the Knowledge was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 2;
  // The date and time in GMT when the Knowledge was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 3;
  // The type of knowledge source.
  string description = 4;
  // The embedding model to be used for the knowledge source.
  string embedding_model = 5;
  // The description of knowledge.
  string id = 6;
  // The details of the knowledge source based on the type.
  ListKnowledgeResponseBodyKnowledgeItemKnowledgeSourceDetails knowledge_source_details = 7;
  // The name of the knowledge source.
  string name = 8;
  // The status of processing the knowledge source ('QUEUED', 'PROCESSING', 'COMPLETED', 'FAILED')
  string status = 9;
  // The type of knowledge source ('Web', 'Database', 'Text', 'File')
  string type = 10;
  // The url of the knowledge resource.
  string url = 11;
}

// The details of the knowledge source based on the type.
message ListKnowledgeResponseBodyKnowledgeItemKnowledgeSourceDetails {
  // Scalar value wrapper.
  string value = 1;
}

message ListKnowledgeResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// List all knowledge
message ListKnowledgeRequest {
  // The page index. This value is simply for client state.
  int64 page = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
  // Json array of tag and value pairs for tag filtering.
  string tags = 4;
}

// Canonical success response.
message ListKnowledgeResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListKnowledgeResponseBody body = 2;
}

// Create knowledge
message CreateKnowledgeRequest {
  // Request body from media type "application/json".
  KnowledgeV1ServiceCreateKnowledgeRequest body = 1;
}

// Canonical success response.
message CreateKnowledgeResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  KnowledgeV1ServiceKnowledge body = 2;
}

// Get knowledge
message FetchKnowledgeRequest {
  // OpenAPI path parameter "id". Required.
  string id = 1;
}

// Canonical success response.
message FetchKnowledgeResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  KnowledgeV1ServiceKnowledge body = 2;
}

// Update knowledge
message UpdateKnowledgeRequest {
  // OpenAPI path parameter "id". Required.
  string id = 1;
  // Request body from media type "application/json".
  KnowledgeV1ServiceUpdateKnowledgeRequest body = 2;
}

// Canonical success response.
message UpdateKnowledgeResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  KnowledgeV1ServiceKnowledge body = 2;
}

// Delete knowledge
message DeleteKnowledgeRequest {
  // the Knowledge ID. Required.
  string id = 1;
}

// Canonical success response.
message DeleteKnowledgeResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListKnowledgeChunksResponse
message ListKnowledgeChunksResponseBody {
  repeated KnowledgeV1ServiceKnowledgeChunk chunks = 1;
  ListKnowledgeChunksResponseBodyMeta meta = 2;
}

message ListKnowledgeChunksResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// List knowledge chunks
message ListKnowledgeChunksRequest {
  // The knowledge ID. Required.
  string id = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
}

// Canonical success response.
message ListKnowledgeChunksResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListKnowledgeChunksResponseBody body = 2;
}

// Get knowledge status
message FetchKnowledgeStatusRequest {
  // the Knowledge ID. Required.
  string id = 1;
}

// Canonical success response.
message FetchKnowledgeStatusResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  KnowledgeV1ServiceKnowledgeStatus body = 2;
}

// Generated from twilio.knowledge.v1 operations.
service V1Service {
  // List all knowledge
  rpc ListKnowledge(ListKnowledgeRequest) returns (ListKnowledgeResponse) {
    option (google.api.http) = {
      get: "/v1/Knowledge"
      response_body: "body"
    };
  }
  // Create knowledge
  rpc CreateKnowledge(CreateKnowledgeRequest) returns (CreateKnowledgeResponse) {
    option (google.api.http) = {
      post: "/v1/Knowledge"
      body: "body"
      response_body: "body"
    };
  }
  // Get knowledge
  rpc FetchKnowledge(FetchKnowledgeRequest) returns (FetchKnowledgeResponse) {
    option (google.api.http) = {
      get: "/v1/Knowledge/{id}"
      response_body: "body"
    };
  }
  // Update knowledge
  rpc UpdateKnowledge(UpdateKnowledgeRequest) returns (UpdateKnowledgeResponse) {
    option (google.api.http) = {
      put: "/v1/Knowledge/{id}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete knowledge
  rpc DeleteKnowledge(DeleteKnowledgeRequest) returns (DeleteKnowledgeResponse) {
    option (google.api.http) = {
      delete: "/v1/Knowledge/{id}"
    };
  }
  // List knowledge chunks
  rpc ListKnowledgeChunks(ListKnowledgeChunksRequest) returns (ListKnowledgeChunksResponse) {
    option (google.api.http) = {
      get: "/v1/Knowledge/{id}/Chunks"
      response_body: "body"
    };
  }
  // Get knowledge status
  rpc FetchKnowledgeStatus(FetchKnowledgeStatusRequest) returns (FetchKnowledgeStatusResponse) {
    option (google.api.http) = {
      get: "/v1/Knowledge/{id}/Status"
      response_body: "body"
    };
  }
}

