// Code generated by openapi2proto. DO NOT EDIT.
// Source: twilio_voice_v1.json

syntax = "proto3";

package twilio.voice.v1;

option go_package = "twilio/voice/v1;twiliovoicev1";

import "google/api/annotations.proto";

message RecordingEnumSource {
  // Scalar value wrapper.
  string value = 1;
}

message RecordingEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

message VoiceV1ArchivedCall {
  // The date
  string date = 1;
  // The call sid
  string sid = 2;
  // The absolute URL of the resource.
  string url = 3;
}

message VoiceV1ByocTrunk {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the BYOC Trunk resource.
  string account_sid = 1;
  // Whether Caller ID Name (CNAM) lookup is enabled for the trunk. If enabled, all inbound calls to the BYOC Trunk from the United States and Canada automatically perform a CNAM Lookup and display Caller ID data on your phone. See [CNAM Lookups](https://www.twilio.com/docs/sip-trunking#CNAM) for more information.
  bool cnam_lookup_enabled = 2;
  // The SID of the Connection Policy that Twilio will use when routing traffic to your communications infrastructure.
  string connection_policy_sid = 3;
  // The date and time in GMT that the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 4;
  // The date and time in GMT that the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 5;
  // The string that you assigned to describe the resource.
  string friendly_name = 6;
  // The SID of the SIP Domain that should be used in the `From` header of originating calls sent to your SIP infrastructure. If your SIP infrastructure allows users to "call back" an incoming call, configure this with a [SIP Domain](https://www.twilio.com/docs/voice/api/sending-sip) to ensure proper routing. If not configured, the from domain will default to "sip.twilio.com".
  string from_domain_sid = 7;
  // The unique string that that we created to identify the BYOC Trunk resource.
  string sid = 8;
  // The HTTP method we use to call `status_callback_url`. Either `GET` or `POST`.
  string status_callback_method = 9;
  // The URL that we call to pass status parameters (such as call ended) to your application.
  string status_callback_url = 10;
  // The absolute URL of the resource.
  string url = 11;
  // The HTTP method we use to call `voice_fallback_url`. Can be: `GET` or `POST`.
  string voice_fallback_method = 12;
  // The URL that we call when an error occurs while retrieving or executing the TwiML requested from `voice_url`.
  string voice_fallback_url = 13;
  // The HTTP method we use to call `voice_url`. Can be: `GET` or `POST`.
  string voice_method = 14;
  // The URL we call using the `voice_method` when the BYOC Trunk receives a call.
  string voice_url = 15;
}

message VoiceV1ConnectionPolicy {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Connection Policy resource.
  string account_sid = 1;
  // The date and time in GMT when the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 2;
  // The date and time in GMT when the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 3;
  // The string that you assigned to describe the resource.
  string friendly_name = 4;
  // The URLs of related resources.
  VoiceV1ConnectionPolicyLinks links = 5;
  // The unique string that we created to identify the Connection Policy resource.
  string sid = 6;
  // The absolute URL of the resource.
  string url = 7;
}

// The URLs of related resources.
message VoiceV1ConnectionPolicyLinks {
  // Scalar value wrapper.
  string value = 1;
}

message VoiceV1ConnectionPolicyConnectionPolicyTarget {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Target resource.
  string account_sid = 1;
  // The SID of the Connection Policy that owns the Target.
  string connection_policy_sid = 2;
  // The date and time in GMT when the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 3;
  // The date and time in GMT when the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 4;
  // Whether the target is enabled. The default is `true`.
  bool enabled = 5;
  // The string that you assigned to describe the resource.
  string friendly_name = 6;
  // The relative importance of the target. Can be an integer from 0 to 65535, inclusive, and the default is 10. The lowest number represents the most important target.
  int64 priority = 7;
  // The unique string that we created to identify the Target resource.
  string sid = 8;
  // The SIP address you want Twilio to route your calls to. This must be a `sip:` schema. `sips` is NOT supported.
  string target = 9;
  // The absolute URL of the resource.
  string url = 10;
  // The value that determines the relative share of the load the Target should receive compared to other Targets with the same priority. Can be an integer from 1 to 65535, inclusive, and the default is 10. Targets with higher values receive more load than those with lower ones with the same priority.
  int64 weight = 11;
}

message VoiceV1DialingPermissions {
  // Scalar value wrapper.
  string value = 1;
}

message VoiceV1DialingPermissionsDialingPermissionsCountry {
  // The name of the continent in which the country is located.
  string continent = 1;
  // The E.164 assigned [country codes(s)](https://www.itu.int/itudoc/itu-t/ob-lists/icc/e164_763.html)
  repeated string country_codes = 2;
  // Whether dialing to high-risk special services numbers is enabled. These prefixes include number ranges allocated by the country and include premium numbers, special services, shared cost, and others
  bool high_risk_special_numbers_enabled = 3;
  // Whether dialing to high-risk [toll fraud](https://www.twilio.com/blog/how-to-protect-your-account-from-toll-fraud-with-voice-dialing-geo-permissions-html) numbers is enabled. These prefixes include narrow number ranges that have a high-risk of international revenue sharing fraud (IRSF) attacks, also known as [toll fraud](https://www.twilio.com/blog/how-to-protect-your-account-from-toll-fraud-with-voice-dialing-geo-permissions-html). These prefixes are collected from anti-fraud databases and verified by analyzing calls on our network. These prefixes are not available for download and are updated frequently
  bool high_risk_tollfraud_numbers_enabled = 4;
  // The [ISO country code](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2).
  string iso_code = 5;
  // A list of URLs related to this resource.
  VoiceV1DialingPermissionsDialingPermissionsCountryLinks links = 6;
  // Whether dialing to low-risk numbers is enabled.
  bool low_risk_numbers_enabled = 7;
  // The name of the country.
  string name = 8;
  // The absolute URL of this resource.
  string url = 9;
}

// A list of URLs related to this resource.
message VoiceV1DialingPermissionsDialingPermissionsCountryLinks {
  // Scalar value wrapper.
  string value = 1;
}

message VoiceV1DialingPermissionsDialingPermissionsCountryInstance {
  // The name of the continent in which the country is located.
  string continent = 1;
  // The E.164 assigned [country codes(s)](https://www.itu.int/itudoc/itu-t/ob-lists/icc/e164_763.html)
  repeated string country_codes = 2;
  // Whether dialing to high-risk special services numbers is enabled. These prefixes include number ranges allocated by the country and include premium numbers, special services, shared cost, and others
  bool high_risk_special_numbers_enabled = 3;
  // Whether dialing to high-risk [toll fraud](https://www.twilio.com/blog/how-to-protect-your-account-from-toll-fraud-with-voice-dialing-geo-permissions-html) numbers is enabled. These prefixes include narrow number ranges that have a high-risk of international revenue sharing fraud (IRSF) attacks, also known as [toll fraud](https://www.twilio.com/blog/how-to-protect-your-account-from-toll-fraud-with-voice-dialing-geo-permissions-html). These prefixes are collected from anti-fraud databases and verified by analyzing calls on our network. These prefixes are not available for download and are updated frequently
  bool high_risk_tollfraud_numbers_enabled = 4;
  // The [ISO country code](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2).
  string iso_code = 5;
  // A list of URLs related to this resource.
  VoiceV1DialingPermissionsDialingPermissionsCountryInstanceLinks links = 6;
  // Whether dialing to low-risk numbers is enabled.
  bool low_risk_numbers_enabled = 7;
  // The name of the country.
  string name = 8;
  // The absolute URL of this resource.
  string url = 9;
}

// A list of URLs related to this resource.
message VoiceV1DialingPermissionsDialingPermissionsCountryInstanceLinks {
  // Scalar value wrapper.
  string value = 1;
}

message VoiceV1DialingPermissionsDialingPermissionsCountryDialingPermissionsHrsPrefixes {
  // A prefix is a contiguous number range for a block of E.164 numbers that includes the E.164 assigned country code. For example, a North American Numbering Plan prefix like `+1510720` written like `+1(510) 720` matches all numbers inclusive from `+1(510) 720-0000` to `+1(510) 720-9999`.
  string prefix = 1;
}

message VoiceV1DialingPermissionsDialingPermissionsCountryBulkUpdate {
  // The number of countries updated
  int64 update_count = 1;
  // A bulk update request to change voice dialing country permissions stored as a URL-encoded, JSON array of update objects. For example : `[ { "iso_code": "GB", "low_risk_numbers_enabled": "true", "high_risk_special_numbers_enabled":"true", "high_risk_tollfraud_numbers_enabled": "false" } ]`
  string update_request = 2;
}

message VoiceV1DialingPermissionsDialingPermissionsSettings {
  // `true` if the sub-account will inherit voice dialing permissions from the Master Project; otherwise `false`.
  bool dialing_permissions_inheritance = 1;
  // The absolute URL of this resource.
  string url = 2;
}

message VoiceV1IpRecord {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the IP Record resource.
  string account_sid = 1;
  // An integer representing the length of the [CIDR](https://tools.ietf.org/html/rfc4632) prefix to use with this IP address. By default the entire IP address is used, which for IPv4 is value 32.
  int64 cidr_prefix_length = 2;
  // The date and time in GMT that the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 3;
  // The date and time in GMT that the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 4;
  // The string that you assigned to describe the resource.
  string friendly_name = 5;
  // An IP address in dotted decimal notation, IPv4 only.
  string ip_address = 6;
  // The unique string that we created to identify the IP Record resource.
  string sid = 7;
  // The absolute URL of the resource.
  string url = 8;
}

message VoiceV1SourceIpMapping {
  // The date and time in GMT that the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 1;
  // The date and time in GMT that the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 2;
  // The Twilio-provided string that uniquely identifies the IP Record resource to map from.
  string ip_record_sid = 3;
  // The unique string that we created to identify the IP Record resource.
  string sid = 4;
  // The SID of the SIP Domain that the IP Record is mapped to.
  string sip_domain_sid = 5;
  // The absolute URL of the resource.
  string url = 6;
}

// Delete an archived call record from Bulk Export. Note: this does not also delete the record from the Voice API.
message DeleteArchivedCallRequest {
  // The date of the Call in UTC. Required.
  string date = 1;
  // The Twilio-provided Call SID that uniquely identifies the Call resource to delete Required.
  string sid = 2;
}

// Canonical success response.
message DeleteArchivedCallResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListByocTrunkResponse
message ListByocTrunkResponseBody {
  repeated VoiceV1ByocTrunk byoc_trunks = 1;
  ListByocTrunkResponseBodyMeta meta = 2;
}

message ListByocTrunkResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListByocTrunkRequest {
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 1;
  // The page index. This value is simply for client state.
  int64 page = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
}

// Canonical success response.
message ListByocTrunkResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListByocTrunkResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateByocTrunkRequest
message CreateByocTrunkRequestBody {
  // Whether Caller ID Name (CNAM) lookup is enabled for the trunk. If enabled, all inbound calls to the BYOC Trunk from the United States and Canada automatically perform a CNAM Lookup and display Caller ID data on your phone. See [CNAM Lookups](https://www.twilio.com/docs/sip-trunking#CNAM) for more information.
  bool cnam_lookup_enabled = 1;
  // The SID of the Connection Policy that Twilio will use when routing traffic to your communications infrastructure.
  string connection_policy_sid = 2;
  // A descriptive string that you create to describe the resource. It is not unique and can be up to 255 characters long.
  string friendly_name = 3;
  // The SID of the SIP Domain that should be used in the `From` header of originating calls sent to your SIP infrastructure. If your SIP infrastructure allows users to "call back" an incoming call, configure this with a [SIP Domain](https://www.twilio.com/docs/voice/api/sending-sip) to ensure proper routing. If not configured, the from domain will default to "sip.twilio.com".
  string from_domain_sid = 4;
  // The HTTP method we should use to call `status_callback_url`. Can be: `GET` or `POST`.
  string status_callback_method = 5;
  // The URL that we should call to pass status parameters (such as call ended) to your application.
  string status_callback_url = 6;
  // The HTTP method we should use to call `voice_fallback_url`. Can be: `GET` or `POST`.
  string voice_fallback_method = 7;
  // The URL that we should call when an error occurs while retrieving or executing the TwiML from `voice_url`.
  string voice_fallback_url = 8;
  // The HTTP method we should use to call `voice_url`. Can be: `GET` or `POST`.
  string voice_method = 9;
  // The URL we should call when the BYOC Trunk receives a call.
  string voice_url = 10;
}

message CreateByocTrunkRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateByocTrunkRequestBody body = 1;
}

// Canonical success response.
message CreateByocTrunkResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VoiceV1ByocTrunk body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchByocTrunkRequest {
  // The Twilio-provided string that uniquely identifies the BYOC Trunk resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchByocTrunkResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VoiceV1ByocTrunk body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateByocTrunkRequest
message UpdateByocTrunkRequestBody {
  // Whether Caller ID Name (CNAM) lookup is enabled for the trunk. If enabled, all inbound calls to the BYOC Trunk from the United States and Canada automatically perform a CNAM Lookup and display Caller ID data on your phone. See [CNAM Lookups](https://www.twilio.com/docs/sip-trunking#CNAM) for more information.
  bool cnam_lookup_enabled = 1;
  // The SID of the Connection Policy that Twilio will use when routing traffic to your communications infrastructure.
  string connection_policy_sid = 2;
  // A descriptive string that you create to describe the resource. It is not unique and can be up to 255 characters long.
  string friendly_name = 3;
  // The SID of the SIP Domain that should be used in the `From` header of originating calls sent to your SIP infrastructure. If your SIP infrastructure allows users to "call back" an incoming call, configure this with a [SIP Domain](https://www.twilio.com/docs/voice/api/sending-sip) to ensure proper routing. If not configured, the from domain will default to "sip.twilio.com".
  string from_domain_sid = 4;
  // The HTTP method we should use to call `status_callback_url`. Can be: `GET` or `POST`.
  string status_callback_method = 5;
  // The URL that we should call to pass status parameters (such as call ended) to your application.
  string status_callback_url = 6;
  // The HTTP method we should use to call `voice_fallback_url`. Can be: `GET` or `POST`.
  string voice_fallback_method = 7;
  // The URL that we should call when an error occurs while retrieving or executing the TwiML requested by `voice_url`.
  string voice_fallback_url = 8;
  // The HTTP method we should use to call `voice_url`
  string voice_method = 9;
  // The URL we should call when the BYOC Trunk receives a call.
  string voice_url = 10;
}

message UpdateByocTrunkRequest {
  // The Twilio-provided string that uniquely identifies the BYOC Trunk resource to update. Required.
  string sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateByocTrunkRequestBody body = 2;
}

// Canonical success response.
message UpdateByocTrunkResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VoiceV1ByocTrunk body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeleteByocTrunkRequest {
  // The Twilio-provided string that uniquely identifies the BYOC Trunk resource to delete. Required.
  string sid = 1;
}

// Canonical success response.
message DeleteByocTrunkResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListConnectionPolicyResponse
message ListConnectionPolicyResponseBody {
  repeated VoiceV1ConnectionPolicy connection_policies = 1;
  ListConnectionPolicyResponseBodyMeta meta = 2;
}

message ListConnectionPolicyResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListConnectionPolicyRequest {
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 1;
  // The page index. This value is simply for client state.
  int64 page = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
}

// Canonical success response.
message ListConnectionPolicyResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListConnectionPolicyResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateConnectionPolicyRequest
message CreateConnectionPolicyRequestBody {
  // A descriptive string that you create to describe the resource. It is not unique and can be up to 255 characters long.
  string friendly_name = 1;
}

message CreateConnectionPolicyRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateConnectionPolicyRequestBody body = 1;
}

// Canonical success response.
message CreateConnectionPolicyResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VoiceV1ConnectionPolicy body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListConnectionPolicyTargetResponse
message ListConnectionPolicyTargetResponseBody {
  ListConnectionPolicyTargetResponseBodyMeta meta = 1;
  repeated VoiceV1ConnectionPolicyConnectionPolicyTarget targets = 2;
}

message ListConnectionPolicyTargetResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListConnectionPolicyTargetRequest {
  // The SID of the Connection Policy from which to read the Targets. Required.
  string connection_policy_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListConnectionPolicyTargetResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListConnectionPolicyTargetResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateConnectionPolicyTargetRequest
message CreateConnectionPolicyTargetRequestBody {
  // Whether the Target is enabled. The default is `true`.
  bool enabled = 1;
  // A descriptive string that you create to describe the resource. It is not unique and can be up to 255 characters long.
  string friendly_name = 2;
  // The relative importance of the target. Can be an integer from 0 to 65535, inclusive, and the default is 10. The lowest number represents the most important target.
  int64 priority = 3;
  // The SIP address you want Twilio to route your calls to. This must be a `sip:` schema. `sips` is NOT supported.
  string target = 4;
  // The value that determines the relative share of the load the Target should receive compared to other Targets with the same priority. Can be an integer from 1 to 65535, inclusive, and the default is 10. Targets with higher values receive more load than those with lower ones with the same priority.
  int64 weight = 5;
}

message CreateConnectionPolicyTargetRequest {
  // The SID of the Connection Policy that owns the Target. Required.
  string connection_policy_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateConnectionPolicyTargetRequestBody body = 2;
}

// Canonical success response.
message CreateConnectionPolicyTargetResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VoiceV1ConnectionPolicyConnectionPolicyTarget body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchConnectionPolicyTargetRequest {
  // The SID of the Connection Policy that owns the Target. Required.
  string connection_policy_sid = 1;
  // The unique string that we created to identify the Target resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchConnectionPolicyTargetResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VoiceV1ConnectionPolicyConnectionPolicyTarget body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateConnectionPolicyTargetRequest
message UpdateConnectionPolicyTargetRequestBody {
  // Whether the Target is enabled.
  bool enabled = 1;
  // A descriptive string that you create to describe the resource. It is not unique and can be up to 255 characters long.
  string friendly_name = 2;
  // The relative importance of the target. Can be an integer from 0 to 65535, inclusive. The lowest number represents the most important target.
  int64 priority = 3;
  // The SIP address you want Twilio to route your calls to. This must be a `sip:` schema. `sips` is NOT supported.
  string target = 4;
  // The value that determines the relative share of the load the Target should receive compared to other Targets with the same priority. Can be an integer from 1 to 65535, inclusive. Targets with higher values receive more load than those with lower ones with the same priority.
  int64 weight = 5;
}

message UpdateConnectionPolicyTargetRequest {
  // The SID of the Connection Policy that owns the Target. Required.
  string connection_policy_sid = 1;
  // The unique string that we created to identify the Target resource to update. Required.
  string sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateConnectionPolicyTargetRequestBody body = 3;
}

// Canonical success response.
message UpdateConnectionPolicyTargetResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VoiceV1ConnectionPolicyConnectionPolicyTarget body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeleteConnectionPolicyTargetRequest {
  // The SID of the Connection Policy that owns the Target. Required.
  string connection_policy_sid = 1;
  // The unique string that we created to identify the Target resource to delete. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteConnectionPolicyTargetResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

message FetchConnectionPolicyRequest {
  // The unique string that we created to identify the Connection Policy resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchConnectionPolicyResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VoiceV1ConnectionPolicy body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateConnectionPolicyRequest
message UpdateConnectionPolicyRequestBody {
  // A descriptive string that you create to describe the resource. It is not unique and can be up to 255 characters long.
  string friendly_name = 1;
}

message UpdateConnectionPolicyRequest {
  // The unique string that we created to identify the Connection Policy resource to update. Required.
  string sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateConnectionPolicyRequestBody body = 2;
}

// Canonical success response.
message UpdateConnectionPolicyResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VoiceV1ConnectionPolicy body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeleteConnectionPolicyRequest {
  // The unique string that we created to identify the Connection Policy resource to delete. Required.
  string sid = 1;
}

// Canonical success response.
message DeleteConnectionPolicyResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// CreateDialingPermissionsCountryBulkUpdateRequest
message CreateDialingPermissionsCountryBulkUpdateRequestBody {
  // URL encoded JSON array of update objects. example : `[ { "iso_code": "GB", "low_risk_numbers_enabled": "true", "high_risk_special_numbers_enabled":"true", "high_risk_tollfraud_numbers_enabled": "false" } ]`
  string update_request = 1;
}

// Create a bulk update request to change voice dialing country permissions of one or more countries identified by the corresponding [ISO country code](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)
message CreateDialingPermissionsCountryBulkUpdateRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateDialingPermissionsCountryBulkUpdateRequestBody body = 1;
}

// Canonical success response.
message CreateDialingPermissionsCountryBulkUpdateResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VoiceV1DialingPermissionsDialingPermissionsCountryBulkUpdate body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListDialingPermissionsCountryResponse
message ListDialingPermissionsCountryResponseBody {
  repeated VoiceV1DialingPermissionsDialingPermissionsCountry content = 1;
  ListDialingPermissionsCountryResponseBodyMeta meta = 2;
}

message ListDialingPermissionsCountryResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve all voice dialing country permissions for this account
message ListDialingPermissionsCountryRequest {
  // Filter to retrieve the country permissions by specifying the [ISO country code](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)
  string iso_code = 1;
  // Filter to retrieve the country permissions by specifying the continent
  string continent = 2;
  // Filter the results by specified [country codes](https://www.itu.int/itudoc/itu-t/ob-lists/icc/e164_763.html)
  string country_code = 3;
  // Filter to retrieve the country permissions with dialing to low-risk numbers enabled. Can be: `true` or `false`.
  bool low_risk_numbers_enabled = 4;
  // Filter to retrieve the country permissions with dialing to high-risk special service numbers enabled. Can be: `true` or `false`
  bool high_risk_special_numbers_enabled = 5;
  // Filter to retrieve the country permissions with dialing to high-risk [toll fraud](https://www.twilio.com/blog/how-to-protect-your-account-from-toll-fraud-with-voice-dialing-geo-permissions-html) numbers enabled. Can be: `true` or `false`.
  bool high_risk_tollfraud_numbers_enabled = 6;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 7;
  // The page index. This value is simply for client state.
  int64 page = 8;
  // The page token. This is provided by the API.
  string page_token = 9;
}

// Canonical success response.
message ListDialingPermissionsCountryResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListDialingPermissionsCountryResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Retrieve voice dialing country permissions identified by the given ISO country code
message FetchDialingPermissionsCountryRequest {
  // The [ISO country code](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) of the DialingPermissions Country resource to fetch Required.
  string iso_code = 1;
}

// Canonical success response.
message FetchDialingPermissionsCountryResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VoiceV1DialingPermissionsDialingPermissionsCountryInstance body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListDialingPermissionsHrsPrefixesResponse
message ListDialingPermissionsHrsPrefixesResponseBody {
  repeated VoiceV1DialingPermissionsDialingPermissionsCountryDialingPermissionsHrsPrefixes content = 1;
  ListDialingPermissionsHrsPrefixesResponseBodyMeta meta = 2;
}

message ListDialingPermissionsHrsPrefixesResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Fetch the high-risk special services prefixes from the country resource corresponding to the [ISO country code](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)
message ListDialingPermissionsHrsPrefixesRequest {
  // The [ISO 3166-1 country code](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) to identify the country permissions from which high-risk special service number prefixes are fetched Required.
  string iso_code = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListDialingPermissionsHrsPrefixesResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListDialingPermissionsHrsPrefixesResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListIpRecordResponse
message ListIpRecordResponseBody {
  repeated VoiceV1IpRecord ip_records = 1;
  ListIpRecordResponseBodyMeta meta = 2;
}

message ListIpRecordResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListIpRecordRequest {
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 1;
  // The page index. This value is simply for client state.
  int64 page = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
}

// Canonical success response.
message ListIpRecordResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListIpRecordResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateIpRecordRequest
message CreateIpRecordRequestBody {
  // An integer representing the length of the [CIDR](https://tools.ietf.org/html/rfc4632) prefix to use with this IP address. By default the entire IP address is used, which for IPv4 is value 32.
  int64 cidr_prefix_length = 1;
  // A descriptive string that you create to describe the resource. It is not unique and can be up to 255 characters long.
  string friendly_name = 2;
  // An IP address in dotted decimal notation, IPv4 only.
  string ip_address = 3;
}

message CreateIpRecordRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateIpRecordRequestBody body = 1;
}

// Canonical success response.
message CreateIpRecordResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VoiceV1IpRecord body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchIpRecordRequest {
  // The Twilio-provided string that uniquely identifies the IP Record resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchIpRecordResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VoiceV1IpRecord body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateIpRecordRequest
message UpdateIpRecordRequestBody {
  // A descriptive string that you create to describe the resource. It is not unique and can be up to 255 characters long.
  string friendly_name = 1;
}

message UpdateIpRecordRequest {
  // The Twilio-provided string that uniquely identifies the IP Record resource to update. Required.
  string sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateIpRecordRequestBody body = 2;
}

// Canonical success response.
message UpdateIpRecordResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VoiceV1IpRecord body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeleteIpRecordRequest {
  // The Twilio-provided string that uniquely identifies the IP Record resource to delete. Required.
  string sid = 1;
}

// Canonical success response.
message DeleteIpRecordResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// Retrieve voice dialing permissions inheritance for the sub-account
message FetchDialingPermissionsSettingsRequest {
}

// Canonical success response.
message FetchDialingPermissionsSettingsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VoiceV1DialingPermissionsDialingPermissionsSettings body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateDialingPermissionsSettingsRequest
message UpdateDialingPermissionsSettingsRequestBody {
  // `true` for the sub-account to inherit voice dialing permissions from the Master Project; otherwise `false`.
  bool dialing_permissions_inheritance = 1;
}

// Update voice dialing permissions inheritance for the sub-account
message UpdateDialingPermissionsSettingsRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateDialingPermissionsSettingsRequestBody body = 1;
}

// Canonical success response.
message UpdateDialingPermissionsSettingsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VoiceV1DialingPermissionsDialingPermissionsSettings body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListSourceIpMappingResponse
message ListSourceIpMappingResponseBody {
  ListSourceIpMappingResponseBodyMeta meta = 1;
  repeated VoiceV1SourceIpMapping source_ip_mappings = 2;
}

message ListSourceIpMappingResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListSourceIpMappingRequest {
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 1;
  // The page index. This value is simply for client state.
  int64 page = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
}

// Canonical success response.
message ListSourceIpMappingResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListSourceIpMappingResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateSourceIpMappingRequest
message CreateSourceIpMappingRequestBody {
  // The Twilio-provided string that uniquely identifies the IP Record resource to map from.
  string ip_record_sid = 1;
  // The SID of the SIP Domain that the IP Record should be mapped to.
  string sip_domain_sid = 2;
}

message CreateSourceIpMappingRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateSourceIpMappingRequestBody body = 1;
}

// Canonical success response.
message CreateSourceIpMappingResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VoiceV1SourceIpMapping body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchSourceIpMappingRequest {
  // The Twilio-provided string that uniquely identifies the IP Record resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchSourceIpMappingResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VoiceV1SourceIpMapping body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateSourceIpMappingRequest
message UpdateSourceIpMappingRequestBody {
  // The SID of the SIP Domain that the IP Record should be mapped to.
  string sip_domain_sid = 1;
}

message UpdateSourceIpMappingRequest {
  // The Twilio-provided string that uniquely identifies the IP Record resource to update. Required.
  string sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateSourceIpMappingRequestBody body = 2;
}

// Canonical success response.
message UpdateSourceIpMappingResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  VoiceV1SourceIpMapping body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeleteSourceIpMappingRequest {
  // The Twilio-provided string that uniquely identifies the IP Record resource to delete. Required.
  string sid = 1;
}

// Canonical success response.
message DeleteSourceIpMappingResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// Generated from VoiceV1ArchivedCall operations.
service VoiceV1ArchivedCallService {
  // Delete an archived call record from Bulk Export. Note: this does not also delete the record from the Voice API.
  rpc DeleteArchivedCall(DeleteArchivedCallRequest) returns (DeleteArchivedCallResponse) {
    option (google.api.http) = {
      delete: "/v1/Archives/{date}/Calls/{sid}"
    };
  }
}

// Generated from VoiceV1ByocTrunk operations.
service VoiceV1ByocTrunkService {
  rpc ListByocTrunk(ListByocTrunkRequest) returns (ListByocTrunkResponse) {
    option (google.api.http) = {
      get: "/v1/ByocTrunks"
      response_body: "body"
    };
  }
  rpc CreateByocTrunk(CreateByocTrunkRequest) returns (CreateByocTrunkResponse) {
    option (google.api.http) = {
      post: "/v1/ByocTrunks"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchByocTrunk(FetchByocTrunkRequest) returns (FetchByocTrunkResponse) {
    option (google.api.http) = {
      get: "/v1/ByocTrunks/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateByocTrunk(UpdateByocTrunkRequest) returns (UpdateByocTrunkResponse) {
    option (google.api.http) = {
      post: "/v1/ByocTrunks/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteByocTrunk(DeleteByocTrunkRequest) returns (DeleteByocTrunkResponse) {
    option (google.api.http) = {
      delete: "/v1/ByocTrunks/{sid}"
    };
  }
}

// Generated from VoiceV1ConnectionPolicy operations.
service VoiceV1ConnectionPolicyService {
  rpc ListConnectionPolicy(ListConnectionPolicyRequest) returns (ListConnectionPolicyResponse) {
    option (google.api.http) = {
      get: "/v1/ConnectionPolicies"
      response_body: "body"
    };
  }
  rpc CreateConnectionPolicy(CreateConnectionPolicyRequest) returns (CreateConnectionPolicyResponse) {
    option (google.api.http) = {
      post: "/v1/ConnectionPolicies"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchConnectionPolicy(FetchConnectionPolicyRequest) returns (FetchConnectionPolicyResponse) {
    option (google.api.http) = {
      get: "/v1/ConnectionPolicies/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateConnectionPolicy(UpdateConnectionPolicyRequest) returns (UpdateConnectionPolicyResponse) {
    option (google.api.http) = {
      post: "/v1/ConnectionPolicies/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteConnectionPolicy(DeleteConnectionPolicyRequest) returns (DeleteConnectionPolicyResponse) {
    option (google.api.http) = {
      delete: "/v1/ConnectionPolicies/{sid}"
    };
  }
}

// Generated from VoiceV1ConnectionPolicyTarget operations.
service VoiceV1ConnectionPolicyTargetService {
  rpc ListConnectionPolicyTarget(ListConnectionPolicyTargetRequest) returns (ListConnectionPolicyTargetResponse) {
    option (google.api.http) = {
      get: "/v1/ConnectionPolicies/{connection_policy_sid}/Targets"
      response_body: "body"
    };
  }
  rpc CreateConnectionPolicyTarget(CreateConnectionPolicyTargetRequest) returns (CreateConnectionPolicyTargetResponse) {
    option (google.api.http) = {
      post: "/v1/ConnectionPolicies/{connection_policy_sid}/Targets"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchConnectionPolicyTarget(FetchConnectionPolicyTargetRequest) returns (FetchConnectionPolicyTargetResponse) {
    option (google.api.http) = {
      get: "/v1/ConnectionPolicies/{connection_policy_sid}/Targets/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateConnectionPolicyTarget(UpdateConnectionPolicyTargetRequest) returns (UpdateConnectionPolicyTargetResponse) {
    option (google.api.http) = {
      post: "/v1/ConnectionPolicies/{connection_policy_sid}/Targets/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteConnectionPolicyTarget(DeleteConnectionPolicyTargetRequest) returns (DeleteConnectionPolicyTargetResponse) {
    option (google.api.http) = {
      delete: "/v1/ConnectionPolicies/{connection_policy_sid}/Targets/{sid}"
    };
  }
}

// Generated from VoiceV1BulkCountryUpdate operations.
service VoiceV1BulkCountryUpdateService {
  // Create a bulk update request to change voice dialing country permissions of one or more countries identified by the corresponding [ISO country code](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)
  rpc CreateDialingPermissionsCountryBulkUpdate(CreateDialingPermissionsCountryBulkUpdateRequest) returns (CreateDialingPermissionsCountryBulkUpdateResponse) {
    option (google.api.http) = {
      post: "/v1/DialingPermissions/BulkCountryUpdates"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from VoiceV1Country operations.
service VoiceV1CountryService {
  // Retrieve all voice dialing country permissions for this account
  rpc ListDialingPermissionsCountry(ListDialingPermissionsCountryRequest) returns (ListDialingPermissionsCountryResponse) {
    option (google.api.http) = {
      get: "/v1/DialingPermissions/Countries"
      response_body: "body"
    };
  }
  // Retrieve voice dialing country permissions identified by the given ISO country code
  rpc FetchDialingPermissionsCountry(FetchDialingPermissionsCountryRequest) returns (FetchDialingPermissionsCountryResponse) {
    option (google.api.http) = {
      get: "/v1/DialingPermissions/Countries/{iso_code}"
      response_body: "body"
    };
  }
}

// Generated from VoiceV1HighriskSpecialPrefix operations.
service VoiceV1HighriskSpecialPrefixService {
  // Fetch the high-risk special services prefixes from the country resource corresponding to the [ISO country code](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)
  rpc ListDialingPermissionsHrsPrefixes(ListDialingPermissionsHrsPrefixesRequest) returns (ListDialingPermissionsHrsPrefixesResponse) {
    option (google.api.http) = {
      get: "/v1/DialingPermissions/Countries/{iso_code}/HighRiskSpecialPrefixes"
      response_body: "body"
    };
  }
}

// Generated from VoiceV1IpRecord operations.
service VoiceV1IpRecordService {
  rpc ListIpRecord(ListIpRecordRequest) returns (ListIpRecordResponse) {
    option (google.api.http) = {
      get: "/v1/IpRecords"
      response_body: "body"
    };
  }
  rpc CreateIpRecord(CreateIpRecordRequest) returns (CreateIpRecordResponse) {
    option (google.api.http) = {
      post: "/v1/IpRecords"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchIpRecord(FetchIpRecordRequest) returns (FetchIpRecordResponse) {
    option (google.api.http) = {
      get: "/v1/IpRecords/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateIpRecord(UpdateIpRecordRequest) returns (UpdateIpRecordResponse) {
    option (google.api.http) = {
      post: "/v1/IpRecords/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteIpRecord(DeleteIpRecordRequest) returns (DeleteIpRecordResponse) {
    option (google.api.http) = {
      delete: "/v1/IpRecords/{sid}"
    };
  }
}

// Generated from VoiceV1Settings operations.
service VoiceV1SettingsService {
  // Retrieve voice dialing permissions inheritance for the sub-account
  rpc FetchDialingPermissionsSettings(FetchDialingPermissionsSettingsRequest) returns (FetchDialingPermissionsSettingsResponse) {
    option (google.api.http) = {
      get: "/v1/Settings"
      response_body: "body"
    };
  }
  // Update voice dialing permissions inheritance for the sub-account
  rpc UpdateDialingPermissionsSettings(UpdateDialingPermissionsSettingsRequest) returns (UpdateDialingPermissionsSettingsResponse) {
    option (google.api.http) = {
      post: "/v1/Settings"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from VoiceV1SourceIpMapping operations.
service VoiceV1SourceIpMappingService {
  rpc ListSourceIpMapping(ListSourceIpMappingRequest) returns (ListSourceIpMappingResponse) {
    option (google.api.http) = {
      get: "/v1/SourceIpMappings"
      response_body: "body"
    };
  }
  rpc CreateSourceIpMapping(CreateSourceIpMappingRequest) returns (CreateSourceIpMappingResponse) {
    option (google.api.http) = {
      post: "/v1/SourceIpMappings"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchSourceIpMapping(FetchSourceIpMappingRequest) returns (FetchSourceIpMappingResponse) {
    option (google.api.http) = {
      get: "/v1/SourceIpMappings/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateSourceIpMapping(UpdateSourceIpMappingRequest) returns (UpdateSourceIpMappingResponse) {
    option (google.api.http) = {
      post: "/v1/SourceIpMappings/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteSourceIpMapping(DeleteSourceIpMappingRequest) returns (DeleteSourceIpMappingResponse) {
    option (google.api.http) = {
      delete: "/v1/SourceIpMappings/{sid}"
    };
  }
}

