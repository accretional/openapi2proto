// Code generated by openapi2proto. DO NOT EDIT.
// Source: twilio_intelligence_v2.json

syntax = "proto3";

package twilio.intelligence.v2;

option go_package = "twilio/intelligence/v2;twiliointelligencev2";

import "google/api/annotations.proto";

// Custom Operator availability status. Possible values: internal, beta, public, retired.
message CustomOperatorEnumAvailability {
  // Scalar value wrapper.
  string value = 1;
}

message IntelligenceV2CustomOperator {
  // The unique SID identifier of the Account the Custom Operator belongs to.
  string account_sid = 1;
  // The creator of the Custom Operator. Custom Operators can only be created by a Twilio Account.
  string author = 2;
  // OpenAPI property "availability".
  CustomOperatorEnumAvailability availability = 3;
  // Operator configuration, following the schema defined by the Operator Type. Only available on Operators created by the Account.
  string config = 4;
  // The date that this Custom Operator was created, given in ISO 8601 format.
  string date_created = 5;
  // The date that this Custom Operator was updated, given in ISO 8601 format.
  string date_updated = 6;
  // A human-readable description of this resource, longer than the friendly name.
  string description = 7;
  // A human-readable name of this resource, up to 64 characters.
  string friendly_name = 8;
  // Operator Type for this Operator. References an existing Operator Type resource.
  string operator_type = 9;
  // A 34 character string that uniquely identifies this Custom Operator.
  string sid = 10;
  // The URL of this resource.
  string url = 11;
  // Numeric Custom Operator version. Incremented with each update on the resource, used to ensure integrity when updating the Custom Operator.
  int64 version = 12;
}

message IntelligenceV2Operator {
  // The unique SID identifier of the Account the Operator belongs to.
  string account_sid = 1;
  // The creator of the Operator. Either Twilio or the creating Account.
  string author = 2;
  // OpenAPI property "availability".
  OperatorEnumAvailability availability = 3;
  // Operator configuration, following the schema defined by the Operator Type. Only available on Custom Operators created by the Account.
  string config = 4;
  // The date that this Operator was created, given in ISO 8601 format.
  string date_created = 5;
  // The date that this Operator was updated, given in ISO 8601 format.
  string date_updated = 6;
  // A human-readable description of this resource, longer than the friendly name.
  string description = 7;
  // A human-readable name of this resource, up to 64 characters.
  string friendly_name = 8;
  // Operator Type for this Operator. References an existing Operator Type resource.
  string operator_type = 9;
  // A 34 character string that uniquely identifies this Operator.
  string sid = 10;
  // The URL of this resource.
  string url = 11;
  // Numeric Operator version. Incremented with each update on the resource, used to ensure integrity when updating the Operator.
  int64 version = 12;
}

// Operator availability status. Possible values: internal, beta, public, retired.
message OperatorEnumAvailability {
  // Scalar value wrapper.
  string value = 1;
}

message IntelligenceV2OperatorAttachment {
  // The unique SID identifier of the Operator.
  string operator_sid = 1;
  // The unique SID identifier of the Service.
  string service_sid = 2;
  // The URL of this resource.
  string url = 3;
}

message IntelligenceV2OperatorAttachments {
  // List of Operator SIDs attached to the service. Includes both Custom and Pre-built Operators.
  repeated string operator_sids = 1;
  // The unique SID identifier of the Service.
  string service_sid = 2;
  // The URL of this resource.
  string url = 3;
}

message IntelligenceV2OperatorType {
  // OpenAPI property "availability".
  OperatorTypeEnumAvailability availability = 1;
  // JSON Schema for configuring an Operator with this Operator Type. Following https://json-schema.org/
  string config_schema = 2;
  // Operators can be created from configurable Operator Types.
  bool configurable = 3;
  // The date that this Operator Type was created, given in ISO 8601 format.
  string date_created = 4;
  // The date that this Operator Type was updated, given in ISO 8601 format.
  string date_updated = 5;
  // A human-readable description of this resource, longer than the friendly name.
  string description = 6;
  // Additional documentation for the Operator Type.
  string docs_link = 7;
  // A human-readable name of this resource, up to 64 characters.
  string friendly_name = 8;
  // A unique name that references an Operator's Operator Type.
  string name = 9;
  // OpenAPI property "output_type".
  OperatorTypeEnumOutputType output_type = 10;
  // OpenAPI property "provider".
  OperatorTypeEnumProvider provider = 11;
  // A 34 character string that uniquely identifies this Operator Type.
  string sid = 12;
  // List of languages this Operator Type supports.
  repeated string supported_languages = 13;
  // The URL of this resource.
  string url = 14;
}

// Operator Type availability status. Possible values: internal, beta, general-availability, retired, deprecated.
message OperatorTypeEnumAvailability {
  // Scalar value wrapper.
  string value = 1;
}

// Operator Results for this Operator Type will follow this format. Possible values: text-classification, text-extraction, text-extraction-normalized, text-generation.
message OperatorTypeEnumOutputType {
  // Scalar value wrapper.
  string value = 1;
}

// Operators with this Operator Type are executed using this provider. Possible values: twilio, amazon, openai.
message OperatorTypeEnumProvider {
  // Scalar value wrapper.
  string value = 1;
}

message IntelligenceV2PrebuiltOperator {
  // The unique SID identifier of the Account the Pre-built Operator belongs to.
  string account_sid = 1;
  // The creator of the Operator. Pre-built Operators can only be created by Twilio.
  string author = 2;
  // OpenAPI property "availability".
  PrebuiltOperatorEnumAvailability availability = 3;
  // Operator configuration, following the schema defined by the Operator Type. Only available on Custom Operators created by the Account, will be empty for Pre-Built Operators.
  string config = 4;
  // The date that this Pre-built Operator was created, given in ISO 8601 format.
  string date_created = 5;
  // The date that this Pre-built Operator was updated, given in ISO 8601 format.
  string date_updated = 6;
  // A human-readable description of this resource, longer than the friendly name.
  string description = 7;
  // A human-readable name of this resource, up to 64 characters.
  string friendly_name = 8;
  // Operator Type for this Operator. References an existing Operator Type resource.
  string operator_type = 9;
  // A 34 character string that uniquely identifies this Pre-built Operator.
  string sid = 10;
  // The URL of this resource.
  string url = 11;
  // Numeric Operator version. Incremented with each update on the resource, used to ensure integrity when updating the Operator.
  int64 version = 12;
}

// Operator availability status. Possible values: internal, beta, public, retired.
message PrebuiltOperatorEnumAvailability {
  // Scalar value wrapper.
  string value = 1;
}

message IntelligenceV2Service {
  // The unique SID identifier of the Account the Service belongs to.
  string account_sid = 1;
  // Instructs the Speech Recognition service to automatically redact PII from all transcripts made on this service.
  bool auto_redaction = 2;
  // Instructs the Speech Recognition service to automatically transcribe all recordings made on the account.
  bool auto_transcribe = 3;
  // Data logging allows Twilio to improve the quality of the speech recognition & language understanding services through using customer data to refine, fine tune and evaluate machine learning models.
  // Note: Data logging cannot be activated via API, only via www.twilio.com, as it requires additional consent.
  bool data_logging = 4;
  // The date that this Service was created, given in ISO 8601 format.
  string date_created = 5;
  // The date that this Service was updated, given in ISO 8601 format.
  string date_updated = 6;
  // The unique SID identifier of the Public Key resource used to encrypt the sentences and operator results.
  string encryption_credential_sid = 7;
  // A human readable description of this resource, up to 64 characters.
  string friendly_name = 8;
  // The language code set during Service creation determines the Transcription language for all call recordings processed by that Service. The default is en-US if no language code is set. A Service can only support one language code, and it cannot be updated once it's set.
  string language_code = 9;
  // Instructs the Speech Recognition service to automatically redact PII from all transcripts media made on this service. The auto_redaction flag must be enabled, results in error otherwise.
  bool media_redaction = 10;
  // Operator sids attached to this service, read only
  repeated string read_only_attached_operator_sids = 11;
  // A 34 character string that uniquely identifies this Service.
  string sid = 12;
  // Provides a unique and addressable name to be assigned to this Service, assigned by the developer, to be optionally used in addition to SID.
  string unique_name = 13;
  // The URL of this resource.
  string url = 14;
  // The version number of this Service.
  int64 version = 15;
  // OpenAPI property "webhook_http_method".
  ServiceEnumHttpMethod webhook_http_method = 16;
  // The URL Twilio will request when executing the Webhook.
  string webhook_url = 17;
}

// The HTTP method for the Webhook. One of `GET` or `POST`.
message ServiceEnumHttpMethod {
  // Scalar value wrapper.
  string value = 1;
}

message IntelligenceV2Transcript {
  // The unique SID identifier of the Account.
  string account_sid = 1;
  // Media Channel describing Transcript Source and Participant Mapping
  string channel = 2;
  string customer_key = 3;
  // Data logging allows Twilio to improve the quality of the speech recognition & language understanding services through using customer data to refine, fine tune and evaluate machine learning models.
  // Note: Data logging cannot be activated via API, only via www.twilio.com, as it requires additional consent.
  bool data_logging = 4;
  // The date that this Transcript was created, given in ISO 8601 format.
  string date_created = 5;
  // The date that this Transcript was updated, given in ISO 8601 format.
  string date_updated = 6;
  // The duration of this Transcript's source
  int64 duration = 7;
  // The unique SID identifier of the Public Key resource used to encrypt the sentences and operator results.
  string encryption_credential_sid = 8;
  // The default language code of the audio.
  string language_code = 9;
  IntelligenceV2TranscriptLinks links = 10;
  // The date that this Transcript's media was started, given in ISO 8601 format.
  string media_start_time = 11;
  // If the transcript has been redacted, a redacted alternative of the transcript will be available.
  bool redaction = 12;
  // The unique SID identifier of the Service.
  string service_sid = 13;
  // A 34 character string that uniquely identifies this Transcript.
  string sid = 14;
  // OpenAPI property "status".
  TranscriptEnumStatus status = 15;
  // The URL of this resource.
  string url = 16;
}

message IntelligenceV2TranscriptLinks {
  // Scalar value wrapper.
  string value = 1;
}

// The Status of this Transcript. One of `queued`, `in-progress`, `completed`, `failed` or `canceled`.
message TranscriptEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

message IntelligenceV2TranscriptEncryptedOperatorResults {
  // The locations of the encrypted operator results.
  repeated string locations = 1;
  string transcript_sid = 2;
  string url = 3;
}

message IntelligenceV2TranscriptEncryptedSentences {
  // The location of the encrypted sentences.
  string location = 1;
  string transcript_sid = 2;
  string url = 3;
}

message IntelligenceV2TranscriptMedia {
  // The unique SID identifier of the Account.
  string account_sid = 1;
  // Downloadable URL for media, if stored in Twilio AI.
  string media_url = 2;
  // The unique SID identifier of the Service.
  string service_sid = 3;
  // The unique SID identifier of the Transcript.
  string sid = 4;
  // The URL of this resource.
  string url = 5;
}

message IntelligenceV2TranscriptOperatorResult {
  // Boolean to tell if extract Language Understanding Processing model matches results.
  bool extract_match = 1;
  // List of text extraction results. This might be available on classify-extract model outputs.
  string extract_results = 2;
  string json_results = 3;
  // The labels probabilities. This might be available on conversation classify model outputs.
  string label_probabilities = 4;
  // Percentage of 'matching' class needed to consider a sentence matches
  double match_probability = 5;
  // The name of the applied Language Understanding.
  string name = 6;
  // Normalized output of extraction stage which matches Label.
  string normalized_result = 7;
  // A 34 character string that identifies this Language Understanding operator sid.
  string operator_sid = 8;
  // OpenAPI property "operator_type".
  OperatorResultEnumOperatorType operator_type = 9;
  // The 'matching' class. This might be available on conversation classify model outputs.
  string predicted_label = 10;
  // Percentage of 'matching' class needed to consider a sentence matches.
  double predicted_probability = 11;
  // Output of a text generation operator for example Conversation Sumamary.
  string text_generation_results = 12;
  // A 34 character string that uniquely identifies this Transcript.
  string transcript_sid = 13;
  // The URL of this resource.
  string url = 14;
  // Boolean to tell if Utterance matches results.
  bool utterance_match = 15;
  // List of mapped utterance object which matches sentences.
  repeated string utterance_results = 16;
}

// The type of the applied Language Understanding Operator. One of conversation-classify, utterance-classify, extract, extract-normalize, or pii-extract
message OperatorResultEnumOperatorType {
  // Scalar value wrapper.
  string value = 1;
}

message IntelligenceV2TranscriptSentence {
  string confidence = 1;
  // Offset from the beginning of the transcript when this sentence ends.
  string end_time = 2;
  // The channel number.
  int64 media_channel = 3;
  // The index of the sentence in the transcript.
  int64 sentence_index = 4;
  // A 34 character string that uniquely identifies this Sentence.
  string sid = 5;
  // Offset from the beginning of the transcript when this sentence starts.
  string start_time = 6;
  // Transcript text.
  string transcript = 7;
  // Detailed information for each of the words of the given Sentence.
  repeated string words = 8;
}

message TranscriptEnumCallDirection {
  // Scalar value wrapper.
  string value = 1;
}

// ListOperatorTypeResponse
message ListOperatorTypeResponseBody {
  ListOperatorTypeResponseBodyMeta meta = 1;
  repeated IntelligenceV2OperatorType operator_types = 2;
}

message ListOperatorTypeResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieves a list of all Operator Types for an Account.
message ListOperatorTypeRequest {
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 1;
  // The page index. This value is simply for client state.
  int64 page = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
  // Returns Operator Types that support the provided language code.
  string language_code = 4;
}

// Canonical success response.
message ListOperatorTypeResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListOperatorTypeResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// Fetch a specific Operator Type.
message FetchOperatorTypeRequest {
  // Either a 34 character string that uniquely identifies this Operator Type or the unique name that references an Operator Type. Required.
  string sid = 1;
}

// Canonical success response.
message FetchOperatorTypeResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IntelligenceV2OperatorType body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// ListOperatorResponse
message ListOperatorResponseBody {
  ListOperatorResponseBodyMeta meta = 1;
  repeated IntelligenceV2Operator operators = 2;
}

message ListOperatorResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieves a list of all Operators for an Account, both Custom and Pre-built.
message ListOperatorRequest {
  // Returns Operators with the provided availability type. Possible values: internal, beta, public, retired.
  OperatorEnumAvailability availability = 1;
  // Returns Operators that support the provided language code.
  string language_code = 2;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 3;
  // The page index. This value is simply for client state.
  int64 page = 4;
  // The page token. This is provided by the API.
  string page_token = 5;
}

// Canonical success response.
message ListOperatorResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListOperatorResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// ListCustomOperatorResponse
message ListCustomOperatorResponseBody {
  ListCustomOperatorResponseBodyMeta meta = 1;
  repeated IntelligenceV2CustomOperator operators = 2;
}

message ListCustomOperatorResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieves a list of all Custom Operators for an Account.
message ListCustomOperatorRequest {
  // Returns Custom Operators with the provided availability type. Possible values: internal, beta, public, retired.
  CustomOperatorEnumAvailability availability = 1;
  // Returns Custom Operators that support the provided language code.
  string language_code = 2;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 3;
  // The page index. This value is simply for client state.
  int64 page = 4;
  // The page token. This is provided by the API.
  string page_token = 5;
}

// Canonical success response.
message ListCustomOperatorResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListCustomOperatorResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// CreateCustomOperatorRequest
message CreateCustomOperatorRequestBody {
  // Operator configuration, following the schema defined by the Operator Type.
  string config = 1;
  // A human readable description of the new Operator, up to 64 characters.
  string friendly_name = 2;
  // Operator Type for this Operator. References an existing Operator Type resource.
  string operator_type = 3;
}

// Create a new Custom Operator for the given Account
message CreateCustomOperatorRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateCustomOperatorRequestBody body = 1;
}

// Canonical success response.
message CreateCustomOperatorResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IntelligenceV2CustomOperator body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 9;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 10;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 11;
}

// Fetch a specific Custom Operator.
message FetchCustomOperatorRequest {
  // A 34 character string that uniquely identifies this Custom Operator. Required.
  string sid = 1;
}

// Canonical success response.
message FetchCustomOperatorResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IntelligenceV2CustomOperator body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 9;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 10;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 11;
}

// UpdateCustomOperatorRequest
message UpdateCustomOperatorRequestBody {
  // Operator configuration, following the schema defined by the Operator Type.
  string config = 1;
  // A human-readable name of this resource, up to 64 characters.
  string friendly_name = 2;
}

// Update a specific Custom Operator.
message UpdateCustomOperatorRequest {
  // A 34 character string that uniquely identifies this Custom Operator. Required.
  string sid = 1;
  // The If-Match HTTP request header
  string header_if_match = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateCustomOperatorRequestBody body = 3;
}

// Canonical success response.
message UpdateCustomOperatorResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IntelligenceV2CustomOperator body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 9;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 10;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 11;
}

// Delete a specific Custom Operator.
message DeleteCustomOperatorRequest {
  // A 34 character string that uniquely identifies this Custom Operator. Required.
  string sid = 1;
}

// Canonical success response.
message DeleteCustomOperatorResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 2;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 3;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 4;
}

// ListPrebuiltOperatorResponse
message ListPrebuiltOperatorResponseBody {
  ListPrebuiltOperatorResponseBodyMeta meta = 1;
  repeated IntelligenceV2PrebuiltOperator operators = 2;
}

message ListPrebuiltOperatorResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieves a list of all Pre-built Operators for an account.
message ListPrebuiltOperatorRequest {
  // Returns Pre-built Operators with the provided availability type. Possible values: internal, beta, public, retired.
  PrebuiltOperatorEnumAvailability availability = 1;
  // Returns Pre-built Operators that support the provided language code.
  string language_code = 2;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 3;
  // The page index. This value is simply for client state.
  int64 page = 4;
  // The page token. This is provided by the API.
  string page_token = 5;
}

// Canonical success response.
message ListPrebuiltOperatorResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListPrebuiltOperatorResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// Fetch a specific Pre-built Operator.
message FetchPrebuiltOperatorRequest {
  // A 34 character string that uniquely identifies this Pre-built Operator. Required.
  string sid = 1;
}

// Canonical success response.
message FetchPrebuiltOperatorResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IntelligenceV2PrebuiltOperator body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// Fetch a specific Operator. Works for both Custom and Pre-built Operators.
message FetchOperatorRequest {
  // A 34 character string that uniquely identifies this Operator. Required.
  string sid = 1;
}

// Canonical success response.
message FetchOperatorResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IntelligenceV2Operator body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// ListServiceResponse
message ListServiceResponseBody {
  ListServiceResponseBodyMeta meta = 1;
  repeated IntelligenceV2Service services = 2;
}

message ListServiceResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieves a list of all Services for an account.
message ListServiceRequest {
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 1;
  // The page index. This value is simply for client state.
  int64 page = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
}

// Canonical success response.
message ListServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListServiceResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// CreateServiceRequest
message CreateServiceRequestBody {
  // Instructs the Speech Recognition service to automatically redact PII from all transcripts made on this service.
  bool auto_redaction = 1;
  // Instructs the Speech Recognition service to automatically transcribe all recordings made on the account.
  bool auto_transcribe = 2;
  // Data logging allows Twilio to improve the quality of the speech recognition & language understanding services through using customer data to refine, fine tune and evaluate machine learning models.
  // Note: Data logging cannot be activated via API, only via www.twilio.com, as it requires additional consent.
  bool data_logging = 3;
  // The unique SID identifier of the Public Key resource used to encrypt the sentences and operator results.
  string encryption_credential_sid = 4;
  // A human readable description of this resource, up to 64 characters.
  string friendly_name = 5;
  // The language code set during Service creation determines the Transcription language for all call recordings processed by that Service. The default is en-US if no language code is set. A Service can only support one language code, and it cannot be updated once it's set.
  string language_code = 6;
  // Instructs the Speech Recognition service to automatically redact PII from all transcripts media made on this service. The auto_redaction flag must be enabled, results in error otherwise.
  bool media_redaction = 7;
  // Provides a unique and addressable name to be assigned to this Service, assigned by the developer, to be optionally used in addition to SID.
  string unique_name = 8;
  // OpenAPI property "WebhookHttpMethod".
  ServiceEnumHttpMethod webhook_http_method = 9;
  // The URL Twilio will request when executing the Webhook.
  string webhook_url = 10;
}

// Create a new Service for the given Account
message CreateServiceRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateServiceRequestBody body = 1;
}

// Canonical success response.
message CreateServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IntelligenceV2Service body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 9;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 10;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 11;
}

// Retrieve Operators attached to a Service.
message FetchOperatorAttachmentsRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
}

// Canonical success response.
message FetchOperatorAttachmentsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IntelligenceV2OperatorAttachments body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Attach an Operator to a Service.
message CreateOperatorAttachmentRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // The unique SID identifier of the Operator. Allows both Custom and Pre-built Operators. Required.
  string operator_sid = 2;
}

// Canonical success response.
message CreateOperatorAttachmentResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IntelligenceV2OperatorAttachment body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Detach an Operator from a Service.
message DeleteOperatorAttachmentRequest {
  // The unique SID identifier of the Service. Required.
  string service_sid = 1;
  // The unique SID identifier of the Operator. Allows both Custom and Pre-built Operators. Required.
  string operator_sid = 2;
}

// Canonical success response.
message DeleteOperatorAttachmentResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// Fetch a specific Service.
message FetchServiceRequest {
  // A 34 character string that uniquely identifies this Service. Required.
  string sid = 1;
}

// Canonical success response.
message FetchServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IntelligenceV2Service body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 9;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 10;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 11;
}

// UpdateServiceRequest
message UpdateServiceRequestBody {
  // Instructs the Speech Recognition service to automatically redact PII from all transcripts made on this service.
  bool auto_redaction = 1;
  // Instructs the Speech Recognition service to automatically transcribe all recordings made on the account.
  bool auto_transcribe = 2;
  // Data logging allows Twilio to improve the quality of the speech recognition & language understanding services through using customer data to refine, fine tune and evaluate machine learning models.
  // Note: Data logging cannot be activated via API, only via www.twilio.com, as it requires additional consent.
  bool data_logging = 3;
  // The unique SID identifier of the Public Key resource used to encrypt the sentences and operator results.
  string encryption_credential_sid = 4;
  // A human readable description of this resource, up to 64 characters.
  string friendly_name = 5;
  // Instructs the Speech Recognition service to automatically redact PII from all transcripts media made on this service. The auto_redaction flag must be enabled, results in error otherwise.
  bool media_redaction = 6;
  // Provides a unique and addressable name to be assigned to this Service, assigned by the developer, to be optionally used in addition to SID.
  string unique_name = 7;
  // OpenAPI property "WebhookHttpMethod".
  ServiceEnumHttpMethod webhook_http_method = 8;
  // The URL Twilio will request when executing the Webhook.
  string webhook_url = 9;
}

// Update a specific Service.
message UpdateServiceRequest {
  // A 34 character string that uniquely identifies this Service. Required.
  string sid = 1;
  // The If-Match HTTP request header
  string header_if_match = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateServiceRequestBody body = 3;
}

// Canonical success response.
message UpdateServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IntelligenceV2Service body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 9;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 10;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 11;
}

// Delete a specific Service.
message DeleteServiceRequest {
  // A 34 character string that uniquely identifies this Service. Required.
  string sid = 1;
}

// Canonical success response.
message DeleteServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response header "ETag".
  string header_etag = 2;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 3;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 4;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 5;
}

// ListTranscriptResponse
message ListTranscriptResponseBody {
  ListTranscriptResponseBodyMeta meta = 1;
  repeated IntelligenceV2Transcript transcripts = 2;
}

message ListTranscriptResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of Transcripts for a given service.
message ListTranscriptRequest {
  // The unique SID identifier of the Service.
  string service_sid = 1;
  // Filter by before StartTime.
  string before_start_time = 2;
  // Filter by after StartTime.
  string after_start_time = 3;
  // Filter by before DateCreated.
  string before_date_created = 4;
  // Filter by after DateCreated.
  string after_date_created = 5;
  // Filter by status.
  string status = 6;
  // Filter by Language Code.
  string language_code = 7;
  // Filter by SourceSid.
  string source_sid = 8;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 9;
  // The page index. This value is simply for client state.
  int64 page = 10;
  // The page token. This is provided by the API.
  string page_token = 11;
}

// Canonical success response.
message ListTranscriptResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListTranscriptResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// CreateTranscriptRequest
message CreateTranscriptRequestBody {
  // JSON object describing Media Channel including Source and Participants
  string channel = 1;
  // Used to store client provided metadata. Maximum of 64 double-byte UTF8 characters.
  string customer_key = 2;
  // The date that this Transcript's media was started, given in ISO 8601 format.
  string media_start_time = 3;
  // The unique SID identifier of the Service.
  string service_sid = 4;
}

// Create a new Transcript for the service
message CreateTranscriptRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateTranscriptRequestBody body = 1;
}

// Canonical success response.
message CreateTranscriptResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IntelligenceV2Transcript body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// Fetch a specific Transcript.
message FetchTranscriptRequest {
  // A 34 character string that uniquely identifies this Transcript. Required.
  string sid = 1;
}

// Canonical success response.
message FetchTranscriptResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IntelligenceV2Transcript body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// Delete a specific Transcript.
message DeleteTranscriptRequest {
  // A 34 character string that uniquely identifies this Transcript. Required.
  string sid = 1;
}

// Canonical success response.
message DeleteTranscriptResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 2;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 3;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 4;
}

// Get download URLs for media if possible
message FetchMediaRequest {
  // The unique SID identifier of the Transcript. Required.
  string sid = 1;
  // Grant access to PII Redacted/Unredacted Media. If redaction is enabled, the default is `true` to access redacted media.
  bool redacted = 2;
}

// Canonical success response.
message FetchMediaResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IntelligenceV2TranscriptMedia body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// ListOperatorResultResponse
message ListOperatorResultResponseBody {
  ListOperatorResultResponseBodyMeta meta = 1;
  repeated IntelligenceV2TranscriptOperatorResult operator_results = 2;
}

message ListOperatorResultResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of Operator Results for the given Transcript.
message ListOperatorResultRequest {
  // A 34 character string that uniquely identifies this Transcript. Required.
  string transcript_sid = 1;
  // Grant access to PII redacted/unredacted Language Understanding operator. If redaction is enabled, the default is True.
  bool redacted = 2;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 3;
  // The page index. This value is simply for client state.
  int64 page = 4;
  // The page token. This is provided by the API.
  string page_token = 5;
}

// Canonical success response.
message ListOperatorResultResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListOperatorResultResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// Fetch Public Key Encrypted Operator Results by TranscriptSid
message FetchEncryptedOperatorResultsRequest {
  // The unique SID identifier of the Transcript. Required.
  string transcript_sid = 1;
  // Grant access to PII Redacted/Unredacted Operator Results. If redaction is enabled, the default is `true` to access redacted operator results.
  bool redacted = 2;
}

// Canonical success response.
message FetchEncryptedOperatorResultsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IntelligenceV2TranscriptEncryptedOperatorResults body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch a specific Operator Result for the given Transcript.
message FetchOperatorResultRequest {
  // A 34 character string that uniquely identifies this Transcript. Required.
  string transcript_sid = 1;
  // A 34 character string that identifies this Language Understanding operator sid. Required.
  string operator_sid = 2;
  // Grant access to PII redacted/unredacted Language Understanding operator. If redaction is enabled, the default is True.
  bool redacted = 3;
}

// Canonical success response.
message FetchOperatorResultResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IntelligenceV2TranscriptOperatorResult body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// ListSentenceResponse
message ListSentenceResponseBody {
  ListSentenceResponseBodyMeta meta = 1;
  repeated IntelligenceV2TranscriptSentence sentences = 2;
}

message ListSentenceResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Get all Transcript Sentences by TranscriptSid
message ListSentenceRequest {
  // The unique SID identifier of the Transcript. Required.
  string transcript_sid = 1;
  // Grant access to PII Redacted/Unredacted Sentences. If redaction is enabled, the default is `true` to access redacted sentences.
  bool redacted = 2;
  // Returns word level timestamps information, if word_timestamps is enabled. The default is `false`.
  bool word_timestamps = 3;
  // How many resources to return in each list page. The default is 50, and the maximum is 5000.
  int64 page_size = 4;
  // The page index. This value is simply for client state.
  int64 page = 5;
  // The page token. This is provided by the API.
  string page_token = 6;
}

// Canonical success response.
message ListSentenceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListSentenceResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// Fetch Public Key Encrypted Sentences by TranscriptSid
message FetchEncryptedSentencesRequest {
  // The unique SID identifier of the Transcript. Required.
  string transcript_sid = 1;
  // Grant access to PII Redacted/Unredacted Sentences. If redaction is enabled, the default is `true` to access redacted sentences.
  bool redacted = 2;
}

// Canonical success response.
message FetchEncryptedSentencesResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  IntelligenceV2TranscriptEncryptedSentences body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Generated from IntelligenceV2OperatorType operations.
service IntelligenceV2OperatorTypeService {
  // Retrieves a list of all Operator Types for an Account.
  rpc ListOperatorType(ListOperatorTypeRequest) returns (ListOperatorTypeResponse) {
    option (google.api.http) = {
      get: "/v2/OperatorTypes"
      response_body: "body"
    };
  }
  // Fetch a specific Operator Type.
  rpc FetchOperatorType(FetchOperatorTypeRequest) returns (FetchOperatorTypeResponse) {
    option (google.api.http) = {
      get: "/v2/OperatorTypes/{sid}"
      response_body: "body"
    };
  }
}

// Generated from IntelligenceV2Operator operations.
service IntelligenceV2OperatorService {
  // Retrieves a list of all Operators for an Account, both Custom and Pre-built.
  rpc ListOperator(ListOperatorRequest) returns (ListOperatorResponse) {
    option (google.api.http) = {
      get: "/v2/Operators"
      response_body: "body"
    };
  }
  // Fetch a specific Operator. Works for both Custom and Pre-built Operators.
  rpc FetchOperator(FetchOperatorRequest) returns (FetchOperatorResponse) {
    option (google.api.http) = {
      get: "/v2/Operators/{sid}"
      response_body: "body"
    };
  }
}

// Generated from IntelligenceV2CustomOperator operations.
service IntelligenceV2CustomOperatorService {
  // Retrieves a list of all Custom Operators for an Account.
  rpc ListCustomOperator(ListCustomOperatorRequest) returns (ListCustomOperatorResponse) {
    option (google.api.http) = {
      get: "/v2/Operators/Custom"
      response_body: "body"
    };
  }
  // Create a new Custom Operator for the given Account
  rpc CreateCustomOperator(CreateCustomOperatorRequest) returns (CreateCustomOperatorResponse) {
    option (google.api.http) = {
      post: "/v2/Operators/Custom"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch a specific Custom Operator.
  rpc FetchCustomOperator(FetchCustomOperatorRequest) returns (FetchCustomOperatorResponse) {
    option (google.api.http) = {
      get: "/v2/Operators/Custom/{sid}"
      response_body: "body"
    };
  }
  // Update a specific Custom Operator.
  rpc UpdateCustomOperator(UpdateCustomOperatorRequest) returns (UpdateCustomOperatorResponse) {
    option (google.api.http) = {
      post: "/v2/Operators/Custom/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete a specific Custom Operator.
  rpc DeleteCustomOperator(DeleteCustomOperatorRequest) returns (DeleteCustomOperatorResponse) {
    option (google.api.http) = {
      delete: "/v2/Operators/Custom/{sid}"
    };
  }
}

// Generated from IntelligenceV2PrebuiltOperator operations.
service IntelligenceV2PrebuiltOperatorService {
  // Retrieves a list of all Pre-built Operators for an account.
  rpc ListPrebuiltOperator(ListPrebuiltOperatorRequest) returns (ListPrebuiltOperatorResponse) {
    option (google.api.http) = {
      get: "/v2/Operators/PreBuilt"
      response_body: "body"
    };
  }
  // Fetch a specific Pre-built Operator.
  rpc FetchPrebuiltOperator(FetchPrebuiltOperatorRequest) returns (FetchPrebuiltOperatorResponse) {
    option (google.api.http) = {
      get: "/v2/Operators/PreBuilt/{sid}"
      response_body: "body"
    };
  }
}

// Generated from IntelligenceV2Service operations.
service IntelligenceV2ServiceService {
  // Retrieves a list of all Services for an account.
  rpc ListService(ListServiceRequest) returns (ListServiceResponse) {
    option (google.api.http) = {
      get: "/v2/Services"
      response_body: "body"
    };
  }
  // Create a new Service for the given Account
  rpc CreateService(CreateServiceRequest) returns (CreateServiceResponse) {
    option (google.api.http) = {
      post: "/v2/Services"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch a specific Service.
  rpc FetchService(FetchServiceRequest) returns (FetchServiceResponse) {
    option (google.api.http) = {
      get: "/v2/Services/{sid}"
      response_body: "body"
    };
  }
  // Update a specific Service.
  rpc UpdateService(UpdateServiceRequest) returns (UpdateServiceResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete a specific Service.
  rpc DeleteService(DeleteServiceRequest) returns (DeleteServiceResponse) {
    option (google.api.http) = {
      delete: "/v2/Services/{sid}"
    };
  }
}

// Generated from IntelligenceV2OperatorAttachments operations.
service IntelligenceV2OperatorAttachmentsService {
  // Retrieve Operators attached to a Service.
  rpc FetchOperatorAttachments(FetchOperatorAttachmentsRequest) returns (FetchOperatorAttachmentsResponse) {
    option (google.api.http) = {
      get: "/v2/Services/{service_sid}/Operators"
      response_body: "body"
    };
  }
}

// Generated from IntelligenceV2OperatorAttachment operations.
service IntelligenceV2OperatorAttachmentService {
  // Attach an Operator to a Service.
  rpc CreateOperatorAttachment(CreateOperatorAttachmentRequest) returns (CreateOperatorAttachmentResponse) {
    option (google.api.http) = {
      post: "/v2/Services/{service_sid}/Operators/{operator_sid}"
      response_body: "body"
    };
  }
  // Detach an Operator from a Service.
  rpc DeleteOperatorAttachment(DeleteOperatorAttachmentRequest) returns (DeleteOperatorAttachmentResponse) {
    option (google.api.http) = {
      delete: "/v2/Services/{service_sid}/Operators/{operator_sid}"
    };
  }
}

// Generated from IntelligenceV2Transcript operations.
service IntelligenceV2TranscriptService {
  // Retrieve a list of Transcripts for a given service.
  rpc ListTranscript(ListTranscriptRequest) returns (ListTranscriptResponse) {
    option (google.api.http) = {
      get: "/v2/Transcripts"
      response_body: "body"
    };
  }
  // Create a new Transcript for the service
  rpc CreateTranscript(CreateTranscriptRequest) returns (CreateTranscriptResponse) {
    option (google.api.http) = {
      post: "/v2/Transcripts"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch a specific Transcript.
  rpc FetchTranscript(FetchTranscriptRequest) returns (FetchTranscriptResponse) {
    option (google.api.http) = {
      get: "/v2/Transcripts/{sid}"
      response_body: "body"
    };
  }
  // Delete a specific Transcript.
  rpc DeleteTranscript(DeleteTranscriptRequest) returns (DeleteTranscriptResponse) {
    option (google.api.http) = {
      delete: "/v2/Transcripts/{sid}"
    };
  }
}

// Generated from IntelligenceV2Media operations.
service IntelligenceV2MediaService {
  // Get download URLs for media if possible
  rpc FetchMedia(FetchMediaRequest) returns (FetchMediaResponse) {
    option (google.api.http) = {
      get: "/v2/Transcripts/{sid}/Media"
      response_body: "body"
    };
  }
}

// Generated from IntelligenceV2OperatorResult operations.
service IntelligenceV2OperatorResultService {
  // Retrieve a list of Operator Results for the given Transcript.
  rpc ListOperatorResult(ListOperatorResultRequest) returns (ListOperatorResultResponse) {
    option (google.api.http) = {
      get: "/v2/Transcripts/{transcript_sid}/OperatorResults"
      response_body: "body"
    };
  }
  // Fetch a specific Operator Result for the given Transcript.
  rpc FetchOperatorResult(FetchOperatorResultRequest) returns (FetchOperatorResultResponse) {
    option (google.api.http) = {
      get: "/v2/Transcripts/{transcript_sid}/OperatorResults/{operator_sid}"
      response_body: "body"
    };
  }
}

// Generated from IntelligenceV2EncryptedOperatorResults operations.
service IntelligenceV2EncryptedOperatorResultsService {
  // Fetch Public Key Encrypted Operator Results by TranscriptSid
  rpc FetchEncryptedOperatorResults(FetchEncryptedOperatorResultsRequest) returns (FetchEncryptedOperatorResultsResponse) {
    option (google.api.http) = {
      get: "/v2/Transcripts/{transcript_sid}/OperatorResults/Encrypted"
      response_body: "body"
    };
  }
}

// Generated from IntelligenceV2Sentence operations.
service IntelligenceV2SentenceService {
  // Get all Transcript Sentences by TranscriptSid
  rpc ListSentence(ListSentenceRequest) returns (ListSentenceResponse) {
    option (google.api.http) = {
      get: "/v2/Transcripts/{transcript_sid}/Sentences"
      response_body: "body"
    };
  }
}

// Generated from IntelligenceV2EncryptedSentences operations.
service IntelligenceV2EncryptedSentencesService {
  // Fetch Public Key Encrypted Sentences by TranscriptSid
  rpc FetchEncryptedSentences(FetchEncryptedSentencesRequest) returns (FetchEncryptedSentencesResponse) {
    option (google.api.http) = {
      get: "/v2/Transcripts/{transcript_sid}/Sentences/Encrypted"
      response_body: "body"
    };
  }
}

