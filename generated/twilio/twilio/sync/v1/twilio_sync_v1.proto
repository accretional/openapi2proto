// Code generated by openapi2proto. DO NOT EDIT.
// Source: twilio_sync_v1.json

syntax = "proto3";

package twilio.sync.v1;

option go_package = "twilio/sync/v1;twiliosyncv1";

import "google/api/annotations.proto";

message SyncV1Service {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Service resource.
  string account_sid = 1;
  // Whether token identities in the Service must be granted access to Sync objects by using the [Permissions](https://www.twilio.com/docs/sync/api/sync-permissions) resource. It is disabled (false) by default.
  bool acl_enabled = 2;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 4;
  // The string that you assigned to describe the resource.
  string friendly_name = 5;
  // The URLs of related resources.
  SyncV1ServiceLinks links = 6;
  // Whether every `endpoint_disconnected` event should occur after a configurable delay. The default is `false`, where the `endpoint_disconnected` event occurs immediately after disconnection. When `true`, intervening reconnections can prevent the `endpoint_disconnected` event.
  bool reachability_debouncing_enabled = 7;
  // The reachability event delay in milliseconds if `reachability_debouncing_enabled` = `true`.  Must be between 1,000 and 30,000 and defaults to 5,000. This is the number of milliseconds after the last running client disconnects, and a Sync identity is declared offline, before `webhook_url` is called, if all endpoints remain offline. A reconnection from the same identity by any endpoint during this interval prevents the reachability event from occurring.
  int64 reachability_debouncing_window = 8;
  // Whether the service instance calls `webhook_url` when client endpoints connect to Sync. The default is `false`.
  bool reachability_webhooks_enabled = 9;
  // The unique string that we created to identify the Service resource.
  string sid = 10;
  // An application-defined string that uniquely identifies the resource. It can be used in place of the resource's `sid` in the URL to address the resource. It is a read-only property, it cannot be assigned using REST API.
  string unique_name = 11;
  // The absolute URL of the Service resource.
  string url = 12;
  // The URL we call when Sync objects are manipulated.
  string webhook_url = 13;
  // Whether the Service instance should call `webhook_url` when the REST API is used to update Sync objects. The default is `false`.
  bool webhooks_from_rest_enabled = 14;
}

// The URLs of related resources.
message SyncV1ServiceLinks {
  // Scalar value wrapper.
  string value = 1;
}

message SyncV1ServiceDocument {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Document resource.
  string account_sid = 1;
  // The identity of the Sync Document's creator. If the Sync Document is created from the client SDK, the value matches the Access Token's `identity` field. If the Sync Document was created from the REST API, the value is `system`.
  string created_by = 2;
  // An arbitrary, schema-less object that the Sync Document stores. Can be up to 16 KiB in length.
  string data = 3;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 4;
  // The date and time in GMT when the Sync Document expires and will be deleted, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format. If the Sync Document does not expire, this value is `null`. The Document resource might not be deleted immediately after it expires.
  string date_expires = 5;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 6;
  // The URLs of resources related to the Sync Document.
  SyncV1ServiceDocumentLinks links = 7;
  // The current revision of the Sync Document, represented as a string. The `revision` property is used with conditional updates to ensure data consistency.
  string revision = 8;
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) the resource is associated with.
  string service_sid = 9;
  // The unique string that we created to identify the Document resource.
  string sid = 10;
  // An application-defined string that uniquely identifies the resource. It can be used in place of the resource's `sid` in the URL to address the resource and can be up to 320 characters long.
  string unique_name = 11;
  // The absolute URL of the Document resource.
  string url = 12;
}

// The URLs of resources related to the Sync Document.
message SyncV1ServiceDocumentLinks {
  // Scalar value wrapper.
  string value = 1;
}

message SyncV1ServiceDocumentDocumentPermission {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Document Permission resource.
  string account_sid = 1;
  // The SID of the Sync Document to which the Document Permission applies.
  string document_sid = 2;
  // The application-defined string that uniquely identifies the resource's User within the Service to an FPA token.
  string identity = 3;
  // Whether the identity can delete the Sync Document.
  bool manage = 4;
  // Whether the identity can read the Sync Document.
  bool read = 5;
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) the resource is associated with.
  string service_sid = 6;
  // The absolute URL of the Sync Document Permission resource.
  string url = 7;
  // Whether the identity can update the Sync Document.
  bool write = 8;
}

message SyncV1ServiceSyncList {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Sync List resource.
  string account_sid = 1;
  // The identity of the Sync List's creator. If the Sync List is created from the client SDK, the value matches the Access Token's `identity` field. If the Sync List was created from the REST API, the value is `system`.
  string created_by = 2;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The date and time in GMT when the Sync List expires and will be deleted, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format. If the Sync List does not expire, this value is `null`. The Sync List might not be deleted immediately after it expires.
  string date_expires = 4;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 5;
  // The URLs of the Sync List's nested resources.
  SyncV1ServiceSyncListLinks links = 6;
  // The current revision of the Sync List, represented as a string.
  string revision = 7;
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) the resource is associated with.
  string service_sid = 8;
  // The unique string that we created to identify the Sync List resource.
  string sid = 9;
  // An application-defined string that uniquely identifies the resource. It can be used in place of the resource's `sid` in the URL to address the resource.
  string unique_name = 10;
  // The absolute URL of the Sync List resource.
  string url = 11;
}

// The URLs of the Sync List's nested resources.
message SyncV1ServiceSyncListLinks {
  // Scalar value wrapper.
  string value = 1;
}

message SyncV1ServiceSyncListSyncListItem {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the List Item resource.
  string account_sid = 1;
  // The identity of the List Item's creator. If the item is created from the client SDK, the value matches the Access Token's `identity` field. If the item was created from the REST API, the value is `system`.
  string created_by = 2;
  // An arbitrary, schema-less object that the List Item stores. Can be up to 16 KiB in length.
  string data = 3;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 4;
  // The date and time in GMT when the List Item expires and will be deleted, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format. If the List Item does not expire, this value is `null`. The List Item resource might not be deleted immediately after it expires.
  string date_expires = 5;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 6;
  // The automatically generated index of the List Item. The `index` values of the List Items in a Sync List can have gaps in their sequence.
  int64 index = 7;
  // The SID of the Sync List that contains the List Item.
  string list_sid = 8;
  // The current revision of the item, represented as a string.
  string revision = 9;
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) the resource is associated with.
  string service_sid = 10;
  // The absolute URL of the List Item resource.
  string url = 11;
}

message SyncV1ServiceSyncListSyncListPermission {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Sync List Permission resource.
  string account_sid = 1;
  // The application-defined string that uniquely identifies the resource's User within the Service to an FPA token.
  string identity = 2;
  // The SID of the Sync List to which the Permission applies.
  string list_sid = 3;
  // Whether the identity can delete the Sync List.
  bool manage = 4;
  // Whether the identity can read the Sync List and its Items.
  bool read = 5;
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) the resource is associated with.
  string service_sid = 6;
  // The absolute URL of the Sync List Permission resource.
  string url = 7;
  // Whether the identity can create, update, and delete Items in the Sync List.
  bool write = 8;
}

message SyncV1ServiceSyncMap {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Sync Map resource.
  string account_sid = 1;
  // The identity of the Sync Map's creator. If the Sync Map is created from the client SDK, the value matches the Access Token's `identity` field. If the Sync Map was created from the REST API, the value is `system`.
  string created_by = 2;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The date and time in GMT when the Sync Map expires and will be deleted, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format. If the Sync Map does not expire, this value is `null`. The Sync Map might not be deleted immediately after it expires.
  string date_expires = 4;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 5;
  // The URLs of the Sync Map's nested resources.
  SyncV1ServiceSyncMapLinks links = 6;
  // The current revision of the Sync Map, represented as a string.
  string revision = 7;
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) the resource is associated with.
  string service_sid = 8;
  // The unique string that we created to identify the Sync Map resource.
  string sid = 9;
  // An application-defined string that uniquely identifies the resource. It can be used in place of the resource's `sid` in the URL to address the resource.
  string unique_name = 10;
  // The absolute URL of the Sync Map resource.
  string url = 11;
}

// The URLs of the Sync Map's nested resources.
message SyncV1ServiceSyncMapLinks {
  // Scalar value wrapper.
  string value = 1;
}

message SyncV1ServiceSyncMapSyncMapItem {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Map Item resource.
  string account_sid = 1;
  // The identity of the Map Item's creator. If the Map Item is created from the client SDK, the value matches the Access Token's `identity` field. If the Map Item was created from the REST API, the value is `system`.
  string created_by = 2;
  // An arbitrary, schema-less object that the Map Item stores. Can be up to 16 KiB in length.
  string data = 3;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 4;
  // The date and time in GMT when the Map Item expires and will be deleted, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format. If the Map Item does not expire, this value is `null`.  The Map Item might not be deleted immediately after it expires.
  string date_expires = 5;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 6;
  // The unique, user-defined key for the Map Item.
  string key = 7;
  // The SID of the Sync Map that contains the Map Item.
  string map_sid = 8;
  // The current revision of the Map Item, represented as a string.
  string revision = 9;
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) the resource is associated with.
  string service_sid = 10;
  // The absolute URL of the Map Item resource.
  string url = 11;
}

message SyncV1ServiceSyncMapSyncMapPermission {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Sync Map Permission resource.
  string account_sid = 1;
  // The application-defined string that uniquely identifies the resource's User within the Service to an FPA token.
  string identity = 2;
  // Whether the identity can delete the Sync Map.
  bool manage = 3;
  // The SID of the Sync Map to which the Permission applies.
  string map_sid = 4;
  // Whether the identity can read the Sync Map and its Items.
  bool read = 5;
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) the resource is associated with.
  string service_sid = 6;
  // The absolute URL of the Sync Map Permission resource.
  string url = 7;
  // Whether the identity can create, update, and delete Items in the Sync Map.
  bool write = 8;
}

message SyncV1ServiceSyncStream {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Sync Stream resource.
  string account_sid = 1;
  // The identity of the Stream's creator. If the Stream is created from the client SDK, the value matches the Access Token's `identity` field. If the Stream was created from the REST API, the value is 'system'.
  string created_by = 2;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The date and time in GMT when the Message Stream expires and will be deleted, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format. If the Message Stream does not expire, this value is `null`. The Stream might not be deleted immediately after it expires.
  string date_expires = 4;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 5;
  // The URLs of the Stream's nested resources.
  SyncV1ServiceSyncStreamLinks links = 6;
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) the resource is associated with.
  string service_sid = 7;
  // The unique string that we created to identify the Sync Stream resource.
  string sid = 8;
  // An application-defined string that uniquely identifies the resource. It can be used in place of the resource's `sid` in the URL to address the resource.
  string unique_name = 9;
  // The absolute URL of the Message Stream resource.
  string url = 10;
}

// The URLs of the Stream's nested resources.
message SyncV1ServiceSyncStreamLinks {
  // Scalar value wrapper.
  string value = 1;
}

message SyncV1ServiceSyncStreamStreamMessage {
  // An arbitrary, schema-less object that contains the Stream Message body. Can be up to 4 KiB in length.
  string data = 1;
  // The unique string that we created to identify the Stream Message resource.
  string sid = 2;
}

message SyncListItemEnumQueryFromBoundType {
  // Scalar value wrapper.
  string value = 1;
}

message SyncListItemEnumQueryResultOrder {
  // Scalar value wrapper.
  string value = 1;
}

message SyncMapItemEnumQueryFromBoundType {
  // Scalar value wrapper.
  string value = 1;
}

message SyncMapItemEnumQueryResultOrder {
  // Scalar value wrapper.
  string value = 1;
}

// ListServiceResponse
message ListServiceResponseBody {
  ListServiceResponseBodyMeta meta = 1;
  repeated SyncV1Service services = 2;
}

message ListServiceResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListServiceRequest {
  // How many resources to return in each list page. The default is 50, and the maximum is 100.
  int64 page_size = 1;
  // The page index. This value is simply for client state.
  int64 page = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
}

// Canonical success response.
message ListServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListServiceResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateServiceRequest
message CreateServiceRequestBody {
  // Whether token identities in the Service must be granted access to Sync objects by using the [Permissions](https://www.twilio.com/docs/sync/api/sync-permissions) resource.
  bool acl_enabled = 1;
  // A string that you assign to describe the resource.
  string friendly_name = 2;
  // Whether every `endpoint_disconnected` event should occur after a configurable delay. The default is `false`, where the `endpoint_disconnected` event occurs immediately after disconnection. When `true`, intervening reconnections can prevent the `endpoint_disconnected` event.
  bool reachability_debouncing_enabled = 3;
  // The reachability event delay in milliseconds if `reachability_debouncing_enabled` = `true`.  Must be between 1,000 and 30,000 and defaults to 5,000. This is the number of milliseconds after the last running client disconnects, and a Sync identity is declared offline, before the `webhook_url` is called if all endpoints remain offline. A reconnection from the same identity by any endpoint during this interval prevents the call to `webhook_url`.
  int64 reachability_debouncing_window = 4;
  // Whether the service instance should call `webhook_url` when client endpoints connect to Sync. The default is `false`.
  bool reachability_webhooks_enabled = 5;
  // The URL we should call when Sync objects are manipulated.
  string webhook_url = 6;
  // Whether the Service instance should call `webhook_url` when the REST API is used to update Sync objects. The default is `false`.
  bool webhooks_from_rest_enabled = 7;
}

message CreateServiceRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateServiceRequestBody body = 1;
}

// Canonical success response.
message CreateServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1Service body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListDocumentResponse
message ListDocumentResponseBody {
  repeated SyncV1ServiceDocument documents = 1;
  ListDocumentResponseBodyMeta meta = 2;
}

message ListDocumentResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListDocumentRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Document resources to read. Required.
  string service_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 100.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListDocumentResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListDocumentResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateDocumentRequest
message CreateDocumentRequestBody {
  // A JSON string that represents an arbitrary, schema-less object that the Sync Document stores. Can be up to 16 KiB in length.
  string data = 1;
  // How long, [in seconds](https://www.twilio.com/docs/sync/limits#sync-payload-limits), before the Sync Document expires and is deleted (the Sync Document's time-to-live).
  int64 ttl = 2;
  // An application-defined string that uniquely identifies the Sync Document
  string unique_name = 3;
}

message CreateDocumentRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) to create the new Document resource in. Required.
  string service_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateDocumentRequestBody body = 2;
}

// Canonical success response.
message CreateDocumentResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceDocument body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
}

// ListDocumentPermissionResponse
message ListDocumentPermissionResponseBody {
  ListDocumentPermissionResponseBodyMeta meta = 1;
  repeated SyncV1ServiceDocumentDocumentPermission permissions = 2;
}

message ListDocumentPermissionResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Permissions applying to a Sync Document.
message ListDocumentPermissionRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Document Permission resources to read. Required.
  string service_sid = 1;
  // The SID of the Sync Document with the Document Permission resources to read. Can be the Document resource's `sid` or its `unique_name`. Required.
  string document_sid = 2;
  // How many resources to return in each list page. The default is 50, and the maximum is 100.
  int64 page_size = 3;
  // The page index. This value is simply for client state.
  int64 page = 4;
  // The page token. This is provided by the API.
  string page_token = 5;
}

// Canonical success response.
message ListDocumentPermissionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListDocumentPermissionResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch a specific Sync Document Permission.
message FetchDocumentPermissionRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Document Permission resource to fetch. Required.
  string service_sid = 1;
  // The SID of the Sync Document with the Document Permission resource to fetch. Can be the Document resource's `sid` or its `unique_name`. Required.
  string document_sid = 2;
  // The application-defined string that uniquely identifies the User's Document Permission resource to fetch. Required.
  string identity = 3;
}

// Canonical success response.
message FetchDocumentPermissionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceDocumentDocumentPermission body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateDocumentPermissionRequest
message UpdateDocumentPermissionRequestBody {
  // Whether the identity can delete the Sync Document. Default value is `false`.
  bool manage = 1;
  // Whether the identity can read the Sync Document. Default value is `false`.
  bool read = 2;
  // Whether the identity can update the Sync Document. Default value is `false`.
  bool write = 3;
}

// Update an identity's access to a specific Sync Document.
message UpdateDocumentPermissionRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Document Permission resource to update. Required.
  string service_sid = 1;
  // The SID of the Sync Document with the Document Permission resource to update. Can be the Document resource's `sid` or its `unique_name`. Required.
  string document_sid = 2;
  // The application-defined string that uniquely identifies the User's Document Permission resource to update. Required.
  string identity = 3;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateDocumentPermissionRequestBody body = 4;
}

// Canonical success response.
message UpdateDocumentPermissionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceDocumentDocumentPermission body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete a specific Sync Document Permission.
message DeleteDocumentPermissionRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Document Permission resource to delete. Required.
  string service_sid = 1;
  // The SID of the Sync Document with the Document Permission resource to delete. Can be the Document resource's `sid` or its `unique_name`. Required.
  string document_sid = 2;
  // The application-defined string that uniquely identifies the User's Document Permission resource to delete. Required.
  string identity = 3;
}

// Canonical success response.
message DeleteDocumentPermissionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

message FetchDocumentRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Document resource to fetch. Required.
  string service_sid = 1;
  // The SID of the Document resource to fetch. Can be the Document resource's `sid` or its `unique_name`. Required.
  string sid = 2;
}

// Canonical success response.
message FetchDocumentResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceDocument body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
}

// UpdateDocumentRequest
message UpdateDocumentRequestBody {
  // A JSON string that represents an arbitrary, schema-less object that the Sync Document stores. Can be up to 16 KiB in length.
  string data = 1;
  // How long, [in seconds](https://www.twilio.com/docs/sync/limits#sync-payload-limits), before the Sync Document expires and is deleted (time-to-live).
  int64 ttl = 2;
}

message UpdateDocumentRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Document resource to update. Required.
  string service_sid = 1;
  // The SID of the Document resource to update. Can be the Document resource's `sid` or its `unique_name`. Required.
  string sid = 2;
  // The If-Match HTTP request header
  string header_if_match = 3;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateDocumentRequestBody body = 4;
}

// Canonical success response.
message UpdateDocumentResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceDocument body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
}

message DeleteDocumentRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Document resource to delete. Required.
  string service_sid = 1;
  // The SID of the Document resource to delete. Can be the Document resource's `sid` or its `unique_name`. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteDocumentResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListSyncListResponse
message ListSyncListResponseBody {
  repeated SyncV1ServiceSyncList lists = 1;
  ListSyncListResponseBodyMeta meta = 2;
}

message ListSyncListResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListSyncListRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync List resources to read. Required.
  string service_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 100.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListSyncListResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListSyncListResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateSyncListRequest
message CreateSyncListRequestBody {
  // How long, [in seconds](https://www.twilio.com/docs/sync/limits#sync-payload-limits), before the Sync List expires (time-to-live) and is deleted.
  int64 collection_ttl = 1;
  // Alias for collection_ttl. If both are provided, this value is ignored.
  int64 ttl = 2;
  // An application-defined string that uniquely identifies the resource. This value must be unique within its Service and it can be up to 320 characters long. The `unique_name` value can be used as an alternative to the `sid` in the URL path to address the resource.
  string unique_name = 3;
}

message CreateSyncListRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) to create the new Sync List in. Required.
  string service_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateSyncListRequestBody body = 2;
}

// Canonical success response.
message CreateSyncListResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceSyncList body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListSyncListItemResponse
message ListSyncListItemResponseBody {
  repeated SyncV1ServiceSyncListSyncListItem items = 1;
  ListSyncListItemResponseBodyMeta meta = 2;
}

message ListSyncListItemResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListSyncListItemRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the List Item resources to read. Required.
  string service_sid = 1;
  // The SID of the Sync List with the List Items to read. Can be the Sync List resource's `sid` or its `unique_name`. Required.
  string list_sid = 2;
  // How to order the List Items returned by their `index` value. Can be: `asc` (ascending) or `desc` (descending) and the default is ascending.
  SyncListItemEnumQueryResultOrder order = 3;
  // The `index` of the first Sync List Item resource to read. See also `bounds`.
  string from = 4;
  // Whether to include the List Item referenced by the `from` parameter. Can be: `inclusive` to include the List Item referenced by the `from` parameter or `exclusive` to start with the next List Item. The default value is `inclusive`.
  SyncListItemEnumQueryFromBoundType bounds = 5;
  // How many resources to return in each list page. The default is 50, and the maximum is 100.
  int64 page_size = 6;
  // The page index. This value is simply for client state.
  int64 page = 7;
  // The page token. This is provided by the API.
  string page_token = 8;
}

// Canonical success response.
message ListSyncListItemResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListSyncListItemResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateSyncListItemRequest
message CreateSyncListItemRequestBody {
  // How long, [in seconds](https://www.twilio.com/docs/sync/limits#sync-payload-limits), before the List Item's parent Sync List expires (time-to-live) and is deleted.
  int64 collection_ttl = 1;
  // A JSON string that represents an arbitrary, schema-less object that the List Item stores. Can be up to 16 KiB in length.
  string data = 2;
  // How long, [in seconds](https://www.twilio.com/docs/sync/limits#sync-payload-limits), before the List Item expires (time-to-live) and is deleted.
  int64 item_ttl = 3;
  // An alias for `item_ttl`. If both parameters are provided, this value is ignored.
  int64 ttl = 4;
}

message CreateSyncListItemRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) to create the new List Item in. Required.
  string service_sid = 1;
  // The SID of the Sync List to add the new List Item to. Can be the Sync List resource's `sid` or its `unique_name`. Required.
  string list_sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateSyncListItemRequestBody body = 3;
}

// Canonical success response.
message CreateSyncListItemResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceSyncListSyncListItem body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
}

message FetchSyncListItemRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync List Item resource to fetch. Required.
  string service_sid = 1;
  // The SID of the Sync List with the Sync List Item resource to fetch. Can be the Sync List resource's `sid` or its `unique_name`. Required.
  string list_sid = 2;
  // The index of the Sync List Item resource to fetch. Required.
  int64 index = 3;
}

// Canonical success response.
message FetchSyncListItemResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceSyncListSyncListItem body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
}

// UpdateSyncListItemRequest
message UpdateSyncListItemRequestBody {
  // How long, [in seconds](https://www.twilio.com/docs/sync/limits#sync-payload-limits), before the List Item's parent Sync List expires (time-to-live) and is deleted. This parameter can only be used when the List Item's `data` or `ttl` is updated in the same request.
  int64 collection_ttl = 1;
  // A JSON string that represents an arbitrary, schema-less object that the List Item stores. Can be up to 16 KiB in length.
  string data = 2;
  // How long, [in seconds](https://www.twilio.com/docs/sync/limits#sync-payload-limits), before the List Item expires (time-to-live) and is deleted.
  int64 item_ttl = 3;
  // An alias for `item_ttl`. If both parameters are provided, this value is ignored.
  int64 ttl = 4;
}

message UpdateSyncListItemRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync List Item resource to update. Required.
  string service_sid = 1;
  // The SID of the Sync List with the Sync List Item resource to update. Can be the Sync List resource's `sid` or its `unique_name`. Required.
  string list_sid = 2;
  // The index of the Sync List Item resource to update. Required.
  int64 index = 3;
  // If provided, applies this mutation if (and only if) the “revision” field of this [map item] matches the provided value. This matches the semantics of (and is implemented with) the HTTP [If-Match header](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-Match).
  string header_if_match = 4;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateSyncListItemRequestBody body = 5;
}

// Canonical success response.
message UpdateSyncListItemResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceSyncListSyncListItem body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
}

message DeleteSyncListItemRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync List Item resource to delete. Required.
  string service_sid = 1;
  // The SID of the Sync List with the Sync List Item resource to delete. Can be the Sync List resource's `sid` or its `unique_name`. Required.
  string list_sid = 2;
  // The index of the Sync List Item resource to delete. Required.
  int64 index = 3;
  // If provided, applies this mutation if (and only if) the “revision” field of this [map item] matches the provided value. This matches the semantics of (and is implemented with) the HTTP [If-Match header](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-Match).
  string header_if_match = 4;
}

// Canonical success response.
message DeleteSyncListItemResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListSyncListPermissionResponse
message ListSyncListPermissionResponseBody {
  ListSyncListPermissionResponseBodyMeta meta = 1;
  repeated SyncV1ServiceSyncListSyncListPermission permissions = 2;
}

message ListSyncListPermissionResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Permissions applying to a Sync List.
message ListSyncListPermissionRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync List Permission resources to read. Required.
  string service_sid = 1;
  // The SID of the Sync List with the Sync List Permission resources to read. Can be the Sync List resource's `sid` or its `unique_name`. Required.
  string list_sid = 2;
  // How many resources to return in each list page. The default is 50, and the maximum is 100.
  int64 page_size = 3;
  // The page index. This value is simply for client state.
  int64 page = 4;
  // The page token. This is provided by the API.
  string page_token = 5;
}

// Canonical success response.
message ListSyncListPermissionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListSyncListPermissionResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch a specific Sync List Permission.
message FetchSyncListPermissionRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync List Permission resource to fetch. Required.
  string service_sid = 1;
  // The SID of the Sync List with the Sync List Permission resource to fetch. Can be the Sync List resource's `sid` or its `unique_name`. Required.
  string list_sid = 2;
  // The application-defined string that uniquely identifies the User's Sync List Permission resource to fetch. Required.
  string identity = 3;
}

// Canonical success response.
message FetchSyncListPermissionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceSyncListSyncListPermission body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateSyncListPermissionRequest
message UpdateSyncListPermissionRequestBody {
  // Whether the identity can delete the Sync List. Default value is `false`.
  bool manage = 1;
  // Whether the identity can read the Sync List and its Items. Default value is `false`.
  bool read = 2;
  // Whether the identity can create, update, and delete Items in the Sync List. Default value is `false`.
  bool write = 3;
}

// Update an identity's access to a specific Sync List.
message UpdateSyncListPermissionRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync List Permission resource to update. Required.
  string service_sid = 1;
  // The SID of the Sync List with the Sync List Permission resource to update. Can be the Sync List resource's `sid` or its `unique_name`. Required.
  string list_sid = 2;
  // The application-defined string that uniquely identifies the User's Sync List Permission resource to update. Required.
  string identity = 3;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateSyncListPermissionRequestBody body = 4;
}

// Canonical success response.
message UpdateSyncListPermissionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceSyncListSyncListPermission body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete a specific Sync List Permission.
message DeleteSyncListPermissionRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync List Permission resource to delete. Required.
  string service_sid = 1;
  // The SID of the Sync List with the Sync List Permission resource to delete. Can be the Sync List resource's `sid` or its `unique_name`. Required.
  string list_sid = 2;
  // The application-defined string that uniquely identifies the User's Sync List Permission resource to delete. Required.
  string identity = 3;
}

// Canonical success response.
message DeleteSyncListPermissionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

message FetchSyncListRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync List resource to fetch. Required.
  string service_sid = 1;
  // The SID of the Sync List resource to fetch. Can be the Sync List resource's `sid` or its `unique_name`. Required.
  string sid = 2;
}

// Canonical success response.
message FetchSyncListResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceSyncList body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateSyncListRequest
message UpdateSyncListRequestBody {
  // How long, [in seconds](https://www.twilio.com/docs/sync/limits#sync-payload-limits), before the Sync List expires (time-to-live) and is deleted.
  int64 collection_ttl = 1;
  // An alias for `collection_ttl`. If both are provided, this value is ignored.
  int64 ttl = 2;
}

message UpdateSyncListRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync List resource to update. Required.
  string service_sid = 1;
  // The SID of the Sync List resource to update. Can be the Sync List resource's `sid` or its `unique_name`. Required.
  string sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateSyncListRequestBody body = 3;
}

// Canonical success response.
message UpdateSyncListResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceSyncList body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeleteSyncListRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync List resource to delete. Required.
  string service_sid = 1;
  // The SID of the Sync List resource to delete. Can be the Sync List resource's `sid` or its `unique_name`. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteSyncListResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListSyncMapResponse
message ListSyncMapResponseBody {
  repeated SyncV1ServiceSyncMap maps = 1;
  ListSyncMapResponseBodyMeta meta = 2;
}

message ListSyncMapResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListSyncMapRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync Map resources to read. Required.
  string service_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 100.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListSyncMapResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListSyncMapResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateSyncMapRequest
message CreateSyncMapRequestBody {
  // How long, [in seconds](https://www.twilio.com/docs/sync/limits#sync-payload-limits), before the Sync Map expires (time-to-live) and is deleted.
  int64 collection_ttl = 1;
  // An alias for `collection_ttl`. If both parameters are provided, this value is ignored.
  int64 ttl = 2;
  // An application-defined string that uniquely identifies the resource. It can be used as an alternative to the `sid` in the URL path to address the resource.
  string unique_name = 3;
}

message CreateSyncMapRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) to create the Sync Map in. Required.
  string service_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateSyncMapRequestBody body = 2;
}

// Canonical success response.
message CreateSyncMapResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceSyncMap body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListSyncMapItemResponse
message ListSyncMapItemResponseBody {
  repeated SyncV1ServiceSyncMapSyncMapItem items = 1;
  ListSyncMapItemResponseBodyMeta meta = 2;
}

message ListSyncMapItemResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListSyncMapItemRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Map Item resources to read. Required.
  string service_sid = 1;
  // The SID of the Sync Map with the Sync Map Item resource to fetch. Can be the Sync Map resource's `sid` or its `unique_name`. Required.
  string map_sid = 2;
  // How to order the Map Items returned by their `key` value. Can be: `asc` (ascending) or `desc` (descending) and the default is ascending. Map Items are [ordered lexicographically](https://en.wikipedia.org/wiki/Lexicographical_order) by Item key.
  SyncMapItemEnumQueryResultOrder order = 3;
  // The `key` of the first Sync Map Item resource to read. See also `bounds`.
  string from = 4;
  // Whether to include the Map Item referenced by the `from` parameter. Can be: `inclusive` to include the Map Item referenced by the `from` parameter or `exclusive` to start with the next Map Item. The default value is `inclusive`.
  SyncMapItemEnumQueryFromBoundType bounds = 5;
  // How many resources to return in each list page. The default is 50, and the maximum is 100.
  int64 page_size = 6;
  // The page index. This value is simply for client state.
  int64 page = 7;
  // The page token. This is provided by the API.
  string page_token = 8;
}

// Canonical success response.
message ListSyncMapItemResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListSyncMapItemResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateSyncMapItemRequest
message CreateSyncMapItemRequestBody {
  // How long, [in seconds](https://www.twilio.com/docs/sync/limits#sync-payload-limits), before the Map Item's parent Sync Map expires (time-to-live) and is deleted.
  int64 collection_ttl = 1;
  // A JSON string that represents an arbitrary, schema-less object that the Map Item stores. Can be up to 16 KiB in length.
  string data = 2;
  // How long, [in seconds](https://www.twilio.com/docs/sync/limits#sync-payload-limits), before the Map Item expires (time-to-live) and is deleted.
  int64 item_ttl = 3;
  // The unique, user-defined key for the Map Item. Can be up to 320 characters long.
  string key = 4;
  // An alias for `item_ttl`. If both parameters are provided, this value is ignored.
  int64 ttl = 5;
}

message CreateSyncMapItemRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) to create the Map Item in. Required.
  string service_sid = 1;
  // The SID of the Sync Map to add the new Map Item to. Can be the Sync Map resource's `sid` or its `unique_name`. Required.
  string map_sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateSyncMapItemRequestBody body = 3;
}

// Canonical success response.
message CreateSyncMapItemResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceSyncMapSyncMapItem body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
}

message FetchSyncMapItemRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync Map Item resource to fetch. Required.
  string service_sid = 1;
  // The SID of the Sync Map with the Sync Map Item resource to fetch. Can be the Sync Map resource's `sid` or its `unique_name`. Required.
  string map_sid = 2;
  // The `key` value of the Sync Map Item resource to fetch. Required.
  string key = 3;
}

// Canonical success response.
message FetchSyncMapItemResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceSyncMapSyncMapItem body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
}

// UpdateSyncMapItemRequest
message UpdateSyncMapItemRequestBody {
  // How long, [in seconds](https://www.twilio.com/docs/sync/limits#sync-payload-limits), before the Map Item's parent Sync Map expires (time-to-live) and is deleted. This parameter can only be used when the Map Item's `data` or `ttl` is updated in the same request.
  int64 collection_ttl = 1;
  // A JSON string that represents an arbitrary, schema-less object that the Map Item stores. Can be up to 16 KiB in length.
  string data = 2;
  // How long, [in seconds](https://www.twilio.com/docs/sync/limits#sync-payload-limits), before the Map Item expires (time-to-live) and is deleted.
  int64 item_ttl = 3;
  // An alias for `item_ttl`. If both parameters are provided, this value is ignored.
  int64 ttl = 4;
}

message UpdateSyncMapItemRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync Map Item resource to update. Required.
  string service_sid = 1;
  // The SID of the Sync Map with the Sync Map Item resource to update. Can be the Sync Map resource's `sid` or its `unique_name`. Required.
  string map_sid = 2;
  // The `key` value of the Sync Map Item resource to update. Required.
  string key = 3;
  // If provided, applies this mutation if (and only if) the “revision” field of this [map item] matches the provided value. This matches the semantics of (and is implemented with) the HTTP [If-Match header](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-Match).
  string header_if_match = 4;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateSyncMapItemRequestBody body = 5;
}

// Canonical success response.
message UpdateSyncMapItemResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceSyncMapSyncMapItem body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
}

message DeleteSyncMapItemRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync Map Item resource to delete. Required.
  string service_sid = 1;
  // The SID of the Sync Map with the Sync Map Item resource to delete. Can be the Sync Map resource's `sid` or its `unique_name`. Required.
  string map_sid = 2;
  // The `key` value of the Sync Map Item resource to delete. Required.
  string key = 3;
  // If provided, applies this mutation if (and only if) the “revision” field of this [map item] matches the provided value. This matches the semantics of (and is implemented with) the HTTP [If-Match header](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-Match).
  string header_if_match = 4;
}

// Canonical success response.
message DeleteSyncMapItemResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListSyncMapPermissionResponse
message ListSyncMapPermissionResponseBody {
  ListSyncMapPermissionResponseBodyMeta meta = 1;
  repeated SyncV1ServiceSyncMapSyncMapPermission permissions = 2;
}

message ListSyncMapPermissionResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Permissions applying to a Sync Map.
message ListSyncMapPermissionRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync Map Permission resources to read. Can be the Service's `sid` value or `default`. Required.
  string service_sid = 1;
  // The SID of the Sync Map with the Permission resources to read. Can be the Sync Map resource's `sid` or its `unique_name`. Required.
  string map_sid = 2;
  // How many resources to return in each list page. The default is 50, and the maximum is 100.
  int64 page_size = 3;
  // The page index. This value is simply for client state.
  int64 page = 4;
  // The page token. This is provided by the API.
  string page_token = 5;
}

// Canonical success response.
message ListSyncMapPermissionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListSyncMapPermissionResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch a specific Sync Map Permission.
message FetchSyncMapPermissionRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync Map Permission resource to fetch. Can be the Service's `sid` value or `default`. Required.
  string service_sid = 1;
  // The SID of the Sync Map with the Sync Map Permission resource to fetch. Can be the Sync Map resource's `sid` or its `unique_name`. Required.
  string map_sid = 2;
  // The application-defined string that uniquely identifies the User's Sync Map Permission resource to fetch. Required.
  string identity = 3;
}

// Canonical success response.
message FetchSyncMapPermissionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceSyncMapSyncMapPermission body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateSyncMapPermissionRequest
message UpdateSyncMapPermissionRequestBody {
  // Whether the identity can delete the Sync Map. Default value is `false`.
  bool manage = 1;
  // Whether the identity can read the Sync Map and its Items. Default value is `false`.
  bool read = 2;
  // Whether the identity can create, update, and delete Items in the Sync Map. Default value is `false`.
  bool write = 3;
}

// Update an identity's access to a specific Sync Map.
message UpdateSyncMapPermissionRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync Map Permission resource to update. Can be the Service's `sid` value or `default`. Required.
  string service_sid = 1;
  // The SID of the Sync Map with the Sync Map Permission resource to update. Can be the Sync Map resource's `sid` or its `unique_name`. Required.
  string map_sid = 2;
  // The application-defined string that uniquely identifies the User's Sync Map Permission resource to update. Required.
  string identity = 3;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateSyncMapPermissionRequestBody body = 4;
}

// Canonical success response.
message UpdateSyncMapPermissionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceSyncMapSyncMapPermission body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete a specific Sync Map Permission.
message DeleteSyncMapPermissionRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync Map Permission resource to delete. Can be the Service's `sid` value or `default`. Required.
  string service_sid = 1;
  // The SID of the Sync Map with the Sync Map Permission resource to delete. Can be the Sync Map resource's `sid` or its `unique_name`. Required.
  string map_sid = 2;
  // The application-defined string that uniquely identifies the User's Sync Map Permission resource to delete. Required.
  string identity = 3;
}

// Canonical success response.
message DeleteSyncMapPermissionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

message FetchSyncMapRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync Map resource to fetch. Required.
  string service_sid = 1;
  // The SID of the Sync Map resource to fetch. Can be the Sync Map's `sid` or its `unique_name`. Required.
  string sid = 2;
}

// Canonical success response.
message FetchSyncMapResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceSyncMap body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateSyncMapRequest
message UpdateSyncMapRequestBody {
  // How long, [in seconds](https://www.twilio.com/docs/sync/limits#sync-payload-limits), before the Sync Map expires (time-to-live) and is deleted.
  int64 collection_ttl = 1;
  // An alias for `collection_ttl`. If both parameters are provided, this value is ignored.
  int64 ttl = 2;
}

message UpdateSyncMapRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync Map resource to update. Required.
  string service_sid = 1;
  // The SID of the Sync Map resource to update. Can be the Sync Map's `sid` or its `unique_name`. Required.
  string sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateSyncMapRequestBody body = 3;
}

// Canonical success response.
message UpdateSyncMapResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceSyncMap body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeleteSyncMapRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync Map resource to delete. Required.
  string service_sid = 1;
  // The SID of the Sync Map resource to delete. Can be the Sync Map's `sid` or its `unique_name`. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteSyncMapResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListSyncStreamResponse
message ListSyncStreamResponseBody {
  ListSyncStreamResponseBodyMeta meta = 1;
  repeated SyncV1ServiceSyncStream streams = 2;
}

message ListSyncStreamResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Streams in a Service Instance.
message ListSyncStreamRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Stream resources to read. Required.
  string service_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 100.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListSyncStreamResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListSyncStreamResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateSyncStreamRequest
message CreateSyncStreamRequestBody {
  // How long, [in seconds](https://www.twilio.com/docs/sync/limits#sync-payload-limits), before the Stream expires and is deleted (time-to-live).
  int64 ttl = 1;
  // An application-defined string that uniquely identifies the resource. This value must be unique within its Service and it can be up to 320 characters long. The `unique_name` value can be used as an alternative to the `sid` in the URL path to address the resource.
  string unique_name = 2;
}

// Create a new Stream.
message CreateSyncStreamRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) to create the new Stream in. Required.
  string service_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateSyncStreamRequestBody body = 2;
}

// Canonical success response.
message CreateSyncStreamResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceSyncStream body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch a specific Stream.
message FetchSyncStreamRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync Stream resource to fetch. Required.
  string service_sid = 1;
  // The SID of the Stream resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchSyncStreamResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceSyncStream body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateSyncStreamRequest
message UpdateSyncStreamRequestBody {
  // How long, [in seconds](https://www.twilio.com/docs/sync/limits#sync-payload-limits), before the Stream expires and is deleted (time-to-live).
  int64 ttl = 1;
}

// Update a specific Stream.
message UpdateSyncStreamRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync Stream resource to update. Required.
  string service_sid = 1;
  // The SID of the Stream resource to update. Required.
  string sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateSyncStreamRequestBody body = 3;
}

// Canonical success response.
message UpdateSyncStreamResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceSyncStream body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete a specific Stream.
message DeleteSyncStreamRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Sync Stream resource to delete. Required.
  string service_sid = 1;
  // The SID of the Stream resource to delete. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteSyncStreamResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// CreateStreamMessageRequest
message CreateStreamMessageRequestBody {
  // A JSON string that represents an arbitrary, schema-less object that makes up the Stream Message body. Can be up to 4 KiB in length.
  string data = 1;
}

// Create a new Stream Message.
message CreateStreamMessageRequest {
  // The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) to create the new Stream Message in. Required.
  string service_sid = 1;
  // The SID of the Sync Stream to create the new Stream Message resource for. Required.
  string stream_sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateStreamMessageRequestBody body = 3;
}

// Canonical success response.
message CreateStreamMessageResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1ServiceSyncStreamStreamMessage body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchServiceRequest {
  // The SID of the Service resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1Service body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateServiceRequest
message UpdateServiceRequestBody {
  // Whether token identities in the Service must be granted access to Sync objects by using the [Permissions](https://www.twilio.com/docs/sync/api/sync-permissions) resource.
  bool acl_enabled = 1;
  // A string that you assign to describe the resource.
  string friendly_name = 2;
  // Whether every `endpoint_disconnected` event should occur after a configurable delay. The default is `false`, where the `endpoint_disconnected` event occurs immediately after disconnection. When `true`, intervening reconnections can prevent the `endpoint_disconnected` event.
  bool reachability_debouncing_enabled = 3;
  // The reachability event delay in milliseconds if `reachability_debouncing_enabled` = `true`.  Must be between 1,000 and 30,000 and defaults to 5,000. This is the number of milliseconds after the last running client disconnects, and a Sync identity is declared offline, before the webhook is called if all endpoints remain offline. A reconnection from the same identity by any endpoint during this interval prevents the webhook from being called.
  int64 reachability_debouncing_window = 4;
  // Whether the service instance should call `webhook_url` when client endpoints connect to Sync. The default is `false`.
  bool reachability_webhooks_enabled = 5;
  // The URL we should call when Sync objects are manipulated.
  string webhook_url = 6;
  // Whether the Service instance should call `webhook_url` when the REST API is used to update Sync objects. The default is `false`.
  bool webhooks_from_rest_enabled = 7;
}

message UpdateServiceRequest {
  // The SID of the Service resource to update. Required.
  string sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateServiceRequestBody body = 2;
}

// Canonical success response.
message UpdateServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  SyncV1Service body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message DeleteServiceRequest {
  // The SID of the Service resource to delete. Required.
  string sid = 1;
}

// Canonical success response.
message DeleteServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// Generated from SyncV1Service operations.
service SyncV1ServiceService {
  rpc ListService(ListServiceRequest) returns (ListServiceResponse) {
    option (google.api.http) = {
      get: "/v1/Services"
      response_body: "body"
    };
  }
  rpc CreateService(CreateServiceRequest) returns (CreateServiceResponse) {
    option (google.api.http) = {
      post: "/v1/Services"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchService(FetchServiceRequest) returns (FetchServiceResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateService(UpdateServiceRequest) returns (UpdateServiceResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteService(DeleteServiceRequest) returns (DeleteServiceResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{sid}"
    };
  }
}

// Generated from SyncV1Document operations.
service SyncV1DocumentService {
  rpc ListDocument(ListDocumentRequest) returns (ListDocumentResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Documents"
      response_body: "body"
    };
  }
  rpc CreateDocument(CreateDocumentRequest) returns (CreateDocumentResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/Documents"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchDocument(FetchDocumentRequest) returns (FetchDocumentResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Documents/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateDocument(UpdateDocumentRequest) returns (UpdateDocumentResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/Documents/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteDocument(DeleteDocumentRequest) returns (DeleteDocumentResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{service_sid}/Documents/{sid}"
    };
  }
}

// Generated from SyncV1DocumentPermission operations.
service SyncV1DocumentPermissionService {
  // Retrieve a list of all Permissions applying to a Sync Document.
  rpc ListDocumentPermission(ListDocumentPermissionRequest) returns (ListDocumentPermissionResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Documents/{document_sid}/Permissions"
      response_body: "body"
    };
  }
  // Fetch a specific Sync Document Permission.
  rpc FetchDocumentPermission(FetchDocumentPermissionRequest) returns (FetchDocumentPermissionResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Documents/{document_sid}/Permissions/{identity}"
      response_body: "body"
    };
  }
  // Update an identity's access to a specific Sync Document.
  rpc UpdateDocumentPermission(UpdateDocumentPermissionRequest) returns (UpdateDocumentPermissionResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/Documents/{document_sid}/Permissions/{identity}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete a specific Sync Document Permission.
  rpc DeleteDocumentPermission(DeleteDocumentPermissionRequest) returns (DeleteDocumentPermissionResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{service_sid}/Documents/{document_sid}/Permissions/{identity}"
    };
  }
}

// Generated from SyncV1SyncList operations.
service SyncV1SyncListService {
  rpc ListSyncList(ListSyncListRequest) returns (ListSyncListResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Lists"
      response_body: "body"
    };
  }
  rpc CreateSyncList(CreateSyncListRequest) returns (CreateSyncListResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/Lists"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchSyncList(FetchSyncListRequest) returns (FetchSyncListResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Lists/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateSyncList(UpdateSyncListRequest) returns (UpdateSyncListResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/Lists/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteSyncList(DeleteSyncListRequest) returns (DeleteSyncListResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{service_sid}/Lists/{sid}"
    };
  }
}

// Generated from SyncV1SyncListItem operations.
service SyncV1SyncListItemService {
  rpc ListSyncListItem(ListSyncListItemRequest) returns (ListSyncListItemResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Lists/{list_sid}/Items"
      response_body: "body"
    };
  }
  rpc CreateSyncListItem(CreateSyncListItemRequest) returns (CreateSyncListItemResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/Lists/{list_sid}/Items"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchSyncListItem(FetchSyncListItemRequest) returns (FetchSyncListItemResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Lists/{list_sid}/Items/{index}"
      response_body: "body"
    };
  }
  rpc UpdateSyncListItem(UpdateSyncListItemRequest) returns (UpdateSyncListItemResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/Lists/{list_sid}/Items/{index}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteSyncListItem(DeleteSyncListItemRequest) returns (DeleteSyncListItemResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{service_sid}/Lists/{list_sid}/Items/{index}"
    };
  }
}

// Generated from SyncV1SyncListPermission operations.
service SyncV1SyncListPermissionService {
  // Retrieve a list of all Permissions applying to a Sync List.
  rpc ListSyncListPermission(ListSyncListPermissionRequest) returns (ListSyncListPermissionResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Lists/{list_sid}/Permissions"
      response_body: "body"
    };
  }
  // Fetch a specific Sync List Permission.
  rpc FetchSyncListPermission(FetchSyncListPermissionRequest) returns (FetchSyncListPermissionResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Lists/{list_sid}/Permissions/{identity}"
      response_body: "body"
    };
  }
  // Update an identity's access to a specific Sync List.
  rpc UpdateSyncListPermission(UpdateSyncListPermissionRequest) returns (UpdateSyncListPermissionResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/Lists/{list_sid}/Permissions/{identity}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete a specific Sync List Permission.
  rpc DeleteSyncListPermission(DeleteSyncListPermissionRequest) returns (DeleteSyncListPermissionResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{service_sid}/Lists/{list_sid}/Permissions/{identity}"
    };
  }
}

// Generated from SyncV1SyncMap operations.
service SyncV1SyncMapService {
  rpc ListSyncMap(ListSyncMapRequest) returns (ListSyncMapResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Maps"
      response_body: "body"
    };
  }
  rpc CreateSyncMap(CreateSyncMapRequest) returns (CreateSyncMapResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/Maps"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchSyncMap(FetchSyncMapRequest) returns (FetchSyncMapResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Maps/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateSyncMap(UpdateSyncMapRequest) returns (UpdateSyncMapResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/Maps/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteSyncMap(DeleteSyncMapRequest) returns (DeleteSyncMapResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{service_sid}/Maps/{sid}"
    };
  }
}

// Generated from SyncV1SyncMapItem operations.
service SyncV1SyncMapItemService {
  rpc ListSyncMapItem(ListSyncMapItemRequest) returns (ListSyncMapItemResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Maps/{map_sid}/Items"
      response_body: "body"
    };
  }
  rpc CreateSyncMapItem(CreateSyncMapItemRequest) returns (CreateSyncMapItemResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/Maps/{map_sid}/Items"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchSyncMapItem(FetchSyncMapItemRequest) returns (FetchSyncMapItemResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Maps/{map_sid}/Items/{key}"
      response_body: "body"
    };
  }
  rpc UpdateSyncMapItem(UpdateSyncMapItemRequest) returns (UpdateSyncMapItemResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/Maps/{map_sid}/Items/{key}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteSyncMapItem(DeleteSyncMapItemRequest) returns (DeleteSyncMapItemResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{service_sid}/Maps/{map_sid}/Items/{key}"
    };
  }
}

// Generated from SyncV1SyncMapPermission operations.
service SyncV1SyncMapPermissionService {
  // Retrieve a list of all Permissions applying to a Sync Map.
  rpc ListSyncMapPermission(ListSyncMapPermissionRequest) returns (ListSyncMapPermissionResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Maps/{map_sid}/Permissions"
      response_body: "body"
    };
  }
  // Fetch a specific Sync Map Permission.
  rpc FetchSyncMapPermission(FetchSyncMapPermissionRequest) returns (FetchSyncMapPermissionResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Maps/{map_sid}/Permissions/{identity}"
      response_body: "body"
    };
  }
  // Update an identity's access to a specific Sync Map.
  rpc UpdateSyncMapPermission(UpdateSyncMapPermissionRequest) returns (UpdateSyncMapPermissionResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/Maps/{map_sid}/Permissions/{identity}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete a specific Sync Map Permission.
  rpc DeleteSyncMapPermission(DeleteSyncMapPermissionRequest) returns (DeleteSyncMapPermissionResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{service_sid}/Maps/{map_sid}/Permissions/{identity}"
    };
  }
}

// Generated from SyncV1SyncStream operations.
service SyncV1SyncStreamService {
  // Retrieve a list of all Streams in a Service Instance.
  rpc ListSyncStream(ListSyncStreamRequest) returns (ListSyncStreamResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Streams"
      response_body: "body"
    };
  }
  // Create a new Stream.
  rpc CreateSyncStream(CreateSyncStreamRequest) returns (CreateSyncStreamResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/Streams"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch a specific Stream.
  rpc FetchSyncStream(FetchSyncStreamRequest) returns (FetchSyncStreamResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Streams/{sid}"
      response_body: "body"
    };
  }
  // Update a specific Stream.
  rpc UpdateSyncStream(UpdateSyncStreamRequest) returns (UpdateSyncStreamResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/Streams/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete a specific Stream.
  rpc DeleteSyncStream(DeleteSyncStreamRequest) returns (DeleteSyncStreamResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{service_sid}/Streams/{sid}"
    };
  }
}

// Generated from SyncV1StreamMessage operations.
service SyncV1StreamMessageService {
  // Create a new Stream Message.
  rpc CreateStreamMessage(CreateStreamMessageRequest) returns (CreateStreamMessageResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/Streams/{stream_sid}/Messages"
      body: "body"
      response_body: "body"
    };
  }
}

