// Code generated by openapi2proto. DO NOT EDIT.
// Source: twilio_proxy_v1.json

syntax = "proto3";

package twilio.proxy.v1;

option go_package = "twilio/proxy/v1;twilioproxyv1";

import "google/api/annotations.proto";

// The inbound resource status of the Interaction. Will always be `delivered` for messages and `in-progress` for calls.
message InteractionEnumResourceStatus {
  // Scalar value wrapper.
  string value = 1;
}

// The Type of the Interaction. Can be: `message`, `voice` or `unknown`.
message InteractionEnumType {
  // Scalar value wrapper.
  string value = 1;
}

// Always empty for created Message Interactions.
message MessageInteractionEnumResourceStatus {
  // Scalar value wrapper.
  string value = 1;
}

// The Type of Message Interaction. This value is always `message`.
message MessageInteractionEnumType {
  // Scalar value wrapper.
  string value = 1;
}

message ProxyV1Service {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Service resource.
  string account_sid = 1;
  // The URL we call when the interaction status changes.
  string callback_url = 2;
  // The SID of the Chat Service Instance managed by Proxy Service. The Chat Service enables Proxy to forward SMS and channel messages to this chat instance. This is a one-to-one relationship.
  string chat_instance_sid = 3;
  // The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date and time in GMT when the resource was created.
  string date_created = 4;
  // The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date and time in GMT when the resource was last updated.
  string date_updated = 5;
  // The default `ttl` value for Sessions created in the Service. The TTL (time to live) is measured in seconds after the Session's last create or last Interaction. The default value of `0` indicates an unlimited Session length. You can override a Session's default TTL value by setting its `ttl` value.
  int64 default_ttl = 6;
  // OpenAPI property "geo_match_level".
  ServiceEnumGeoMatchLevel geo_match_level = 7;
  // The URL we call on each interaction. If we receive a 403 status, we block the interaction; otherwise the interaction continues.
  string intercept_callback_url = 8;
  // The URLs of resources related to the Service.
  ProxyV1ServiceLinks links = 9;
  // OpenAPI property "number_selection_behavior".
  ServiceEnumNumberSelectionBehavior number_selection_behavior = 10;
  // The URL we call when an inbound call or SMS action occurs on a closed or non-existent Session. If your server (or a Twilio [function](https://www.twilio.com/en-us/serverless/functions)) responds with valid [TwiML](https://www.twilio.com/docs/voice/twiml), we will process it. This means it is possible, for example, to play a message for a call, send an automated text message response, or redirect a call to another Phone Number. See [Out-of-Session Callback Response Guide](https://www.twilio.com/docs/proxy/out-session-callback-response-guide) for more information.
  string out_of_session_callback_url = 11;
  // The unique string that we created to identify the Service resource.
  string sid = 12;
  // An application-defined string that uniquely identifies the resource. This value must be 191 characters or fewer in length and be unique. Supports UTF-8 characters. **This value should not have PII.**
  string unique_name = 13;
  // The absolute URL of the Service resource.
  string url = 14;
}

// Where a proxy number must be located relative to the participant identifier. Can be: `country`, `area-code`, or `extended-area-code`. The default value is `country` and more specific areas than `country` are only available in North America.
message ServiceEnumGeoMatchLevel {
  // Scalar value wrapper.
  string value = 1;
}

// The URLs of resources related to the Service.
message ProxyV1ServiceLinks {
  // Scalar value wrapper.
  string value = 1;
}

// The preference for Proxy Number selection in the Service instance. Can be: `prefer-sticky` or `avoid-sticky`. `prefer-sticky` means that we will try and select the same Proxy Number for a given participant if they have previous [Sessions](https://www.twilio.com/docs/proxy/api/session), but we will not fail if that Proxy Number cannot be used.  `avoid-sticky` means that we will try to use different Proxy Numbers as long as that is possible within a given pool rather than try and use a previously assigned number.
message ServiceEnumNumberSelectionBehavior {
  // Scalar value wrapper.
  string value = 1;
}

message ProxyV1ServicePhoneNumber {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the PhoneNumber resource.
  string account_sid = 1;
  // The capabilities of the phone number.
  ProxyV1ServicePhoneNumberCapabilities capabilities = 2;
  // The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date and time in GMT when the resource was created.
  string date_created = 3;
  // The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date and time in GMT when the resource was last updated.
  string date_updated = 4;
  // The string that you assigned to describe the resource.
  string friendly_name = 5;
  // The number of open session assigned to the number. See the [How many Phone Numbers do I need?](https://www.twilio.com/docs/proxy/phone-numbers-needed) guide for more information.
  int64 in_use = 6;
  // Whether the phone number should be reserved and not be assigned to a participant using proxy pool logic. See [Reserved Phone Numbers](https://www.twilio.com/docs/proxy/reserved-phone-numbers) for more information.
  bool is_reserved = 7;
  // The ISO Country Code for the phone number.
  string iso_country = 8;
  // The phone number in [E.164](https://www.twilio.com/docs/glossary/what-e164) format, which consists of a + followed by the country code and subscriber number.
  string phone_number = 9;
  // The SID of the PhoneNumber resource's parent [Service](https://www.twilio.com/docs/proxy/api/service) resource.
  string service_sid = 10;
  // The unique string that we created to identify the PhoneNumber resource.
  string sid = 11;
  // The absolute URL of the PhoneNumber resource.
  string url = 12;
}

// The capabilities of the phone number.
message ProxyV1ServicePhoneNumberCapabilities {
  bool fax = 1;
  bool mms = 2;
  bool sms = 3;
  bool voice = 4;
}

message ProxyV1ServiceSession {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Session resource.
  string account_sid = 1;
  // The reason the Session ended.
  string closed_reason = 2;
  // The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date and time in GMT when the resource was created.
  string date_created = 3;
  // The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date when the Session ended.
  string date_ended = 4;
  // The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date when the Session should expire. If this is value is present, it overrides the `ttl` value.
  string date_expiry = 5;
  // The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date when the Session last had an interaction.
  string date_last_interaction = 6;
  // The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date when the Session started.
  string date_started = 7;
  // The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date and time in GMT when the resource was last updated.
  string date_updated = 8;
  // The URLs of resources related to the Session.
  ProxyV1ServiceSessionLinks links = 9;
  // OpenAPI property "mode".
  SessionEnumMode mode = 10;
  // The SID of the [Service](https://www.twilio.com/docs/proxy/api/service) the session is associated with.
  string service_sid = 11;
  // The unique string that we created to identify the Session resource.
  string sid = 12;
  // OpenAPI property "status".
  SessionEnumStatus status = 13;
  // The time, in seconds, when the session will expire. The time is measured from the last Session create or the Session's last Interaction.
  int64 ttl = 14;
  // An application-defined string that uniquely identifies the resource. This value must be 191 characters or fewer in length and be unique. Supports UTF-8 characters. **This value should not have PII.**
  string unique_name = 15;
  // The absolute URL of the Session resource.
  string url = 16;
}

// The URLs of resources related to the Session.
message ProxyV1ServiceSessionLinks {
  // Scalar value wrapper.
  string value = 1;
}

// The Mode of the Session. Can be: `message-only`, `voice-only`, or `voice-and-message`.
message SessionEnumMode {
  // Scalar value wrapper.
  string value = 1;
}

// The status of the Session. Can be: `open`, `in-progress`, `closed`, `failed`, or `unknown`.
message SessionEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

message ProxyV1ServiceSessionInteraction {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Interaction resource.
  string account_sid = 1;
  // A JSON string that includes the message body of message interactions (e.g. `{"body": "hello"}`) or the call duration (when available) of a call (e.g. `{"duration": "5"}`).
  string data = 2;
  // The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date and time in GMT when the Interaction was created.
  string date_created = 3;
  // The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date and time in GMT when the resource was last updated.
  string date_updated = 4;
  // The SID of the inbound [Participant](https://www.twilio.com/docs/proxy/api/participant) resource.
  string inbound_participant_sid = 5;
  // The SID of the inbound resource; either the [Call](https://www.twilio.com/docs/voice/api/call-resource) or [Message](https://www.twilio.com/docs/sms/api/message-resource).
  string inbound_resource_sid = 6;
  // OpenAPI property "inbound_resource_status".
  InteractionEnumResourceStatus inbound_resource_status = 7;
  // The inbound resource type. Can be [Call](https://www.twilio.com/docs/voice/api/call-resource) or [Message](https://www.twilio.com/docs/sms/api/message-resource).
  string inbound_resource_type = 8;
  // The URL of the Twilio inbound resource
  string inbound_resource_url = 9;
  // The SID of the outbound [Participant](https://www.twilio.com/docs/proxy/api/participant)).
  string outbound_participant_sid = 10;
  // The SID of the outbound resource; either the [Call](https://www.twilio.com/docs/voice/api/call-resource) or [Message](https://www.twilio.com/docs/sms/api/message-resource).
  string outbound_resource_sid = 11;
  // OpenAPI property "outbound_resource_status".
  InteractionEnumResourceStatus outbound_resource_status = 12;
  // The outbound resource type. Can be: [Call](https://www.twilio.com/docs/voice/api/call-resource) or [Message](https://www.twilio.com/docs/sms/api/message-resource).
  string outbound_resource_type = 13;
  // The URL of the Twilio outbound resource.
  string outbound_resource_url = 14;
  // The SID of the parent [Service](https://www.twilio.com/docs/proxy/api/service) resource.
  string service_sid = 15;
  // The SID of the parent [Session](https://www.twilio.com/docs/proxy/api/session) resource.
  string session_sid = 16;
  // The unique string that we created to identify the Interaction resource.
  string sid = 17;
  // OpenAPI property "type".
  InteractionEnumType type = 18;
  // The absolute URL of the Interaction resource.
  string url = 19;
}

message ProxyV1ServiceSessionParticipant {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Participant resource.
  string account_sid = 1;
  // The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date and time in GMT when the resource was created.
  string date_created = 2;
  // The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date when the Participant was removed from the session.
  string date_deleted = 3;
  // The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date and time in GMT when the resource was last updated.
  string date_updated = 4;
  // The string that you assigned to describe the participant. This value must be 255 characters or fewer. Supports UTF-8 characters. **This value should not have PII.**
  string friendly_name = 5;
  // The phone number or channel identifier of the Participant. This value must be 191 characters or fewer. Supports UTF-8 characters.
  string identifier = 6;
  // The URLs to resources related the participant.
  ProxyV1ServiceSessionParticipantLinks links = 7;
  // The phone number or short code (masked number) of the participant's partner. The participant will call or message the partner participant at this number.
  string proxy_identifier = 8;
  // The SID of the Proxy Identifier assigned to the Participant.
  string proxy_identifier_sid = 9;
  // The SID of the resource's parent [Service](https://www.twilio.com/docs/proxy/api/service) resource.
  string service_sid = 10;
  // The SID of the parent [Session](https://www.twilio.com/docs/proxy/api/session) resource.
  string session_sid = 11;
  // The unique string that we created to identify the Participant resource.
  string sid = 12;
  // The absolute URL of the Participant resource.
  string url = 13;
}

// The URLs to resources related the participant.
message ProxyV1ServiceSessionParticipantLinks {
  // Scalar value wrapper.
  string value = 1;
}

message ProxyV1ServiceSessionParticipantMessageInteraction {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the MessageInteraction resource.
  string account_sid = 1;
  // A JSON string that includes the message body sent to the participant. (e.g. `{"body": "hello"}`)
  string data = 2;
  // The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date and time in GMT when the resource was created.
  string date_created = 3;
  // The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date and time in GMT when the resource was last updated.
  string date_updated = 4;
  // Always empty for created Message Interactions.
  string inbound_participant_sid = 5;
  // Always empty for created Message Interactions.
  string inbound_resource_sid = 6;
  // OpenAPI property "inbound_resource_status".
  MessageInteractionEnumResourceStatus inbound_resource_status = 7;
  // Always empty for created Message Interactions.
  string inbound_resource_type = 8;
  // Always empty for created Message Interactions.
  string inbound_resource_url = 9;
  // The SID of the outbound [Participant](https://www.twilio.com/docs/proxy/api/participant) resource.
  string outbound_participant_sid = 10;
  // The SID of the outbound [Message](https://www.twilio.com/docs/sms/api/message-resource) resource.
  string outbound_resource_sid = 11;
  // OpenAPI property "outbound_resource_status".
  MessageInteractionEnumResourceStatus outbound_resource_status = 12;
  // The outbound resource type. This value is always `Message`.
  string outbound_resource_type = 13;
  // The URL of the Twilio message resource.
  string outbound_resource_url = 14;
  // The SID of the [Participant](https://www.twilio.com/docs/proxy/api/participant) resource.
  string participant_sid = 15;
  // The SID of the parent [Service](https://www.twilio.com/docs/proxy/api/service) resource.
  string service_sid = 16;
  // The SID of the parent [Session](https://www.twilio.com/docs/proxy/api/session) resource.
  string session_sid = 17;
  // The unique string that we created to identify the MessageInteraction resource.
  string sid = 18;
  // OpenAPI property "type".
  MessageInteractionEnumType type = 19;
  // The absolute URL of the MessageInteraction resource.
  string url = 20;
}

// ListServiceResponse
message ListServiceResponseBody {
  ListServiceResponseBodyMeta meta = 1;
  repeated ProxyV1Service services = 2;
}

message ListServiceResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Services for Twilio Proxy. A maximum of 100 records will be returned per page.
message ListServiceRequest {
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 1;
  // The page index. This value is simply for client state.
  int64 page = 2;
  // The page token. This is provided by the API.
  string page_token = 3;
}

// Canonical success response.
message ListServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListServiceResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateServiceRequest
message CreateServiceRequestBody {
  // The URL we should call when the interaction status changes.
  string callback_url = 1;
  // The SID of the Chat Service Instance managed by Proxy Service. The Chat Service enables Proxy to forward SMS and channel messages to this chat instance. This is a one-to-one relationship.
  string chat_instance_sid = 2;
  // The default `ttl` value to set for Sessions created in the Service. The TTL (time to live) is measured in seconds after the Session's last create or last Interaction. The default value of `0` indicates an unlimited Session length. You can override a Session's default TTL value by setting its `ttl` value.
  int64 default_ttl = 3;
  // OpenAPI property "GeoMatchLevel".
  ServiceEnumGeoMatchLevel geo_match_level = 4;
  // The URL we call on each interaction. If we receive a 403 status, we block the interaction; otherwise the interaction continues.
  string intercept_callback_url = 5;
  // OpenAPI property "NumberSelectionBehavior".
  ServiceEnumNumberSelectionBehavior number_selection_behavior = 6;
  // The URL we should call when an inbound call or SMS action occurs on a closed or non-existent Session. If your server (or a Twilio [function](https://www.twilio.com/en-us/serverless/functions)) responds with valid [TwiML](https://www.twilio.com/docs/voice/twiml), we will process it. This means it is possible, for example, to play a message for a call, send an automated text message response, or redirect a call to another Phone Number. See [Out-of-Session Callback Response Guide](https://www.twilio.com/docs/proxy/out-session-callback-response-guide) for more information.
  string out_of_session_callback_url = 7;
  // An application-defined string that uniquely identifies the resource. This value must be 191 characters or fewer in length and be unique. **This value should not have PII.**
  string unique_name = 8;
}

// Create a new Service for Twilio Proxy
message CreateServiceRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateServiceRequestBody body = 1;
}

// Canonical success response.
message CreateServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ProxyV1Service body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListPhoneNumberResponse
message ListPhoneNumberResponseBody {
  ListPhoneNumberResponseBodyMeta meta = 1;
  repeated ProxyV1ServicePhoneNumber phone_numbers = 2;
}

message ListPhoneNumberResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Phone Numbers in the Proxy Number Pool for a Service. A maximum of 100 records will be returned per page.
message ListPhoneNumberRequest {
  // The SID of the parent [Service](https://www.twilio.com/docs/proxy/api/service) of the PhoneNumber resources to read. Required.
  string service_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListPhoneNumberResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListPhoneNumberResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreatePhoneNumberRequest
message CreatePhoneNumberRequestBody {
  // Whether the new phone number should be reserved and not be assigned to a participant using proxy pool logic. See [Reserved Phone Numbers](https://www.twilio.com/docs/proxy/reserved-phone-numbers) for more information.
  bool is_reserved = 1;
  // The phone number in [E.164](https://www.twilio.com/docs/glossary/what-e164) format.  E.164 phone numbers consist of a + followed by the country code and subscriber number without punctuation characters. For example, +14155551234.
  string phone_number = 2;
  // The SID of a Twilio [IncomingPhoneNumber](https://www.twilio.com/docs/phone-numbers/api/incomingphonenumber-resource) resource that represents the Twilio Number you would like to assign to your Proxy Service.
  string sid = 3;
}

// Add a Phone Number to a Service's Proxy Number Pool.
message CreatePhoneNumberRequest {
  // The SID parent [Service](https://www.twilio.com/docs/proxy/api/service) resource of the new PhoneNumber resource. Required.
  string service_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreatePhoneNumberRequestBody body = 2;
}

// Canonical success response.
message CreatePhoneNumberResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ProxyV1ServicePhoneNumber body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch a specific Phone Number.
message FetchPhoneNumberRequest {
  // The SID of the parent [Service](https://www.twilio.com/docs/proxy/api/service) of the PhoneNumber resource to fetch. Required.
  string service_sid = 1;
  // The Twilio-provided string that uniquely identifies the PhoneNumber resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchPhoneNumberResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ProxyV1ServicePhoneNumber body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdatePhoneNumberRequest
message UpdatePhoneNumberRequestBody {
  // Whether the phone number should be reserved and not be assigned to a participant using proxy pool logic. See [Reserved Phone Numbers](https://www.twilio.com/docs/proxy/reserved-phone-numbers) for more information.
  bool is_reserved = 1;
}

// Update a specific Proxy Number.
message UpdatePhoneNumberRequest {
  // The SID of the parent [Service](https://www.twilio.com/docs/proxy/api/service) of the PhoneNumber resource to update. Required.
  string service_sid = 1;
  // The Twilio-provided string that uniquely identifies the PhoneNumber resource to update. Required.
  string sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdatePhoneNumberRequestBody body = 3;
}

// Canonical success response.
message UpdatePhoneNumberResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ProxyV1ServicePhoneNumber body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete a specific Phone Number from a Service.
message DeletePhoneNumberRequest {
  // The SID of the parent [Service](https://www.twilio.com/docs/proxy/api/service) of the PhoneNumber resource to delete. Required.
  string service_sid = 1;
  // The Twilio-provided string that uniquely identifies the PhoneNumber resource to delete. Required.
  string sid = 2;
}

// Canonical success response.
message DeletePhoneNumberResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListSessionResponse
message ListSessionResponseBody {
  ListSessionResponseBodyMeta meta = 1;
  repeated ProxyV1ServiceSession sessions = 2;
}

message ListSessionResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Sessions for the Service. A maximum of 100 records will be returned per page.
message ListSessionRequest {
  // The SID of the parent [Service](https://www.twilio.com/docs/proxy/api/service) of the resource to read. Required.
  string service_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListSessionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListSessionResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateSessionRequest
message CreateSessionRequestBody {
  // The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date when the Session should expire. If this is value is present, it overrides the `ttl` value.
  string date_expiry = 1;
  // OpenAPI property "Mode".
  SessionEnumMode mode = 2;
  // The Participant objects to include in the new session.
  repeated string participants = 3;
  // OpenAPI property "Status".
  SessionEnumStatus status = 4;
  // The time, in seconds, when the session will expire. The time is measured from the last Session create or the Session's last Interaction.
  int64 ttl = 5;
  // An application-defined string that uniquely identifies the resource. This value must be 191 characters or fewer in length and be unique. **This value should not have PII.**
  string unique_name = 6;
}

// Create a new Session
message CreateSessionRequest {
  // The SID of the parent [Service](https://www.twilio.com/docs/proxy/api/service) resource. Required.
  string service_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateSessionRequestBody body = 2;
}

// Canonical success response.
message CreateSessionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ProxyV1ServiceSession body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListInteractionResponse
message ListInteractionResponseBody {
  repeated ProxyV1ServiceSessionInteraction interactions = 1;
  ListInteractionResponseBodyMeta meta = 2;
}

message ListInteractionResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Interactions for a Session. A maximum of 100 records will be returned per page.
message ListInteractionRequest {
  // The SID of the parent [Service](https://www.twilio.com/docs/proxy/api/service) to read the resources from. Required.
  string service_sid = 1;
  // The SID of the parent [Session](https://www.twilio.com/docs/proxy/api/session) to read the resources from. Required.
  string session_sid = 2;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 3;
  // The page index. This value is simply for client state.
  int64 page = 4;
  // The page token. This is provided by the API.
  string page_token = 5;
}

// Canonical success response.
message ListInteractionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListInteractionResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Retrieve a list of Interactions for a given [Session](https://www.twilio.com/docs/proxy/api/session).
message FetchInteractionRequest {
  // The SID of the parent [Service](https://www.twilio.com/docs/proxy/api/service) of the resource to fetch. Required.
  string service_sid = 1;
  // The SID of the parent [Session](https://www.twilio.com/docs/proxy/api/session) of the resource to fetch. Required.
  string session_sid = 2;
  // The Twilio-provided string that uniquely identifies the Interaction resource to fetch. Required.
  string sid = 3;
}

// Canonical success response.
message FetchInteractionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ProxyV1ServiceSessionInteraction body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete a specific Interaction.
message DeleteInteractionRequest {
  // The SID of the parent [Service](https://www.twilio.com/docs/proxy/api/service) of the resource to delete. Required.
  string service_sid = 1;
  // The SID of the parent [Session](https://www.twilio.com/docs/proxy/api/session) of the resource to delete. Required.
  string session_sid = 2;
  // The Twilio-provided string that uniquely identifies the Interaction resource to delete. Required.
  string sid = 3;
}

// Canonical success response.
message DeleteInteractionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// ListParticipantResponse
message ListParticipantResponseBody {
  ListParticipantResponseBodyMeta meta = 1;
  repeated ProxyV1ServiceSessionParticipant participants = 2;
}

message ListParticipantResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of all Participants in a Session.
message ListParticipantRequest {
  // The SID of the parent [Service](https://www.twilio.com/docs/proxy/api/service) of the resources to read. Required.
  string service_sid = 1;
  // The SID of the parent [Session](https://www.twilio.com/docs/proxy/api/session) of the resources to read. Required.
  string session_sid = 2;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 3;
  // The page index. This value is simply for client state.
  int64 page = 4;
  // The page token. This is provided by the API.
  string page_token = 5;
}

// Canonical success response.
message ListParticipantResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListParticipantResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateParticipantRequest
message CreateParticipantRequestBody {
  // The string that you assigned to describe the participant. This value must be 255 characters or fewer. **This value should not have PII.**
  string friendly_name = 1;
  // The phone number of the Participant.
  string identifier = 2;
  // The proxy phone number to use for the Participant. If not specified, Proxy will select a number from the pool.
  string proxy_identifier = 3;
  // The SID of the Proxy Identifier to assign to the Participant.
  string proxy_identifier_sid = 4;
}

// Add a new Participant to the Session
message CreateParticipantRequest {
  // The SID of the parent [Service](https://www.twilio.com/docs/proxy/api/service) resource. Required.
  string service_sid = 1;
  // The SID of the parent [Session](https://www.twilio.com/docs/proxy/api/session) resource. Required.
  string session_sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateParticipantRequestBody body = 3;
}

// Canonical success response.
message CreateParticipantResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ProxyV1ServiceSessionParticipant body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// ListMessageInteractionResponse
message ListMessageInteractionResponseBody {
  repeated ProxyV1ServiceSessionParticipantMessageInteraction interactions = 1;
  ListMessageInteractionResponseBodyMeta meta = 2;
}

message ListMessageInteractionResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListMessageInteractionRequest {
  // The SID of the parent [Service](https://www.twilio.com/docs/proxy/api/service) to read the resources from. Required.
  string service_sid = 1;
  // The SID of the parent [Session](https://www.twilio.com/docs/proxy/api/session) to read the resources from. Required.
  string session_sid = 2;
  // The SID of the [Participant](https://www.twilio.com/docs/proxy/api/participant) to read the resources from. Required.
  string participant_sid = 3;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 4;
  // The page index. This value is simply for client state.
  int64 page = 5;
  // The page token. This is provided by the API.
  string page_token = 6;
}

// Canonical success response.
message ListMessageInteractionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListMessageInteractionResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// CreateMessageInteractionRequest
message CreateMessageInteractionRequestBody {
  // The message to send to the participant
  string body = 1;
  // Reserved. Not currently supported.
  repeated string media_url = 2;
}

// Create a new message Interaction to send directly from your system to one [Participant](https://www.twilio.com/docs/proxy/api/participant).  The `inbound` properties for the Interaction will always be empty.
message CreateMessageInteractionRequest {
  // The SID of the parent [Service](https://www.twilio.com/docs/proxy/api/service) resource. Required.
  string service_sid = 1;
  // The SID of the parent [Session](https://www.twilio.com/docs/proxy/api/session) resource. Required.
  string session_sid = 2;
  // The SID of the [Participant](https://www.twilio.com/docs/proxy/api/participant) resource. Required.
  string participant_sid = 3;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateMessageInteractionRequestBody body = 4;
}

// Canonical success response.
message CreateMessageInteractionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ProxyV1ServiceSessionParticipantMessageInteraction body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchMessageInteractionRequest {
  // The SID of the parent [Service](https://www.twilio.com/docs/proxy/api/service) of the resource to fetch. Required.
  string service_sid = 1;
  // The SID of the parent [Session](https://www.twilio.com/docs/proxy/api/session) of the resource to fetch. Required.
  string session_sid = 2;
  // The SID of the [Participant](https://www.twilio.com/docs/proxy/api/participant) resource. Required.
  string participant_sid = 3;
  // The Twilio-provided string that uniquely identifies the MessageInteraction resource to fetch. Required.
  string sid = 4;
}

// Canonical success response.
message FetchMessageInteractionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ProxyV1ServiceSessionParticipantMessageInteraction body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Fetch a specific Participant.
message FetchParticipantRequest {
  // The SID of the parent [Service](https://www.twilio.com/docs/proxy/api/service) of the resource to fetch. Required.
  string service_sid = 1;
  // The SID of the parent [Session](https://www.twilio.com/docs/proxy/api/session) of the resource to fetch. Required.
  string session_sid = 2;
  // The Twilio-provided string that uniquely identifies the Participant resource to fetch. Required.
  string sid = 3;
}

// Canonical success response.
message FetchParticipantResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ProxyV1ServiceSessionParticipant body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete a specific Participant. This is a soft-delete. The participant remains associated with the session and cannot be re-added. Participants are only permanently deleted when the [Session](https://www.twilio.com/docs/proxy/api/session) is deleted.
message DeleteParticipantRequest {
  // The SID of the parent [Service](https://www.twilio.com/docs/proxy/api/service) of the resource to delete. Required.
  string service_sid = 1;
  // The SID of the parent [Session](https://www.twilio.com/docs/proxy/api/session) of the resource to delete. Required.
  string session_sid = 2;
  // The Twilio-provided string that uniquely identifies the Participant resource to delete. Required.
  string sid = 3;
}

// Canonical success response.
message DeleteParticipantResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// Fetch a specific Session.
message FetchSessionRequest {
  // The SID of the parent [Service](https://www.twilio.com/docs/proxy/api/service) of the resource to fetch. Required.
  string service_sid = 1;
  // The Twilio-provided string that uniquely identifies the Session resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchSessionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ProxyV1ServiceSession body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateSessionRequest
message UpdateSessionRequestBody {
  // The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date when the Session should expire. If this is value is present, it overrides the `ttl` value.
  string date_expiry = 1;
  // OpenAPI property "Status".
  SessionEnumStatus status = 2;
  // The time, in seconds, when the session will expire. The time is measured from the last Session create or the Session's last Interaction.
  int64 ttl = 3;
}

// Update a specific Session.
message UpdateSessionRequest {
  // The SID of the parent [Service](https://www.twilio.com/docs/proxy/api/service) of the resource to update. Required.
  string service_sid = 1;
  // The Twilio-provided string that uniquely identifies the Session resource to update. Required.
  string sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateSessionRequestBody body = 3;
}

// Canonical success response.
message UpdateSessionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ProxyV1ServiceSession body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete a specific Session.
message DeleteSessionRequest {
  // The SID of the parent [Service](https://www.twilio.com/docs/proxy/api/service) of the resource to delete. Required.
  string service_sid = 1;
  // The Twilio-provided string that uniquely identifies the Session resource to delete. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteSessionResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// Fetch a specific Service.
message FetchServiceRequest {
  // The Twilio-provided string that uniquely identifies the Service resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ProxyV1Service body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// UpdateServiceRequest
message UpdateServiceRequestBody {
  // The URL we should call when the interaction status changes.
  string callback_url = 1;
  // The SID of the Chat Service Instance managed by Proxy Service. The Chat Service enables Proxy to forward SMS and channel messages to this chat instance. This is a one-to-one relationship.
  string chat_instance_sid = 2;
  // The default `ttl` value to set for Sessions created in the Service. The TTL (time to live) is measured in seconds after the Session's last create or last Interaction. The default value of `0` indicates an unlimited Session length. You can override a Session's default TTL value by setting its `ttl` value.
  int64 default_ttl = 3;
  // OpenAPI property "GeoMatchLevel".
  ServiceEnumGeoMatchLevel geo_match_level = 4;
  // The URL we call on each interaction. If we receive a 403 status, we block the interaction; otherwise the interaction continues.
  string intercept_callback_url = 5;
  // OpenAPI property "NumberSelectionBehavior".
  ServiceEnumNumberSelectionBehavior number_selection_behavior = 6;
  // The URL we should call when an inbound call or SMS action occurs on a closed or non-existent Session. If your server (or a Twilio [function](https://www.twilio.com/en-us/serverless/functions)) responds with valid [TwiML](https://www.twilio.com/docs/voice/twiml), we will process it. This means it is possible, for example, to play a message for a call, send an automated text message response, or redirect a call to another Phone Number. See [Out-of-Session Callback Response Guide](https://www.twilio.com/docs/proxy/out-session-callback-response-guide) for more information.
  string out_of_session_callback_url = 7;
  // An application-defined string that uniquely identifies the resource. This value must be 191 characters or fewer in length and be unique. **This value should not have PII.**
  string unique_name = 8;
}

// Update a specific Service.
message UpdateServiceRequest {
  // The Twilio-provided string that uniquely identifies the Service resource to update. Required.
  string sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateServiceRequestBody body = 2;
}

// Canonical success response.
message UpdateServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ProxyV1Service body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Delete a specific Service.
message DeleteServiceRequest {
  // The Twilio-provided string that uniquely identifies the Service resource to delete. Required.
  string sid = 1;
}

// Canonical success response.
message DeleteServiceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
}

// Generated from ProxyV1Service operations.
service ProxyV1ServiceService {
  // Retrieve a list of all Services for Twilio Proxy. A maximum of 100 records will be returned per page.
  rpc ListService(ListServiceRequest) returns (ListServiceResponse) {
    option (google.api.http) = {
      get: "/v1/Services"
      response_body: "body"
    };
  }
  // Create a new Service for Twilio Proxy
  rpc CreateService(CreateServiceRequest) returns (CreateServiceResponse) {
    option (google.api.http) = {
      post: "/v1/Services"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch a specific Service.
  rpc FetchService(FetchServiceRequest) returns (FetchServiceResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{sid}"
      response_body: "body"
    };
  }
  // Update a specific Service.
  rpc UpdateService(UpdateServiceRequest) returns (UpdateServiceResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete a specific Service.
  rpc DeleteService(DeleteServiceRequest) returns (DeleteServiceResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{sid}"
    };
  }
}

// Generated from ProxyV1PhoneNumber operations.
service ProxyV1PhoneNumberService {
  // Retrieve a list of all Phone Numbers in the Proxy Number Pool for a Service. A maximum of 100 records will be returned per page.
  rpc ListPhoneNumber(ListPhoneNumberRequest) returns (ListPhoneNumberResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/PhoneNumbers"
      response_body: "body"
    };
  }
  // Add a Phone Number to a Service's Proxy Number Pool.
  rpc CreatePhoneNumber(CreatePhoneNumberRequest) returns (CreatePhoneNumberResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/PhoneNumbers"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch a specific Phone Number.
  rpc FetchPhoneNumber(FetchPhoneNumberRequest) returns (FetchPhoneNumberResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/PhoneNumbers/{sid}"
      response_body: "body"
    };
  }
  // Update a specific Proxy Number.
  rpc UpdatePhoneNumber(UpdatePhoneNumberRequest) returns (UpdatePhoneNumberResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/PhoneNumbers/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete a specific Phone Number from a Service.
  rpc DeletePhoneNumber(DeletePhoneNumberRequest) returns (DeletePhoneNumberResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{service_sid}/PhoneNumbers/{sid}"
    };
  }
}

// Generated from ProxyV1Session operations.
service ProxyV1SessionService {
  // Retrieve a list of all Sessions for the Service. A maximum of 100 records will be returned per page.
  rpc ListSession(ListSessionRequest) returns (ListSessionResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Sessions"
      response_body: "body"
    };
  }
  // Create a new Session
  rpc CreateSession(CreateSessionRequest) returns (CreateSessionResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/Sessions"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch a specific Session.
  rpc FetchSession(FetchSessionRequest) returns (FetchSessionResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Sessions/{sid}"
      response_body: "body"
    };
  }
  // Update a specific Session.
  rpc UpdateSession(UpdateSessionRequest) returns (UpdateSessionResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/Sessions/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete a specific Session.
  rpc DeleteSession(DeleteSessionRequest) returns (DeleteSessionResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{service_sid}/Sessions/{sid}"
    };
  }
}

// Generated from ProxyV1Interaction operations.
service ProxyV1InteractionService {
  // Retrieve a list of all Interactions for a Session. A maximum of 100 records will be returned per page.
  rpc ListInteraction(ListInteractionRequest) returns (ListInteractionResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Sessions/{session_sid}/Interactions"
      response_body: "body"
    };
  }
  // Retrieve a list of Interactions for a given [Session](https://www.twilio.com/docs/proxy/api/session).
  rpc FetchInteraction(FetchInteractionRequest) returns (FetchInteractionResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Sessions/{session_sid}/Interactions/{sid}"
      response_body: "body"
    };
  }
  // Delete a specific Interaction.
  rpc DeleteInteraction(DeleteInteractionRequest) returns (DeleteInteractionResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{service_sid}/Sessions/{session_sid}/Interactions/{sid}"
    };
  }
}

// Generated from ProxyV1Participant operations.
service ProxyV1ParticipantService {
  // Retrieve a list of all Participants in a Session.
  rpc ListParticipant(ListParticipantRequest) returns (ListParticipantResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Sessions/{session_sid}/Participants"
      response_body: "body"
    };
  }
  // Add a new Participant to the Session
  rpc CreateParticipant(CreateParticipantRequest) returns (CreateParticipantResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/Sessions/{session_sid}/Participants"
      body: "body"
      response_body: "body"
    };
  }
  // Fetch a specific Participant.
  rpc FetchParticipant(FetchParticipantRequest) returns (FetchParticipantResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Sessions/{session_sid}/Participants/{sid}"
      response_body: "body"
    };
  }
  // Delete a specific Participant. This is a soft-delete. The participant remains associated with the session and cannot be re-added. Participants are only permanently deleted when the [Session](https://www.twilio.com/docs/proxy/api/session) is deleted.
  rpc DeleteParticipant(DeleteParticipantRequest) returns (DeleteParticipantResponse) {
    option (google.api.http) = {
      delete: "/v1/Services/{service_sid}/Sessions/{session_sid}/Participants/{sid}"
    };
  }
}

// Generated from ProxyV1MessageInteraction operations.
service ProxyV1MessageInteractionService {
  rpc ListMessageInteraction(ListMessageInteractionRequest) returns (ListMessageInteractionResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Sessions/{session_sid}/Participants/{participant_sid}/MessageInteractions"
      response_body: "body"
    };
  }
  // Create a new message Interaction to send directly from your system to one [Participant](https://www.twilio.com/docs/proxy/api/participant).  The `inbound` properties for the Interaction will always be empty.
  rpc CreateMessageInteraction(CreateMessageInteractionRequest) returns (CreateMessageInteractionResponse) {
    option (google.api.http) = {
      post: "/v1/Services/{service_sid}/Sessions/{session_sid}/Participants/{participant_sid}/MessageInteractions"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchMessageInteraction(FetchMessageInteractionRequest) returns (FetchMessageInteractionResponse) {
    option (google.api.http) = {
      get: "/v1/Services/{service_sid}/Sessions/{session_sid}/Participants/{participant_sid}/MessageInteractions/{sid}"
      response_body: "body"
    };
  }
}

