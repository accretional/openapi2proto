// Code generated by openapi2proto. DO NOT EDIT.
// Source: twilio_messaging_v2.json

syntax = "proto3";

package twilio.messaging.v2;

option go_package = "twilio/messaging/v2;twiliomessagingv2";

import "google/api/annotations.proto";

// The status of the sender.
message ChannelsSenderEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

// The configuration settings for creating a sender.
message MessagingV2ChannelsSenderConfiguration {
  // The account type for ISV Account Type Migration. Set to 'ISV' or 'ISVSubAccount' to configure, null or empty string to clear, or omit to preserve the existing value.
  string account_type = 1;
  // The verification code.
  string verification_code = 2;
  // The verification method.
  string verification_method = 3;
  // The SID of the Twilio Voice application.
  string voice_application_sid = 4;
  // The ID of the WhatsApp Business Account (WABA) to use for this sender.
  string waba_id = 5;
}

// The ID of the sender in `whatsapp:<E.164_PHONE_NUMBER>` format.
message MessagingV2ChannelsSenderFieldsSenderId {
  // Scalar value wrapper.
  string value = 1;
}

// The reasons why the sender is offline.
message MessagingV2ChannelsSenderOfflineReasons {
  // Array values.
  repeated MessagingV2ChannelsSenderOfflineReasonsItems items = 1;
}

message MessagingV2ChannelsSenderOfflineReasonsItems {
  // The error code.
  string code = 1;
  // The error message.
  string message_field = 2;
  // The URL to get more information about the error.
  string more_info = 3;
}

// The profile information for the sender.
message MessagingV2ChannelsSenderProfile {
  // The profile about text for the sender.
  string about = 1;
  // The color theme of the sender. Must be in hex format and have at least a 4:5:1 contrast ratio against white.
  string accent_color = 2;
  // The address of the sender.
  string address = 3;
  // The banner URL of the sender.
  string banner_url = 4;
  // The description of the sender.
  string description = 5;
  // The emails of the sender.
  string emails = 6;
  // The logo URL of the sender.
  string logo_url = 7;
  // The name of the sender. Required for WhatsApp senders and must follow [Meta's display name guidelines](https://www.facebook.com/business/help/757569725593362).
  string name = 8;
  // The phone numbers of the sender.
  string phone_numbers = 9;
  // The privacy URL of the sender. Must be a publicly accessible HTTP or HTTPS URI associated with the sender.
  string privacy_url = 10;
  // The terms of service URL of the sender.
  string terms_of_service_url = 11;
  // The vertical of the sender. Allowed values are:
  // - `Alcohol`
  // - `Automotive`
  // - `Beauty, Spa and Salon`
  // - `Clothing and Apparel`
  // - `Education`
  // - `Entertainment`
  // - `Event Planning and Service`
  // - `Finance and Banking`
  // - `Food and Grocery`
  // - `Hotel and Lodging`
  // - `Matrimony Service`
  // - `Medical and Health`
  // - `Non-profit`
  // - `Online Gambling`
  // - `OTC Drugs`
  // - `Other`
  // - `Physical Gambling`
  // - `Professional Services`
  // - `Public Service`
  // - `Restaurant`
  // - `Shopping and Retail`
  // - `Travel and Transportation`
  string vertical = 12;
  // The websites of the sender.
  string websites = 13;
}

// The profile information for the sender.
message MessagingV2ChannelsSenderProfileGenericResponse {
  // The profile about text for the sender.
  string about = 1;
  // The color theme of the sender. Must be in hex format and have at least a 4:5:1 contrast ratio against white.
  string accent_color = 2;
  // The address of the sender.
  string address = 3;
  // The banner URL of the sender.
  string banner_url = 4;
  // The description of the sender.
  string description = 5;
  // The emails of the sender.
  repeated MessagingV2ChannelsSenderProfileGenericResponseEmailsItem emails = 6;
  // The logo URL of the sender.
  string logo_url = 7;
  // The name of the sender.
  string name = 8;
  // The phone numbers of the sender.
  repeated MessagingV2ChannelsSenderProfileGenericResponsePhoneNumbersItem phone_numbers = 9;
  // The privacy URL of the sender. Must be a publicly accessible HTTP or HTTPS URI associated with the sender.
  string privacy_url = 10;
  // The terms of service URL of the sender.
  string terms_of_service_url = 11;
  // The vertical of the sender. Allowed values are:
  // - `Alcohol`
  // - `Automotive`
  // - `Beauty, Spa and Salon`
  // - `Clothing and Apparel`
  // - `Education`
  // - `Entertainment`
  // - `Event Planning and Service`
  // - `Finance and Banking`
  // - `Food and Grocery`
  // - `Hotel and Lodging`
  // - `Matrimony Service`
  // - `Medical and Health`
  // - `Non-profit`
  // - `Online Gambling`
  // - `OTC Drugs`
  // - `Other`
  // - `Physical Gambling`
  // - `Professional Services`
  // - `Public Service`
  // - `Restaurant`
  // - `Shopping and Retail`
  // - `Travel and Transportation`
  string vertical = 12;
  // The websites of the sender.
  repeated MessagingV2ChannelsSenderProfileGenericResponseWebsitesItem websites = 13;
}

message MessagingV2ChannelsSenderProfileGenericResponseEmailsItem {
  string email = 1;
  string label = 2;
}

message MessagingV2ChannelsSenderProfileGenericResponsePhoneNumbersItem {
  string label = 1;
  string phone_number = 2;
}

message MessagingV2ChannelsSenderProfileGenericResponseWebsitesItem {
  string label = 1;
  string website = 2;
}

// The additional properties for the sender.
message MessagingV2ChannelsSenderProperties {
  // The messaging limit of the sender.
  string messaging_limit = 1;
  // The quality rating of the sender.
  string quality_rating = 2;
}

message MessagingV2ChannelsSenderRequestsCreate {
  // OpenAPI property "configuration".
  MessagingV2ChannelsSenderConfiguration configuration = 1;
  // OpenAPI property "profile".
  MessagingV2ChannelsSenderProfile profile = 2;
  // OpenAPI property "sender_id".
  MessagingV2ChannelsSenderFieldsSenderId sender_id = 3;
  // OpenAPI property "webhook".
  MessagingV2ChannelsSenderWebhook webhook = 4;
}

// The configuration settings for webhooks.
message MessagingV2ChannelsSenderWebhook {
  // The HTTP method for the webhook.
  string callback_method = 1;
  // The URL to send the webhook to.
  string callback_url = 2;
  // The HTTP method for the fallback webhook.
  string fallback_method = 3;
  // The URL to send the fallback webhook to.
  string fallback_url = 4;
  // The HTTP method for the status callback.
  string status_callback_method = 5;
  // The URL to send the status callback to.
  string status_callback_url = 6;
}

message MessagingV2ChannelsSenderRequestsUpdate {
  // OpenAPI property "configuration".
  MessagingV2ChannelsSenderConfiguration configuration = 1;
  // OpenAPI property "profile".
  MessagingV2ChannelsSenderProfile profile = 2;
  // OpenAPI property "webhook".
  MessagingV2ChannelsSenderWebhook webhook = 3;
}

message MessagingV2ChannelsSenderResponse {
  // OpenAPI property "compliance".
  MessagingV2RcsComplianceResponse compliance = 1;
  // OpenAPI property "configuration".
  MessagingV2ChannelsSenderConfiguration configuration = 2;
  // OpenAPI property "offline_reasons".
  MessagingV2ChannelsSenderOfflineReasons offline_reasons = 3;
  // OpenAPI property "profile".
  MessagingV2ChannelsSenderProfileGenericResponse profile = 4;
  // OpenAPI property "properties".
  MessagingV2ChannelsSenderProperties properties = 5;
  // OpenAPI property "sender_id".
  MessagingV2ChannelsSenderFieldsSenderId sender_id = 6;
  // The SID of the sender.
  string sid = 7;
  // OpenAPI property "status".
  ChannelsSenderEnumStatus status = 8;
  // The URL of the resource.
  string url = 9;
  // OpenAPI property "webhook".
  MessagingV2ChannelsSenderWebhook webhook = 10;
}

// The KYC compliance information. This section consists of response to the request launch.
message MessagingV2RcsComplianceResponse {
  // A list of country-specific compliance details.
  repeated MessagingV2RcsComplianceCountryResponse countries = 1;
  // The default compliance registration SID (e.g., from CR-Google) that applies to all countries unless overridden in the `countries` array.
  string registration_sid = 2;
}

message MessagingV2RcsComplianceCountryResponse {
  repeated MessagingV2RcsCarrier carriers = 1;
  // The ISO 3166-1 alpha-2 country code.
  string country = 2;
  // The default compliance registration SID (e.g., from CR-Google) that applies to all countries unless overridden in the `countries` array.
  string registration_sid = 3;
  // OpenAPI property "status".
  MessagingV2RcsCountryStatus status = 4;
}

message MessagingV2RcsCarrier {
  // The name of the carrier. For example, `Verizon` or `AT&T` for US.
  string name = 1;
  // OpenAPI property "status".
  MessagingV2RcsCarrierStatus status = 2;
}

// The carrier-level status.
message MessagingV2RcsCarrierStatus {
  // Scalar value wrapper.
  string value = 1;
}

// The country-level status. Based on the aggregation of the carrier-level status.
message MessagingV2RcsCountryStatus {
  // Scalar value wrapper.
  string value = 1;
}

message MessagingV2DomainCertV4 {
  // Optional JSON field describing the status and upload date of a new certificate in the process of validation
  string cert_in_validation = 1;
  // The unique string that we created to identify this Certificate resource.
  string certificate_sid = 2;
  // Date that this Domain was registered to the Twilio platform to create a new Domain object.
  string date_created = 3;
  // Date that the private certificate associated with this domain expires. You will need to update the certificate before that date to ensure your shortened links will continue to work.
  string date_expires = 4;
  // Date that this Domain was last updated.
  string date_updated = 5;
  // Full url path for this domain.
  string domain_name = 6;
  // The unique string that we created to identify the Domain resource.
  string domain_sid = 7;
  // Boolean field that indicates whether the certificate is managed by Twilio or uploaded by the customer.
  bool managed = 8;
  // Boolean field that indicates whether a Twilio managed cert request is in progress or completed. True indicates a request is in progress and false indicates the request has completed or not requested yet.
  bool requesting = 9;
  string url = 10;
}

message MessagingV2PresignedUrlRequest {
  // Base64-encoded MD5 hash of the image
  string image_content_md5 = 1;
  // MIME type of the image (e.g., image/png, image/jpeg)
  string image_content_type = 2;
  // Height of the image in pixels
  int64 image_height = 3;
  // Type of image (logo, hero, etc.)
  string image_kind = 4;
  // Name of the image file
  string image_name = 5;
  // Size of the image in bytes
  int64 image_size_bytes = 6;
  // Width of the image in pixels
  int64 image_width = 7;
}

message MessagingV2PresignedUrlResponse {
  string expires_at = 1;
  string fetch_url = 2;
  MessagingV2PresignedUrlResponseUploadFields upload_fields = 3;
  string upload_url = 4;
}

message MessagingV2PresignedUrlResponseUploadFields {
  // Scalar value wrapper.
  string value = 1;
}

message MessagingV2RcsComplianceCountryUpdatePatchResponse {
  // The ISO 3166-1 alpha-2 country code.
  string country = 1;
  // The default compliance registration SID (e.g., from CR-Google) that applies to all countries unless overridden in the `countries` array.
  string registration_sid = 2;
  // OpenAPI property "status".
  MessagingV2RcsCountryStatus status = 3;
}

// The KYC compliance information. This section consists of response to the request launch.
message MessagingV2RcsCompliancePatchUpdateResponse {
  // A list of country-specific compliance details.
  repeated MessagingV2RcsComplianceCountryUpdatePatchResponse countries = 1;
  // The default compliance registration SID (e.g., from CR-Google) that applies to all countries unless overridden within the `countries` array.
  string registration_sid = 2;
}

// - Payload for typing indicator request
message MessagingV2WhatsappTypingIndicator {
  // Shared channel identifier
  string channel = 1;
  // Message SID that identifies the conversation thread for the typing indicator. Must be a valid Twilio Message SID (SM*) or Media SID (MM*) from an existing WhatsApp conversation.
  string message_id = 2;
}

// ListChannelsSenderResponse
message ListChannelsSenderResponseBody {
  ListChannelsSenderResponseBodyMeta meta = 1;
  repeated MessagingV2ChannelsSenderResponse senders = 2;
}

message ListChannelsSenderResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

// Retrieve a list of Senders
message ListChannelsSenderRequest {
  // OpenAPI query parameter "Channel". Required.
  string channel = 1;
  // The number of items to return per page. For WhatsApp, the default is `20`.
  int64 page_size = 2;
  // The page index. Use only for client state.
  int64 page = 3;
  // The page token provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListChannelsSenderResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListChannelsSenderResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "T-Request-Id".
  string header_t_request_id = 8;
}

// Create a Sender
message CreateChannelsSenderRequest {
  // Request body from media type "application/json".
  MessagingV2ChannelsSenderRequestsCreate body = 1;
}

// Canonical success response.
message CreateChannelsSenderResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV2ChannelsSenderResponse body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "T-Request-Id".
  string header_t_request_id = 8;
}

// Retrieve a Sender
message FetchChannelsSenderRequest {
  // The SID of the sender. Required.
  string sid = 1;
}

// Canonical success response.
message FetchChannelsSenderResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV2ChannelsSenderResponse body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "T-Request-Id".
  string header_t_request_id = 8;
}

// Update a sender's information, including `profile`, `webhook`, and `configuration`.
message UpdateChannelsSenderRequest {
  // The SID of the sender. Required.
  string sid = 1;
  // Request body from media type "application/json".
  MessagingV2ChannelsSenderRequestsUpdate body = 2;
}

// Canonical success response.
message UpdateChannelsSenderResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV2ChannelsSenderResponse body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "T-Request-Id".
  string header_t_request_id = 8;
}

// Delete a Sender
message DeleteChannelsSenderRequest {
  // The SID of the sender. Required.
  string sid = 1;
}

// Canonical success response.
message DeleteChannelsSenderResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 2;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 3;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 4;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 5;
  // Headers exposed to the client
  string header_access_control_expose_headers = 6;
  // Response header "T-Request-Id".
  string header_t_request_id = 7;
}

message CreateTypingIndicatorResponseBody {
  // Indicates if the typing indicator was sent successfully.
  bool success = 1;
}

// Send a typing indicator
message CreateTypingIndicatorRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  MessagingV2WhatsappTypingIndicator body = 1;
}

// Canonical success response.
message CreateTypingIndicatorResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  CreateTypingIndicatorResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

message FetchDomainCertV4Request {
  // Unique string used to identify the domain that this certificate should be associated with. Required.
  string domain_sid = 1;
}

// Canonical success response.
message FetchDomainCertV4Response {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  MessagingV2DomainCertV4 body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
}

// Generated from MessagingV2ChannelsSender operations.
service MessagingV2ChannelsSenderService {
  // Retrieve a list of Senders
  rpc ListChannelsSender(ListChannelsSenderRequest) returns (ListChannelsSenderResponse) {
    option (google.api.http) = {
      get: "/v2/Channels/Senders"
      response_body: "body"
    };
  }
  // Create a Sender
  rpc CreateChannelsSender(CreateChannelsSenderRequest) returns (CreateChannelsSenderResponse) {
    option (google.api.http) = {
      post: "/v2/Channels/Senders"
      body: "body"
      response_body: "body"
    };
  }
  // Retrieve a Sender
  rpc FetchChannelsSender(FetchChannelsSenderRequest) returns (FetchChannelsSenderResponse) {
    option (google.api.http) = {
      get: "/v2/Channels/Senders/{sid}"
      response_body: "body"
    };
  }
  // Update a sender's information, including `profile`, `webhook`, and `configuration`.
  rpc UpdateChannelsSender(UpdateChannelsSenderRequest) returns (UpdateChannelsSenderResponse) {
    option (google.api.http) = {
      post: "/v2/Channels/Senders/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  // Delete a Sender
  rpc DeleteChannelsSender(DeleteChannelsSenderRequest) returns (DeleteChannelsSenderResponse) {
    option (google.api.http) = {
      delete: "/v2/Channels/Senders/{sid}"
    };
  }
}

// Generated from MessagingV2TypingIndicator operations.
service MessagingV2TypingIndicatorService {
  // Send a typing indicator
  rpc CreateTypingIndicator(CreateTypingIndicatorRequest) returns (CreateTypingIndicatorResponse) {
    option (google.api.http) = {
      post: "/v2/Indicators/Typing.json"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from MessagingV2DomainCerts operations.
service MessagingV2DomainCertsService {
  rpc FetchDomainCertV4(FetchDomainCertV4Request) returns (FetchDomainCertV4Response) {
    option (google.api.http) = {
      get: "/v2/LinkShortening/Domains/{domain_sid}/Certificate"
      response_body: "body"
    };
  }
}

