// Code generated by openapi2proto. DO NOT EDIT.
// Source: twilio_taskrouter_v1.json

syntax = "proto3";

package twilio.taskrouter.v1;

option go_package = "twilio/taskrouter/v1;twiliotaskrouterv1";

import "google/api/annotations.proto";

// The current status of the Task's assignment. Can be: `pending`, `reserved`, `assigned`, `canceled`, `wrapping`, or `completed`.
message TaskEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

// How Tasks will be assigned to Workers. Set this parameter to `LIFO` to assign most recently created Task first or `FIFO` to assign the oldest Task. Default is FIFO. [Click here](https://www.twilio.com/docs/taskrouter/queue-ordering-last-first-out-lifo) to learn more.
message TaskQueueEnumTaskOrder {
  // Scalar value wrapper.
  string value = 1;
}

message TaskReservationEnumCallStatus {
  // Scalar value wrapper.
  string value = 1;
}

message TaskReservationEnumConferenceEvent {
  // Scalar value wrapper.
  string value = 1;
}

// The current status of the reservation. Can be: `pending`, `accepted`, `rejected`, or `timeout`.
message TaskReservationEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

message TaskReservationEnumSupervisorMode {
  // Scalar value wrapper.
  string value = 1;
}

message TaskrouterV1Workspace {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Workspace resource.
  string account_sid = 1;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 2;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 3;
  // The name of the default activity.
  string default_activity_name = 4;
  // The SID of the Activity that will be used when new Workers are created in the Workspace.
  string default_activity_sid = 5;
  // The URL we call when an event occurs. If provided, the Workspace will publish events to this URL, for example, to collect data for reporting. See [Workspace Events](https://www.twilio.com/docs/taskrouter/api/event) for more information. This parameter supports Twilio's [Webhooks (HTTP callbacks) Connection Overrides](https://www.twilio.com/docs/usage/webhooks/webhooks-connection-overrides).
  string event_callback_url = 6;
  // The list of Workspace events for which to call `event_callback_url`. For example, if `EventsFilter=task.created, task.canceled, worker.activity.update`, then TaskRouter will call event_callback_url only when a task is created, canceled, or a Worker activity is updated.
  string events_filter = 7;
  // The string that you assigned to describe the Workspace resource. For example `Customer Support` or `2014 Election Campaign`.
  string friendly_name = 8;
  // The URLs of related resources.
  TaskrouterV1WorkspaceLinks links = 9;
  // Whether multi-tasking is enabled. The default is `true`, which enables multi-tasking. Multi-tasking allows Workers to handle multiple Tasks simultaneously. When enabled (`true`), each Worker can receive parallel reservations up to the per-channel maximums defined in the Workers section. In single-tasking each Worker would only receive a new reservation when the previous task is completed. Learn more at [Multitasking](https://www.twilio.com/docs/taskrouter/multitasking).
  bool multi_task_enabled = 10;
  // OpenAPI property "prioritize_queue_order".
  WorkspaceEnumQueueOrder prioritize_queue_order = 11;
  // The unique string that we created to identify the Workspace resource.
  string sid = 12;
  // The name of the timeout activity.
  string timeout_activity_name = 13;
  // The SID of the Activity that will be assigned to a Worker when a Task reservation times out without a response.
  string timeout_activity_sid = 14;
  // The absolute URL of the Workspace resource.
  string url = 15;
}

// The URLs of related resources.
message TaskrouterV1WorkspaceLinks {
  // Scalar value wrapper.
  string value = 1;
}

// The type of TaskQueue to prioritize when Workers are receiving Tasks from both types of TaskQueues. Can be: `LIFO` or `FIFO` and the default is `FIFO`. For more information, see [Queue Ordering](https://www.twilio.com/docs/taskrouter/queue-ordering-last-first-out-lifo).
message WorkspaceEnumQueueOrder {
  // Scalar value wrapper.
  string value = 1;
}

message TaskrouterV1WorkspaceActivity {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Activity resource.
  string account_sid = 1;
  // Whether the Worker is eligible to receive a Task when it occupies the Activity. A value of `true`, `1`, or `yes` indicates the Activity is available. All other values indicate that it is not. The value cannot be changed after the Activity is created.
  bool available = 2;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 4;
  // The string that you assigned to describe the Activity resource.
  string friendly_name = 5;
  TaskrouterV1WorkspaceActivityLinks links = 6;
  // The unique string that we created to identify the Activity resource.
  string sid = 7;
  // The absolute URL of the Activity resource.
  string url = 8;
  // The SID of the Workspace that contains the Activity.
  string workspace_sid = 9;
}

message TaskrouterV1WorkspaceActivityLinks {
  // Scalar value wrapper.
  string value = 1;
}

message TaskrouterV1WorkspaceEvent {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Event resource.
  string account_sid = 1;
  // The SID of the resource that triggered the event.
  string actor_sid = 2;
  // The type of resource that triggered the event.
  string actor_type = 3;
  // The absolute URL of the resource that triggered the event.
  string actor_url = 4;
  // A description of the event.
  string description = 5;
  // Data about the event. For more information, see [Event types](https://www.twilio.com/docs/taskrouter/api/event#event-types).
  string event_data = 6;
  // The time the event was sent, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string event_date = 7;
  // The time the event was sent in milliseconds.
  int64 event_date_ms = 8;
  // The identifier for the event.
  string event_type = 9;
  // The SID of the object the event is most relevant to, such as a TaskSid, ReservationSid, or a  WorkerSid.
  string resource_sid = 10;
  // The type of object the event is most relevant to, such as a Task, Reservation, or a Worker).
  string resource_type = 11;
  // The URL of the resource the event is most relevant to.
  string resource_url = 12;
  // The unique string that we created to identify the Event resource.
  string sid = 13;
  // Where the Event originated.
  string source = 14;
  // The IP from which the Event originated.
  string source_ip_address = 15;
  // The absolute URL of the Event resource.
  string url = 16;
  // The SID of the Workspace that contains the Event.
  string workspace_sid = 17;
}

message TaskrouterV1WorkspaceTask {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Task resource.
  string account_sid = 1;
  // An object that contains the [Add-on](https://www.twilio.com/docs/add-ons) data for all installed Add-ons.
  string addons = 2;
  // The number of seconds since the Task was created.
  int64 age = 3;
  // OpenAPI property "assignment_status".
  TaskEnumStatus assignment_status = 4;
  // The JSON string with custom attributes of the work. **Note** If this property has been assigned a value, it will only be displayed in FETCH action that returns a single resource. Otherwise, it will be null.
  string attributes = 5;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 6;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 7;
  // A boolean that indicates if the Task should respect a Worker's capacity and availability during assignment. This field can only be used when the `RoutingTarget` field is set to a Worker SID. By setting `IgnoreCapacity` to a value of `true`, `1`, or `yes`, the Task will be routed to the Worker without respecting their capacity and availability. Any other value will enforce the Worker's capacity and availability. The default value of `IgnoreCapacity` is `true` when the `RoutingTarget` is set to a Worker SID.
  bool ignore_capacity = 8;
  // The URLs of related resources.
  TaskrouterV1WorkspaceTaskLinks links = 9;
  // The current priority score of the Task as assigned to a Worker by the workflow. Tasks with higher priority values will be assigned before Tasks with lower values.
  int64 priority = 10;
  // The reason the Task was canceled or completed, if applicable.
  string reason = 11;
  // A SID of a Worker, Queue, or Workflow to route a Task to
  string routing_target = 12;
  // The unique string that we created to identify the Task resource.
  string sid = 13;
  // The SID of the TaskChannel.
  string task_channel_sid = 14;
  // The unique name of the TaskChannel.
  string task_channel_unique_name = 15;
  // The date and time in GMT when the Task entered the TaskQueue, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string task_queue_entered_date = 16;
  // The friendly name of the TaskQueue.
  string task_queue_friendly_name = 17;
  // The SID of the TaskQueue.
  string task_queue_sid = 18;
  // The amount of time in seconds that the Task can live before being assigned.
  int64 timeout = 19;
  // The absolute URL of the Task resource.
  string url = 20;
  // The date and time in GMT indicating the ordering for routing of the Task specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string virtual_start_time = 21;
  // The friendly name of the Workflow that is controlling the Task.
  string workflow_friendly_name = 22;
  // The SID of the Workflow that is controlling the Task.
  string workflow_sid = 23;
  // The SID of the Workspace that contains the Task.
  string workspace_sid = 24;
}

// The URLs of related resources.
message TaskrouterV1WorkspaceTaskLinks {
  // Scalar value wrapper.
  string value = 1;
}

message TaskrouterV1WorkspaceTaskTaskReservation {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the TaskReservation resource.
  string account_sid = 1;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 2;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 3;
  // The URLs of related resources.
  TaskrouterV1WorkspaceTaskTaskReservationLinks links = 4;
  // OpenAPI property "reservation_status".
  TaskReservationEnumStatus reservation_status = 5;
  // The unique string that we created to identify the TaskReservation resource.
  string sid = 6;
  // The SID of the reserved Task resource.
  string task_sid = 7;
  // The absolute URL of the TaskReservation reservation.
  string url = 8;
  // The `friendly_name` of the Worker that is reserved.
  string worker_name = 9;
  // The SID of the reserved Worker resource.
  string worker_sid = 10;
  // The SID of the Workspace that this task is contained within.
  string workspace_sid = 11;
}

// The URLs of related resources.
message TaskrouterV1WorkspaceTaskTaskReservationLinks {
  // Scalar value wrapper.
  string value = 1;
}

message TaskrouterV1WorkspaceTaskChannel {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Task Channel resource.
  string account_sid = 1;
  // Whether the Task Channel will prioritize Workers that have been idle. When `true`, Workers that have been idle the longest are prioritized.
  bool channel_optimized_routing = 2;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 3;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 4;
  // The string that you assigned to describe the resource.
  string friendly_name = 5;
  // The URLs of related resources.
  TaskrouterV1WorkspaceTaskChannelLinks links = 6;
  // The unique string that we created to identify the Task Channel resource.
  string sid = 7;
  // An application-defined string that uniquely identifies the Task Channel, such as `voice` or `sms`.
  string unique_name = 8;
  // The absolute URL of the Task Channel resource.
  string url = 9;
  // The SID of the Workspace that contains the Task Channel.
  string workspace_sid = 10;
}

// The URLs of related resources.
message TaskrouterV1WorkspaceTaskChannelLinks {
  // Scalar value wrapper.
  string value = 1;
}

message TaskrouterV1WorkspaceTaskQueue {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the TaskQueue resource.
  string account_sid = 1;
  // The name of the Activity to assign Workers when a task is assigned for them.
  string assignment_activity_name = 2;
  // The SID of the Activity to assign Workers when a task is assigned for them.
  string assignment_activity_sid = 3;
  // The date and time in GMT when the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 4;
  // The date and time in GMT when the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 5;
  // The string that you assigned to describe the resource.
  string friendly_name = 6;
  // The URLs of related resources.
  TaskrouterV1WorkspaceTaskQueueLinks links = 7;
  // The maximum number of Workers to reserve for the assignment of a task in the queue. Can be an integer between 1 and 50, inclusive and defaults to 1.
  int64 max_reserved_workers = 8;
  // The name of the Activity to assign Workers once a task is reserved for them.
  string reservation_activity_name = 9;
  // The SID of the Activity to assign Workers once a task is reserved for them.
  string reservation_activity_sid = 10;
  // The unique string that we created to identify the TaskQueue resource.
  string sid = 11;
  // A string describing the Worker selection criteria for any Tasks that enter the TaskQueue. For example `'"language" == "spanish"'` If no TargetWorkers parameter is provided, Tasks will wait in the TaskQueue until they are either deleted or moved to another TaskQueue. Additional examples on how to describing Worker selection criteria below. Defaults to 1==1.
  string target_workers = 12;
  // OpenAPI property "task_order".
  TaskQueueEnumTaskOrder task_order = 13;
  // The absolute URL of the TaskQueue resource.
  string url = 14;
  // The SID of the Workspace that contains the TaskQueue.
  string workspace_sid = 15;
}

// The URLs of related resources.
message TaskrouterV1WorkspaceTaskQueueLinks {
  // Scalar value wrapper.
  string value = 1;
}

message TaskrouterV1WorkspaceTaskQueueTaskQueueBulkRealTimeStatistics {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the TaskQueue resource.
  string account_sid = 1;
  // The real-time statistics for each requested TaskQueue SID. `task_queue_data` returns the following attributes:
  // `task_queue_sid`: The SID of the TaskQueue from which these statistics were calculated.
  // `total_available_workers`: The total number of Workers available for Tasks in the TaskQueue.
  // `total_eligible_workers`: The total number of Workers eligible for Tasks in the TaskQueue, regardless of their Activity state.
  // `total_tasks`: The total number of Tasks.
  // `longest_task_waiting_age`: The age of the longest waiting Task.
  // `longest_task_waiting_sid`: The SID of the longest waiting Task.
  // `tasks_by_status`: The number of Tasks grouped by their current status.
  // `tasks_by_priority`: The number of Tasks grouped by priority.
  // `activity_statistics`: The number of current Workers grouped by Activity.
  repeated string task_queue_data = 2;
  // The number of TaskQueue statistics received in task_queue_data.
  int64 task_queue_response_count = 3;
  // The absolute URL of the TaskQueue statistics resource.
  string url = 4;
  // The SID of the Workspace that contains the TaskQueue.
  string workspace_sid = 5;
}

message TaskrouterV1WorkspaceTaskQueueTaskQueueCumulativeStatistics {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the TaskQueue resource.
  string account_sid = 1;
  // The average time in seconds between Task creation and acceptance.
  int64 avg_task_acceptance_time = 2;
  // The end of the interval during which these statistics were calculated, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string end_time = 3;
  // The total number of Reservations accepted for Tasks in the TaskQueue.
  int64 reservations_accepted = 4;
  // The total number of Reservations canceled for Tasks in the TaskQueue.
  int64 reservations_canceled = 5;
  // The total number of Reservations created for Tasks in the TaskQueue.
  int64 reservations_created = 6;
  // The total number of Reservations rejected for Tasks in the TaskQueue.
  int64 reservations_rejected = 7;
  // The total number of Reservations rescinded.
  int64 reservations_rescinded = 8;
  // The total number of Reservations that timed out for Tasks in the TaskQueue.
  int64 reservations_timed_out = 9;
  // A list of objects that describe the number of Tasks canceled and reservations accepted above and below the thresholds specified in seconds.
  string split_by_wait_time = 10;
  // The beginning of the interval during which these statistics were calculated, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string start_time = 11;
  // The SID of the TaskQueue from which these statistics were calculated.
  string task_queue_sid = 12;
  // The total number of Tasks canceled in the TaskQueue.
  int64 tasks_canceled = 13;
  // The total number of Tasks completed in the TaskQueue.
  int64 tasks_completed = 14;
  // The total number of Tasks deleted in the TaskQueue.
  int64 tasks_deleted = 15;
  // The total number of Tasks entered into the TaskQueue.
  int64 tasks_entered = 16;
  // The total number of Tasks that were moved from one queue to another.
  int64 tasks_moved = 17;
  // The absolute URL of the TaskQueue statistics resource.
  string url = 18;
  // The relative wait duration statistics (`avg`, `min`, `max`, `total`) for Tasks accepted while in the TaskQueue. Calculation is based on the time when the Tasks entered the TaskQueue.
  string wait_duration_in_queue_until_accepted = 19;
  // The wait duration statistics (`avg`, `min`, `max`, `total`) for Tasks accepted while in the TaskQueue. Calculation is based on the time when the Tasks were created. For transfers, the wait duration is counted from the moment ***the Task was created***, and not from when the transfer was initiated.
  string wait_duration_until_accepted = 20;
  // The wait duration statistics (`avg`, `min`, `max`, `total`) for Tasks canceled while in the TaskQueue.
  string wait_duration_until_canceled = 21;
  // The SID of the Workspace that contains the TaskQueue.
  string workspace_sid = 22;
}

message TaskrouterV1WorkspaceTaskQueueTaskQueueRealTimeStatistics {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the TaskQueue resource.
  string account_sid = 1;
  // The number of current Workers by Activity.
  repeated string activity_statistics = 2;
  // The relative age in the TaskQueue for the longest waiting Task. Calculation is based on the time when the Task entered the TaskQueue.
  int64 longest_relative_task_age_in_queue = 3;
  // The Task SID of the Task waiting in the TaskQueue the longest. Calculation is based on the time when the Task entered the TaskQueue.
  string longest_relative_task_sid_in_queue = 4;
  // The age of the longest waiting Task.
  int64 longest_task_waiting_age = 5;
  // The SID of the longest waiting Task.
  string longest_task_waiting_sid = 6;
  // The SID of the TaskQueue from which these statistics were calculated.
  string task_queue_sid = 7;
  // The number of Tasks by priority. For example: `{"0": "10", "99": "5"}` shows 10 Tasks at priority 0 and 5 at priority 99.
  string tasks_by_priority = 8;
  // The number of Tasks by their current status. For example: `{"pending": "1", "reserved": "3", "assigned": "2", "completed": "5"}`.
  string tasks_by_status = 9;
  // The total number of Workers in the TaskQueue with an `available` status. Workers with an `available` status may already have active interactions or may have none.
  int64 total_available_workers = 10;
  // The total number of Workers eligible for Tasks in the TaskQueue, independent of their Activity state.
  int64 total_eligible_workers = 11;
  // The total number of Tasks.
  int64 total_tasks = 12;
  // The absolute URL of the TaskQueue statistics resource.
  string url = 13;
  // The SID of the Workspace that contains the TaskQueue.
  string workspace_sid = 14;
}

message TaskrouterV1WorkspaceTaskQueueTaskQueueStatistics {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the TaskQueue resource.
  string account_sid = 1;
  // An object that contains the cumulative statistics for the TaskQueue.
  string cumulative = 2;
  // An object that contains the real-time statistics for the TaskQueue.
  string realtime = 3;
  // The SID of the TaskQueue from which these statistics were calculated.
  string task_queue_sid = 4;
  // The absolute URL of the TaskQueue statistics resource.
  string url = 5;
  // The SID of the Workspace that contains the TaskQueue.
  string workspace_sid = 6;
}

message TaskrouterV1WorkspaceTaskQueueTaskQueuesStatistics {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the TaskQueue resource.
  string account_sid = 1;
  // An object that contains the cumulative statistics for the TaskQueues.
  string cumulative = 2;
  // An object that contains the real-time statistics for the TaskQueues.
  string realtime = 3;
  // The SID of the TaskQueue from which these statistics were calculated.
  string task_queue_sid = 4;
  // The SID of the Workspace that contains the TaskQueues.
  string workspace_sid = 5;
}

message TaskrouterV1WorkspaceWorker {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Worker resource.
  string account_sid = 1;
  // The `friendly_name` of the Worker's current Activity.
  string activity_name = 2;
  // The SID of the Worker's current Activity.
  string activity_sid = 3;
  // The JSON string that describes the Worker. For example: `{ "email": "Bob@example.com", "phone": "+5095551234" }`. **Note** If this property has been assigned a value, it will only be displayed in FETCH actions that return a single resource. Otherwise, this property will be null, even if it has a value. This data is passed to the `assignment_callback_url` when TaskRouter assigns a Task to the Worker.
  string attributes = 4;
  // Whether the Worker is available to perform tasks.
  bool available = 5;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 6;
  // The date and time in GMT of the last change to the Worker's activity specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format. Used to calculate Workflow statistics.
  string date_status_changed = 7;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 8;
  // The string that you assigned to describe the resource. Friendly names are case insensitive, and unique within the TaskRouter Workspace.
  string friendly_name = 9;
  // The URLs of related resources.
  TaskrouterV1WorkspaceWorkerLinks links = 10;
  // The unique string that we created to identify the Worker resource.
  string sid = 11;
  // The absolute URL of the Worker resource.
  string url = 12;
  // The SID of the Workspace that contains the Worker.
  string workspace_sid = 13;
}

// The URLs of related resources.
message TaskrouterV1WorkspaceWorkerLinks {
  // Scalar value wrapper.
  string value = 1;
}

message TaskrouterV1WorkspaceWorkerWorkerChannel {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Worker resource.
  string account_sid = 1;
  // The total number of Tasks assigned to Worker for the TaskChannel type.
  int64 assigned_tasks = 2;
  // Whether the Worker should receive Tasks of the TaskChannel type.
  bool available = 3;
  // The current percentage of capacity the TaskChannel has available. Can be a number between `0` and `100`. A value of `0` indicates that TaskChannel has no capacity available and a value of `100` means the  Worker is available to receive any Tasks of this TaskChannel type.
  int64 available_capacity_percentage = 4;
  // The current configured capacity for the WorkerChannel. TaskRouter will not create any reservations after the assigned Tasks for the Worker reaches the value.
  int64 configured_capacity = 5;
  // The date and time in GMT when the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 6;
  // The date and time in GMT when the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 7;
  // The unique string that we created to identify the WorkerChannel resource.
  string sid = 8;
  // The SID of the TaskChannel.
  string task_channel_sid = 9;
  // The unique name of the TaskChannel, such as `voice` or `sms`.
  string task_channel_unique_name = 10;
  // The absolute URL of the WorkerChannel resource.
  string url = 11;
  // The SID of the Worker that contains the WorkerChannel.
  string worker_sid = 12;
  // The SID of the Workspace that contains the WorkerChannel.
  string workspace_sid = 13;
}

message TaskrouterV1WorkspaceWorkerWorkerInstanceStatistics {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Worker resource.
  string account_sid = 1;
  // An object that contains the cumulative statistics for the Worker.
  string cumulative = 2;
  // The absolute URL of the WorkerChannel statistics resource.
  string url = 3;
  // The SID of the Worker that contains the WorkerChannel.
  string worker_sid = 4;
  // The SID of the Workspace that contains the WorkerChannel.
  string workspace_sid = 5;
}

message TaskrouterV1WorkspaceWorkerWorkerReservation {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the WorkerReservation resource.
  string account_sid = 1;
  // The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_created = 2;
  // The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string date_updated = 3;
  // The URLs of related resources.
  TaskrouterV1WorkspaceWorkerWorkerReservationLinks links = 4;
  // OpenAPI property "reservation_status".
  WorkerReservationEnumStatus reservation_status = 5;
  // The unique string that we created to identify the WorkerReservation resource.
  string sid = 6;
  // The SID of the reserved Task resource.
  string task_sid = 7;
  // The absolute URL of the WorkerReservation resource.
  string url = 8;
  // The `friendly_name` of the Worker that is reserved.
  string worker_name = 9;
  // The SID of the reserved Worker resource.
  string worker_sid = 10;
  // The SID of the Workspace that this worker is contained within.
  string workspace_sid = 11;
}

// The URLs of related resources.
message TaskrouterV1WorkspaceWorkerWorkerReservationLinks {
  // Scalar value wrapper.
  string value = 1;
}

// The current status of the reservation. Can be: `pending`, `accepted`, `rejected`, `timeout`, `canceled`, or `rescinded`.
message WorkerReservationEnumStatus {
  // Scalar value wrapper.
  string value = 1;
}

message TaskrouterV1WorkspaceWorkerWorkerStatistics {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Worker resource.
  string account_sid = 1;
  // An object that contains the cumulative statistics for the Worker.
  string cumulative = 2;
  // An object that contains the real-time statistics for the Worker.
  string realtime = 3;
  // The absolute URL of the Worker statistics resource.
  string url = 4;
  // The SID of the Workspace that contains the Worker.
  string workspace_sid = 5;
}

message TaskrouterV1WorkspaceWorkerWorkersCumulativeStatistics {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Worker resource.
  string account_sid = 1;
  // The minimum, average, maximum, and total time (in seconds) that Workers spent in each Activity.
  repeated string activity_durations = 2;
  // The end of the interval during which these statistics were calculated, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string end_time = 3;
  // The total number of Reservations that were accepted.
  int64 reservations_accepted = 4;
  // The total number of Reservations that were canceled.
  int64 reservations_canceled = 5;
  // The total number of Reservations that were created.
  int64 reservations_created = 6;
  // The total number of Reservations that were rejected.
  int64 reservations_rejected = 7;
  // The total number of Reservations that were rescinded.
  int64 reservations_rescinded = 8;
  // The total number of Reservations that were timed out.
  int64 reservations_timed_out = 9;
  // The beginning of the interval during which these statistics were calculated, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string start_time = 10;
  // The absolute URL of the Workers statistics resource.
  string url = 11;
  // The SID of the Workspace that contains the Workers.
  string workspace_sid = 12;
}

message TaskrouterV1WorkspaceWorkerWorkersRealTimeStatistics {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Worker resource.
  string account_sid = 1;
  // The number of current Workers by Activity.
  repeated string activity_statistics = 2;
  // The total number of Workers.
  int64 total_workers = 3;
  // The absolute URL of the Workers statistics resource.
  string url = 4;
  // The SID of the Workspace that contains the Workers.
  string workspace_sid = 5;
}

message TaskrouterV1WorkspaceWorkflow {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Workflow resource.
  string account_sid = 1;
  // The URL that we call when a task managed by the Workflow is assigned to a Worker. See Assignment Callback URL for more information.
  string assignment_callback_url = 2;
  // A JSON string that contains the Workflow's configuration. See [Configuring Workflows](https://www.twilio.com/docs/taskrouter/workflow-configuration) for more information.
  string configuration = 3;
  // The date and time in GMT when the resource was created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_created = 4;
  // The date and time in GMT when the resource was last updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
  string date_updated = 5;
  // The MIME type of the document.
  string document_content_type = 6;
  // The URL that we call when a call to the `assignment_callback_url` fails.
  string fallback_assignment_callback_url = 7;
  // The string that you assigned to describe the Workflow resource. For example, `Customer Support` or `2014 Election Campaign`.
  string friendly_name = 8;
  // The URLs of related resources.
  TaskrouterV1WorkspaceWorkflowLinks links = 9;
  // The unique string that we created to identify the Workflow resource.
  string sid = 10;
  // How long TaskRouter will wait for a confirmation response from your application after it assigns a Task to a Worker. Can be up to `86,400` (24 hours) and the default is `120`.
  int64 task_reservation_timeout = 11;
  // The absolute URL of the Workflow resource.
  string url = 12;
  // The SID of the Workspace that contains the Workflow.
  string workspace_sid = 13;
}

// The URLs of related resources.
message TaskrouterV1WorkspaceWorkflowLinks {
  // Scalar value wrapper.
  string value = 1;
}

message TaskrouterV1WorkspaceWorkflowWorkflowCumulativeStatistics {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Workflow resource.
  string account_sid = 1;
  // The average time in seconds between Task creation and acceptance.
  int64 avg_task_acceptance_time = 2;
  // The end of the interval during which these statistics were calculated, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string end_time = 3;
  // The total number of Reservations accepted by Workers.
  int64 reservations_accepted = 4;
  // The total number of Reservations that were canceled.
  int64 reservations_canceled = 5;
  // The total number of Reservations that were created for Workers.
  int64 reservations_created = 6;
  // The total number of Reservations that were rejected.
  int64 reservations_rejected = 7;
  // The total number of Reservations that were rescinded.
  int64 reservations_rescinded = 8;
  // The total number of Reservations that were timed out.
  int64 reservations_timed_out = 9;
  // A list of objects that describe the number of Tasks canceled and reservations accepted above and below the thresholds specified in seconds.
  string split_by_wait_time = 10;
  // The beginning of the interval during which these statistics were calculated, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string start_time = 11;
  // The total number of Tasks that were canceled.
  int64 tasks_canceled = 12;
  // The total number of Tasks that were completed.
  int64 tasks_completed = 13;
  // The total number of Tasks that were deleted.
  int64 tasks_deleted = 14;
  // The total number of Tasks that entered the Workflow.
  int64 tasks_entered = 15;
  // The total number of Tasks that were moved from one queue to another.
  int64 tasks_moved = 16;
  // The total number of Tasks that were timed out of their Workflows (and deleted).
  int64 tasks_timed_out_in_workflow = 17;
  // The absolute URL of the Workflow statistics resource.
  string url = 18;
  // The wait duration statistics (`avg`, `min`, `max`, `total`) for Tasks that were accepted.
  string wait_duration_until_accepted = 19;
  // The wait duration statistics (`avg`, `min`, `max`, `total`) for Tasks that were canceled.
  string wait_duration_until_canceled = 20;
  // Returns the list of Tasks that are being controlled by the Workflow with the specified Sid value.
  string workflow_sid = 21;
  // The SID of the Workspace that contains the Workflow.
  string workspace_sid = 22;
}

message TaskrouterV1WorkspaceWorkflowWorkflowRealTimeStatistics {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Workflow resource.
  string account_sid = 1;
  // The age of the longest waiting Task.
  int64 longest_task_waiting_age = 2;
  // The SID of the longest waiting Task.
  string longest_task_waiting_sid = 3;
  // The number of Tasks by priority. For example: `{"0": "10", "99": "5"}` shows 10 Tasks at priority 0 and 5 at priority 99.
  string tasks_by_priority = 4;
  // The number of Tasks by their current status. For example: `{"pending": "1", "reserved": "3", "assigned": "2", "completed": "5"}`.
  string tasks_by_status = 5;
  // The total number of Tasks.
  int64 total_tasks = 6;
  // The absolute URL of the Workflow statistics resource.
  string url = 7;
  // Returns the list of Tasks that are being controlled by the Workflow with the specified SID value.
  string workflow_sid = 8;
  // The SID of the Workspace that contains the Workflow.
  string workspace_sid = 9;
}

message TaskrouterV1WorkspaceWorkflowWorkflowStatistics {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Workflow resource.
  string account_sid = 1;
  // An object that contains the cumulative statistics for the Workflow.
  string cumulative = 2;
  // An object that contains the real-time statistics for the Workflow.
  string realtime = 3;
  // The absolute URL of the Workflow statistics resource.
  string url = 4;
  // Returns the list of Tasks that are being controlled by the Workflow with the specified SID value.
  string workflow_sid = 5;
  // The SID of the Workspace that contains the Workflow.
  string workspace_sid = 6;
}

message TaskrouterV1WorkspaceWorkspaceCumulativeStatistics {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Workspace resource.
  string account_sid = 1;
  // The average time in seconds between Task creation and acceptance.
  int64 avg_task_acceptance_time = 2;
  // The end of the interval during which these statistics were calculated, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string end_time = 3;
  // The total number of Reservations accepted by Workers.
  int64 reservations_accepted = 4;
  // The total number of Reservations that were canceled.
  int64 reservations_canceled = 5;
  // The total number of Reservations that were created for Workers.
  int64 reservations_created = 6;
  // The total number of Reservations that were rejected.
  int64 reservations_rejected = 7;
  // The total number of Reservations that were rescinded.
  int64 reservations_rescinded = 8;
  // The total number of Reservations that were timed out.
  int64 reservations_timed_out = 9;
  // A list of objects that describe the number of Tasks canceled and reservations accepted above and below the thresholds specified in seconds.
  string split_by_wait_time = 10;
  // The beginning of the interval during which these statistics were calculated, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string start_time = 11;
  // The total number of Tasks that were canceled.
  int64 tasks_canceled = 12;
  // The total number of Tasks that were completed.
  int64 tasks_completed = 13;
  // The total number of Tasks created.
  int64 tasks_created = 14;
  // The total number of Tasks that were deleted.
  int64 tasks_deleted = 15;
  // The total number of Tasks that were moved from one queue to another.
  int64 tasks_moved = 16;
  // The total number of Tasks that were timed out of their Workflows (and deleted).
  int64 tasks_timed_out_in_workflow = 17;
  // The absolute URL of the Workspace statistics resource.
  string url = 18;
  // The wait duration statistics (`avg`, `min`, `max`, `total`) for Tasks that were accepted.
  string wait_duration_until_accepted = 19;
  // The wait duration statistics (`avg`, `min`, `max`, `total`) for Tasks that were canceled.
  string wait_duration_until_canceled = 20;
  // The SID of the Workspace.
  string workspace_sid = 21;
}

message TaskrouterV1WorkspaceWorkspaceRealTimeStatistics {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Workspace resource.
  string account_sid = 1;
  // The number of current Workers by Activity.
  repeated string activity_statistics = 2;
  // The age of the longest waiting Task.
  int64 longest_task_waiting_age = 3;
  // The SID of the longest waiting Task.
  string longest_task_waiting_sid = 4;
  // The number of Tasks by priority. For example: `{"0": "10", "99": "5"}` shows 10 Tasks at priority 0 and 5 at priority 99.
  string tasks_by_priority = 5;
  // The number of Tasks by their current status. For example: `{"pending": "1", "reserved": "3", "assigned": "2", "completed": "5"}`.
  string tasks_by_status = 6;
  // The total number of Tasks.
  int64 total_tasks = 7;
  // The total number of Workers in the Workspace.
  int64 total_workers = 8;
  // The absolute URL of the Workspace statistics resource.
  string url = 9;
  // The SID of the Workspace.
  string workspace_sid = 10;
}

message TaskrouterV1WorkspaceWorkspaceStatistics {
  // The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Workspace resource.
  string account_sid = 1;
  // An object that contains the cumulative statistics for the Workspace.
  string cumulative = 2;
  // An object that contains the real-time statistics for the Workspace.
  string realtime = 3;
  // The absolute URL of the Workspace statistics resource.
  string url = 4;
  // The SID of the Workspace.
  string workspace_sid = 5;
}

message WorkerReservationEnumCallStatus {
  // Scalar value wrapper.
  string value = 1;
}

message WorkerReservationEnumConferenceEvent {
  // Scalar value wrapper.
  string value = 1;
}

// ListWorkspaceResponse
message ListWorkspaceResponseBody {
  ListWorkspaceResponseBodyMeta meta = 1;
  repeated TaskrouterV1Workspace workspaces = 2;
}

message ListWorkspaceResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListWorkspaceRequest {
  // The `friendly_name` of the Workspace resources to read. For example `Customer Support` or `2014 Election Campaign`.
  string friendly_name = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListWorkspaceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListWorkspaceResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// CreateWorkspaceRequest
message CreateWorkspaceRequestBody {
  // The URL we should call when an event occurs. If provided, the Workspace will publish events to this URL, for example, to collect data for reporting. See [Workspace Events](https://www.twilio.com/docs/taskrouter/api/event) for more information. This parameter supports Twilio's [Webhooks (HTTP callbacks) Connection Overrides](https://www.twilio.com/docs/usage/webhooks/webhooks-connection-overrides).
  string event_callback_url = 1;
  // The list of Workspace events for which to call event_callback_url. For example, if `EventsFilter=task.created, task.canceled, worker.activity.update`, then TaskRouter will call event_callback_url only when a task is created, canceled, or a Worker activity is updated.
  string events_filter = 2;
  // A descriptive string that you create to describe the Workspace resource. It can be up to 64 characters long. For example: `Customer Support` or `2014 Election Campaign`.
  string friendly_name = 3;
  // Whether to enable multi-tasking. Can be: `true` to enable multi-tasking, or `false` to disable it. However, all workspaces should be created as multi-tasking. The default is `true`. Multi-tasking allows Workers to handle multiple Tasks simultaneously. When enabled (`true`), each Worker can receive parallel reservations up to the per-channel maximums defined in the Workers section. In single-tasking mode (legacy mode), each Worker will only receive a new reservation when the previous task is completed. Learn more at [Multitasking](https://www.twilio.com/docs/taskrouter/multitasking).
  bool multi_task_enabled = 4;
  // OpenAPI property "PrioritizeQueueOrder".
  WorkspaceEnumQueueOrder prioritize_queue_order = 5;
  // An available template name. Can be: `NONE` or `FIFO` and the default is `NONE`. Pre-configures the Workspace with the Workflow and Activities specified in the template. `NONE` will create a Workspace with only a set of default activities. `FIFO` will configure TaskRouter with a set of default activities and a single TaskQueue for first-in, first-out distribution, which can be useful when you are getting started with TaskRouter.
  string template = 6;
}

message CreateWorkspaceRequest {
  // Request body from media type "application/x-www-form-urlencoded".
  CreateWorkspaceRequestBody body = 1;
}

// Canonical success response.
message CreateWorkspaceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1Workspace body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message FetchWorkspaceRequest {
  // The SID of the Workspace resource to fetch. Required.
  string sid = 1;
}

// Canonical success response.
message FetchWorkspaceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1Workspace body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// UpdateWorkspaceRequest
message UpdateWorkspaceRequestBody {
  // The SID of the Activity that will be used when new Workers are created in the Workspace.
  string default_activity_sid = 1;
  // The URL we should call when an event occurs. See [Workspace Events](https://www.twilio.com/docs/taskrouter/api/event) for more information. This parameter supports Twilio's [Webhooks (HTTP callbacks) Connection Overrides](https://www.twilio.com/docs/usage/webhooks/webhooks-connection-overrides).
  string event_callback_url = 2;
  // The list of Workspace events for which to call event_callback_url. For example if `EventsFilter=task.created,task.canceled,worker.activity.update`, then TaskRouter will call event_callback_url only when a task is created, canceled, or a Worker activity is updated.
  string events_filter = 3;
  // A descriptive string that you create to describe the Workspace resource. For example: `Sales Call Center` or `Customer Support Team`.
  string friendly_name = 4;
  // Whether to enable multi-tasking. Can be: `true` to enable multi-tasking, or `false` to disable it. However, all workspaces should be maintained as multi-tasking. There is no default when omitting this parameter. A multi-tasking Workspace can't be updated to single-tasking unless it is not a Flex Project and another (legacy) single-tasking Workspace exists. Multi-tasking allows Workers to handle multiple Tasks simultaneously. In multi-tasking mode, each Worker can receive parallel reservations up to the per-channel maximums defined in the Workers section. In single-tasking mode (legacy mode), each Worker will only receive a new reservation when the previous task is completed. Learn more at [Multitasking](https://www.twilio.com/docs/taskrouter/multitasking).
  bool multi_task_enabled = 5;
  // OpenAPI property "PrioritizeQueueOrder".
  WorkspaceEnumQueueOrder prioritize_queue_order = 6;
  // The SID of the Activity that will be assigned to a Worker when a Task reservation times out without a response.
  string timeout_activity_sid = 7;
}

message UpdateWorkspaceRequest {
  // The SID of the Workspace resource to update. Required.
  string sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateWorkspaceRequestBody body = 2;
}

// Canonical success response.
message UpdateWorkspaceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1Workspace body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message DeleteWorkspaceRequest {
  // The SID of the Workspace resource to delete. Required.
  string sid = 1;
}

// Canonical success response.
message DeleteWorkspaceResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 2;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 3;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 4;
}

// ListActivityResponse
message ListActivityResponseBody {
  repeated TaskrouterV1WorkspaceActivity activities = 1;
  ListActivityResponseBodyMeta meta = 2;
}

message ListActivityResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListActivityRequest {
  // The SID of the Workspace with the Activity resources to read. Required.
  string workspace_sid = 1;
  // The `friendly_name` of the Activity resources to read.
  string friendly_name = 2;
  // Whether return only Activity resources that are available or unavailable. A value of `true` returns only available activities. Values of '1' or `yes` also indicate `true`. All other values represent `false` and return activities that are unavailable.
  string available = 3;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 4;
  // The page index. This value is simply for client state.
  int64 page = 5;
  // The page token. This is provided by the API.
  string page_token = 6;
}

// Canonical success response.
message ListActivityResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListActivityResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// CreateActivityRequest
message CreateActivityRequestBody {
  // Whether the Worker should be eligible to receive a Task when it occupies the Activity. A value of `true`, `1`, or `yes` specifies the Activity is available. All other values specify that it is not. The value cannot be changed after the Activity is created.
  bool available = 1;
  // A descriptive string that you create to describe the Activity resource. It can be up to 64 characters long. These names are used to calculate and expose statistics about Workers, and provide visibility into the state of each Worker. Examples of friendly names include: `on-call`, `break`, and `email`.
  string friendly_name = 2;
}

message CreateActivityRequest {
  // The SID of the Workspace that the new Activity belongs to. Required.
  string workspace_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateActivityRequestBody body = 2;
}

// Canonical success response.
message CreateActivityResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceActivity body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message FetchActivityRequest {
  // The SID of the Workspace with the Activity resources to fetch. Required.
  string workspace_sid = 1;
  // The SID of the Activity resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchActivityResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceActivity body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// UpdateActivityRequest
message UpdateActivityRequestBody {
  // A descriptive string that you create to describe the Activity resource. It can be up to 64 characters long. These names are used to calculate and expose statistics about Workers, and provide visibility into the state of each Worker. Examples of friendly names include: `on-call`, `break`, and `email`.
  string friendly_name = 1;
}

message UpdateActivityRequest {
  // The SID of the Workspace with the Activity resources to update. Required.
  string workspace_sid = 1;
  // The SID of the Activity resource to update. Required.
  string sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateActivityRequestBody body = 3;
}

// Canonical success response.
message UpdateActivityResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceActivity body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message DeleteActivityRequest {
  // The SID of the Workspace with the Activity resources to delete. Required.
  string workspace_sid = 1;
  // The SID of the Activity resource to delete. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteActivityResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 2;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 3;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 4;
}

message FetchWorkspaceCumulativeStatisticsRequest {
  // The SID of the Workspace to fetch. Required.
  string workspace_sid = 1;
  // Only include usage that occurred on or before this date, specified in GMT as an [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date-time.
  string end_date = 2;
  // Only calculate statistics since this many minutes in the past. The default 15 minutes. This is helpful for displaying statistics for the last 15 minutes, 240 minutes (4 hours), and 480 minutes (8 hours) to see trends.
  int64 minutes = 3;
  // Only calculate statistics from this date and time and later, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string start_date = 4;
  // Only calculate cumulative statistics on this TaskChannel. Can be the TaskChannel's SID or its `unique_name`, such as `voice`, `sms`, or `default`.
  string task_channel = 5;
  // A comma separated list of values that describes the thresholds, in seconds, to calculate statistics on. For each threshold specified, the number of Tasks canceled and reservations accepted above and below the specified thresholds in seconds are computed. For example, `5,30` would show splits of Tasks that were canceled or accepted before and after 5 seconds and before and after 30 seconds. This can be used to show short abandoned Tasks or Tasks that failed to meet an SLA. TaskRouter will calculate statistics on up to 10,000 Tasks for any given threshold.
  string split_by_wait_time = 6;
}

// Canonical success response.
message FetchWorkspaceCumulativeStatisticsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceWorkspaceCumulativeStatistics body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// ListEventResponse
message ListEventResponseBody {
  repeated TaskrouterV1WorkspaceEvent events = 1;
  ListEventResponseBodyMeta meta = 2;
}

message ListEventResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListEventRequest {
  // The SID of the Workspace with the Events to read. Returns only the Events that pertain to the specified Workspace. Required.
  string workspace_sid = 1;
  // Only include Events that occurred on or before this date, specified in GMT as an [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date-time.
  string end_date = 2;
  // The type of Events to read. Returns only Events of the type specified.
  string event_type = 3;
  // The period of events to read in minutes. Returns only Events that occurred since this many minutes in the past. The default is `15` minutes. Task Attributes for Events occuring more 43,200 minutes ago will be redacted.
  int64 minutes = 4;
  // The SID of the Reservation with the Events to read. Returns only Events that pertain to the specified Reservation.
  string reservation_sid = 5;
  // Only include Events from on or after this date and time, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format. Task Attributes for Events older than 30 days will be redacted.
  string start_date = 6;
  // The SID of the TaskQueue with the Events to read. Returns only the Events that pertain to the specified TaskQueue.
  string task_queue_sid = 7;
  // The SID of the Task with the Events to read. Returns only the Events that pertain to the specified Task.
  string task_sid = 8;
  // The SID of the Worker with the Events to read. Returns only the Events that pertain to the specified Worker.
  string worker_sid = 9;
  // The SID of the Workflow with the Events to read. Returns only the Events that pertain to the specified Workflow.
  string workflow_sid = 10;
  // The TaskChannel with the Events to read. Returns only the Events that pertain to the specified TaskChannel.
  string task_channel = 11;
  // The SID of the Event resource to read.
  string sid = 12;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 13;
  // The page index. This value is simply for client state.
  int64 page = 14;
  // The page token. This is provided by the API.
  string page_token = 15;
}

// Canonical success response.
message ListEventResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListEventResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message FetchEventRequest {
  // The SID of the Workspace with the Event to fetch. Required.
  string workspace_sid = 1;
  // The SID of the Event resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchEventResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceEvent body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message FetchWorkspaceRealTimeStatisticsRequest {
  // The SID of the Workspace to fetch. Required.
  string workspace_sid = 1;
  // Only calculate real-time statistics on this TaskChannel. Can be the TaskChannel's SID or its `unique_name`, such as `voice`, `sms`, or `default`.
  string task_channel = 2;
}

// Canonical success response.
message FetchWorkspaceRealTimeStatisticsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceWorkspaceRealTimeStatistics body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message FetchWorkspaceStatisticsRequest {
  // The SID of the Workspace to fetch. Required.
  string workspace_sid = 1;
  // Only calculate statistics since this many minutes in the past. The default 15 minutes. This is helpful for displaying statistics for the last 15 minutes, 240 minutes (4 hours), and 480 minutes (8 hours) to see trends.
  int64 minutes = 2;
  // Only calculate statistics from this date and time and later, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string start_date = 3;
  // Only calculate statistics from this date and time and earlier, specified in GMT as an [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date-time.
  string end_date = 4;
  // Only calculate statistics on this TaskChannel. Can be the TaskChannel's SID or its `unique_name`, such as `voice`, `sms`, or `default`.
  string task_channel = 5;
  // A comma separated list of values that describes the thresholds, in seconds, to calculate statistics on. For each threshold specified, the number of Tasks canceled and reservations accepted above and below the specified thresholds in seconds are computed. For example, `5,30` would show splits of Tasks that were canceled or accepted before and after 5 seconds and before and after 30 seconds. This can be used to show short abandoned Tasks or Tasks that failed to meet an SLA.
  string split_by_wait_time = 6;
}

// Canonical success response.
message FetchWorkspaceStatisticsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceWorkspaceStatistics body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// ListTaskChannelResponse
message ListTaskChannelResponseBody {
  repeated TaskrouterV1WorkspaceTaskChannel channels = 1;
  ListTaskChannelResponseBodyMeta meta = 2;
}

message ListTaskChannelResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListTaskChannelRequest {
  // The SID of the Workspace with the Task Channel to read. Required.
  string workspace_sid = 1;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 2;
  // The page index. This value is simply for client state.
  int64 page = 3;
  // The page token. This is provided by the API.
  string page_token = 4;
}

// Canonical success response.
message ListTaskChannelResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListTaskChannelResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// CreateTaskChannelRequest
message CreateTaskChannelRequestBody {
  // Whether the Task Channel should prioritize Workers that have been idle. If `true`, Workers that have been idle the longest are prioritized.
  bool channel_optimized_routing = 1;
  // A descriptive string that you create to describe the Task Channel. It can be up to 64 characters long.
  string friendly_name = 2;
  // An application-defined string that uniquely identifies the Task Channel, such as `voice` or `sms`.
  string unique_name = 3;
}

message CreateTaskChannelRequest {
  // The SID of the Workspace that the new Task Channel belongs to. Required.
  string workspace_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateTaskChannelRequestBody body = 2;
}

// Canonical success response.
message CreateTaskChannelResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceTaskChannel body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message FetchTaskChannelRequest {
  // The SID of the Workspace with the Task Channel to fetch. Required.
  string workspace_sid = 1;
  // The SID of the Task Channel resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchTaskChannelResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceTaskChannel body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// UpdateTaskChannelRequest
message UpdateTaskChannelRequestBody {
  // Whether the TaskChannel should prioritize Workers that have been idle. If `true`, Workers that have been idle the longest are prioritized.
  bool channel_optimized_routing = 1;
  // A descriptive string that you create to describe the Task Channel. It can be up to 64 characters long.
  string friendly_name = 2;
}

message UpdateTaskChannelRequest {
  // The SID of the Workspace with the Task Channel to update. Required.
  string workspace_sid = 1;
  // The SID of the Task Channel resource to update. Required.
  string sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateTaskChannelRequestBody body = 3;
}

// Canonical success response.
message UpdateTaskChannelResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceTaskChannel body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message DeleteTaskChannelRequest {
  // The SID of the Workspace with the Task Channel to delete. Required.
  string workspace_sid = 1;
  // The SID of the Task Channel resource to delete. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteTaskChannelResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 2;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 3;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 4;
}

// ListTaskQueueResponse
message ListTaskQueueResponseBody {
  ListTaskQueueResponseBodyMeta meta = 1;
  repeated TaskrouterV1WorkspaceTaskQueue task_queues = 2;
}

message ListTaskQueueResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListTaskQueueRequest {
  // The SID of the Workspace with the TaskQueue to read. Required.
  string workspace_sid = 1;
  // The `friendly_name` of the TaskQueue resources to read.
  string friendly_name = 2;
  // The attributes of the Workers to read. Returns the TaskQueues with Workers that match the attributes specified in this parameter.
  string evaluate_worker_attributes = 3;
  // The SID of the Worker with the TaskQueue resources to read.
  string worker_sid = 4;
  // Sorting parameter for TaskQueues
  string ordering = 5;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 6;
  // The page index. This value is simply for client state.
  int64 page = 7;
  // The page token. This is provided by the API.
  string page_token = 8;
}

// Canonical success response.
message ListTaskQueueResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListTaskQueueResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// CreateTaskQueueRequest
message CreateTaskQueueRequestBody {
  // The SID of the Activity to assign Workers when a task is assigned to them.
  string assignment_activity_sid = 1;
  // A descriptive string that you create to describe the TaskQueue. For example `Support-Tier 1`, `Sales`, or `Escalation`.
  string friendly_name = 2;
  // The maximum number of Workers to reserve for the assignment of a Task in the queue. Can be an integer between 1 and 50, inclusive and defaults to 1.
  int64 max_reserved_workers = 3;
  // The SID of the Activity to assign Workers when a task is reserved for them.
  string reservation_activity_sid = 4;
  // A string that describes the Worker selection criteria for any Tasks that enter the TaskQueue. For example, `'"language" == "spanish"'`. The default value is `1==1`. If this value is empty, Tasks will wait in the TaskQueue until they are deleted or moved to another TaskQueue. For more information about Worker selection, see [Describing Worker selection criteria](https://www.twilio.com/docs/taskrouter/api/taskqueues#target-workers).
  string target_workers = 5;
  // OpenAPI property "TaskOrder".
  TaskQueueEnumTaskOrder task_order = 6;
}

message CreateTaskQueueRequest {
  // The SID of the Workspace that the new TaskQueue belongs to. Required.
  string workspace_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateTaskQueueRequestBody body = 2;
}

// Canonical success response.
message CreateTaskQueueResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceTaskQueue body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// CreateTaskQueueBulkRealTimeStatisticsRequest
message CreateTaskQueueBulkRealTimeStatisticsRequestBody {
  // Scalar value wrapper.
  string value = 1;
}

// Fetch a Task Queue Real Time Statistics in bulk for the array of TaskQueue SIDs, support upto 50 in a request.
message CreateTaskQueueBulkRealTimeStatisticsRequest {
  // The unique SID identifier of the Workspace. Required.
  string workspace_sid = 1;
  // Request body from media type "application/json".
  CreateTaskQueueBulkRealTimeStatisticsRequestBody body = 2;
}

// Canonical success response.
message CreateTaskQueueBulkRealTimeStatisticsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceTaskQueueTaskQueueBulkRealTimeStatistics body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// ListTaskQueuesStatisticsResponse
message ListTaskQueuesStatisticsResponseBody {
  ListTaskQueuesStatisticsResponseBodyMeta meta = 1;
  repeated TaskrouterV1WorkspaceTaskQueueTaskQueuesStatistics task_queues_statistics = 2;
}

message ListTaskQueuesStatisticsResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListTaskQueuesStatisticsRequest {
  // The SID of the Workspace with the TaskQueues to read. Required.
  string workspace_sid = 1;
  // Only calculate statistics from this date and time and earlier, specified in GMT as an [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date-time.
  string end_date = 2;
  // The `friendly_name` of the TaskQueue statistics to read.
  string friendly_name = 3;
  // Only calculate statistics since this many minutes in the past. The default is 15 minutes.
  int64 minutes = 4;
  // Only calculate statistics from this date and time and later, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string start_date = 5;
  // Only calculate statistics on this TaskChannel. Can be the TaskChannel's SID or its `unique_name`, such as `voice`, `sms`, or `default`.
  string task_channel = 6;
  // A comma separated list of values that describes the thresholds, in seconds, to calculate statistics on. For each threshold specified, the number of Tasks canceled and reservations accepted above and below the specified thresholds in seconds are computed.
  string split_by_wait_time = 7;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 8;
  // The page index. This value is simply for client state.
  int64 page = 9;
  // The page token. This is provided by the API.
  string page_token = 10;
}

// Canonical success response.
message ListTaskQueuesStatisticsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListTaskQueuesStatisticsResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message FetchTaskQueueRequest {
  // The SID of the Workspace with the TaskQueue to fetch. Required.
  string workspace_sid = 1;
  // The SID of the TaskQueue resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchTaskQueueResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceTaskQueue body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// UpdateTaskQueueRequest
message UpdateTaskQueueRequestBody {
  // The SID of the Activity to assign Workers when a task is assigned for them.
  string assignment_activity_sid = 1;
  // A descriptive string that you create to describe the TaskQueue. For example `Support-Tier 1`, `Sales`, or `Escalation`.
  string friendly_name = 2;
  // The maximum number of Workers to create reservations for the assignment of a task while in the queue. Maximum of 50.
  int64 max_reserved_workers = 3;
  // The SID of the Activity to assign Workers when a task is reserved for them.
  string reservation_activity_sid = 4;
  // A string describing the Worker selection criteria for any Tasks that enter the TaskQueue. For example '"language" == "spanish"' If no TargetWorkers parameter is provided, Tasks will wait in the queue until they are either deleted or moved to another queue. Additional examples on how to describing Worker selection criteria below.
  string target_workers = 5;
  // OpenAPI property "TaskOrder".
  TaskQueueEnumTaskOrder task_order = 6;
}

message UpdateTaskQueueRequest {
  // The SID of the Workspace with the TaskQueue to update. Required.
  string workspace_sid = 1;
  // The SID of the TaskQueue resource to update. Required.
  string sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateTaskQueueRequestBody body = 3;
}

// Canonical success response.
message UpdateTaskQueueResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceTaskQueue body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message DeleteTaskQueueRequest {
  // The SID of the Workspace with the TaskQueue to delete. Required.
  string workspace_sid = 1;
  // The SID of the TaskQueue resource to delete. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteTaskQueueResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 2;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 3;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 4;
}

message FetchTaskQueueCumulativeStatisticsRequest {
  // The SID of the Workspace with the TaskQueue to fetch. Required.
  string workspace_sid = 1;
  // The SID of the TaskQueue for which to fetch statistics. Required.
  string task_queue_sid = 2;
  // Only calculate statistics from this date and time and earlier, specified in GMT as an [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date-time.
  string end_date = 3;
  // Only calculate statistics since this many minutes in the past. The default is 15 minutes.
  int64 minutes = 4;
  // Only calculate statistics from this date and time and later, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string start_date = 5;
  // Only calculate cumulative statistics on this TaskChannel. Can be the TaskChannel's SID or its `unique_name`, such as `voice`, `sms`, or `default`.
  string task_channel = 6;
  // A comma separated list of values that describes the thresholds, in seconds, to calculate statistics on. For each threshold specified, the number of Tasks canceled and reservations accepted above and below the specified thresholds in seconds are computed. TaskRouter will calculate statistics on up to 10,000 Tasks/Reservations for any given threshold.
  string split_by_wait_time = 7;
}

// Canonical success response.
message FetchTaskQueueCumulativeStatisticsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceTaskQueueTaskQueueCumulativeStatistics body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message FetchTaskQueueRealTimeStatisticsRequest {
  // The SID of the Workspace with the TaskQueue to fetch. Required.
  string workspace_sid = 1;
  // The SID of the TaskQueue for which to fetch statistics. Required.
  string task_queue_sid = 2;
  // The TaskChannel for which to fetch statistics. Can be the TaskChannel's SID or its `unique_name`, such as `voice`, `sms`, or `default`.
  string task_channel = 3;
}

// Canonical success response.
message FetchTaskQueueRealTimeStatisticsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceTaskQueueTaskQueueRealTimeStatistics body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message FetchTaskQueueStatisticsRequest {
  // The SID of the Workspace with the TaskQueue to fetch. Required.
  string workspace_sid = 1;
  // The SID of the TaskQueue for which to fetch statistics. Required.
  string task_queue_sid = 2;
  // Only calculate statistics from this date and time and earlier, specified in GMT as an [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date-time.
  string end_date = 3;
  // Only calculate statistics since this many minutes in the past. The default is 15 minutes.
  int64 minutes = 4;
  // Only calculate statistics from this date and time and later, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string start_date = 5;
  // Only calculate real-time and cumulative statistics for the specified TaskChannel. Can be the TaskChannel's SID or its `unique_name`, such as `voice`, `sms`, or `default`.
  string task_channel = 6;
  // A comma separated list of values that describes the thresholds, in seconds, to calculate statistics on. For each threshold specified, the number of Tasks canceled and reservations accepted above and below the specified thresholds in seconds are computed.
  string split_by_wait_time = 7;
}

// Canonical success response.
message FetchTaskQueueStatisticsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceTaskQueueTaskQueueStatistics body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// ListTaskResponse
message ListTaskResponseBody {
  ListTaskResponseBodyMeta meta = 1;
  repeated TaskrouterV1WorkspaceTask tasks = 2;
}

message ListTaskResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListTaskRequest {
  // The SID of the Workspace with the Tasks to read. Required.
  string workspace_sid = 1;
  // The priority value of the Tasks to read. Returns the list of all Tasks in the Workspace with the specified priority.
  int64 priority = 2;
  // The `assignment_status` of the Tasks you want to read. Can be: `pending`, `reserved`, `assigned`, `canceled`, `wrapping`, or `completed`. Returns all Tasks in the Workspace with the specified `assignment_status`.
  repeated string assignment_status = 3;
  // The SID of the Workflow with the Tasks to read. Returns the Tasks controlled by the Workflow identified by this SID.
  string workflow_sid = 4;
  // The friendly name of the Workflow with the Tasks to read. Returns the Tasks controlled by the Workflow identified by this friendly name.
  string workflow_name = 5;
  // The SID of the TaskQueue with the Tasks to read. Returns the Tasks waiting in the TaskQueue identified by this SID.
  string task_queue_sid = 6;
  // The `friendly_name` of the TaskQueue with the Tasks to read. Returns the Tasks waiting in the TaskQueue identified by this friendly name.
  string task_queue_name = 7;
  // The attributes of the Tasks to read. Returns the Tasks that match the attributes specified in this parameter.
  string evaluate_task_attributes = 8;
  // A SID of a Worker, Queue, or Workflow to route a Task to
  string routing_target = 9;
  // How to order the returned Task resources. By default, Tasks are sorted by ascending DateCreated. This value is specified as: `Attribute:Order`, where `Attribute` can be either `DateCreated`, `Priority`, or `VirtualStartTime` and `Order` can be either `asc` or `desc`. For example, `Priority:desc` returns Tasks ordered in descending order of their Priority. Pairings of sort orders can be specified in a comma-separated list such as `Priority:desc,DateCreated:asc`, which returns the Tasks in descending Priority order and ascending DateCreated Order. The only ordering pairing not allowed is DateCreated and VirtualStartTime.
  string ordering = 10;
  // Whether to read Tasks with Add-ons. If `true`, returns only Tasks with Add-ons. If `false`, returns only Tasks without Add-ons.
  bool has_addons = 11;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 12;
  // The page index. This value is simply for client state.
  int64 page = 13;
  // The page token. This is provided by the API.
  string page_token = 14;
}

// Canonical success response.
message ListTaskResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListTaskResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 9;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 10;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 11;
}

// CreateTaskRequest
message CreateTaskRequestBody {
  // A JSON string with the attributes of the new task. This value is passed to the Workflow's `assignment_callback_url` when the Task is assigned to a Worker. For example: `{ "task_type": "call", "twilio_call_sid": "CAxxx", "customer_ticket_number": "12345" }`.
  string attributes = 1;
  // A boolean that indicates if the Task should respect a Worker's capacity and availability during assignment. This field can only be used when the `RoutingTarget` field is set to a Worker SID. By setting `IgnoreCapacity` to a value of `true`, `1`, or `yes`, the Task will be routed to the Worker without respecting their capacity and availability. Any other value will enforce the Worker's capacity and availability. The default value of `IgnoreCapacity` is `true` when the `RoutingTarget` is set to a Worker SID.
  string ignore_capacity = 2;
  // The priority to assign the new task and override the default. When supplied, the new Task will have this priority unless it matches a Workflow Target with a Priority set. When not supplied, the new Task will have the priority of the matching Workflow Target. Value can be 0 to 2^31^ (2,147,483,647).
  int64 priority = 3;
  // A SID of a Worker, Queue, or Workflow to route a Task to
  string routing_target = 4;
  // When MultiTasking is enabled, specify the TaskChannel by passing either its `unique_name` or `sid`. Default value is `default`.
  string task_channel = 5;
  // The SID of the TaskQueue in which the Task belongs
  string task_queue_sid = 6;
  // The amount of time in seconds the new task can live before being assigned. Can be up to a maximum of 2 weeks (1,209,600 seconds). The default value is 24 hours (86,400 seconds). On timeout, the `task.canceled` event will fire with description `Task TTL Exceeded`.
  int64 timeout = 7;
  // The virtual start time to assign the new task and override the default. When supplied, the new task will have this virtual start time. When not supplied, the new task will have the virtual start time equal to `date_created`. Value can't be in the future or before the year of 1900.
  string virtual_start_time = 8;
  // The SID of the Workflow that you would like to handle routing for the new Task. If there is only one Workflow defined for the Workspace that you are posting the new task to, this parameter is optional.
  string workflow_sid = 9;
}

message CreateTaskRequest {
  // The SID of the Workspace that the new Task belongs to. Required.
  string workspace_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateTaskRequestBody body = 2;
}

// Canonical success response.
message CreateTaskResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceTask body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 9;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 10;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 11;
}

message FetchTaskRequest {
  // The SID of the Workspace with the Task to fetch. Required.
  string workspace_sid = 1;
  // The SID of the Task resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchTaskResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceTask body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 9;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 10;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 11;
}

// UpdateTaskRequest
message UpdateTaskRequestBody {
  // OpenAPI property "AssignmentStatus".
  TaskEnumStatus assignment_status = 1;
  // The JSON string that describes the custom attributes of the task.
  string attributes = 2;
  // The Task's new priority value. When supplied, the Task takes on the specified priority unless it matches a Workflow Target with a Priority set. Value can be 0 to 2^31^ (2,147,483,647).
  int64 priority = 3;
  // The reason that the Task was canceled or completed. This parameter is required only if the Task is canceled or completed. Setting this value queues the task for deletion and logs the reason.
  string reason = 4;
  // When MultiTasking is enabled, specify the TaskChannel with the task to update. Can be the TaskChannel's SID or its `unique_name`, such as `voice`, `sms`, or `default`.
  string task_channel = 5;
  // The task's new virtual start time value. When supplied, the Task takes on the specified virtual start time. Value can't be in the future or before the year of 1900.
  string virtual_start_time = 6;
}

message UpdateTaskRequest {
  // The SID of the Workspace with the Task to update. Required.
  string workspace_sid = 1;
  // The SID of the Task resource to update. Required.
  string sid = 2;
  // If provided, applies this mutation if (and only if) the [ETag](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/ETag) header of the Task matches the provided value. This matches the semantics of (and is implemented with) the HTTP [If-Match header](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-Match).
  string header_if_match = 3;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateTaskRequestBody body = 4;
}

// Canonical success response.
message UpdateTaskResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceTask body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 9;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 10;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 11;
}

message DeleteTaskRequest {
  // The SID of the Workspace with the Task to delete. Required.
  string workspace_sid = 1;
  // The SID of the Task resource to delete. Required.
  string sid = 2;
  // If provided, deletes this Task if (and only if) the [ETag](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/ETag) header of the Task matches the provided value. This matches the semantics of (and is implemented with) the HTTP [If-Match header](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-Match).
  string header_if_match = 3;
}

// Canonical success response.
message DeleteTaskResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 2;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 3;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 4;
}

// ListTaskReservationResponse
message ListTaskReservationResponseBody {
  ListTaskReservationResponseBodyMeta meta = 1;
  repeated TaskrouterV1WorkspaceTaskTaskReservation reservations = 2;
}

message ListTaskReservationResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListTaskReservationRequest {
  // The SID of the Workspace with the TaskReservation resources to read. Required.
  string workspace_sid = 1;
  // The SID of the reserved Task resource with the TaskReservation resources to read. Required.
  string task_sid = 2;
  // Returns the list of reservations for a task with a specified ReservationStatus.  Can be: `pending`, `accepted`, `rejected`, or `timeout`.
  TaskReservationEnumStatus reservation_status = 3;
  // The SID of the reserved Worker resource to read.
  string worker_sid = 4;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 5;
  // The page index. This value is simply for client state.
  int64 page = 6;
  // The page token. This is provided by the API.
  string page_token = 7;
}

// Canonical success response.
message ListTaskReservationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListTaskReservationResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 9;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 10;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 11;
}

message FetchTaskReservationRequest {
  // The SID of the Workspace with the TaskReservation resource to fetch. Required.
  string workspace_sid = 1;
  // The SID of the reserved Task resource with the TaskReservation resource to fetch. Required.
  string task_sid = 2;
  // The SID of the TaskReservation resource to fetch. Required.
  string sid = 3;
}

// Canonical success response.
message FetchTaskReservationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceTaskTaskReservation body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "Etag".
  string header_etag = 8;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 9;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 10;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 11;
}

// UpdateTaskReservationRequest
message UpdateTaskReservationRequestBody {
  // Whether to play a notification beep when the participant joins or when to play a beep. Can be: `true`, `false`, `onEnter`, or `onExit`. The default value is `true`.
  string beep = 1;
  // Whether to play a notification beep when the customer joins.
  bool beep_on_customer_entrance = 2;
  // Whether to accept a reservation when executing a Call instruction.
  bool call_accept = 3;
  // The Caller ID of the outbound call when executing a Call instruction.
  string call_from = 4;
  // Whether to record both legs of a call when executing a Call instruction or which leg to record.
  string call_record = 5;
  // The URL to call  for the completed call event when executing a Call instruction.
  string call_status_callback_url = 6;
  // Timeout for call when executing a Call instruction.
  int64 call_timeout = 7;
  // The Contact URI of the worker when executing a Call instruction.  Can be the URI of the Twilio Client, the SIP URI for Programmable SIP, or the [E.164](https://www.twilio.com/docs/glossary/what-e164) formatted phone number, depending on the destination.
  string call_to = 8;
  // TwiML URI executed on answering the worker's leg as a result of the Call instruction.
  string call_url = 9;
  // Whether to record the conference the participant is joining or when to record the conference. Can be: `true`, `false`, `record-from-start`, and `do-not-record`. The default value is `false`.
  string conference_record = 10;
  // The URL we should call using the `conference_recording_status_callback_method` when the conference recording is available.
  string conference_recording_status_callback = 11;
  // The HTTP method we should use to call `conference_recording_status_callback`. Can be: `GET` or `POST` and defaults to `POST`.
  string conference_recording_status_callback_method = 12;
  // The URL we should call using the `conference_status_callback_method` when the conference events in `conference_status_callback_event` occur. Only the value set by the first participant to join the conference is used. Subsequent `conference_status_callback` values are ignored.
  string conference_status_callback = 13;
  // The conference status events that we will send to `conference_status_callback`. Can be: `start`, `end`, `join`, `leave`, `mute`, `hold`, `speaker`.
  repeated TaskReservationEnumConferenceEvent conference_status_callback_event = 14;
  // The HTTP method we should use to call `conference_status_callback`. Can be: `GET` or `POST` and defaults to `POST`.
  string conference_status_callback_method = 15;
  // How to trim the leading and trailing silence from your recorded conference audio files. Can be: `trim-silence` or `do-not-trim` and defaults to `trim-silence`.
  string conference_trim = 16;
  // The Caller ID of the call to the worker when executing a Dequeue instruction.
  string dequeue_from = 17;
  // The SID of the Activity resource to start after executing a Dequeue instruction.
  string dequeue_post_work_activity_sid = 18;
  // Whether to record both legs of a call when executing a Dequeue instruction or which leg to record.
  string dequeue_record = 19;
  // The Call progress events sent via webhooks as a result of a Dequeue instruction.
  repeated string dequeue_status_callback_event = 20;
  // The Callback URL for completed call event when executing a Dequeue instruction.
  string dequeue_status_callback_url = 21;
  // Timeout for call when executing a Dequeue instruction.
  int64 dequeue_timeout = 22;
  // The Contact URI of the worker when executing a Dequeue instruction. Can be the URI of the Twilio Client, the SIP URI for Programmable SIP, or the [E.164](https://www.twilio.com/docs/glossary/what-e164) formatted phone number, depending on the destination.
  string dequeue_to = 23;
  // Whether to allow an agent to hear the state of the outbound call, including ringing or disconnect messages. The default is `true`.
  bool early_media = 24;
  // Whether to end the conference when the customer leaves.
  bool end_conference_on_customer_exit = 25;
  // Whether to end the conference when the agent leaves.
  bool end_conference_on_exit = 26;
  // The Caller ID of the call to the worker when executing a Conference instruction.
  string from = 27;
  // The assignment instruction for reservation.
  string instruction = 28;
  // The jitter buffer size for conference. Can be: `small`, `medium`, `large`, `off`.
  string jitter_buffer_size = 29;
  // The maximum number of participants in the conference. Can be a positive integer from `2` to `250`. The default value is `250`.
  int64 max_participants = 30;
  // Whether the agent is muted in the conference. The default is `false`.
  bool muted = 31;
  // The new worker activity SID after executing a Conference instruction.
  string post_work_activity_sid = 32;
  // Whether to record the participant and their conferences, including the time between conferences. The default is `false`.
  bool record = 33;
  // The recording channels for the final recording. Can be: `mono` or `dual` and the default is `mono`.
  string recording_channels = 34;
  // The URL that we should call using the `recording_status_callback_method` when the recording status changes.
  string recording_status_callback = 35;
  // The HTTP method we should use when we call `recording_status_callback`. Can be: `GET` or `POST` and defaults to `POST`.
  string recording_status_callback_method = 36;
  // Whether the reservation should be accepted when executing a Redirect instruction.
  bool redirect_accept = 37;
  // The Call SID of the call parked in the queue when executing a Redirect instruction.
  string redirect_call_sid = 38;
  // TwiML URI to redirect the call to when executing the Redirect instruction.
  string redirect_url = 39;
  // The [region](https://support.twilio.com/hc/en-us/articles/223132167-How-global-low-latency-routing-and-region-selection-work-for-conferences-and-Client-calls) where we should mix the recorded audio. Can be:`us1`, `us2`, `ie1`, `de1`, `sg1`, `br1`, `au1`, or `jp1`.
  string region = 40;
  // OpenAPI property "ReservationStatus".
  TaskReservationEnumStatus reservation_status = 41;
  // The SIP password for authentication.
  string sip_auth_password = 42;
  // The SIP username used for authentication.
  string sip_auth_username = 43;
  // Whether to start the conference when the participant joins, if it has not already started. The default is `true`. If `false` and the conference has not started, the participant is muted and hears background music until another participant starts the conference.
  bool start_conference_on_enter = 44;
  // The URL we should call using the `status_callback_method` to send status information to your application.
  string status_callback = 45;
  // The call progress events that we will send to `status_callback`. Can be: `initiated`, `ringing`, `answered`, or `completed`.
  repeated TaskReservationEnumCallStatus status_callback_event = 46;
  // The HTTP method we should use to call `status_callback`. Can be: `POST` or `GET` and the default is `POST`.
  string status_callback_method = 47;
  // The Supervisor SID/URI when executing the Supervise instruction.
  string supervisor = 48;
  // OpenAPI property "SupervisorMode".
  TaskReservationEnumSupervisorMode supervisor_mode = 49;
  // Timeout for call when executing a Conference instruction.
  int64 timeout = 50;
  // The Contact URI of the worker when executing a Conference instruction. Can be the URI of the Twilio Client, the SIP URI for Programmable SIP, or the [E.164](https://www.twilio.com/docs/glossary/what-e164) formatted phone number, depending on the destination.
  string to_field = 51;
  // The HTTP method we should use to call `wait_url`. Can be `GET` or `POST` and the default is `POST`. When using a static audio file, this should be `GET` so that we can cache the file.
  string wait_method = 52;
  // The URL we should call using the `wait_method` for the music to play while participants are waiting for the conference to start. The default value is the URL of our standard hold music. [Learn more about hold music](https://www.twilio.com/labs/twimlets/holdmusic).
  string wait_url = 53;
  // The new worker activity SID if rejecting a reservation.
  string worker_activity_sid = 54;
}

message UpdateTaskReservationRequest {
  // The SID of the Workspace with the TaskReservation resources to update. Required.
  string workspace_sid = 1;
  // The SID of the reserved Task resource with the TaskReservation resources to update. Required.
  string task_sid = 2;
  // The SID of the TaskReservation resource to update. Required.
  string sid = 3;
  // The If-Match HTTP request header
  string header_if_match = 4;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateTaskReservationRequestBody body = 5;
}

// Canonical success response.
message UpdateTaskReservationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceTaskTaskReservation body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 9;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 10;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 11;
}

// ListWorkerResponse
message ListWorkerResponseBody {
  ListWorkerResponseBodyMeta meta = 1;
  repeated TaskrouterV1WorkspaceWorker workers = 2;
}

message ListWorkerResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListWorkerRequest {
  // The SID of the Workspace with the Workers to read. Required.
  string workspace_sid = 1;
  // The `activity_name` of the Worker resources to read.
  string activity_name = 2;
  // The `activity_sid` of the Worker resources to read.
  string activity_sid = 3;
  // Whether to return only Worker resources that are available or unavailable. Can be `true`, `1`, or `yes` to return Worker resources that are available, and `false`, or any value returns the Worker resources that are not available.
  string available = 4;
  // The `friendly_name` of the Worker resources to read.
  string friendly_name = 5;
  // Filter by Workers that would match an expression. In addition to fields in the workers' attributes, the expression can include the following worker fields: `sid`, `friendly_name`, `activity_sid`, or `activity_name`
  string target_workers_expression = 6;
  // The `friendly_name` of the TaskQueue that the Workers to read are eligible for.
  string task_queue_name = 7;
  // The SID of the TaskQueue that the Workers to read are eligible for.
  string task_queue_sid = 8;
  // Sorting parameter for Workers
  string ordering = 9;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 10;
  // The page index. This value is simply for client state.
  int64 page = 11;
  // The page token. This is provided by the API.
  string page_token = 12;
}

// Canonical success response.
message ListWorkerResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListWorkerResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 9;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 10;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 11;
}

// CreateWorkerRequest
message CreateWorkerRequestBody {
  // The SID of a valid Activity that will describe the new Worker's initial state. See [Activities](https://www.twilio.com/docs/taskrouter/api/activity) for more information. If not provided, the new Worker's initial state is the `default_activity_sid` configured on the Workspace.
  string activity_sid = 1;
  // A valid JSON string that describes the new Worker. For example: `{ "email": "Bob@example.com", "phone": "+5095551234" }`. This data is passed to the `assignment_callback_url` when TaskRouter assigns a Task to the Worker. Defaults to {}.
  string attributes = 2;
  // A descriptive string that you create to describe the new Worker. It can be up to 64 characters long.
  string friendly_name = 3;
}

message CreateWorkerRequest {
  // The SID of the Workspace that the new Worker belongs to. Required.
  string workspace_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateWorkerRequestBody body = 2;
}

// Canonical success response.
message CreateWorkerResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceWorker body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 9;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 10;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 11;
}

message FetchWorkersCumulativeStatisticsRequest {
  // The SID of the Workspace with the resource to fetch. Required.
  string workspace_sid = 1;
  // Only calculate statistics from this date and time and earlier, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string end_date = 2;
  // Only calculate statistics since this many minutes in the past. The default 15 minutes. This is helpful for displaying statistics for the last 15 minutes, 240 minutes (4 hours), and 480 minutes (8 hours) to see trends.
  int64 minutes = 3;
  // Only calculate statistics from this date and time and later, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string start_date = 4;
  // Only calculate cumulative statistics on this TaskChannel. Can be the TaskChannel's SID or its `unique_name`, such as `voice`, `sms`, or `default`.
  string task_channel = 5;
}

// Canonical success response.
message FetchWorkersCumulativeStatisticsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceWorkerWorkersCumulativeStatistics body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message FetchWorkersRealTimeStatisticsRequest {
  // The SID of the Workspace with the resource to fetch. Required.
  string workspace_sid = 1;
  // Only calculate real-time statistics on this TaskChannel. Can be the TaskChannel's SID or its `unique_name`, such as `voice`, `sms`, or `default`.
  string task_channel = 2;
}

// Canonical success response.
message FetchWorkersRealTimeStatisticsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceWorkerWorkersRealTimeStatistics body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message FetchWorkerStatisticsRequest {
  // The SID of the Workspace with the Worker to fetch. Required.
  string workspace_sid = 1;
  // Only calculate statistics since this many minutes in the past. The default 15 minutes. This is helpful for displaying statistics for the last 15 minutes, 240 minutes (4 hours), and 480 minutes (8 hours) to see trends.
  int64 minutes = 2;
  // Only calculate statistics from this date and time and later, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string start_date = 3;
  // Only calculate statistics from this date and time and earlier, specified in GMT as an [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date-time.
  string end_date = 4;
  // The SID of the TaskQueue for which to fetch Worker statistics.
  string task_queue_sid = 5;
  // The `friendly_name` of the TaskQueue for which to fetch Worker statistics.
  string task_queue_name = 6;
  // Only include Workers with `friendly_name` values that match this parameter.
  string friendly_name = 7;
  // Only calculate statistics on this TaskChannel. Can be the TaskChannel's SID or its `unique_name`, such as `voice`, `sms`, or `default`.
  string task_channel = 8;
}

// Canonical success response.
message FetchWorkerStatisticsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceWorkerWorkerStatistics body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message FetchWorkerRequest {
  // The SID of the Workspace with the Worker to fetch. Required.
  string workspace_sid = 1;
  // The SID of the Worker resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchWorkerResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceWorker body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 9;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 10;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 11;
}

// UpdateWorkerRequest
message UpdateWorkerRequestBody {
  // The SID of a valid Activity that will describe the Worker's initial state. See [Activities](https://www.twilio.com/docs/taskrouter/api/activity) for more information.
  string activity_sid = 1;
  // The JSON string that describes the Worker. For example: `{ "email": "Bob@example.com", "phone": "+5095551234" }`. This data is passed to the `assignment_callback_url` when TaskRouter assigns a Task to the Worker. Defaults to {}.
  string attributes = 2;
  // A descriptive string that you create to describe the Worker. It can be up to 64 characters long.
  string friendly_name = 3;
  // Whether to reject the Worker's pending reservations. This option is only valid if the Worker's new [Activity](https://www.twilio.com/docs/taskrouter/api/activity) resource has its `availability` property set to `False`.
  bool reject_pending_reservations = 4;
}

message UpdateWorkerRequest {
  // The SID of the Workspace with the Worker to update. Required.
  string workspace_sid = 1;
  // The SID of the Worker resource to update. Required.
  string sid = 2;
  // The If-Match HTTP request header
  string header_if_match = 3;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateWorkerRequestBody body = 4;
}

// Canonical success response.
message UpdateWorkerResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceWorker body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "ETag".
  string header_etag = 8;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 9;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 10;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 11;
}

message DeleteWorkerRequest {
  // The SID of the Workspace with the Worker to delete. Required.
  string workspace_sid = 1;
  // The SID of the Worker resource to delete. Required.
  string sid = 2;
  // The If-Match HTTP request header
  string header_if_match = 3;
}

// Canonical success response.
message DeleteWorkerResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 2;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 3;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 4;
}

// ListWorkerChannelResponse
message ListWorkerChannelResponseBody {
  repeated TaskrouterV1WorkspaceWorkerWorkerChannel channels = 1;
  ListWorkerChannelResponseBodyMeta meta = 2;
}

message ListWorkerChannelResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListWorkerChannelRequest {
  // The SID of the Workspace with the WorkerChannels to read. Required.
  string workspace_sid = 1;
  // The SID of the Worker with the WorkerChannels to read. Required.
  string worker_sid = 2;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 3;
  // The page index. This value is simply for client state.
  int64 page = 4;
  // The page token. This is provided by the API.
  string page_token = 5;
}

// Canonical success response.
message ListWorkerChannelResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListWorkerChannelResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message FetchWorkerChannelRequest {
  // The SID of the Workspace with the WorkerChannel to fetch. Required.
  string workspace_sid = 1;
  // The SID of the Worker with the WorkerChannel to fetch. Required.
  string worker_sid = 2;
  // The SID of the WorkerChannel to fetch. Required.
  string sid = 3;
}

// Canonical success response.
message FetchWorkerChannelResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceWorkerWorkerChannel body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// UpdateWorkerChannelRequest
message UpdateWorkerChannelRequestBody {
  // Whether the WorkerChannel is available. Set to `false` to prevent the Worker from receiving any new Tasks of this TaskChannel type.
  bool available = 1;
  // The total number of Tasks that the Worker should handle for the TaskChannel type. TaskRouter creates reservations for Tasks of this TaskChannel type up to the specified capacity. If the capacity is 0, no new reservations will be created.
  int64 capacity = 2;
}

message UpdateWorkerChannelRequest {
  // The SID of the Workspace with the WorkerChannel to update. Required.
  string workspace_sid = 1;
  // The SID of the Worker with the WorkerChannel to update. Required.
  string worker_sid = 2;
  // The SID of the WorkerChannel to update. Required.
  string sid = 3;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateWorkerChannelRequestBody body = 4;
}

// Canonical success response.
message UpdateWorkerChannelResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceWorkerWorkerChannel body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// ListWorkerReservationResponse
message ListWorkerReservationResponseBody {
  ListWorkerReservationResponseBodyMeta meta = 1;
  repeated TaskrouterV1WorkspaceWorkerWorkerReservation reservations = 2;
}

message ListWorkerReservationResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListWorkerReservationRequest {
  // The SID of the Workspace with the WorkerReservation resources to read. Required.
  string workspace_sid = 1;
  // The SID of the reserved Worker resource with the WorkerReservation resources to read. Required.
  string worker_sid = 2;
  // Returns the list of reservations for a worker with a specified ReservationStatus. Can be: `pending`, `accepted`, `rejected`, `timeout`, `canceled`, or `rescinded`.
  WorkerReservationEnumStatus reservation_status = 3;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 4;
  // The page index. This value is simply for client state.
  int64 page = 5;
  // The page token. This is provided by the API.
  string page_token = 6;
}

// Canonical success response.
message ListWorkerReservationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListWorkerReservationResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "Etag".
  string header_etag = 8;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 9;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 10;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 11;
}

message FetchWorkerReservationRequest {
  // The SID of the Workspace with the WorkerReservation resource to fetch. Required.
  string workspace_sid = 1;
  // The SID of the reserved Worker resource with the WorkerReservation resource to fetch. Required.
  string worker_sid = 2;
  // The SID of the WorkerReservation resource to fetch. Required.
  string sid = 3;
}

// Canonical success response.
message FetchWorkerReservationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceWorkerWorkerReservation body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// UpdateWorkerReservationRequest
message UpdateWorkerReservationRequestBody {
  // Whether to play a notification beep when the participant joins or when to play a beep. Can be: `true`, `false`, `onEnter`, or `onExit`. The default value is `true`.
  string beep = 1;
  // Whether to play a notification beep when the customer joins.
  bool beep_on_customer_entrance = 2;
  // Whether to accept a reservation when executing a Call instruction.
  bool call_accept = 3;
  // The Caller ID of the outbound call when executing a Call instruction.
  string call_from = 4;
  // Whether to record both legs of a call when executing a Call instruction.
  string call_record = 5;
  // The URL to call for the completed call event when executing a Call instruction.
  string call_status_callback_url = 6;
  // The timeout for a call when executing a Call instruction.
  int64 call_timeout = 7;
  // The contact URI of the worker when executing a Call instruction. Can be the URI of the Twilio Client, the SIP URI for Programmable SIP, or the [E.164](https://www.twilio.com/docs/glossary/what-e164) formatted phone number, depending on the destination.
  string call_to = 8;
  // TwiML URI executed on answering the worker's leg as a result of the Call instruction.
  string call_url = 9;
  // Whether to record the conference the participant is joining or when to record the conference. Can be: `true`, `false`, `record-from-start`, and `do-not-record`. The default value is `false`.
  string conference_record = 10;
  // The URL we should call using the `conference_recording_status_callback_method` when the conference recording is available.
  string conference_recording_status_callback = 11;
  // The HTTP method we should use to call `conference_recording_status_callback`. Can be: `GET` or `POST` and defaults to `POST`.
  string conference_recording_status_callback_method = 12;
  // The URL we should call using the `conference_status_callback_method` when the conference events in `conference_status_callback_event` occur. Only the value set by the first participant to join the conference is used. Subsequent `conference_status_callback` values are ignored.
  string conference_status_callback = 13;
  // The conference status events that we will send to `conference_status_callback`. Can be: `start`, `end`, `join`, `leave`, `mute`, `hold`, `speaker`.
  repeated WorkerReservationEnumConferenceEvent conference_status_callback_event = 14;
  // The HTTP method we should use to call `conference_status_callback`. Can be: `GET` or `POST` and defaults to `POST`.
  string conference_status_callback_method = 15;
  // Whether to trim leading and trailing silence from your recorded conference audio files. Can be: `trim-silence` or `do-not-trim` and defaults to `trim-silence`.
  string conference_trim = 16;
  // The caller ID of the call to the worker when executing a Dequeue instruction.
  string dequeue_from = 17;
  // The SID of the Activity resource to start after executing a Dequeue instruction.
  string dequeue_post_work_activity_sid = 18;
  // Whether to record both legs of a call when executing a Dequeue instruction or which leg to record.
  string dequeue_record = 19;
  // The call progress events sent via webhooks as a result of a Dequeue instruction.
  repeated string dequeue_status_callback_event = 20;
  // The callback URL for completed call event when executing a Dequeue instruction.
  string dequeue_status_callback_url = 21;
  // The timeout for call when executing a Dequeue instruction.
  int64 dequeue_timeout = 22;
  // The contact URI of the worker when executing a Dequeue instruction. Can be the URI of the Twilio Client, the SIP URI for Programmable SIP, or the [E.164](https://www.twilio.com/docs/glossary/what-e164) formatted phone number, depending on the destination.
  string dequeue_to = 23;
  // Whether to allow an agent to hear the state of the outbound call, including ringing or disconnect messages. The default is `true`.
  bool early_media = 24;
  // Whether to end the conference when the customer leaves.
  bool end_conference_on_customer_exit = 25;
  // Whether to end the conference when the agent leaves.
  bool end_conference_on_exit = 26;
  // The caller ID of the call to the worker when executing a Conference instruction.
  string from = 27;
  // The assignment instruction for the reservation.
  string instruction = 28;
  // The jitter buffer size for conference. Can be: `small`, `medium`, `large`, `off`.
  string jitter_buffer_size = 29;
  // The maximum number of participants allowed in the conference. Can be a positive integer from `2` to `250`. The default value is `250`.
  int64 max_participants = 30;
  // Whether the agent is muted in the conference. Defaults to `false`.
  bool muted = 31;
  // The new worker activity SID after executing a Conference instruction.
  string post_work_activity_sid = 32;
  // Whether to record the participant and their conferences, including the time between conferences. Can be `true` or `false` and the default is `false`.
  bool record = 33;
  // The recording channels for the final recording. Can be: `mono` or `dual` and the default is `mono`.
  string recording_channels = 34;
  // The URL that we should call using the `recording_status_callback_method` when the recording status changes.
  string recording_status_callback = 35;
  // The HTTP method we should use when we call `recording_status_callback`. Can be: `GET` or `POST` and defaults to `POST`.
  string recording_status_callback_method = 36;
  // Whether the reservation should be accepted when executing a Redirect instruction.
  bool redirect_accept = 37;
  // The Call SID of the call parked in the queue when executing a Redirect instruction.
  string redirect_call_sid = 38;
  // TwiML URI to redirect the call to when executing the Redirect instruction.
  string redirect_url = 39;
  // The [region](https://support.twilio.com/hc/en-us/articles/223132167-How-global-low-latency-routing-and-region-selection-work-for-conferences-and-Client-calls) where we should mix the recorded audio. Can be:`us1`, `us2`, `ie1`, `de1`, `sg1`, `br1`, `au1`, or `jp1`.
  string region = 40;
  // OpenAPI property "ReservationStatus".
  WorkerReservationEnumStatus reservation_status = 41;
  // The SIP password for authentication.
  string sip_auth_password = 42;
  // The SIP username used for authentication.
  string sip_auth_username = 43;
  // Whether to start the conference when the participant joins, if it has not already started. Can be: `true` or `false` and the default is `true`. If `false` and the conference has not started, the participant is muted and hears background music until another participant starts the conference.
  bool start_conference_on_enter = 44;
  // The URL we should call using the `status_callback_method` to send status information to your application.
  string status_callback = 45;
  // The call progress events that we will send to `status_callback`. Can be: `initiated`, `ringing`, `answered`, or `completed`.
  repeated WorkerReservationEnumCallStatus status_callback_event = 46;
  // The HTTP method we should use to call `status_callback`. Can be: `POST` or `GET` and the default is `POST`.
  string status_callback_method = 47;
  // The timeout for a call when executing a Conference instruction.
  int64 timeout = 48;
  // The Contact URI of the worker when executing a Conference instruction. Can be the URI of the Twilio Client, the SIP URI for Programmable SIP, or the [E.164](https://www.twilio.com/docs/glossary/what-e164) formatted phone number, depending on the destination.
  string to_field = 49;
  // The HTTP method we should use to call `wait_url`. Can be `GET` or `POST` and the default is `POST`. When using a static audio file, this should be `GET` so that we can cache the file.
  string wait_method = 50;
  // The URL we should call using the `wait_method` for the music to play while participants are waiting for the conference to start. The default value is the URL of our standard hold music. [Learn more about hold music](https://www.twilio.com/labs/twimlets/holdmusic).
  string wait_url = 51;
  // The new worker activity SID if rejecting a reservation.
  string worker_activity_sid = 52;
}

message UpdateWorkerReservationRequest {
  // The SID of the Workspace with the WorkerReservation resources to update. Required.
  string workspace_sid = 1;
  // The SID of the reserved Worker resource with the WorkerReservation resources to update. Required.
  string worker_sid = 2;
  // The SID of the WorkerReservation resource to update. Required.
  string sid = 3;
  // The If-Match HTTP request header
  string header_if_match = 4;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateWorkerReservationRequestBody body = 5;
}

// Canonical success response.
message UpdateWorkerReservationResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceWorkerWorkerReservation body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message FetchWorkerInstanceStatisticsRequest {
  // The SID of the Workspace with the WorkerChannel to fetch. Required.
  string workspace_sid = 1;
  // The SID of the Worker with the WorkerChannel to fetch. Required.
  string worker_sid = 2;
  // Only calculate statistics since this many minutes in the past. The default 15 minutes. This is helpful for displaying statistics for the last 15 minutes, 240 minutes (4 hours), and 480 minutes (8 hours) to see trends.
  int64 minutes = 3;
  // Only calculate statistics from this date and time and later, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string start_date = 4;
  // Only include usage that occurred on or before this date, specified in GMT as an [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date-time.
  string end_date = 5;
  // Only calculate statistics on this TaskChannel. Can be the TaskChannel's SID or its `unique_name`, such as `voice`, `sms`, or `default`.
  string task_channel = 6;
}

// Canonical success response.
message FetchWorkerInstanceStatisticsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceWorkerWorkerInstanceStatistics body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// ListWorkflowResponse
message ListWorkflowResponseBody {
  ListWorkflowResponseBodyMeta meta = 1;
  repeated TaskrouterV1WorkspaceWorkflow workflows = 2;
}

message ListWorkflowResponseBodyMeta {
  string first_page_url = 1;
  string key = 2;
  string next_page_url = 3;
  int64 page = 4;
  int64 page_size = 5;
  string previous_page_url = 6;
  string url = 7;
}

message ListWorkflowRequest {
  // The SID of the Workspace with the Workflow to read. Required.
  string workspace_sid = 1;
  // The `friendly_name` of the Workflow resources to read.
  string friendly_name = 2;
  // How many resources to return in each list page. The default is 50, and the maximum is 1000.
  int64 page_size = 3;
  // The page index. This value is simply for client state.
  int64 page = 4;
  // The page token. This is provided by the API.
  string page_token = 5;
}

// Canonical success response.
message ListWorkflowResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  ListWorkflowResponseBody body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// CreateWorkflowRequest
message CreateWorkflowRequestBody {
  // The URL from your application that will process task assignment events. See [Handling Task Assignment Callback](https://www.twilio.com/docs/taskrouter/handle-assignment-callbacks) for more details.
  string assignment_callback_url = 1;
  // A JSON string that contains the rules to apply to the Workflow. See [Configuring Workflows](https://www.twilio.com/docs/taskrouter/workflow-configuration) for more information.
  string configuration = 2;
  // The URL that we should call when a call to the `assignment_callback_url` fails.
  string fallback_assignment_callback_url = 3;
  // A descriptive string that you create to describe the Workflow resource. For example, `Inbound Call Workflow` or `2014 Outbound Campaign`.
  string friendly_name = 4;
  // How long TaskRouter will wait for a confirmation response from your application after it assigns a Task to a Worker. Can be up to `86,400` (24 hours) and the default is `120`.
  int64 task_reservation_timeout = 5;
}

message CreateWorkflowRequest {
  // The SID of the Workspace that the new Workflow to create belongs to. Required.
  string workspace_sid = 1;
  // Request body from media type "application/x-www-form-urlencoded".
  CreateWorkflowRequestBody body = 2;
}

// Canonical success response.
message CreateWorkflowResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceWorkflow body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message FetchWorkflowRequest {
  // The SID of the Workspace with the Workflow to fetch. Required.
  string workspace_sid = 1;
  // The SID of the Workflow resource to fetch. Required.
  string sid = 2;
}

// Canonical success response.
message FetchWorkflowResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceWorkflow body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// UpdateWorkflowRequest
message UpdateWorkflowRequestBody {
  // The URL from your application that will process task assignment events. See [Handling Task Assignment Callback](https://www.twilio.com/docs/taskrouter/handle-assignment-callbacks) for more details.
  string assignment_callback_url = 1;
  // A JSON string that contains the rules to apply to the Workflow. See [Configuring Workflows](https://www.twilio.com/docs/taskrouter/workflow-configuration) for more information.
  string configuration = 2;
  // The URL that we should call when a call to the `assignment_callback_url` fails.
  string fallback_assignment_callback_url = 3;
  // A descriptive string that you create to describe the Workflow resource. For example, `Inbound Call Workflow` or `2014 Outbound Campaign`.
  string friendly_name = 4;
  // Whether or not to re-evaluate Tasks. The default is `false`, which means Tasks in the Workflow will not be processed through the assignment loop again.
  string re_evaluate_tasks = 5;
  // How long TaskRouter will wait for a confirmation response from your application after it assigns a Task to a Worker. Can be up to `86,400` (24 hours) and the default is `120`.
  int64 task_reservation_timeout = 6;
}

message UpdateWorkflowRequest {
  // The SID of the Workspace with the Workflow to update. Required.
  string workspace_sid = 1;
  // The SID of the Workflow resource to update. Required.
  string sid = 2;
  // Request body from media type "application/x-www-form-urlencoded".
  UpdateWorkflowRequestBody body = 3;
}

// Canonical success response.
message UpdateWorkflowResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceWorkflow body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message DeleteWorkflowRequest {
  // The SID of the Workspace with the Workflow to delete. Required.
  string workspace_sid = 1;
  // The SID of the Workflow resource to delete. Required.
  string sid = 2;
}

// Canonical success response.
message DeleteWorkflowResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 2;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 3;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 4;
}

message FetchWorkflowCumulativeStatisticsRequest {
  // The SID of the Workspace with the resource to fetch. Required.
  string workspace_sid = 1;
  // Returns the list of Tasks that are being controlled by the Workflow with the specified Sid value. Required.
  string workflow_sid = 2;
  // Only include usage that occurred on or before this date, specified in GMT as an [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date-time.
  string end_date = 3;
  // Only calculate statistics since this many minutes in the past. The default 15 minutes. This is helpful for displaying statistics for the last 15 minutes, 240 minutes (4 hours), and 480 minutes (8 hours) to see trends.
  int64 minutes = 4;
  // Only calculate statistics from this date and time and later, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string start_date = 5;
  // Only calculate cumulative statistics on this TaskChannel. Can be the TaskChannel's SID or its `unique_name`, such as `voice`, `sms`, or `default`.
  string task_channel = 6;
  // A comma separated list of values that describes the thresholds, in seconds, to calculate statistics on. For each threshold specified, the number of Tasks canceled and reservations accepted above and below the specified thresholds in seconds are computed. For example, `5,30` would show splits of Tasks that were canceled or accepted before and after 5 seconds and before and after 30 seconds. This can be used to show short abandoned Tasks or Tasks that failed to meet an SLA. TaskRouter will calculate statistics on up to 10,000 Tasks for any given threshold.
  string split_by_wait_time = 7;
}

// Canonical success response.
message FetchWorkflowCumulativeStatisticsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceWorkflowWorkflowCumulativeStatistics body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message FetchWorkflowRealTimeStatisticsRequest {
  // The SID of the Workspace with the Workflow to fetch. Required.
  string workspace_sid = 1;
  // Returns the list of Tasks that are being controlled by the Workflow with the specified SID value. Required.
  string workflow_sid = 2;
  // Only calculate real-time statistics on this TaskChannel. Can be the TaskChannel's SID or its `unique_name`, such as `voice`, `sms`, or `default`.
  string task_channel = 3;
}

// Canonical success response.
message FetchWorkflowRealTimeStatisticsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceWorkflowWorkflowRealTimeStatistics body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

message FetchWorkflowStatisticsRequest {
  // The SID of the Workspace with the Workflow to fetch. Required.
  string workspace_sid = 1;
  // Returns the list of Tasks that are being controlled by the Workflow with the specified SID value. Required.
  string workflow_sid = 2;
  // Only calculate statistics since this many minutes in the past. The default 15 minutes. This is helpful for displaying statistics for the last 15 minutes, 240 minutes (4 hours), and 480 minutes (8 hours) to see trends.
  int64 minutes = 3;
  // Only calculate statistics from this date and time and later, specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
  string start_date = 4;
  // Only calculate statistics from this date and time and earlier, specified in GMT as an [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date-time.
  string end_date = 5;
  // Only calculate real-time statistics on this TaskChannel. Can be the TaskChannel's SID or its `unique_name`, such as `voice`, `sms`, or `default`.
  string task_channel = 6;
  // A comma separated list of values that describes the thresholds, in seconds, to calculate statistics on. For each threshold specified, the number of Tasks canceled and reservations accepted above and below the specified thresholds in seconds are computed. For example, `5,30` would show splits of Tasks that were canceled or accepted before and after 5 seconds and before and after 30 seconds. This can be used to show short abandoned Tasks or Tasks that failed to meet an SLA.
  string split_by_wait_time = 7;
}

// Canonical success response.
message FetchWorkflowStatisticsResponse {
  // HTTP status code represented by this response.
  int32 http_status_code = 1;
  // Response body from media type "application/json".
  TaskrouterV1WorkspaceWorkflowWorkflowStatistics body = 2;
  // Indicates whether the browser should include credentials
  bool header_access_control_allow_credentials = 3;
  // Specify the headers allowed when accessing the resource
  string header_access_control_allow_headers = 4;
  // Specify the HTTP methods allowed when accessing the resource
  string header_access_control_allow_methods = 5;
  // Specify the origin(s) allowed to access the resource
  string header_access_control_allow_origin = 6;
  // Headers exposed to the client
  string header_access_control_expose_headers = 7;
  // Response header "X-Rate-Limit-Config".
  string header_x_rate_limit_config = 8;
  // Response header "X-Rate-Limit-Limit".
  string header_x_rate_limit_limit = 9;
  // Response header "X-Rate-Limit-Remaining".
  string header_x_rate_limit_remaining = 10;
}

// Generated from TaskrouterV1Workspace operations.
service TaskrouterV1WorkspaceService {
  rpc ListWorkspace(ListWorkspaceRequest) returns (ListWorkspaceResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces"
      response_body: "body"
    };
  }
  rpc CreateWorkspace(CreateWorkspaceRequest) returns (CreateWorkspaceResponse) {
    option (google.api.http) = {
      post: "/v1/Workspaces"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchWorkspace(FetchWorkspaceRequest) returns (FetchWorkspaceResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateWorkspace(UpdateWorkspaceRequest) returns (UpdateWorkspaceResponse) {
    option (google.api.http) = {
      post: "/v1/Workspaces/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteWorkspace(DeleteWorkspaceRequest) returns (DeleteWorkspaceResponse) {
    option (google.api.http) = {
      delete: "/v1/Workspaces/{sid}"
    };
  }
}

// Generated from TaskrouterV1Activity operations.
service TaskrouterV1ActivityService {
  rpc ListActivity(ListActivityRequest) returns (ListActivityResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Activities"
      response_body: "body"
    };
  }
  rpc CreateActivity(CreateActivityRequest) returns (CreateActivityResponse) {
    option (google.api.http) = {
      post: "/v1/Workspaces/{workspace_sid}/Activities"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchActivity(FetchActivityRequest) returns (FetchActivityResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Activities/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateActivity(UpdateActivityRequest) returns (UpdateActivityResponse) {
    option (google.api.http) = {
      post: "/v1/Workspaces/{workspace_sid}/Activities/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteActivity(DeleteActivityRequest) returns (DeleteActivityResponse) {
    option (google.api.http) = {
      delete: "/v1/Workspaces/{workspace_sid}/Activities/{sid}"
    };
  }
}

// Generated from TaskrouterV1WorkspaceCumulativeStatistics operations.
service TaskrouterV1WorkspaceCumulativeStatisticsService {
  rpc FetchWorkspaceCumulativeStatistics(FetchWorkspaceCumulativeStatisticsRequest) returns (FetchWorkspaceCumulativeStatisticsResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/CumulativeStatistics"
      response_body: "body"
    };
  }
}

// Generated from TaskrouterV1Event operations.
service TaskrouterV1EventService {
  rpc ListEvent(ListEventRequest) returns (ListEventResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Events"
      response_body: "body"
    };
  }
  rpc FetchEvent(FetchEventRequest) returns (FetchEventResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Events/{sid}"
      response_body: "body"
    };
  }
}

// Generated from TaskrouterV1WorkspaceRealTimeStatistics operations.
service TaskrouterV1WorkspaceRealTimeStatisticsService {
  rpc FetchWorkspaceRealTimeStatistics(FetchWorkspaceRealTimeStatisticsRequest) returns (FetchWorkspaceRealTimeStatisticsResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/RealTimeStatistics"
      response_body: "body"
    };
  }
}

// Generated from TaskrouterV1WorkspaceStatistics operations.
service TaskrouterV1WorkspaceStatisticsService {
  rpc FetchWorkspaceStatistics(FetchWorkspaceStatisticsRequest) returns (FetchWorkspaceStatisticsResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Statistics"
      response_body: "body"
    };
  }
}

// Generated from TaskrouterV1TaskChannel operations.
service TaskrouterV1TaskChannelService {
  rpc ListTaskChannel(ListTaskChannelRequest) returns (ListTaskChannelResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/TaskChannels"
      response_body: "body"
    };
  }
  rpc CreateTaskChannel(CreateTaskChannelRequest) returns (CreateTaskChannelResponse) {
    option (google.api.http) = {
      post: "/v1/Workspaces/{workspace_sid}/TaskChannels"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchTaskChannel(FetchTaskChannelRequest) returns (FetchTaskChannelResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/TaskChannels/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateTaskChannel(UpdateTaskChannelRequest) returns (UpdateTaskChannelResponse) {
    option (google.api.http) = {
      post: "/v1/Workspaces/{workspace_sid}/TaskChannels/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteTaskChannel(DeleteTaskChannelRequest) returns (DeleteTaskChannelResponse) {
    option (google.api.http) = {
      delete: "/v1/Workspaces/{workspace_sid}/TaskChannels/{sid}"
    };
  }
}

// Generated from TaskrouterV1TaskQueue operations.
service TaskrouterV1TaskQueueService {
  rpc ListTaskQueue(ListTaskQueueRequest) returns (ListTaskQueueResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/TaskQueues"
      response_body: "body"
    };
  }
  rpc CreateTaskQueue(CreateTaskQueueRequest) returns (CreateTaskQueueResponse) {
    option (google.api.http) = {
      post: "/v1/Workspaces/{workspace_sid}/TaskQueues"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchTaskQueue(FetchTaskQueueRequest) returns (FetchTaskQueueResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/TaskQueues/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateTaskQueue(UpdateTaskQueueRequest) returns (UpdateTaskQueueResponse) {
    option (google.api.http) = {
      post: "/v1/Workspaces/{workspace_sid}/TaskQueues/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteTaskQueue(DeleteTaskQueueRequest) returns (DeleteTaskQueueResponse) {
    option (google.api.http) = {
      delete: "/v1/Workspaces/{workspace_sid}/TaskQueues/{sid}"
    };
  }
}

// Generated from TaskrouterV1TaskQueueBulkRealTimeStatistics operations.
service TaskrouterV1TaskQueueBulkRealTimeStatisticsService {
  // Fetch a Task Queue Real Time Statistics in bulk for the array of TaskQueue SIDs, support upto 50 in a request.
  rpc CreateTaskQueueBulkRealTimeStatistics(CreateTaskQueueBulkRealTimeStatisticsRequest) returns (CreateTaskQueueBulkRealTimeStatisticsResponse) {
    option (google.api.http) = {
      post: "/v1/Workspaces/{workspace_sid}/TaskQueues/RealTimeStatistics"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from TaskrouterV1TaskQueuesStatistics operations.
service TaskrouterV1TaskQueuesStatisticsService {
  rpc ListTaskQueuesStatistics(ListTaskQueuesStatisticsRequest) returns (ListTaskQueuesStatisticsResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/TaskQueues/Statistics"
      response_body: "body"
    };
  }
}

// Generated from TaskrouterV1TaskQueueCumulativeStatistics operations.
service TaskrouterV1TaskQueueCumulativeStatisticsService {
  rpc FetchTaskQueueCumulativeStatistics(FetchTaskQueueCumulativeStatisticsRequest) returns (FetchTaskQueueCumulativeStatisticsResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/TaskQueues/{task_queue_sid}/CumulativeStatistics"
      response_body: "body"
    };
  }
}

// Generated from TaskrouterV1TaskQueueRealTimeStatistics operations.
service TaskrouterV1TaskQueueRealTimeStatisticsService {
  rpc FetchTaskQueueRealTimeStatistics(FetchTaskQueueRealTimeStatisticsRequest) returns (FetchTaskQueueRealTimeStatisticsResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/TaskQueues/{task_queue_sid}/RealTimeStatistics"
      response_body: "body"
    };
  }
}

// Generated from TaskrouterV1TaskQueueStatistics operations.
service TaskrouterV1TaskQueueStatisticsService {
  rpc FetchTaskQueueStatistics(FetchTaskQueueStatisticsRequest) returns (FetchTaskQueueStatisticsResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/TaskQueues/{task_queue_sid}/Statistics"
      response_body: "body"
    };
  }
}

// Generated from TaskrouterV1Task operations.
service TaskrouterV1TaskService {
  rpc ListTask(ListTaskRequest) returns (ListTaskResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Tasks"
      response_body: "body"
    };
  }
  rpc CreateTask(CreateTaskRequest) returns (CreateTaskResponse) {
    option (google.api.http) = {
      post: "/v1/Workspaces/{workspace_sid}/Tasks"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchTask(FetchTaskRequest) returns (FetchTaskResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Tasks/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateTask(UpdateTaskRequest) returns (UpdateTaskResponse) {
    option (google.api.http) = {
      post: "/v1/Workspaces/{workspace_sid}/Tasks/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteTask(DeleteTaskRequest) returns (DeleteTaskResponse) {
    option (google.api.http) = {
      delete: "/v1/Workspaces/{workspace_sid}/Tasks/{sid}"
    };
  }
}

// Generated from TaskrouterV1TaskReservation operations.
service TaskrouterV1TaskReservationService {
  rpc ListTaskReservation(ListTaskReservationRequest) returns (ListTaskReservationResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Tasks/{task_sid}/Reservations"
      response_body: "body"
    };
  }
  rpc FetchTaskReservation(FetchTaskReservationRequest) returns (FetchTaskReservationResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Tasks/{task_sid}/Reservations/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateTaskReservation(UpdateTaskReservationRequest) returns (UpdateTaskReservationResponse) {
    option (google.api.http) = {
      post: "/v1/Workspaces/{workspace_sid}/Tasks/{task_sid}/Reservations/{sid}"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from TaskrouterV1Worker operations.
service TaskrouterV1WorkerService {
  rpc ListWorker(ListWorkerRequest) returns (ListWorkerResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Workers"
      response_body: "body"
    };
  }
  rpc CreateWorker(CreateWorkerRequest) returns (CreateWorkerResponse) {
    option (google.api.http) = {
      post: "/v1/Workspaces/{workspace_sid}/Workers"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchWorker(FetchWorkerRequest) returns (FetchWorkerResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Workers/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateWorker(UpdateWorkerRequest) returns (UpdateWorkerResponse) {
    option (google.api.http) = {
      post: "/v1/Workspaces/{workspace_sid}/Workers/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteWorker(DeleteWorkerRequest) returns (DeleteWorkerResponse) {
    option (google.api.http) = {
      delete: "/v1/Workspaces/{workspace_sid}/Workers/{sid}"
    };
  }
}

// Generated from TaskrouterV1WorkersCumulativeStatistics operations.
service TaskrouterV1WorkersCumulativeStatisticsService {
  rpc FetchWorkersCumulativeStatistics(FetchWorkersCumulativeStatisticsRequest) returns (FetchWorkersCumulativeStatisticsResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Workers/CumulativeStatistics"
      response_body: "body"
    };
  }
}

// Generated from TaskrouterV1WorkersRealTimeStatistics operations.
service TaskrouterV1WorkersRealTimeStatisticsService {
  rpc FetchWorkersRealTimeStatistics(FetchWorkersRealTimeStatisticsRequest) returns (FetchWorkersRealTimeStatisticsResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Workers/RealTimeStatistics"
      response_body: "body"
    };
  }
}

// Generated from TaskrouterV1WorkersStatistics operations.
service TaskrouterV1WorkersStatisticsService {
  rpc FetchWorkerStatistics(FetchWorkerStatisticsRequest) returns (FetchWorkerStatisticsResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Workers/Statistics"
      response_body: "body"
    };
  }
}

// Generated from TaskrouterV1WorkerChannel operations.
service TaskrouterV1WorkerChannelService {
  rpc ListWorkerChannel(ListWorkerChannelRequest) returns (ListWorkerChannelResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Workers/{worker_sid}/Channels"
      response_body: "body"
    };
  }
  rpc FetchWorkerChannel(FetchWorkerChannelRequest) returns (FetchWorkerChannelResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Workers/{worker_sid}/Channels/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateWorkerChannel(UpdateWorkerChannelRequest) returns (UpdateWorkerChannelResponse) {
    option (google.api.http) = {
      post: "/v1/Workspaces/{workspace_sid}/Workers/{worker_sid}/Channels/{sid}"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from TaskrouterV1WorkerReservation operations.
service TaskrouterV1WorkerReservationService {
  rpc ListWorkerReservation(ListWorkerReservationRequest) returns (ListWorkerReservationResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Workers/{worker_sid}/Reservations"
      response_body: "body"
    };
  }
  rpc FetchWorkerReservation(FetchWorkerReservationRequest) returns (FetchWorkerReservationResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Workers/{worker_sid}/Reservations/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateWorkerReservation(UpdateWorkerReservationRequest) returns (UpdateWorkerReservationResponse) {
    option (google.api.http) = {
      post: "/v1/Workspaces/{workspace_sid}/Workers/{worker_sid}/Reservations/{sid}"
      body: "body"
      response_body: "body"
    };
  }
}

// Generated from TaskrouterV1WorkerStatistics operations.
service TaskrouterV1WorkerStatisticsService {
  rpc FetchWorkerInstanceStatistics(FetchWorkerInstanceStatisticsRequest) returns (FetchWorkerInstanceStatisticsResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Workers/{worker_sid}/Statistics"
      response_body: "body"
    };
  }
}

// Generated from TaskrouterV1Workflow operations.
service TaskrouterV1WorkflowService {
  rpc ListWorkflow(ListWorkflowRequest) returns (ListWorkflowResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Workflows"
      response_body: "body"
    };
  }
  rpc CreateWorkflow(CreateWorkflowRequest) returns (CreateWorkflowResponse) {
    option (google.api.http) = {
      post: "/v1/Workspaces/{workspace_sid}/Workflows"
      body: "body"
      response_body: "body"
    };
  }
  rpc FetchWorkflow(FetchWorkflowRequest) returns (FetchWorkflowResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Workflows/{sid}"
      response_body: "body"
    };
  }
  rpc UpdateWorkflow(UpdateWorkflowRequest) returns (UpdateWorkflowResponse) {
    option (google.api.http) = {
      post: "/v1/Workspaces/{workspace_sid}/Workflows/{sid}"
      body: "body"
      response_body: "body"
    };
  }
  rpc DeleteWorkflow(DeleteWorkflowRequest) returns (DeleteWorkflowResponse) {
    option (google.api.http) = {
      delete: "/v1/Workspaces/{workspace_sid}/Workflows/{sid}"
    };
  }
}

// Generated from TaskrouterV1WorkflowCumulativeStatistics operations.
service TaskrouterV1WorkflowCumulativeStatisticsService {
  rpc FetchWorkflowCumulativeStatistics(FetchWorkflowCumulativeStatisticsRequest) returns (FetchWorkflowCumulativeStatisticsResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Workflows/{workflow_sid}/CumulativeStatistics"
      response_body: "body"
    };
  }
}

// Generated from TaskrouterV1WorkflowRealTimeStatistics operations.
service TaskrouterV1WorkflowRealTimeStatisticsService {
  rpc FetchWorkflowRealTimeStatistics(FetchWorkflowRealTimeStatisticsRequest) returns (FetchWorkflowRealTimeStatisticsResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Workflows/{workflow_sid}/RealTimeStatistics"
      response_body: "body"
    };
  }
}

// Generated from TaskrouterV1WorkflowStatistics operations.
service TaskrouterV1WorkflowStatisticsService {
  rpc FetchWorkflowStatistics(FetchWorkflowStatisticsRequest) returns (FetchWorkflowStatisticsResponse) {
    option (google.api.http) = {
      get: "/v1/Workspaces/{workspace_sid}/Workflows/{workflow_sid}/Statistics"
      response_body: "body"
    };
  }
}

